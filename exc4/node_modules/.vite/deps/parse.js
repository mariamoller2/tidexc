import { a as __toCommonJS, i as __require, n as __esmMin, r as __exportAll, t as __commonJSMin } from "./rolldown-runtime-BgmEgscG.js";
//#region node_modules/core-js-pure/internals/global-this.js
var require_global_this$6 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var check = function(it) {
		return it && it.Math === Math && it;
	};
	module.exports = check(typeof globalThis == "object" && globalThis) || check(typeof window == "object" && window) || check(typeof self == "object" && self) || check(typeof global == "object" && global) || check(typeof exports == "object" && exports) || (function() {
		return this;
	})() || Function("return this")();
}));
//#endregion
//#region node_modules/core-js-pure/internals/fails.js
var require_fails = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(exec) {
		try {
			return !!exec();
		} catch (error) {
			return true;
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-bind-native.js
var require_function_bind_native = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = !require_fails()(function() {
		var test = function() {}.bind();
		return typeof test != "function" || test.hasOwnProperty("prototype");
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-apply.js
var require_function_apply = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var NATIVE_BIND = require_function_bind_native();
	var FunctionPrototype = Function.prototype;
	var apply = FunctionPrototype.apply;
	var call = FunctionPrototype.call;
	module.exports = typeof Reflect == "object" && Reflect.apply || (NATIVE_BIND ? call.bind(apply) : function() {
		return call.apply(apply, arguments);
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-uncurry-this.js
var require_function_uncurry_this = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var NATIVE_BIND = require_function_bind_native();
	var FunctionPrototype = Function.prototype;
	var call = FunctionPrototype.call;
	var uncurryThisWithBind = NATIVE_BIND && FunctionPrototype.bind.bind(call, call);
	module.exports = NATIVE_BIND ? uncurryThisWithBind : function(fn) {
		return function() {
			return call.apply(fn, arguments);
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/classof-raw.js
var require_classof_raw = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var toString = uncurryThis({}.toString);
	var stringSlice = uncurryThis("".slice);
	module.exports = function(it) {
		return stringSlice(toString(it), 8, -1);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-uncurry-this-clause.js
var require_function_uncurry_this_clause = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classofRaw = require_classof_raw();
	var uncurryThis = require_function_uncurry_this();
	module.exports = function(fn) {
		if (classofRaw(fn) === "Function") return uncurryThis(fn);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-callable.js
var require_is_callable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var documentAll = typeof document == "object" && document.all;
	module.exports = typeof documentAll == "undefined" && documentAll !== void 0 ? function(argument) {
		return typeof argument == "function" || argument === documentAll;
	} : function(argument) {
		return typeof argument == "function";
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/descriptors.js
var require_descriptors = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = !require_fails()(function() {
		return Object.defineProperty({}, 1, { get: function() {
			return 7;
		} })[1] !== 7;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-call.js
var require_function_call = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var NATIVE_BIND = require_function_bind_native();
	var call = Function.prototype.call;
	module.exports = NATIVE_BIND ? call.bind(call) : function() {
		return call.apply(call, arguments);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-property-is-enumerable.js
var require_object_property_is_enumerable = /* @__PURE__ */ __commonJSMin(((exports) => {
	var $propertyIsEnumerable = {}.propertyIsEnumerable;
	var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
	exports.f = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({ 1: 2 }, 1) ? function propertyIsEnumerable(V) {
		var descriptor = getOwnPropertyDescriptor(this, V);
		return !!descriptor && descriptor.enumerable;
	} : $propertyIsEnumerable;
}));
//#endregion
//#region node_modules/core-js-pure/internals/create-property-descriptor.js
var require_create_property_descriptor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(bitmap, value) {
		return {
			enumerable: !(bitmap & 1),
			configurable: !(bitmap & 2),
			writable: !(bitmap & 4),
			value
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/indexed-object.js
var require_indexed_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var fails = require_fails();
	var classof = require_classof_raw();
	var $Object = Object;
	var split = uncurryThis("".split);
	module.exports = fails(function() {
		return !$Object("z").propertyIsEnumerable(0);
	}) ? function(it) {
		return classof(it) === "String" ? split(it, "") : $Object(it);
	} : $Object;
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-null-or-undefined.js
var require_is_null_or_undefined = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(it) {
		return it === null || it === void 0;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/require-object-coercible.js
var require_require_object_coercible = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isNullOrUndefined = require_is_null_or_undefined();
	var $TypeError = TypeError;
	module.exports = function(it) {
		if (isNullOrUndefined(it)) throw new $TypeError("Can't call method on " + it);
		return it;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-indexed-object.js
var require_to_indexed_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var IndexedObject = require_indexed_object();
	var requireObjectCoercible = require_require_object_coercible();
	module.exports = function(it) {
		return IndexedObject(requireObjectCoercible(it));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-object.js
var require_is_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isCallable = require_is_callable();
	module.exports = function(it) {
		return typeof it == "object" ? it !== null : isCallable(it);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/path.js
var require_path = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-built-in.js
var require_get_built_in = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var path = require_path();
	var globalThis = require_global_this$6();
	var isCallable = require_is_callable();
	var aFunction = function(variable) {
		return isCallable(variable) ? variable : void 0;
	};
	module.exports = function(namespace, method) {
		return arguments.length < 2 ? aFunction(path[namespace]) || aFunction(globalThis[namespace]) : path[namespace] && path[namespace][method] || globalThis[namespace] && globalThis[namespace][method];
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-is-prototype-of.js
var require_object_is_prototype_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_function_uncurry_this()({}.isPrototypeOf);
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-user-agent.js
var require_environment_user_agent = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var navigator = require_global_this$6().navigator;
	var userAgent = navigator && navigator.userAgent;
	module.exports = userAgent ? String(userAgent) : "";
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-v8-version.js
var require_environment_v8_version = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var userAgent = require_environment_user_agent();
	var process = globalThis.process;
	var Deno = globalThis.Deno;
	var versions = process && process.versions || Deno && Deno.version;
	var v8 = versions && versions.v8;
	var match;
	var version;
	if (v8) {
		match = v8.split(".");
		version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
	}
	if (!version && userAgent) {
		match = userAgent.match(/Edge\/(\d+)/);
		if (!match || match[1] >= 74) {
			match = userAgent.match(/Chrome\/(\d+)/);
			if (match) version = +match[1];
		}
	}
	module.exports = version;
}));
//#endregion
//#region node_modules/core-js-pure/internals/symbol-constructor-detection.js
var require_symbol_constructor_detection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var V8_VERSION = require_environment_v8_version();
	var fails = require_fails();
	var $String = require_global_this$6().String;
	module.exports = !!Object.getOwnPropertySymbols && !fails(function() {
		var symbol = Symbol("symbol detection");
		return !$String(symbol) || !(Object(symbol) instanceof Symbol) || !Symbol.sham && V8_VERSION && V8_VERSION < 41;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/use-symbol-as-uid.js
var require_use_symbol_as_uid = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_symbol_constructor_detection() && !Symbol.sham && typeof Symbol.iterator == "symbol";
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-symbol.js
var require_is_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var isCallable = require_is_callable();
	var isPrototypeOf = require_object_is_prototype_of();
	var USE_SYMBOL_AS_UID = require_use_symbol_as_uid();
	var $Object = Object;
	module.exports = USE_SYMBOL_AS_UID ? function(it) {
		return typeof it == "symbol";
	} : function(it) {
		var $Symbol = getBuiltIn("Symbol");
		return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/try-to-string.js
var require_try_to_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $String = String;
	module.exports = function(argument) {
		try {
			return $String(argument);
		} catch (error) {
			return "Object";
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-callable.js
var require_a_callable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isCallable = require_is_callable();
	var tryToString = require_try_to_string();
	var $TypeError = TypeError;
	module.exports = function(argument) {
		if (isCallable(argument)) return argument;
		throw new $TypeError(tryToString(argument) + " is not a function");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-method.js
var require_get_method = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aCallable = require_a_callable();
	var isNullOrUndefined = require_is_null_or_undefined();
	module.exports = function(V, P) {
		var func = V[P];
		return isNullOrUndefined(func) ? void 0 : aCallable(func);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/ordinary-to-primitive.js
var require_ordinary_to_primitive = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	var isCallable = require_is_callable();
	var isObject = require_is_object();
	var $TypeError = TypeError;
	module.exports = function(input, pref) {
		var fn, val;
		if (pref === "string" && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
		if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
		if (pref !== "string" && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
		throw new $TypeError("Can't convert object to primitive value");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-pure.js
var require_is_pure = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = true;
}));
//#endregion
//#region node_modules/core-js-pure/internals/define-global-property.js
var require_define_global_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var defineProperty = Object.defineProperty;
	module.exports = function(key, value) {
		try {
			defineProperty(globalThis, key, {
				value,
				configurable: true,
				writable: true
			});
		} catch (error) {
			globalThis[key] = value;
		}
		return value;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/shared-store.js
var require_shared_store = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var IS_PURE = require_is_pure();
	var globalThis = require_global_this$6();
	var defineGlobalProperty = require_define_global_property();
	var SHARED = "__core-js_shared__";
	var store = module.exports = globalThis[SHARED] || defineGlobalProperty(SHARED, {});
	(store.versions || (store.versions = [])).push({
		version: "3.50.0",
		mode: IS_PURE ? "pure" : "global",
		copyright: "© 2013–2025 Denis Pushkarev (zloirock.ru), 2025–2026 CoreJS Company (core-js.io). All rights reserved.",
		license: "https://github.com/zloirock/core-js/blob/v3.50.0/LICENSE",
		source: "https://github.com/zloirock/core-js"
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/shared.js
var require_shared = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var store = require_shared_store();
	var create = Object.create || Object;
	module.exports = function(key, value) {
		return store[key] || (store[key] = value || create(null));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-object.js
var require_to_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var requireObjectCoercible = require_require_object_coercible();
	var $Object = Object;
	module.exports = function(argument) {
		return $Object(requireObjectCoercible(argument));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/has-own-property.js
var require_has_own_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var toObject = require_to_object();
	var hasOwnProperty = uncurryThis({}.hasOwnProperty);
	module.exports = Object.hasOwn || function hasOwn(it, key) {
		return hasOwnProperty(toObject(it), key);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/uid.js
var require_uid = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var id = 0;
	var postfix = Math.random();
	var toString = uncurryThis(1.1.toString);
	module.exports = function(key) {
		return "Symbol(" + (key === void 0 ? "" : key) + ")_" + toString(++id + postfix, 36);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/well-known-symbol.js
var require_well_known_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var shared = require_shared();
	var hasOwn = require_has_own_property();
	var uid = require_uid();
	var NATIVE_SYMBOL = require_symbol_constructor_detection();
	var USE_SYMBOL_AS_UID = require_use_symbol_as_uid();
	var Symbol = globalThis.Symbol;
	var WellKnownSymbolsStore = shared("wks");
	var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol["for"] || Symbol : Symbol && Symbol.withoutSetter || uid;
	module.exports = function(name) {
		if (!hasOwn(WellKnownSymbolsStore, name)) WellKnownSymbolsStore[name] = NATIVE_SYMBOL && hasOwn(Symbol, name) ? Symbol[name] : createWellKnownSymbol("Symbol." + name);
		return WellKnownSymbolsStore[name];
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-primitive.js
var require_to_primitive$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	var isObject = require_is_object();
	var isSymbol = require_is_symbol();
	var getMethod = require_get_method();
	var ordinaryToPrimitive = require_ordinary_to_primitive();
	var wellKnownSymbol = require_well_known_symbol();
	var $TypeError = TypeError;
	var TO_PRIMITIVE = wellKnownSymbol("toPrimitive");
	module.exports = function(input, pref) {
		if (!isObject(input) || isSymbol(input)) return input;
		var exoticToPrim = getMethod(input, TO_PRIMITIVE);
		var result;
		if (exoticToPrim) {
			if (pref === void 0) pref = "default";
			result = call(exoticToPrim, input, pref);
			if (!isObject(result) || isSymbol(result)) return result;
			throw new $TypeError("Can't convert object to primitive value");
		}
		if (pref === void 0) pref = "number";
		return ordinaryToPrimitive(input, pref);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-property-key.js
var require_to_property_key = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toPrimitive = require_to_primitive$5();
	var isSymbol = require_is_symbol();
	module.exports = function(argument) {
		var key = toPrimitive(argument, "string");
		return isSymbol(key) ? key : key + "";
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/document-create-element.js
var require_document_create_element = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var isObject = require_is_object();
	var document = globalThis.document;
	var EXISTS = isObject(document) && isObject(document.createElement);
	module.exports = function(it) {
		return EXISTS ? document.createElement(it) : {};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/ie8-dom-define.js
var require_ie8_dom_define = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var fails = require_fails();
	var createElement = require_document_create_element();
	module.exports = !DESCRIPTORS && !fails(function() {
		return Object.defineProperty(createElement("div"), "a", { get: function() {
			return 7;
		} }).a !== 7;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-get-own-property-descriptor.js
var require_object_get_own_property_descriptor = /* @__PURE__ */ __commonJSMin(((exports) => {
	var DESCRIPTORS = require_descriptors();
	var call = require_function_call();
	var propertyIsEnumerableModule = require_object_property_is_enumerable();
	var createPropertyDescriptor = require_create_property_descriptor();
	var toIndexedObject = require_to_indexed_object();
	var toPropertyKey = require_to_property_key();
	var hasOwn = require_has_own_property();
	var IE8_DOM_DEFINE = require_ie8_dom_define();
	var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
	exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
		O = toIndexedObject(O);
		P = toPropertyKey(P);
		if (IE8_DOM_DEFINE) try {
			return $getOwnPropertyDescriptor(O, P);
		} catch (error) {}
		if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-forced.js
var require_is_forced = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	var isCallable = require_is_callable();
	var replacement = /#|\.prototype\./;
	var isForced = function(feature, detection) {
		var value = data[normalize(feature)];
		return value === POLYFILL ? true : value === NATIVE ? false : isCallable(detection) ? fails(detection) : !!detection;
	};
	var normalize = isForced.normalize = function(string) {
		return String(string).replace(replacement, ".").toLowerCase();
	};
	var data = isForced.data = {};
	var NATIVE = isForced.NATIVE = "N";
	var POLYFILL = isForced.POLYFILL = "P";
	module.exports = isForced;
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-bind-context.js
var require_function_bind_context = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this_clause();
	var aCallable = require_a_callable();
	var NATIVE_BIND = require_function_bind_native();
	var bind = uncurryThis(uncurryThis.bind);
	module.exports = function(fn, that) {
		aCallable(fn);
		return that === void 0 ? fn : NATIVE_BIND ? bind(fn, that) : function() {
			return fn.apply(that, arguments);
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/v8-prototype-define-bug.js
var require_v8_prototype_define_bug = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var fails = require_fails();
	module.exports = DESCRIPTORS && fails(function() {
		return Object.defineProperty(function() {}, "prototype", {
			value: 42,
			writable: false
		}).prototype !== 42;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/an-object.js
var require_an_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	var $String = String;
	var $TypeError = TypeError;
	module.exports = function(argument) {
		if (isObject(argument)) return argument;
		throw new $TypeError($String(argument) + " is not an object");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-define-property.js
var require_object_define_property = /* @__PURE__ */ __commonJSMin(((exports) => {
	var DESCRIPTORS = require_descriptors();
	var IE8_DOM_DEFINE = require_ie8_dom_define();
	var V8_PROTOTYPE_DEFINE_BUG = require_v8_prototype_define_bug();
	var anObject = require_an_object();
	var toPropertyKey = require_to_property_key();
	var $TypeError = TypeError;
	var $defineProperty = Object.defineProperty;
	var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
	var ENUMERABLE = "enumerable";
	var CONFIGURABLE = "configurable";
	var WRITABLE = "writable";
	exports.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
		anObject(O);
		P = toPropertyKey(P);
		anObject(Attributes);
		if (typeof O === "function" && P === "prototype" && "value" in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
			var current = $getOwnPropertyDescriptor(O, P);
			if (current && current[WRITABLE]) {
				O[P] = Attributes.value;
				Attributes = {
					configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
					enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
					writable: false
				};
			}
		}
		return $defineProperty(O, P, Attributes);
	} : $defineProperty : function defineProperty(O, P, Attributes) {
		anObject(O);
		P = toPropertyKey(P);
		anObject(Attributes);
		if (IE8_DOM_DEFINE) try {
			return $defineProperty(O, P, Attributes);
		} catch (error) {}
		if ("get" in Attributes || "set" in Attributes) throw new $TypeError("Accessors not supported");
		if ("value" in Attributes) O[P] = Attributes.value;
		return O;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/create-non-enumerable-property.js
var require_create_non_enumerable_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var definePropertyModule = require_object_define_property();
	var createPropertyDescriptor = require_create_property_descriptor();
	module.exports = DESCRIPTORS ? function(object, key, value) {
		return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
	} : function(object, key, value) {
		object[key] = value;
		return object;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/export.js
var require_export = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var apply = require_function_apply();
	var uncurryThis = require_function_uncurry_this_clause();
	var isCallable = require_is_callable();
	var getOwnPropertyDescriptor = require_object_get_own_property_descriptor().f;
	var isForced = require_is_forced();
	var path = require_path();
	var bind = require_function_bind_context();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var hasOwn = require_has_own_property();
	require_shared_store();
	var wrapConstructor = function(NativeConstructor) {
		var Wrapper = function(a, b, c) {
			if (this instanceof Wrapper) {
				switch (arguments.length) {
					case 0: return new NativeConstructor();
					case 1: return new NativeConstructor(a);
					case 2: return new NativeConstructor(a, b);
				}
				return new NativeConstructor(a, b, c);
			}
			return apply(NativeConstructor, this, arguments);
		};
		Wrapper.prototype = NativeConstructor.prototype;
		return Wrapper;
	};
	module.exports = function(options, source) {
		var TARGET = options.target;
		var GLOBAL = options.global;
		var STATIC = options.stat;
		var PROTO = options.proto;
		var nativeSource = GLOBAL ? globalThis : STATIC ? globalThis[TARGET] : globalThis[TARGET] && globalThis[TARGET].prototype;
		var target = GLOBAL ? path : path[TARGET] || createNonEnumerableProperty(path, TARGET, {})[TARGET];
		var targetPrototype = target.prototype;
		var FORCED, USE_NATIVE, VIRTUAL_PROTOTYPE;
		var key, sourceProperty, targetProperty, nativeProperty, resultProperty, descriptor;
		for (key in source) {
			FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? "." : "#") + key, options.forced);
			USE_NATIVE = !FORCED && nativeSource && hasOwn(nativeSource, key);
			targetProperty = target[key];
			if (USE_NATIVE) if (options.dontCallGetSet) {
				descriptor = getOwnPropertyDescriptor(nativeSource, key);
				nativeProperty = descriptor && descriptor.value;
			} else nativeProperty = nativeSource[key];
			sourceProperty = USE_NATIVE && nativeProperty ? nativeProperty : source[key];
			if (!FORCED && !PROTO && typeof targetProperty == typeof sourceProperty) continue;
			if (options.bind && USE_NATIVE) resultProperty = bind(sourceProperty, globalThis);
			else if (options.wrap && USE_NATIVE) resultProperty = wrapConstructor(sourceProperty);
			else if (PROTO && isCallable(sourceProperty)) resultProperty = uncurryThis(sourceProperty);
			else resultProperty = sourceProperty;
			if (options.sham || sourceProperty && sourceProperty.sham || targetProperty && targetProperty.sham) createNonEnumerableProperty(resultProperty, "sham", true);
			createNonEnumerableProperty(target, key, resultProperty);
			if (PROTO) {
				VIRTUAL_PROTOTYPE = TARGET + "Prototype";
				if (!hasOwn(path, VIRTUAL_PROTOTYPE)) createNonEnumerableProperty(path, VIRTUAL_PROTOTYPE, {});
				createNonEnumerableProperty(path[VIRTUAL_PROTOTYPE], key, sourceProperty);
				if (options.real && targetPrototype && (FORCED || !targetPrototype[key])) createNonEnumerableProperty(targetPrototype, key, sourceProperty);
			}
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/math-trunc.js
var require_math_trunc = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ceil = Math.ceil;
	var floor = Math.floor;
	module.exports = Math.trunc || function trunc(x) {
		var n = +x;
		return (n > 0 ? floor : ceil)(n);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-integer-or-infinity.js
var require_to_integer_or_infinity = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var trunc = require_math_trunc();
	module.exports = function(argument) {
		var number = +argument;
		return number !== number || number === 0 ? 0 : trunc(number);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-absolute-index.js
var require_to_absolute_index = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var max = Math.max;
	var min = Math.min;
	module.exports = function(index, length) {
		var integer = toIntegerOrInfinity(index);
		return integer < 0 ? max(integer + length, 0) : min(integer, length);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-length.js
var require_to_length = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var min = Math.min;
	module.exports = function(argument) {
		var len = toIntegerOrInfinity(argument);
		return len > 0 ? min(len, 9007199254740991) : 0;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/length-of-array-like.js
var require_length_of_array_like = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toLength = require_to_length();
	module.exports = function(obj) {
		return toLength(obj.length);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-includes.js
var require_array_includes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toIndexedObject = require_to_indexed_object();
	var toAbsoluteIndex = require_to_absolute_index();
	var lengthOfArrayLike = require_length_of_array_like();
	var createMethod = function(IS_INCLUDES) {
		return function($this, el, fromIndex) {
			var O = toIndexedObject($this);
			var length = lengthOfArrayLike(O);
			if (length === 0) return !IS_INCLUDES && -1;
			var index = toAbsoluteIndex(fromIndex, length);
			var value;
			if (IS_INCLUDES && el !== el) while (length > index) {
				value = O[index++];
				if (value !== value) return true;
			}
			else for (; length > index; index++) if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
			return !IS_INCLUDES && -1;
		};
	};
	module.exports = {
		includes: createMethod(true),
		indexOf: createMethod(false)
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/hidden-keys.js
var require_hidden_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-keys-internal.js
var require_object_keys_internal = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var hasOwn = require_has_own_property();
	var toIndexedObject = require_to_indexed_object();
	var indexOf = require_array_includes().indexOf;
	var hiddenKeys = require_hidden_keys();
	var push = uncurryThis([].push);
	module.exports = function(object, names) {
		var O = toIndexedObject(object);
		var i = 0;
		var result = [];
		var key;
		for (key in O) !hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
		while (names.length > i) if (hasOwn(O, key = names[i++])) ~indexOf(result, key) || push(result, key);
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/enum-bug-keys.js
var require_enum_bug_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = [
		"constructor",
		"hasOwnProperty",
		"isPrototypeOf",
		"propertyIsEnumerable",
		"toLocaleString",
		"toString",
		"valueOf"
	];
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-keys.js
var require_object_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var internalObjectKeys = require_object_keys_internal();
	var enumBugKeys = require_enum_bug_keys();
	module.exports = Object.keys || function keys(O) {
		return internalObjectKeys(O, enumBugKeys);
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.keys.js
var require_es_object_keys = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var toObject = require_to_object();
	var nativeKeys = require_object_keys();
	$({
		target: "Object",
		stat: true,
		forced: require_fails()(function() {
			nativeKeys(1);
		})
	}, { keys: function keys(it) {
		return nativeKeys(toObject(it));
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/keys.js
var require_keys$6 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_keys();
	module.exports = require_path().Object.keys;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/keys.js
var require_keys$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_keys$6();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/keys.js
var require_keys$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_keys$5();
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-string-tag-support.js
var require_to_string_tag_support = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TO_STRING_TAG = require_well_known_symbol()("toStringTag");
	var test = {};
	test[TO_STRING_TAG] = "z";
	module.exports = String(test) === "[object z]";
}));
//#endregion
//#region node_modules/core-js-pure/internals/classof.js
var require_classof = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TO_STRING_TAG_SUPPORT = require_to_string_tag_support();
	var isCallable = require_is_callable();
	var classofRaw = require_classof_raw();
	var TO_STRING_TAG = require_well_known_symbol()("toStringTag");
	var $Object = Object;
	var CORRECT_ARGUMENTS = classofRaw(function() {
		return arguments;
	}()) === "Arguments";
	var tryGet = function(it, key) {
		try {
			return it[key];
		} catch (error) {}
	};
	module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function(it) {
		var O, tag, result;
		return it === void 0 ? "Undefined" : it === null ? "Null" : typeof (tag = tryGet(O = $Object(it), TO_STRING_TAG)) == "string" ? tag : CORRECT_ARGUMENTS ? classofRaw(O) : (result = classofRaw(O)) === "Object" && isCallable(O.callee) ? "Arguments" : result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/to-string.js
var require_to_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classof = require_classof();
	var $String = String;
	module.exports = function(argument) {
		if (classof(argument) === "Symbol") throw new TypeError("Cannot convert a Symbol value to a string");
		return $String(argument);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-define-properties.js
var require_object_define_properties = /* @__PURE__ */ __commonJSMin(((exports) => {
	var DESCRIPTORS = require_descriptors();
	var V8_PROTOTYPE_DEFINE_BUG = require_v8_prototype_define_bug();
	var definePropertyModule = require_object_define_property();
	var anObject = require_an_object();
	var toIndexedObject = require_to_indexed_object();
	var objectKeys = require_object_keys();
	exports.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
		anObject(O);
		var props = toIndexedObject(Properties);
		var keys = objectKeys(Properties);
		var length = keys.length;
		var index = 0;
		var key;
		while (length > index) definePropertyModule.f(O, key = keys[index++], props[key]);
		return O;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/html.js
var require_html = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_built_in()("document", "documentElement");
}));
//#endregion
//#region node_modules/core-js-pure/internals/shared-key.js
var require_shared_key = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var shared = require_shared();
	var uid = require_uid();
	var keys = shared("keys");
	module.exports = function(key) {
		return keys[key] || (keys[key] = uid(key));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-create.js
var require_object_create = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var anObject = require_an_object();
	var definePropertiesModule = require_object_define_properties();
	var enumBugKeys = require_enum_bug_keys();
	var hiddenKeys = require_hidden_keys();
	var html = require_html();
	var documentCreateElement = require_document_create_element();
	var sharedKey = require_shared_key();
	var GT = ">";
	var LT = "<";
	var PROTOTYPE = "prototype";
	var SCRIPT = "script";
	var IE_PROTO = sharedKey("IE_PROTO");
	var EmptyConstructor = function() {};
	var scriptTag = function(content) {
		return LT + SCRIPT + GT + content + LT + "/" + SCRIPT + GT;
	};
	var NullProtoObjectViaActiveX = function(activeXDocument) {
		activeXDocument.write(scriptTag(""));
		activeXDocument.close();
		var temp = activeXDocument.parentWindow.Object;
		activeXDocument = null;
		return temp;
	};
	var NullProtoObjectViaIFrame = function() {
		var iframe = documentCreateElement("iframe");
		var JS = "java" + SCRIPT + ":";
		var iframeDocument;
		iframe.style.display = "none";
		html.appendChild(iframe);
		iframe.src = String(JS);
		iframeDocument = iframe.contentWindow.document;
		iframeDocument.open();
		iframeDocument.write(scriptTag("document.F=Object"));
		iframeDocument.close();
		return iframeDocument.F;
	};
	var activeXDocument;
	var NullProtoObject = function() {
		try {
			activeXDocument = new ActiveXObject("htmlfile");
		} catch (error) {}
		NullProtoObject = typeof document != "undefined" ? document.domain && activeXDocument ? NullProtoObjectViaActiveX(activeXDocument) : NullProtoObjectViaIFrame() : NullProtoObjectViaActiveX(activeXDocument);
		var length = enumBugKeys.length;
		while (length--) delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
		return NullProtoObject();
	};
	hiddenKeys[IE_PROTO] = true;
	module.exports = Object.create || function create(O, Properties) {
		var result;
		if (O !== null) {
			EmptyConstructor[PROTOTYPE] = anObject(O);
			result = new EmptyConstructor();
			EmptyConstructor[PROTOTYPE] = null;
			result[IE_PROTO] = O;
		} else result = NullProtoObject();
		return Properties === void 0 ? result : definePropertiesModule.f(result, Properties);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-get-own-property-names.js
var require_object_get_own_property_names = /* @__PURE__ */ __commonJSMin(((exports) => {
	var internalObjectKeys = require_object_keys_internal();
	var hiddenKeys = require_enum_bug_keys().concat("length", "prototype");
	exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
		return internalObjectKeys(O, hiddenKeys);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-slice.js
var require_array_slice = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_function_uncurry_this()([].slice);
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-get-own-property-names-external.js
var require_object_get_own_property_names_external = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classof = require_classof_raw();
	var toIndexedObject = require_to_indexed_object();
	var $getOwnPropertyNames = require_object_get_own_property_names().f;
	var arraySlice = require_array_slice();
	var windowNames = typeof window == "object" && window && Object.getOwnPropertyNames ? Object.getOwnPropertyNames(window) : [];
	var getWindowNames = function(it) {
		try {
			return $getOwnPropertyNames(it);
		} catch (error) {
			return arraySlice(windowNames);
		}
	};
	module.exports.f = function getOwnPropertyNames(it) {
		return windowNames && classof(it) === "Window" ? getWindowNames(it) : $getOwnPropertyNames(toIndexedObject(it));
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-get-own-property-symbols.js
var require_object_get_own_property_symbols = /* @__PURE__ */ __commonJSMin(((exports) => {
	exports.f = Object.getOwnPropertySymbols;
}));
//#endregion
//#region node_modules/core-js-pure/internals/define-built-in.js
var require_define_built_in = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	module.exports = function(target, key, value, options) {
		if (options && options.enumerable) target[key] = value;
		else createNonEnumerableProperty(target, key, value);
		return target;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/define-built-in-accessor.js
var require_define_built_in_accessor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var defineProperty = require_object_define_property();
	module.exports = function(target, name, descriptor) {
		return defineProperty.f(target, name, descriptor);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/well-known-symbol-wrapped.js
var require_well_known_symbol_wrapped = /* @__PURE__ */ __commonJSMin(((exports) => {
	exports.f = require_well_known_symbol();
}));
//#endregion
//#region node_modules/core-js-pure/internals/well-known-symbol-define.js
var require_well_known_symbol_define = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var path = require_path();
	var hasOwn = require_has_own_property();
	var wrappedWellKnownSymbolModule = require_well_known_symbol_wrapped();
	var defineProperty = require_object_define_property().f;
	module.exports = function(NAME) {
		var Symbol = path.Symbol || (path.Symbol = {});
		if (!hasOwn(Symbol, NAME)) defineProperty(Symbol, NAME, { value: wrappedWellKnownSymbolModule.f(NAME) });
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/symbol-define-to-primitive.js
var require_symbol_define_to_primitive = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	var getBuiltIn = require_get_built_in();
	var wellKnownSymbol = require_well_known_symbol();
	var defineBuiltIn = require_define_built_in();
	module.exports = function() {
		var Symbol = getBuiltIn("Symbol");
		var SymbolPrototype = Symbol && Symbol.prototype;
		var valueOf = SymbolPrototype && SymbolPrototype.valueOf;
		var TO_PRIMITIVE = wellKnownSymbol("toPrimitive");
		if (SymbolPrototype && !SymbolPrototype[TO_PRIMITIVE]) defineBuiltIn(SymbolPrototype, TO_PRIMITIVE, function(hint) {
			return call(valueOf, this);
		}, { arity: 1 });
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-to-string.js
var require_object_to_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TO_STRING_TAG_SUPPORT = require_to_string_tag_support();
	var classof = require_classof();
	module.exports = TO_STRING_TAG_SUPPORT ? {}.toString : function toString() {
		return "[object " + classof(this) + "]";
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-to-string-tag.js
var require_set_to_string_tag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TO_STRING_TAG_SUPPORT = require_to_string_tag_support();
	var defineProperty = require_object_define_property().f;
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var hasOwn = require_has_own_property();
	var toString = require_object_to_string();
	var TO_STRING_TAG = require_well_known_symbol()("toStringTag");
	module.exports = function(it, TAG, STATIC, SET_METHOD) {
		var target = STATIC ? it : it && it.prototype;
		if (target) {
			if (!hasOwn(target, TO_STRING_TAG)) defineProperty(target, TO_STRING_TAG, {
				configurable: true,
				value: TAG
			});
			if (SET_METHOD && !TO_STRING_TAG_SUPPORT) createNonEnumerableProperty(target, "toString", toString);
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/weak-map-basic-detection.js
var require_weak_map_basic_detection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var isCallable = require_is_callable();
	var WeakMap = globalThis.WeakMap;
	module.exports = isCallable(WeakMap) && /native code/.test(String(WeakMap));
}));
//#endregion
//#region node_modules/core-js-pure/internals/internal-state.js
var require_internal_state = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var NATIVE_WEAK_MAP = require_weak_map_basic_detection();
	var globalThis = require_global_this$6();
	var isObject = require_is_object();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var hasOwn = require_has_own_property();
	var shared = require_shared_store();
	var sharedKey = require_shared_key();
	var hiddenKeys = require_hidden_keys();
	var OBJECT_ALREADY_INITIALIZED = "Object already initialized";
	var TypeError = globalThis.TypeError;
	var WeakMap = globalThis.WeakMap;
	var set;
	var get;
	var has;
	var enforce = function(it) {
		return has(it) ? get(it) : set(it, {});
	};
	var getterFor = function(TYPE) {
		return function(it) {
			var state;
			if (!isObject(it) || (state = get(it)).type !== TYPE) throw new TypeError("Incompatible receiver, " + TYPE + " required");
			return state;
		};
	};
	if (NATIVE_WEAK_MAP || shared.state) {
		var store = shared.state || (shared.state = new WeakMap());
		store.get = store.get;
		store.has = store.has;
		store.set = store.set;
		set = function(it, metadata) {
			if (store.has(it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
			metadata.facade = it;
			store.set(it, metadata);
			return metadata;
		};
		get = function(it) {
			return store.get(it) || {};
		};
		has = function(it) {
			return store.has(it);
		};
	} else {
		var STATE = sharedKey("state");
		hiddenKeys[STATE] = true;
		set = function(it, metadata) {
			if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
			metadata.facade = it;
			createNonEnumerableProperty(it, STATE, metadata);
			return metadata;
		};
		get = function(it) {
			return hasOwn(it, STATE) ? it[STATE] : {};
		};
		has = function(it) {
			return hasOwn(it, STATE);
		};
	}
	module.exports = {
		set,
		get,
		has,
		enforce,
		getterFor
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-array.js
var require_is_array$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classof = require_classof_raw();
	module.exports = Array.isArray || function isArray(argument) {
		return classof(argument) === "Array";
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/inspect-source.js
var require_inspect_source = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var isCallable = require_is_callable();
	var store = require_shared_store();
	var functionToString = uncurryThis(Function.toString);
	if (!isCallable(store.inspectSource)) store.inspectSource = function(it) {
		return functionToString(it);
	};
	module.exports = store.inspectSource;
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-constructor.js
var require_is_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var fails = require_fails();
	var isCallable = require_is_callable();
	var classof = require_classof();
	var getBuiltIn = require_get_built_in();
	var inspectSource = require_inspect_source();
	var noop = function() {};
	var construct = getBuiltIn("Reflect", "construct");
	var constructorRegExp = /^\s*(?:class|function)\b/;
	var exec = uncurryThis(constructorRegExp.exec);
	var INCORRECT_TO_STRING = !constructorRegExp.test(noop);
	var isConstructorModern = function isConstructor(argument) {
		if (!isCallable(argument)) return false;
		try {
			construct(noop, [], argument);
			return true;
		} catch (error) {
			return false;
		}
	};
	var isConstructorLegacy = function isConstructor(argument) {
		if (!isCallable(argument)) return false;
		switch (classof(argument)) {
			case "AsyncFunction":
			case "GeneratorFunction":
			case "AsyncGeneratorFunction": return false;
		}
		try {
			return INCORRECT_TO_STRING || !!exec(constructorRegExp, inspectSource(argument));
		} catch (error) {
			return true;
		}
	};
	isConstructorLegacy.sham = true;
	module.exports = !construct || fails(function() {
		var called;
		return isConstructorModern(isConstructorModern.call) || !isConstructorModern(Object) || !isConstructorModern(function() {
			called = true;
		}) || called;
	}) ? isConstructorLegacy : isConstructorModern;
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-species-constructor.js
var require_array_species_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isArray = require_is_array$3();
	var isConstructor = require_is_constructor();
	var isObject = require_is_object();
	var SPECIES = require_well_known_symbol()("species");
	var $Array = Array;
	module.exports = function(originalArray) {
		var C;
		if (isArray(originalArray)) {
			C = originalArray.constructor;
			if (isConstructor(C) && (C === $Array || isArray(C.prototype))) C = void 0;
			else if (isObject(C)) {
				C = C[SPECIES];
				if (C === null) C = void 0;
			}
		}
		return C === void 0 ? $Array : C;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-species-create.js
var require_array_species_create = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arraySpeciesConstructor = require_array_species_constructor();
	module.exports = function(originalArray, length) {
		return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/create-property.js
var require_create_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var definePropertyModule = require_object_define_property();
	var createPropertyDescriptor = require_create_property_descriptor();
	module.exports = function(object, key, value) {
		if (DESCRIPTORS) definePropertyModule.f(object, key, createPropertyDescriptor(0, value));
		else object[key] = value;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-iteration.js
var require_array_iteration = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var bind = require_function_bind_context();
	var IndexedObject = require_indexed_object();
	var toObject = require_to_object();
	var lengthOfArrayLike = require_length_of_array_like();
	var arraySpeciesCreate = require_array_species_create();
	var createProperty = require_create_property();
	var createMethod = function(TYPE) {
		var IS_MAP = TYPE === 1;
		var IS_FILTER = TYPE === 2;
		var IS_SOME = TYPE === 3;
		var IS_EVERY = TYPE === 4;
		var IS_FIND_INDEX = TYPE === 6;
		var IS_FILTER_REJECT = TYPE === 7;
		var NO_HOLES = TYPE === 5 || IS_FIND_INDEX;
		return function($this, callbackfn, that) {
			var O = toObject($this);
			var self = IndexedObject(O);
			var length = lengthOfArrayLike(self);
			var boundFunction = bind(callbackfn, that);
			var index = 0;
			var resIndex = 0;
			var target = IS_MAP ? arraySpeciesCreate($this, length) : IS_FILTER || IS_FILTER_REJECT ? arraySpeciesCreate($this, 0) : void 0;
			var value, result;
			for (; length > index; index++) if (NO_HOLES || index in self) {
				value = self[index];
				result = boundFunction(value, index, O);
				if (TYPE) {
					if (IS_MAP) createProperty(target, index, result);
					else if (result) switch (TYPE) {
						case 3: return true;
						case 5: return value;
						case 6: return index;
						case 2: createProperty(target, resIndex++, value);
					}
					else switch (TYPE) {
						case 4: return false;
						case 7: createProperty(target, resIndex++, value);
					}
				}
			}
			return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
		};
	};
	module.exports = {
		forEach: createMethod(0),
		map: createMethod(1),
		filter: createMethod(2),
		some: createMethod(3),
		every: createMethod(4),
		find: createMethod(5),
		findIndex: createMethod(6),
		filterReject: createMethod(7)
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.constructor.js
var require_es_symbol_constructor = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	var call = require_function_call();
	var uncurryThis = require_function_uncurry_this();
	var IS_PURE = require_is_pure();
	var DESCRIPTORS = require_descriptors();
	var NATIVE_SYMBOL = require_symbol_constructor_detection();
	var fails = require_fails();
	var hasOwn = require_has_own_property();
	var isPrototypeOf = require_object_is_prototype_of();
	var anObject = require_an_object();
	var toIndexedObject = require_to_indexed_object();
	var toPropertyKey = require_to_property_key();
	var $toString = require_to_string();
	var createPropertyDescriptor = require_create_property_descriptor();
	var nativeObjectCreate = require_object_create();
	var objectKeys = require_object_keys();
	var getOwnPropertyNamesModule = require_object_get_own_property_names();
	var getOwnPropertyNamesExternal = require_object_get_own_property_names_external();
	var getOwnPropertySymbolsModule = require_object_get_own_property_symbols();
	var getOwnPropertyDescriptorModule = require_object_get_own_property_descriptor();
	var definePropertyModule = require_object_define_property();
	var definePropertiesModule = require_object_define_properties();
	var propertyIsEnumerableModule = require_object_property_is_enumerable();
	var defineBuiltIn = require_define_built_in();
	var defineBuiltInAccessor = require_define_built_in_accessor();
	var shared = require_shared();
	var sharedKey = require_shared_key();
	var hiddenKeys = require_hidden_keys();
	var uid = require_uid();
	var wellKnownSymbol = require_well_known_symbol();
	var wrappedWellKnownSymbolModule = require_well_known_symbol_wrapped();
	var defineWellKnownSymbol = require_well_known_symbol_define();
	var defineSymbolToPrimitive = require_symbol_define_to_primitive();
	var setToStringTag = require_set_to_string_tag();
	var InternalStateModule = require_internal_state();
	var $forEach = require_array_iteration().forEach;
	var HIDDEN = sharedKey("hidden");
	var SYMBOL = "Symbol";
	var PROTOTYPE = "prototype";
	var setInternalState = InternalStateModule.set;
	var getInternalState = InternalStateModule.getterFor(SYMBOL);
	var ObjectPrototype = Object[PROTOTYPE];
	var $Symbol = globalThis.Symbol;
	var SymbolPrototype = $Symbol && $Symbol[PROTOTYPE];
	var RangeError = globalThis.RangeError;
	var TypeError = globalThis.TypeError;
	var QObject = globalThis.QObject;
	var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
	var nativeDefineProperty = definePropertyModule.f;
	var nativeGetOwnPropertyNames = getOwnPropertyNamesExternal.f;
	var nativePropertyIsEnumerable = propertyIsEnumerableModule.f;
	var push = uncurryThis([].push);
	var AllSymbols = shared("symbols");
	var ObjectPrototypeSymbols = shared("op-symbols");
	var WellKnownSymbolsStore = shared("wks");
	var USE_SETTER = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;
	var fallbackDefineProperty = function(O, P, Attributes) {
		var ObjectPrototypeDescriptor = nativeGetOwnPropertyDescriptor(ObjectPrototype, P);
		if (ObjectPrototypeDescriptor) delete ObjectPrototype[P];
		nativeDefineProperty(O, P, Attributes);
		if (ObjectPrototypeDescriptor && O !== ObjectPrototype) nativeDefineProperty(ObjectPrototype, P, ObjectPrototypeDescriptor);
		return O;
	};
	var setSymbolDescriptor = DESCRIPTORS && fails(function() {
		return nativeObjectCreate(nativeDefineProperty({}, "a", { get: function() {
			return nativeDefineProperty(this, "a", { value: 7 }).a;
		} })).a !== 7;
	}) ? fallbackDefineProperty : nativeDefineProperty;
	var wrap = function(tag, description) {
		var symbol = AllSymbols[tag] = nativeObjectCreate(SymbolPrototype);
		setInternalState(symbol, {
			type: SYMBOL,
			tag,
			description
		});
		if (!DESCRIPTORS) symbol.description = description;
		return symbol;
	};
	var $defineProperty = function defineProperty(O, P, Attributes) {
		if (O === ObjectPrototype) $defineProperty(ObjectPrototypeSymbols, P, Attributes);
		anObject(O);
		var key = toPropertyKey(P);
		anObject(Attributes);
		if (hasOwn(AllSymbols, key)) {
			if (!("enumerable" in Attributes) ? !hasOwn(O, key) || hasOwn(O, HIDDEN) && O[HIDDEN][key] : !Attributes.enumerable) {
				if (!hasOwn(O, HIDDEN)) nativeDefineProperty(O, HIDDEN, createPropertyDescriptor(1, nativeObjectCreate(null)));
				O[HIDDEN][key] = true;
			} else {
				if (hasOwn(O, HIDDEN) && O[HIDDEN][key]) O[HIDDEN][key] = false;
				Attributes = nativeObjectCreate(Attributes, { enumerable: createPropertyDescriptor(0, false) });
			}
			return setSymbolDescriptor(O, key, Attributes);
		}
		return nativeDefineProperty(O, key, Attributes);
	};
	var $defineProperties = function defineProperties(O, Properties) {
		anObject(O);
		var properties = toIndexedObject(Properties);
		$forEach(objectKeys(properties).concat($getOwnPropertySymbols(properties)), function(key) {
			if (!DESCRIPTORS || call($propertyIsEnumerable, properties, key)) $defineProperty(O, key, properties[key]);
		});
		return O;
	};
	var $create = function create(O, Properties) {
		return Properties === void 0 ? nativeObjectCreate(O) : $defineProperties(nativeObjectCreate(O), Properties);
	};
	var $propertyIsEnumerable = function propertyIsEnumerable(V) {
		var P = toPropertyKey(V);
		var enumerable = call(nativePropertyIsEnumerable, this, P);
		if (this === ObjectPrototype && hasOwn(AllSymbols, P) && !hasOwn(ObjectPrototypeSymbols, P)) return false;
		return enumerable || !hasOwn(this, P) || !hasOwn(AllSymbols, P) || hasOwn(this, HIDDEN) && this[HIDDEN][P] ? enumerable : true;
	};
	var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(O, P) {
		var it = toIndexedObject(O);
		var key = toPropertyKey(P);
		if (it === ObjectPrototype && hasOwn(AllSymbols, key) && !hasOwn(ObjectPrototypeSymbols, key)) return;
		var descriptor = nativeGetOwnPropertyDescriptor(it, key);
		if (descriptor && hasOwn(AllSymbols, key) && !(hasOwn(it, HIDDEN) && it[HIDDEN][key])) descriptor.enumerable = true;
		return descriptor;
	};
	var $getOwnPropertyNames = function getOwnPropertyNames(O) {
		var names = nativeGetOwnPropertyNames(toIndexedObject(O));
		var result = [];
		$forEach(names, function(key) {
			if (!hasOwn(AllSymbols, key) && !hasOwn(hiddenKeys, key)) push(result, key);
		});
		return result;
	};
	var $getOwnPropertySymbols = function(O) {
		var IS_OBJECT_PROTOTYPE = O === ObjectPrototype;
		var names = nativeGetOwnPropertyNames(IS_OBJECT_PROTOTYPE ? ObjectPrototypeSymbols : toIndexedObject(O));
		var result = [];
		$forEach(names, function(key) {
			if (hasOwn(AllSymbols, key) && (!IS_OBJECT_PROTOTYPE || hasOwn(ObjectPrototype, key))) push(result, AllSymbols[key]);
		});
		return result;
	};
	if (!NATIVE_SYMBOL) {
		$Symbol = function Symbol() {
			if (isPrototypeOf(SymbolPrototype, this)) throw new TypeError("Symbol is not a constructor");
			var description = !arguments.length || arguments[0] === void 0 ? void 0 : $toString(arguments[0]);
			var tag = uid(description);
			var setter = function(value) {
				var $this = this === void 0 ? globalThis : this;
				if ($this === ObjectPrototype) call(setter, ObjectPrototypeSymbols, value);
				if (hasOwn($this, HIDDEN) && hasOwn($this[HIDDEN], tag)) $this[HIDDEN][tag] = false;
				var descriptor = createPropertyDescriptor(1, value);
				try {
					setSymbolDescriptor($this, tag, descriptor);
				} catch (error) {
					if (!(error instanceof RangeError)) throw error;
					fallbackDefineProperty($this, tag, descriptor);
				}
			};
			if (DESCRIPTORS && USE_SETTER) setSymbolDescriptor(ObjectPrototype, tag, {
				configurable: true,
				set: setter
			});
			return wrap(tag, description);
		};
		SymbolPrototype = $Symbol[PROTOTYPE];
		defineBuiltIn(SymbolPrototype, "toString", function toString() {
			return getInternalState(this).tag;
		});
		defineBuiltIn($Symbol, "withoutSetter", function(description) {
			return wrap(uid(description), description);
		});
		propertyIsEnumerableModule.f = $propertyIsEnumerable;
		definePropertyModule.f = $defineProperty;
		definePropertiesModule.f = $defineProperties;
		getOwnPropertyDescriptorModule.f = $getOwnPropertyDescriptor;
		getOwnPropertyNamesModule.f = getOwnPropertyNamesExternal.f = $getOwnPropertyNames;
		getOwnPropertySymbolsModule.f = $getOwnPropertySymbols;
		wrappedWellKnownSymbolModule.f = function(name) {
			return wrap(wellKnownSymbol(name), name);
		};
		if (DESCRIPTORS) {
			defineBuiltInAccessor(SymbolPrototype, "description", {
				configurable: true,
				get: function description() {
					return getInternalState(this).description;
				}
			});
			if (!IS_PURE) defineBuiltIn(ObjectPrototype, "propertyIsEnumerable", $propertyIsEnumerable, { unsafe: true });
		}
	}
	$({
		global: true,
		constructor: true,
		wrap: true,
		forced: !NATIVE_SYMBOL,
		sham: !NATIVE_SYMBOL
	}, { Symbol: $Symbol });
	$forEach(objectKeys(WellKnownSymbolsStore), function(name) {
		defineWellKnownSymbol(name);
	});
	$({
		target: SYMBOL,
		stat: true,
		forced: !NATIVE_SYMBOL
	}, {
		useSetter: function() {
			USE_SETTER = true;
		},
		useSimple: function() {
			USE_SETTER = false;
		}
	});
	$({
		target: "Object",
		stat: true,
		forced: !NATIVE_SYMBOL,
		sham: !DESCRIPTORS
	}, {
		create: $create,
		defineProperty: $defineProperty,
		defineProperties: $defineProperties,
		getOwnPropertyDescriptor: $getOwnPropertyDescriptor
	});
	$({
		target: "Object",
		stat: true,
		forced: !NATIVE_SYMBOL
	}, { getOwnPropertyNames: $getOwnPropertyNames });
	defineSymbolToPrimitive();
	setToStringTag($Symbol, SYMBOL);
	hiddenKeys[HIDDEN] = true;
}));
//#endregion
//#region node_modules/core-js-pure/internals/symbol-registry-detection.js
var require_symbol_registry_detection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_symbol_constructor_detection() && !!Symbol["for"] && !!Symbol.keyFor;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.for.js
var require_es_symbol_for = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var getBuiltIn = require_get_built_in();
	var hasOwn = require_has_own_property();
	var toString = require_to_string();
	var shared = require_shared();
	var NATIVE_SYMBOL_REGISTRY = require_symbol_registry_detection();
	var StringToSymbolRegistry = shared("string-to-symbol-registry");
	var SymbolToStringRegistry = shared("symbol-to-string-registry");
	$({
		target: "Symbol",
		stat: true,
		forced: !NATIVE_SYMBOL_REGISTRY
	}, { "for": function(key) {
		var string = toString(key);
		if (hasOwn(StringToSymbolRegistry, string)) return StringToSymbolRegistry[string];
		var symbol = getBuiltIn("Symbol")(string);
		StringToSymbolRegistry[string] = symbol;
		SymbolToStringRegistry[symbol] = string;
		return symbol;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.key-for.js
var require_es_symbol_key_for = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var hasOwn = require_has_own_property();
	var isSymbol = require_is_symbol();
	var tryToString = require_try_to_string();
	var shared = require_shared();
	var NATIVE_SYMBOL_REGISTRY = require_symbol_registry_detection();
	var SymbolToStringRegistry = shared("symbol-to-string-registry");
	$({
		target: "Symbol",
		stat: true,
		forced: !NATIVE_SYMBOL_REGISTRY
	}, { keyFor: function keyFor(sym) {
		if (!isSymbol(sym)) throw new TypeError(tryToString(sym) + " is not a symbol");
		if (hasOwn(SymbolToStringRegistry, sym)) return SymbolToStringRegistry[sym];
	} });
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-raw-json.js
var require_is_raw_json = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	var getInternalState = require_internal_state().get;
	module.exports = function isRawJSON(O) {
		if (!isObject(O)) return false;
		var state = getInternalState(O);
		return !!state && state.type === "RawJSON";
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/this-number-value.js
var require_this_number_value = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_function_uncurry_this()(1.1.valueOf);
}));
//#endregion
//#region node_modules/core-js-pure/internals/parse-json-string.js
var require_parse_json_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var hasOwn = require_has_own_property();
	var $SyntaxError = SyntaxError;
	var $parseInt = parseInt;
	var fromCharCode = String.fromCharCode;
	var at = uncurryThis("".charAt);
	var slice = uncurryThis("".slice);
	var exec = uncurryThis(/./.exec);
	var codePoints = {
		"\\\"": "\"",
		"\\\\": "\\",
		"\\/": "/",
		"\\b": "\b",
		"\\f": "\f",
		"\\n": "\n",
		"\\r": "\r",
		"\\t": "	"
	};
	var IS_4_HEX_DIGITS = /^[\da-f]{4}$/i;
	var IS_C0_CONTROL_CODE = /^[\u0000-\u001F]$/;
	module.exports = function(source, i) {
		var unterminated = true;
		var value = "";
		while (i < source.length) {
			var chr = at(source, i);
			if (chr === "\\") {
				var twoChars = slice(source, i, i + 2);
				if (hasOwn(codePoints, twoChars)) {
					value += codePoints[twoChars];
					i += 2;
				} else if (twoChars === "\\u") {
					i += 2;
					var fourHexDigits = slice(source, i, i + 4);
					if (!exec(IS_4_HEX_DIGITS, fourHexDigits)) throw new $SyntaxError("Bad Unicode escape at: " + i);
					value += fromCharCode($parseInt(fourHexDigits, 16));
					i += 4;
				} else throw new $SyntaxError("Unknown escape sequence: \"" + twoChars + "\"");
			} else if (chr === "\"") {
				unterminated = false;
				i++;
				break;
			} else {
				if (exec(IS_C0_CONTROL_CODE, chr)) throw new $SyntaxError("Bad control character in string literal at: " + i);
				value += chr;
				i++;
			}
		}
		if (unterminated) throw new $SyntaxError("Unterminated string at: " + i);
		return {
			value,
			end: i
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/native-raw-json.js
var require_native_raw_json = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = !require_fails()(function() {
		var unsafeInt = "9007199254740993";
		var raw = JSON.rawJSON(unsafeInt);
		return !JSON.isRawJSON(raw) || JSON.stringify(raw) !== unsafeInt;
	});
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.json.stringify.js
var require_es_json_stringify = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var getBuiltIn = require_get_built_in();
	var call = require_function_call();
	var uncurryThis = require_function_uncurry_this();
	var fails = require_fails();
	var isArray = require_is_array$3();
	var isCallable = require_is_callable();
	var isObject = require_is_object();
	var create = require_object_create();
	var isRawJSON = require_is_raw_json();
	var isSymbol = require_is_symbol();
	var classof = require_classof_raw();
	var thisNumberValue = require_this_number_value();
	var includes = require_array_includes().includes;
	var hasOwn = require_has_own_property();
	var toString = require_to_string();
	var parseJSONString = require_parse_json_string();
	var uid = require_uid();
	var NATIVE_SYMBOL = require_symbol_constructor_detection();
	var NATIVE_RAW_JSON = require_native_raw_json();
	var $String = String;
	var $TypeError = TypeError;
	var $stringify = getBuiltIn("JSON", "stringify");
	var $BigInt = getBuiltIn("BigInt");
	var stringValueOf = uncurryThis("".valueOf);
	var booleanValueOf = uncurryThis(true.valueOf);
	var bigIntValueOf = $BigInt && uncurryThis($BigInt.prototype.valueOf);
	var exec = uncurryThis(/./.exec);
	var charAt = uncurryThis("".charAt);
	var charCodeAt = uncurryThis("".charCodeAt);
	var replace = uncurryThis("".replace);
	var slice = uncurryThis("".slice);
	var push = uncurryThis([].push);
	var pop = uncurryThis([].pop);
	var numberToString = uncurryThis(1.1.toString);
	var surrogates = /[\uD800-\uDFFF]/g;
	var leadingSurrogates = /^[\uD800-\uDBFF]$/;
	var trailingSurrogates = /^[\uDC00-\uDFFF]$/;
	var digits = /^\d+$/;
	var RAW_MARK = uid();
	var KEY_MARK = uid();
	var END_MARK = uid();
	var RAW_MARK_LENGTH = RAW_MARK.length;
	var KEY_MARK_LENGTH = KEY_MARK.length;
	var WRONG_SYMBOLS_CONVERSION = !NATIVE_SYMBOL || fails(function() {
		var symbol = getBuiltIn("Symbol")("stringify detection");
		return $stringify([symbol]) !== "[null]" || $stringify({ a: symbol }) !== "{}" || $stringify(Object(symbol)) !== "{}";
	});
	var ILL_FORMED_UNICODE = fails(function() {
		return $stringify("\udf06\ud834") !== "\"\\udf06\\ud834\"" || $stringify("\udead") !== "\"\\udead\"";
	});
	var isRawJSONValue = NATIVE_RAW_JSON ? getBuiltIn("JSON", "isRawJSON") : isRawJSON;
	var stringifyWithProperSymbolsConversion = WRONG_SYMBOLS_CONVERSION ? function(it, replacer, space) {
		return $stringify(it, function(key, value) {
			var replaced = call(replacer, this, key, value);
			if (!isSymbol(replaced)) return replaced;
		}, space);
	} : $stringify;
	var fixIllFormedJSON = function(match, offset, string) {
		var prev = charAt(string, offset - 1);
		var next = charAt(string, offset + 1);
		if (exec(leadingSurrogates, match) && !exec(trailingSurrogates, next) || exec(trailingSurrogates, match) && !exec(leadingSurrogates, prev)) return "\\u" + numberToString(charCodeAt(match, 0), 16);
		return match;
	};
	var getPropertyList = function(replacer) {
		if (!isArray(replacer)) return;
		var rawLength = replacer.length;
		var propertyList = [];
		var addedKeys = create(null);
		for (var i = 0; i < rawLength; i++) {
			var element = replacer[i];
			var key;
			if (typeof element == "string") key = element;
			else if (typeof element == "number" || classof(element) === "Number" || classof(element) === "String") key = toString(element);
			else continue;
			if (!hasOwn(addedKeys, key)) {
				addedKeys[key] = true;
				push(propertyList, key);
			}
		}
		return propertyList;
	};
	var hasInternalSlot = function(valueOf, it) {
		try {
			valueOf(it);
			return true;
		} catch (error) {
			return false;
		}
	};
	var isBoxedPrimitive = function(it) {
		var kind = classof(it);
		return kind === "Number" && hasInternalSlot(thisNumberValue, it) || kind === "String" && hasInternalSlot(stringValueOf, it) || kind === "Boolean" && hasInternalSlot(booleanValueOf, it) || !!bigIntValueOf && kind === "BigInt" && hasInternalSlot(bigIntValueOf, it);
	};
	var isSerializedAsObject = function(it) {
		if (!isObject(it) || isCallable(it) || isArray(it)) return false;
		try {
			return !isBoxedPrimitive(it);
		} catch (error) {
			return true;
		}
	};
	var createElementHolder = function(holder, key) {
		return { toJSON: function() {
			var element = holder[key];
			if (isObject(element) || typeof element == "bigint") {
				var elementToJSON = element.toJSON;
				if (isCallable(elementToJSON)) element = call(elementToJSON, element, key);
			}
			return element;
		} };
	};
	var getKeyPrefix = function(propertyList) {
		for (var i = 0, length = propertyList.length; i < length; i++) if (exec(digits, propertyList[i])) return KEY_MARK;
		return "";
	};
	var createOrderedObject = function(value, propertyList, keyPrefix) {
		var ordered = create(null);
		for (var i = 0, length = propertyList.length; i < length; i++) {
			var key = propertyList[i];
			ordered[keyPrefix + key] = createElementHolder(value, key);
		}
		ordered[END_MARK] = null;
		return ordered;
	};
	if ($stringify) $({
		target: "JSON",
		stat: true,
		arity: 3,
		forced: WRONG_SYMBOLS_CONVERSION || ILL_FORMED_UNICODE || !NATIVE_RAW_JSON
	}, { stringify: function stringify(text, replacer, space) {
		var replacerFunction = isCallable(replacer) ? replacer : void 0;
		var propertyList = replacerFunction ? void 0 : getPropertyList(replacer);
		var keyPrefix = propertyList && getKeyPrefix(propertyList);
		var rawStrings = [];
		var openObjects = [];
		var parentOrdered = [];
		var currentOrdered;
		var marked = false;
		var root = true;
		var json = stringifyWithProperSymbolsConversion(text, function(key, value) {
			key = $String(key);
			if (propertyList) {
				if (key === END_MARK) {
					pop(openObjects);
					currentOrdered = pop(parentOrdered);
					return;
				}
				if (root) root = false;
				else if (this !== currentOrdered && !isArray(this) && !includes(propertyList, key)) return;
			} else if (replacerFunction) value = call(replacerFunction, this, key, value);
			if (isRawJSONValue(value)) {
				if (NATIVE_RAW_JSON) return value;
				marked = true;
				return RAW_MARK + (push(rawStrings, value.rawJSON) - 1);
			}
			if (propertyList && isSerializedAsObject(value)) {
				if (includes(openObjects, value)) throw new $TypeError("Converting circular structure to JSON");
				var ordered = createOrderedObject(value, propertyList, keyPrefix);
				push(openObjects, value);
				push(parentOrdered, currentOrdered);
				currentOrdered = ordered;
				if (keyPrefix) marked = true;
				return ordered;
			}
			return value;
		}, space);
		if (typeof json != "string") return json;
		if (ILL_FORMED_UNICODE) json = replace(json, surrogates, fixIllFormedJSON);
		if (!marked) return json;
		var result = "";
		var length = json.length;
		for (var i = 0; i < length; i++) {
			var chr = charAt(json, i);
			if (chr === "\"") {
				var end = parseJSONString(json, ++i).end - 1;
				var string = slice(json, i, end);
				if (slice(string, 0, RAW_MARK_LENGTH) === RAW_MARK) result += rawStrings[slice(string, RAW_MARK_LENGTH)];
				else if (slice(string, 0, KEY_MARK_LENGTH) === KEY_MARK) result += "\"" + slice(string, KEY_MARK_LENGTH) + "\"";
				else result += "\"" + string + "\"";
				i = end;
			} else result += chr;
		}
		return result;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.get-own-property-symbols.js
var require_es_object_get_own_property_symbols = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var NATIVE_SYMBOL = require_symbol_constructor_detection();
	var fails = require_fails();
	var getOwnPropertySymbolsModule = require_object_get_own_property_symbols();
	var toObject = require_to_object();
	$({
		target: "Object",
		stat: true,
		forced: !NATIVE_SYMBOL || fails(function() {
			getOwnPropertySymbolsModule.f(1);
		})
	}, { getOwnPropertySymbols: function getOwnPropertySymbols(it) {
		var $getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
		return $getOwnPropertySymbols ? $getOwnPropertySymbols(toObject(it)) : [];
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.js
var require_es_symbol = /* @__PURE__ */ __commonJSMin((() => {
	require_es_symbol_constructor();
	require_es_symbol_for();
	require_es_symbol_key_for();
	require_es_json_stringify();
	require_es_object_get_own_property_symbols();
}));
//#endregion
//#region node_modules/core-js-pure/es/object/get-own-property-symbols.js
var require_get_own_property_symbols$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_symbol();
	module.exports = require_path().Object.getOwnPropertySymbols;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/get-own-property-symbols.js
var require_get_own_property_symbols$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_symbols$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/get-own-property-symbols.js
var require_get_own_property_symbols = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_symbols$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-method-has-species-support.js
var require_array_method_has_species_support = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	var wellKnownSymbol = require_well_known_symbol();
	var V8_VERSION = require_environment_v8_version();
	var SPECIES = wellKnownSymbol("species");
	module.exports = function(METHOD_NAME) {
		return V8_VERSION >= 51 || !fails(function() {
			var array = [];
			var constructor = array.constructor = {};
			constructor[SPECIES] = function() {
				return { foo: 1 };
			};
			return array[METHOD_NAME](Boolean).foo !== 1;
		});
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.filter.js
var require_es_array_filter = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $filter = require_array_iteration().filter;
	$({
		target: "Array",
		proto: true,
		forced: !require_array_method_has_species_support()("filter")
	}, { filter: function filter(callbackfn) {
		return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-built-in-prototype-method.js
var require_get_built_in_prototype_method = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var path = require_path();
	module.exports = function(CONSTRUCTOR, METHOD) {
		var Namespace = path[CONSTRUCTOR + "Prototype"];
		var pureMethod = Namespace && Namespace[METHOD];
		if (pureMethod) return pureMethod;
		var NativeConstructor = globalThis[CONSTRUCTOR];
		var NativePrototype = NativeConstructor && NativeConstructor.prototype;
		return NativePrototype && NativePrototype[METHOD];
	};
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/filter.js
var require_filter$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_filter();
	module.exports = require_get_built_in_prototype_method()("Array", "filter");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/filter.js
var require_filter$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_filter$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.filter;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.filter ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/filter.js
var require_filter$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_filter$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/filter.js
var require_filter = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_filter$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.get-own-property-descriptor.js
var require_es_object_get_own_property_descriptor = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var fails = require_fails();
	var toIndexedObject = require_to_indexed_object();
	var nativeGetOwnPropertyDescriptor = require_object_get_own_property_descriptor().f;
	var DESCRIPTORS = require_descriptors();
	$({
		target: "Object",
		stat: true,
		forced: !DESCRIPTORS || fails(function() {
			nativeGetOwnPropertyDescriptor(1);
		}),
		sham: !DESCRIPTORS
	}, { getOwnPropertyDescriptor: function getOwnPropertyDescriptor(it, key) {
		return nativeGetOwnPropertyDescriptor(toIndexedObject(it), key);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/get-own-property-descriptor.js
var require_get_own_property_descriptor$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_get_own_property_descriptor();
	var Object = require_path().Object;
	var $getOwnPropertyDescriptor = module.exports = function getOwnPropertyDescriptor(it, key) {
		return Object.getOwnPropertyDescriptor(it, key);
	};
	if (Object.getOwnPropertyDescriptor.sham) $getOwnPropertyDescriptor.sham = true;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/get-own-property-descriptor.js
var require_get_own_property_descriptor$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_descriptor$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptor.js
var require_get_own_property_descriptor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_descriptor$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-method-is-strict.js
var require_array_method_is_strict = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	module.exports = function(METHOD_NAME, argument) {
		var method = [][METHOD_NAME];
		return !!method && fails(function() {
			method.call(null, argument || function() {
				return 1;
			}, 1);
		});
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-for-each.js
var require_array_for_each = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $forEach = require_array_iteration().forEach;
	module.exports = !require_array_method_is_strict()("forEach") ? function forEach(callbackfn) {
		return $forEach(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} : [].forEach;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.for-each.js
var require_es_array_for_each = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var forEach = require_array_for_each();
	$({
		target: "Array",
		proto: true,
		forced: [].forEach !== forEach
	}, { forEach });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/for-each.js
var require_for_each$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_for_each();
	module.exports = require_get_built_in_prototype_method()("Array", "forEach");
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/virtual/for-each.js
var require_for_each$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_for_each$3();
}));
//#endregion
//#region node_modules/core-js-pure/modules/web.dom-collections.for-each.js
var require_web_dom_collections_for_each = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/for-each.js
var require_for_each$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classof = require_classof();
	var hasOwn = require_has_own_property();
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_for_each$2();
	require_web_dom_collections_for_each();
	var ArrayPrototype = Array.prototype;
	var DOMIterables = {
		DOMTokenList: true,
		NodeList: true
	};
	module.exports = function(it) {
		var own = it.forEach;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.forEach || hasOwn(DOMIterables, classof(it)) ? method : own;
	};
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/for-each.js
var require_for_each = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_for_each$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/own-keys.js
var require_own_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var uncurryThis = require_function_uncurry_this();
	var getOwnPropertyNamesModule = require_object_get_own_property_names();
	var getOwnPropertySymbolsModule = require_object_get_own_property_symbols();
	var anObject = require_an_object();
	var concat = uncurryThis([].concat);
	module.exports = getBuiltIn("Reflect", "ownKeys") || function ownKeys(it) {
		var keys = getOwnPropertyNamesModule.f(anObject(it));
		var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
		return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.get-own-property-descriptors.js
var require_es_object_get_own_property_descriptors = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var DESCRIPTORS = require_descriptors();
	var ownKeys = require_own_keys();
	var toIndexedObject = require_to_indexed_object();
	var getOwnPropertyDescriptorModule = require_object_get_own_property_descriptor();
	var createProperty = require_create_property();
	$({
		target: "Object",
		stat: true,
		sham: !DESCRIPTORS
	}, { getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
		var O = toIndexedObject(object);
		var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
		var keys = ownKeys(O);
		var result = {};
		var index = 0;
		var key, descriptor;
		while (keys.length > index) {
			descriptor = getOwnPropertyDescriptor(O, key = keys[index++]);
			if (descriptor !== void 0) createProperty(result, key, descriptor);
		}
		return result;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/get-own-property-descriptors.js
var require_get_own_property_descriptors$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_get_own_property_descriptors();
	module.exports = require_path().Object.getOwnPropertyDescriptors;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/get-own-property-descriptors.js
var require_get_own_property_descriptors$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_descriptors$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/get-own-property-descriptors.js
var require_get_own_property_descriptors = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_own_property_descriptors$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.define-properties.js
var require_es_object_define_properties = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var DESCRIPTORS = require_descriptors();
	var defineProperties = require_object_define_properties().f;
	$({
		target: "Object",
		stat: true,
		forced: Object.defineProperties !== defineProperties,
		sham: !DESCRIPTORS
	}, { defineProperties });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/define-properties.js
var require_define_properties$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_define_properties();
	var Object = require_path().Object;
	var $defineProperties = module.exports = function defineProperties(T, D) {
		return Object.defineProperties(T, D);
	};
	if (Object.defineProperties.sham) $defineProperties.sham = true;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/define-properties.js
var require_define_properties$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_properties$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/define-properties.js
var require_define_properties = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_properties$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.define-property.js
var require_es_object_define_property = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var DESCRIPTORS = require_descriptors();
	var defineProperty = require_object_define_property().f;
	$({
		target: "Object",
		stat: true,
		forced: Object.defineProperty !== defineProperty,
		sham: !DESCRIPTORS
	}, { defineProperty });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/define-property.js
var require_define_property$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_define_property();
	var Object = require_path().Object;
	var $defineProperty = module.exports = function defineProperty(it, key, desc) {
		return Object.defineProperty(it, key, desc);
	};
	if (Object.defineProperty.sham) $defineProperty.sham = true;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/define-property.js
var require_define_property$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_property$5();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/define-property.js
var require_define_property$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_property$4();
}));
//#endregion
//#region node_modules/core-js-pure/internals/add-to-unscopables.js
var require_add_to_unscopables = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function() {};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterators.js
var require_iterators = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Object.create ? Object.create(null) : {};
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-name.js
var require_function_name = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var hasOwn = require_has_own_property();
	var FunctionPrototype = Function.prototype;
	var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;
	var EXISTS = hasOwn(FunctionPrototype, "name");
	module.exports = {
		EXISTS,
		PROPER: EXISTS && function something() {}.name === "something",
		CONFIGURABLE: EXISTS && (!DESCRIPTORS || DESCRIPTORS && getDescriptor(FunctionPrototype, "name").configurable)
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/correct-prototype-getter.js
var require_correct_prototype_getter = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = !require_fails()(function() {
		function F() {}
		F.prototype.constructor = null;
		return Object.getPrototypeOf(new F()) !== F.prototype;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-get-prototype-of.js
var require_object_get_prototype_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var hasOwn = require_has_own_property();
	var isCallable = require_is_callable();
	var toObject = require_to_object();
	var sharedKey = require_shared_key();
	var CORRECT_PROTOTYPE_GETTER = require_correct_prototype_getter();
	var IE_PROTO = sharedKey("IE_PROTO");
	var $Object = Object;
	var ObjectPrototype = $Object.prototype;
	module.exports = CORRECT_PROTOTYPE_GETTER ? $Object.getPrototypeOf : function(O) {
		var object = toObject(O);
		if (hasOwn(object, IE_PROTO)) return object[IE_PROTO];
		var constructor = object.constructor;
		if (isCallable(constructor) && object instanceof constructor) return constructor.prototype;
		return object instanceof $Object ? ObjectPrototype : null;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterators-core.js
var require_iterators_core = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	var isCallable = require_is_callable();
	var isObject = require_is_object();
	var create = require_object_create();
	var getPrototypeOf = require_object_get_prototype_of();
	var defineBuiltIn = require_define_built_in();
	var wellKnownSymbol = require_well_known_symbol();
	var IS_PURE = require_is_pure();
	var ITERATOR = wellKnownSymbol("iterator");
	var BUGGY_SAFARI_ITERATORS = false;
	var IteratorPrototype;
	var PrototypeOfArrayIteratorPrototype;
	var arrayIterator;
	if ([].keys) {
		arrayIterator = [].keys();
		if (!("next" in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;
		else {
			PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
			if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
		}
	}
	if (!isObject(IteratorPrototype) || fails(function() {
		var test = {};
		return IteratorPrototype[ITERATOR].call(test) !== test;
	})) IteratorPrototype = {};
	else if (IS_PURE) IteratorPrototype = create(IteratorPrototype);
	if (!isCallable(IteratorPrototype[ITERATOR])) defineBuiltIn(IteratorPrototype, ITERATOR, function() {
		return this;
	});
	module.exports = {
		IteratorPrototype,
		BUGGY_SAFARI_ITERATORS
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterator-create-constructor.js
var require_iterator_create_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var IteratorPrototype = require_iterators_core().IteratorPrototype;
	var create = require_object_create();
	var createPropertyDescriptor = require_create_property_descriptor();
	var setToStringTag = require_set_to_string_tag();
	var Iterators = require_iterators();
	var returnThis = function() {
		return this;
	};
	module.exports = function(IteratorConstructor, NAME, next, ENUMERABLE_NEXT) {
		var TO_STRING_TAG = NAME + " Iterator";
		IteratorConstructor.prototype = create(IteratorPrototype, { next: createPropertyDescriptor(+!ENUMERABLE_NEXT, next) });
		setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
		Iterators[TO_STRING_TAG] = returnThis;
		return IteratorConstructor;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-uncurry-this-accessor.js
var require_function_uncurry_this_accessor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var aCallable = require_a_callable();
	module.exports = function(object, key, method) {
		try {
			return uncurryThis(aCallable(Object.getOwnPropertyDescriptor(object, key)[method]));
		} catch (error) {}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-possible-prototype.js
var require_is_possible_prototype = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	module.exports = function(argument) {
		return isObject(argument) || argument === null;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-possible-prototype.js
var require_a_possible_prototype = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPossiblePrototype = require_is_possible_prototype();
	var $String = String;
	var $TypeError = TypeError;
	module.exports = function(argument) {
		if (isPossiblePrototype(argument)) return argument;
		throw new $TypeError("Can't set " + $String(argument) + " as a prototype");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-set-prototype-of.js
var require_object_set_prototype_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThisAccessor = require_function_uncurry_this_accessor();
	var isObject = require_is_object();
	var requireObjectCoercible = require_require_object_coercible();
	var aPossiblePrototype = require_a_possible_prototype();
	module.exports = Object.setPrototypeOf || ("__proto__" in {} ? function() {
		var CORRECT_SETTER = false;
		var test = {};
		var setter;
		try {
			setter = uncurryThisAccessor(Object.prototype, "__proto__", "set");
			setter(test, []);
			CORRECT_SETTER = test instanceof Array;
		} catch (error) {}
		return function setPrototypeOf(O, proto) {
			requireObjectCoercible(O);
			aPossiblePrototype(proto);
			if (!isObject(O)) return O;
			if (CORRECT_SETTER) setter(O, proto);
			else O.__proto__ = proto;
			return O;
		};
	}() : void 0);
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterator-define.js
var require_iterator_define = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $ = require_export();
	var call = require_function_call();
	var IS_PURE = require_is_pure();
	var FunctionName = require_function_name();
	var isCallable = require_is_callable();
	var createIteratorConstructor = require_iterator_create_constructor();
	var getPrototypeOf = require_object_get_prototype_of();
	var setPrototypeOf = require_object_set_prototype_of();
	var setToStringTag = require_set_to_string_tag();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var defineBuiltIn = require_define_built_in();
	var wellKnownSymbol = require_well_known_symbol();
	var Iterators = require_iterators();
	var IteratorsCore = require_iterators_core();
	var PROPER_FUNCTION_NAME = FunctionName.PROPER;
	var CONFIGURABLE_FUNCTION_NAME = FunctionName.CONFIGURABLE;
	var IteratorPrototype = IteratorsCore.IteratorPrototype;
	var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
	var ITERATOR = wellKnownSymbol("iterator");
	var KEYS = "keys";
	var VALUES = "values";
	var ENTRIES = "entries";
	var returnThis = function() {
		return this;
	};
	module.exports = function(Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
		createIteratorConstructor(IteratorConstructor, NAME, next);
		var getIterationMethod = function(KIND) {
			if (KIND === DEFAULT && defaultIterator) return defaultIterator;
			if (!BUGGY_SAFARI_ITERATORS && KIND && KIND in IterablePrototype) return IterablePrototype[KIND];
			switch (KIND) {
				case KEYS: return function keys() {
					return new IteratorConstructor(this, KIND);
				};
				case VALUES: return function values() {
					return new IteratorConstructor(this, KIND);
				};
				case ENTRIES: return function entries() {
					return new IteratorConstructor(this, KIND);
				};
			}
			return function() {
				return new IteratorConstructor(this);
			};
		};
		var TO_STRING_TAG = NAME + " Iterator";
		var INCORRECT_VALUES_NAME = false;
		var IterablePrototype = Iterable.prototype;
		var nativeIterator = IterablePrototype[ITERATOR] || IterablePrototype["@@iterator"] || DEFAULT && IterablePrototype[DEFAULT];
		var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
		var anyNativeIterator = NAME === "Array" ? IterablePrototype.entries || nativeIterator : nativeIterator;
		var CurrentIteratorPrototype, methods, KEY;
		if (anyNativeIterator) {
			CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));
			if (CurrentIteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
				if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
					if (setPrototypeOf) setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
					else if (!isCallable(CurrentIteratorPrototype[ITERATOR])) defineBuiltIn(CurrentIteratorPrototype, ITERATOR, returnThis);
				}
				setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
				if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
			}
		}
		if (PROPER_FUNCTION_NAME && DEFAULT === VALUES && nativeIterator && nativeIterator.name !== VALUES) {
			if (!IS_PURE && CONFIGURABLE_FUNCTION_NAME) createNonEnumerableProperty(IterablePrototype, "name", VALUES);
			else {
				INCORRECT_VALUES_NAME = true;
				defaultIterator = function values() {
					return call(nativeIterator, this);
				};
			}
		}
		if (DEFAULT) {
			methods = {
				values: getIterationMethod(VALUES),
				keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
				entries: getIterationMethod(ENTRIES)
			};
			if (FORCED) {
				for (KEY in methods) if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) defineBuiltIn(IterablePrototype, KEY, methods[KEY]);
			} else $({
				target: NAME,
				proto: true,
				forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME
			}, methods);
		}
		if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) defineBuiltIn(IterablePrototype, ITERATOR, defaultIterator, { name: DEFAULT });
		Iterators[NAME] = defaultIterator;
		return methods;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/create-iter-result-object.js
var require_create_iter_result_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(value, done) {
		return {
			value,
			done
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.iterator.js
var require_es_array_iterator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toIndexedObject = require_to_indexed_object();
	var addToUnscopables = require_add_to_unscopables();
	var Iterators = require_iterators();
	var InternalStateModule = require_internal_state();
	var defineProperty = require_object_define_property().f;
	var defineIterator = require_iterator_define();
	var createIterResultObject = require_create_iter_result_object();
	var IS_PURE = require_is_pure();
	var DESCRIPTORS = require_descriptors();
	var ARRAY_ITERATOR = "Array Iterator";
	var setInternalState = InternalStateModule.set;
	var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR);
	module.exports = defineIterator(Array, "Array", function(iterated, kind) {
		setInternalState(this, {
			type: ARRAY_ITERATOR,
			target: toIndexedObject(iterated),
			index: 0,
			kind
		});
	}, function() {
		var state = getInternalState(this);
		var target = state.target;
		var index = state.index++;
		if (!target || index >= target.length) {
			state.target = null;
			return createIterResultObject(void 0, true);
		}
		switch (state.kind) {
			case "keys": return createIterResultObject(index, false);
			case "values": return createIterResultObject(target[index], false);
		}
		return createIterResultObject([index, target[index]], false);
	}, "values");
	var values = Iterators.Arguments = Iterators.Array;
	addToUnscopables("keys");
	addToUnscopables("values");
	addToUnscopables("entries");
	if (!IS_PURE && DESCRIPTORS && values.name !== "values") try {
		defineProperty(values, "name", { value: "values" });
	} catch (error) {}
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.to-string.js
var require_es_object_to_string = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/internals/freezing.js
var require_freezing = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = !require_fails()(function() {
		return Object.isExtensible(Object.preventExtensions({}));
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/define-built-ins.js
var require_define_built_ins = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var defineBuiltIn = require_define_built_in();
	module.exports = function(target, src, options) {
		for (var key in src) if (options && options.unsafe && target[key]) target[key] = src[key];
		else defineBuiltIn(target, key, src[key], options);
		return target;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-buffer-non-extensible.js
var require_array_buffer_non_extensible = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_fails()(function() {
		if (typeof ArrayBuffer == "function") {
			var buffer = /* @__PURE__ */ new ArrayBuffer(8);
			if (Object.isExtensible(buffer)) Object.defineProperty(buffer, "a", { value: 8 });
		}
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-is-extensible.js
var require_object_is_extensible = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	var isObject = require_is_object();
	var classof = require_classof_raw();
	var ARRAY_BUFFER_NON_EXTENSIBLE = require_array_buffer_non_extensible();
	var $isExtensible = Object.isExtensible;
	module.exports = fails(function() {
		$isExtensible(1);
	}) || ARRAY_BUFFER_NON_EXTENSIBLE ? function isExtensible(it) {
		if (!isObject(it)) return false;
		if (ARRAY_BUFFER_NON_EXTENSIBLE && classof(it) === "ArrayBuffer") return false;
		return $isExtensible ? $isExtensible(it) : true;
	} : $isExtensible;
}));
//#endregion
//#region node_modules/core-js-pure/internals/internal-metadata.js
var require_internal_metadata = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this();
	var hiddenKeys = require_hidden_keys();
	var isObject = require_is_object();
	var hasOwn = require_has_own_property();
	var defineProperty = require_object_define_property().f;
	var getOwnPropertyNamesModule = require_object_get_own_property_names();
	var getOwnPropertyNamesExternalModule = require_object_get_own_property_names_external();
	var isExtensible = require_object_is_extensible();
	var uid = require_uid();
	var FREEZING = require_freezing();
	var REQUIRED = false;
	var METADATA = uid("meta");
	var id = 0;
	var setMetadata = function(it) {
		defineProperty(it, METADATA, { value: {
			objectID: "O" + id++,
			weakData: {}
		} });
	};
	var fastKey = function(it, create) {
		if (!isObject(it)) return typeof it == "symbol" ? it : (typeof it == "string" ? "S" : "P") + it;
		if (!hasOwn(it, METADATA)) {
			if (!isExtensible(it)) return "F";
			if (!create) return "E";
			setMetadata(it);
		}
		return it[METADATA].objectID;
	};
	var getWeakData = function(it, create) {
		if (!hasOwn(it, METADATA)) {
			if (!isExtensible(it)) return true;
			if (!create) return false;
			setMetadata(it);
		}
		return it[METADATA].weakData;
	};
	var onFreeze = function(it) {
		if (FREEZING && REQUIRED && isExtensible(it) && !hasOwn(it, METADATA)) setMetadata(it);
		return it;
	};
	var enable = function() {
		meta.enable = function() {};
		REQUIRED = true;
		var getOwnPropertyNames = getOwnPropertyNamesModule.f;
		var splice = uncurryThis([].splice);
		var test = {};
		test[METADATA] = 1;
		if (getOwnPropertyNames(test).length) {
			getOwnPropertyNamesModule.f = function(it) {
				var result = getOwnPropertyNames(it);
				for (var i = 0, length = result.length; i < length; i++) if (result[i] === METADATA) {
					splice(result, i, 1);
					break;
				}
				return result;
			};
			$({
				target: "Object",
				stat: true,
				forced: true
			}, { getOwnPropertyNames: getOwnPropertyNamesExternalModule.f });
		}
	};
	var meta = module.exports = {
		enable,
		fastKey,
		getWeakData,
		onFreeze
	};
	hiddenKeys[METADATA] = true;
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-array-iterator-method.js
var require_is_array_iterator_method = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var wellKnownSymbol = require_well_known_symbol();
	var Iterators = require_iterators();
	var ITERATOR = wellKnownSymbol("iterator");
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		return it !== void 0 && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-internal-iterator.js
var require_get_internal_iterator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var classof = require_classof();
	var hasOwn = require_has_own_property();
	var Iterators = require_iterators();
	module.exports = function(it) {
		var klass = classof(it);
		if (klass === "Arguments") klass = "Array";
		return hasOwn(Iterators, klass) ? Iterators[klass] : void 0;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-iterator-method-internal.js
var require_get_iterator_method_internal = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getMethod = require_get_method();
	var isNullOrUndefined = require_is_null_or_undefined();
	var getInternalIterator = require_get_internal_iterator();
	var ITERATOR = require_well_known_symbol()("iterator");
	module.exports = function(it) {
		if (!isNullOrUndefined(it)) return getMethod(it, ITERATOR) || getMethod(it, "@@iterator") || getInternalIterator(it);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-iterator-internal.js
var require_get_iterator_internal = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	var isCallable = require_is_callable();
	var anObject = require_an_object();
	var tryToString = require_try_to_string();
	var getIteratorMethod = require_get_iterator_method_internal();
	var $TypeError = TypeError;
	module.exports = function(argument, usingIterator) {
		var iteratorMethod = arguments.length < 2 ? getIteratorMethod(argument) : usingIterator;
		if (isCallable(iteratorMethod)) return anObject(call(iteratorMethod, argument));
		throw new $TypeError(tryToString(argument) + " is not iterable");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterator-close.js
var require_iterator_close = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	var anObject = require_an_object();
	var getMethod = require_get_method();
	module.exports = function(iterator, kind, value) {
		var innerResult, innerError;
		anObject(iterator);
		try {
			innerResult = getMethod(iterator, "return");
			if (!innerResult) {
				if (kind === "throw") throw value;
				return value;
			}
			innerResult = call(innerResult, iterator);
		} catch (error) {
			innerError = true;
			innerResult = error;
		}
		if (kind === "throw") throw value;
		if (innerError) throw innerResult;
		anObject(innerResult);
		return value;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterate.js
var require_iterate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var bind = require_function_bind_context();
	var call = require_function_call();
	var anObject = require_an_object();
	var tryToString = require_try_to_string();
	var isArrayIteratorMethod = require_is_array_iterator_method();
	var lengthOfArrayLike = require_length_of_array_like();
	var isPrototypeOf = require_object_is_prototype_of();
	var getIterator = require_get_iterator_internal();
	var getIteratorMethod = require_get_iterator_method_internal();
	var iteratorClose = require_iterator_close();
	var $TypeError = TypeError;
	var Result = function(stopped, result) {
		this.stopped = stopped;
		this.result = result;
	};
	var ResultPrototype = Result.prototype;
	module.exports = function(iterable, unboundFunction, options) {
		var that = options && options.that;
		var AS_ENTRIES = !!(options && options.AS_ENTRIES);
		var IS_RECORD = !!(options && options.IS_RECORD);
		var IS_ITERATOR = !!(options && options.IS_ITERATOR);
		var INTERRUPTED = !!(options && options.INTERRUPTED);
		var fn = bind(unboundFunction, that);
		var iterator, iterFn, index, length, result, next, step;
		var stop = function(condition) {
			var $iterator = iterator;
			iterator = void 0;
			if ($iterator) iteratorClose($iterator, "normal");
			return new Result(true, condition);
		};
		var callFn = function(value) {
			if (AS_ENTRIES) {
				anObject(value);
				return INTERRUPTED ? fn(value[0], value[1], stop) : fn(value[0], value[1]);
			}
			return INTERRUPTED ? fn(value, stop) : fn(value);
		};
		if (IS_RECORD) iterator = iterable.iterator;
		else if (IS_ITERATOR) iterator = iterable;
		else {
			iterFn = getIteratorMethod(iterable);
			if (!iterFn) throw new $TypeError(tryToString(iterable) + " is not iterable");
			if (isArrayIteratorMethod(iterFn)) {
				for (index = 0, length = lengthOfArrayLike(iterable); length > index; index++) {
					result = callFn(iterable[index]);
					if (result && isPrototypeOf(ResultPrototype, result)) return result;
				}
				return new Result(false);
			}
			iterator = getIterator(iterable, iterFn);
		}
		next = IS_RECORD ? iterable.next : iterator.next;
		while (!(step = call(next, iterator)).done) {
			var value = step.value;
			try {
				result = callFn(value);
			} catch (error) {
				if (iterator) iteratorClose(iterator, "throw", error);
				else throw error;
			}
			if (typeof result == "object" && result && isPrototypeOf(ResultPrototype, result)) return result;
		}
		return new Result(false);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/an-instance.js
var require_an_instance = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var $TypeError = TypeError;
	module.exports = function(it, Prototype) {
		if (isPrototypeOf(Prototype, it)) return it;
		throw new $TypeError("Incorrect invocation");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/collection.js
var require_collection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	var InternalMetadataModule = require_internal_metadata();
	var call = require_function_call();
	var fails = require_fails();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var iterate = require_iterate();
	var anInstance = require_an_instance();
	var isCallable = require_is_callable();
	var isObject = require_is_object();
	var isNullOrUndefined = require_is_null_or_undefined();
	var setToStringTag = require_set_to_string_tag();
	var defineProperty = require_object_define_property().f;
	var forEach = require_array_iteration().forEach;
	var DESCRIPTORS = require_descriptors();
	var InternalStateModule = require_internal_state();
	var setInternalState = InternalStateModule.set;
	var internalStateGetterFor = InternalStateModule.getterFor;
	module.exports = function(CONSTRUCTOR_NAME, wrapper, common) {
		var IS_MAP = CONSTRUCTOR_NAME.indexOf("Map") !== -1;
		var IS_WEAK = CONSTRUCTOR_NAME.indexOf("Weak") !== -1;
		var ADDER = IS_MAP ? "set" : "add";
		var NativeConstructor = globalThis[CONSTRUCTOR_NAME];
		var NativePrototype = NativeConstructor && NativeConstructor.prototype;
		var exported = {};
		var Constructor;
		if (!DESCRIPTORS || !isCallable(NativeConstructor) || !(IS_WEAK || NativePrototype.forEach && !fails(function() {
			new NativeConstructor().entries().next();
		}))) {
			Constructor = common.getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER);
			InternalMetadataModule.enable();
		} else {
			Constructor = wrapper(function(target, iterable) {
				setInternalState(anInstance(target, Prototype), {
					type: CONSTRUCTOR_NAME,
					collection: new NativeConstructor()
				});
				if (!isNullOrUndefined(iterable)) iterate(iterable, target[ADDER], {
					that: target,
					AS_ENTRIES: IS_MAP
				});
			});
			var Prototype = Constructor.prototype;
			var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);
			forEach([
				"add",
				"clear",
				"delete",
				"forEach",
				"get",
				"has",
				"set",
				"keys",
				"values",
				"entries"
			], function(KEY) {
				var IS_ADDER = KEY === "add" || KEY === "set";
				if (KEY in NativePrototype && !(IS_WEAK && KEY === "clear")) createNonEnumerableProperty(Prototype, KEY, function(a, b) {
					var that = this;
					var collection = getInternalState(that).collection;
					if (!IS_ADDER && IS_WEAK && !isObject(a)) return KEY === "get" ? void 0 : false;
					var result = collection[KEY](KEY === "forEach" ? function(value, key) {
						call(a, b, value, key, that);
					} : a === 0 ? 0 : a, b);
					return IS_ADDER ? that : result;
				});
			});
			IS_WEAK || defineProperty(Prototype, "size", {
				configurable: true,
				get: function() {
					return getInternalState(this).collection.size;
				}
			});
		}
		setToStringTag(Constructor, CONSTRUCTOR_NAME, false, true);
		exported[CONSTRUCTOR_NAME] = Constructor;
		$({
			global: true,
			forced: true
		}, exported);
		if (!IS_WEAK) common.setStrong(Constructor, CONSTRUCTOR_NAME, IS_MAP);
		return Constructor;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/collection-weak.js
var require_collection_weak = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var defineBuiltIns = require_define_built_ins();
	var getWeakData = require_internal_metadata().getWeakData;
	var anInstance = require_an_instance();
	var anObject = require_an_object();
	var isNullOrUndefined = require_is_null_or_undefined();
	var isObject = require_is_object();
	var iterate = require_iterate();
	var ArrayIterationModule = require_array_iteration();
	var hasOwn = require_has_own_property();
	var InternalStateModule = require_internal_state();
	var setInternalState = InternalStateModule.set;
	var internalStateGetterFor = InternalStateModule.getterFor;
	var find = ArrayIterationModule.find;
	var findIndex = ArrayIterationModule.findIndex;
	var splice = uncurryThis([].splice);
	var id = 0;
	var uncaughtFrozenStore = function(state) {
		return state.frozen || (state.frozen = new UncaughtFrozenStore());
	};
	var UncaughtFrozenStore = function() {
		this.entries = [];
	};
	var findUncaughtFrozen = function(store, key) {
		return find(store.entries, function(it) {
			return it[0] === key;
		});
	};
	UncaughtFrozenStore.prototype = {
		get: function(key) {
			var entry = findUncaughtFrozen(this, key);
			if (entry) return entry[1];
		},
		has: function(key) {
			return !!findUncaughtFrozen(this, key);
		},
		set: function(key, value) {
			var entry = findUncaughtFrozen(this, key);
			if (entry) entry[1] = value;
			else this.entries.push([key, value]);
		},
		"delete": function(key) {
			var index = findIndex(this.entries, function(it) {
				return it[0] === key;
			});
			if (~index) splice(this.entries, index, 1);
			return !!~index;
		}
	};
	module.exports = { getConstructor: function(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
		var Constructor = wrapper(function(that, iterable) {
			anInstance(that, Prototype);
			setInternalState(that, {
				type: CONSTRUCTOR_NAME,
				id: id++,
				frozen: null
			});
			if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], {
				that,
				AS_ENTRIES: IS_MAP
			});
		});
		var Prototype = Constructor.prototype;
		var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);
		var define = function(that, key, value) {
			var state = getInternalState(that);
			var data = getWeakData(anObject(key), true);
			if (data === true) uncaughtFrozenStore(state).set(key, value);
			else data[state.id] = value;
			return that;
		};
		defineBuiltIns(Prototype, {
			"delete": function(key) {
				var state = getInternalState(this);
				if (!isObject(key)) return false;
				var data = getWeakData(key);
				if (data === true) return uncaughtFrozenStore(state)["delete"](key);
				return data && hasOwn(data, state.id) && delete data[state.id];
			},
			has: function has(key) {
				var state = getInternalState(this);
				if (!isObject(key)) return false;
				var data = getWeakData(key);
				if (data === true) return uncaughtFrozenStore(state).has(key);
				return data && hasOwn(data, state.id);
			}
		});
		defineBuiltIns(Prototype, IS_MAP ? {
			get: function get(key) {
				var state = getInternalState(this);
				if (isObject(key)) {
					var data = getWeakData(key);
					if (data === true) return uncaughtFrozenStore(state).get(key);
					if (data) return data[state.id];
				}
			},
			set: function set(key, value) {
				return define(this, key, value);
			}
		} : { add: function add(value) {
			return define(this, value, true);
		} });
		return Constructor;
	} };
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.weak-map.constructor.js
var require_es_weak_map_constructor = /* @__PURE__ */ __commonJSMin((() => {
	var FREEZING = require_freezing();
	var globalThis = require_global_this$6();
	var uncurryThis = require_function_uncurry_this();
	var defineBuiltIns = require_define_built_ins();
	var InternalMetadataModule = require_internal_metadata();
	var collection = require_collection();
	var collectionWeak = require_collection_weak();
	var isObject = require_is_object();
	var enforceInternalState = require_internal_state().enforce;
	var fails = require_fails();
	var NATIVE_WEAK_MAP = require_weak_map_basic_detection();
	var $Object = Object;
	var isArray = Array.isArray;
	var isExtensible = $Object.isExtensible;
	var isFrozen = $Object.isFrozen;
	var isSealed = $Object.isSealed;
	var freeze = $Object.freeze;
	var seal = $Object.seal;
	var IS_IE11 = !globalThis.ActiveXObject && "ActiveXObject" in globalThis;
	var InternalWeakMap;
	var wrapper = function(init) {
		return function WeakMap() {
			return init(this, arguments.length ? arguments[0] : void 0);
		};
	};
	var $WeakMap = collection("WeakMap", wrapper, collectionWeak);
	var WeakMapPrototype = $WeakMap.prototype;
	var nativeSet = uncurryThis(WeakMapPrototype.set);
	var hasMSEdgeFreezingBug = function() {
		return FREEZING && fails(function() {
			var frozenArray = freeze([]);
			nativeSet(new $WeakMap(), frozenArray, 1);
			return !isFrozen(frozenArray);
		});
	};
	if (NATIVE_WEAK_MAP) {
		if (IS_IE11) {
			InternalWeakMap = collectionWeak.getConstructor(wrapper, "WeakMap", true);
			InternalMetadataModule.enable();
			var nativeDelete = uncurryThis(WeakMapPrototype["delete"]);
			var nativeHas = uncurryThis(WeakMapPrototype.has);
			var nativeGet = uncurryThis(WeakMapPrototype.get);
			defineBuiltIns(WeakMapPrototype, {
				"delete": function(key) {
					if (isObject(key) && !isExtensible(key)) {
						var state = enforceInternalState(this);
						if (!state.frozen) state.frozen = new InternalWeakMap();
						return nativeDelete(this, key) || state.frozen["delete"](key);
					}
					return nativeDelete(this, key);
				},
				has: function has(key) {
					if (isObject(key) && !isExtensible(key)) {
						var state = enforceInternalState(this);
						if (!state.frozen) state.frozen = new InternalWeakMap();
						return nativeHas(this, key) || state.frozen.has(key);
					}
					return nativeHas(this, key);
				},
				get: function get(key) {
					if (isObject(key) && !isExtensible(key)) {
						var state = enforceInternalState(this);
						if (!state.frozen) state.frozen = new InternalWeakMap();
						return nativeHas(this, key) ? nativeGet(this, key) : state.frozen.get(key);
					}
					return nativeGet(this, key);
				},
				set: function set(key, value) {
					if (isObject(key) && !isExtensible(key)) {
						var state = enforceInternalState(this);
						if (!state.frozen) state.frozen = new InternalWeakMap();
						nativeHas(this, key) ? nativeSet(this, key, value) : state.frozen.set(key, value);
					} else nativeSet(this, key, value);
					return this;
				}
			});
		} else if (hasMSEdgeFreezingBug()) defineBuiltIns(WeakMapPrototype, { set: function set(key, value) {
			var arrayIntegrityLevel;
			if (isArray(key)) {
				if (isFrozen(key)) arrayIntegrityLevel = freeze;
				else if (isSealed(key)) arrayIntegrityLevel = seal;
			}
			nativeSet(this, key, value);
			if (arrayIntegrityLevel) arrayIntegrityLevel(key);
			return this;
		} });
	}
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.weak-map.js
var require_es_weak_map = /* @__PURE__ */ __commonJSMin((() => {
	require_es_weak_map_constructor();
}));
//#endregion
//#region node_modules/core-js-pure/internals/caller.js
var require_caller = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(methodName, numArgs) {
		return numArgs === 1 ? function(object, arg) {
			return object[methodName](arg);
		} : function(object, arg1, arg2) {
			return object[methodName](arg1, arg2);
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/weak-map-helpers.js
var require_weak_map_helpers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var caller = require_caller();
	module.exports = {
		WeakMap: getBuiltIn("WeakMap"),
		set: caller("set", 2),
		get: caller("get", 1),
		has: caller("has", 1),
		remove: caller("delete", 1)
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.weak-map.get-or-insert.js
var require_es_weak_map_get_or_insert = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var WeakMapHelpers = require_weak_map_helpers();
	var IS_PURE = require_is_pure();
	var get = WeakMapHelpers.get;
	var has = WeakMapHelpers.has;
	var set = WeakMapHelpers.set;
	$({
		target: "WeakMap",
		proto: true,
		real: true,
		forced: IS_PURE
	}, { getOrInsert: function getOrInsert(key, value) {
		if (has(this, key)) return get(this, key);
		set(this, key, value);
		return value;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-weak-map.js
var require_a_weak_map = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var tryToString = require_try_to_string();
	var $TypeError = TypeError;
	module.exports = function(it) {
		if (typeof it == "object" && "has" in it && "get" in it && "set" in it && "delete" in it) return it;
		throw new $TypeError(tryToString(it) + " is not a weakmap");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-weak-key.js
var require_a_weak_key = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var WeakMapHelpers = require_weak_map_helpers();
	var weakmap = new WeakMapHelpers.WeakMap();
	var set = WeakMapHelpers.set;
	var remove = WeakMapHelpers.remove;
	module.exports = function(key) {
		set(weakmap, key, 1);
		remove(weakmap, key);
		return key;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.weak-map.get-or-insert-computed.js
var require_es_weak_map_get_or_insert_computed = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var aCallable = require_a_callable();
	var aWeakMap = require_a_weak_map();
	var aWeakKey = require_a_weak_key();
	var WeakMapHelpers = require_weak_map_helpers();
	var IS_PURE = require_is_pure();
	var get = WeakMapHelpers.get;
	var has = WeakMapHelpers.has;
	var set = WeakMapHelpers.set;
	$({
		target: "WeakMap",
		proto: true,
		real: true,
		forced: IS_PURE || !function() {
			try {
				if (WeakMap.prototype.getOrInsertComputed) (/* @__PURE__ */ new WeakMap()).getOrInsertComputed(1, function() {
					throw 1;
				});
			} catch (error) {
				return error instanceof TypeError;
			}
		}()
	}, { getOrInsertComputed: function getOrInsertComputed(key, callbackfn) {
		if (!IS_PURE) aWeakMap(this);
		aWeakKey(key);
		aCallable(callbackfn);
		if (has(this, key)) return get(this, key);
		var value = callbackfn(key);
		set(this, key, value);
		return value;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/weak-map/index.js
var require_weak_map$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	require_es_weak_map();
	require_es_weak_map_get_or_insert();
	require_es_weak_map_get_or_insert_computed();
	module.exports = require_path().WeakMap;
}));
//#endregion
//#region node_modules/core-js-pure/internals/dom-iterables.js
var require_dom_iterables = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		CSSRuleList: 0,
		CSSStyleDeclaration: 0,
		CSSValueList: 0,
		ClientRectList: 0,
		DOMRectList: 0,
		DOMStringList: 0,
		DOMTokenList: 1,
		DataTransferItemList: 0,
		FileList: 0,
		HTMLAllCollection: 0,
		HTMLCollection: 0,
		HTMLFormElement: 0,
		HTMLSelectElement: 0,
		MediaList: 0,
		MimeTypeArray: 0,
		NamedNodeMap: 0,
		NodeList: 1,
		PaintRequestList: 0,
		Plugin: 0,
		PluginArray: 0,
		SVGLengthList: 0,
		SVGNumberList: 0,
		SVGPathSegList: 0,
		SVGPointList: 0,
		SVGStringList: 0,
		SVGTransformList: 0,
		SourceBufferList: 0,
		StyleSheetList: 0,
		TextTrackCueList: 0,
		TextTrackList: 0,
		TouchList: 0
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/web.dom-collections.iterator.js
var require_web_dom_collections_iterator = /* @__PURE__ */ __commonJSMin((() => {
	require_es_array_iterator();
	var DOMIterables = require_dom_iterables();
	var globalThis = require_global_this$6();
	var setToStringTag = require_set_to_string_tag();
	var Iterators = require_iterators();
	for (var COLLECTION_NAME in DOMIterables) {
		setToStringTag(globalThis[COLLECTION_NAME], COLLECTION_NAME);
		Iterators[COLLECTION_NAME] = Iterators.Array;
	}
}));
//#endregion
//#region node_modules/core-js-pure/stable/weak-map/index.js
var require_weak_map$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_weak_map$2();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/weak-map.js
var require_weak_map = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_weak_map$1();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/helpers/interopRequireDefault.js
var require_interopRequireDefault = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	function _interopRequireDefault(e) {
		return e && e.__esModule ? e : { "default": e };
	}
	module.exports = _interopRequireDefault, module.exports.__esModule = true, module.exports["default"] = module.exports;
}));
//#endregion
//#region node_modules/core-js-pure/actual/object/define-property.js
var require_define_property$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_property$4();
}));
//#endregion
//#region node_modules/core-js-pure/full/object/define-property.js
var require_define_property$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_property$2();
}));
//#endregion
//#region node_modules/core-js-pure/features/object/define-property.js
var require_define_property = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_define_property$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/does-not-exceed-safe-integer.js
var require_does_not_exceed_safe_integer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $TypeError = TypeError;
	var MAX_SAFE_INTEGER = 9007199254740991;
	module.exports = function(it) {
		if (it > MAX_SAFE_INTEGER) throw new $TypeError("Maximum allowed index exceeded");
		return it;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-set-length.js
var require_array_set_length = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var isArray = require_is_array$3();
	var $TypeError = TypeError;
	var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
	module.exports = DESCRIPTORS && !function() {
		if (this !== void 0) return true;
		try {
			Object.defineProperty([], "length", { writable: false }).length = 1;
		} catch (error) {
			return error instanceof TypeError;
		}
	}() ? function(O, length) {
		if (isArray(O) && !getOwnPropertyDescriptor(O, "length").writable) throw new $TypeError("Cannot set read only .length");
		return O.length = length;
	} : function(O, length) {
		return O.length = length;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.concat.js
var require_es_array_concat = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var fails = require_fails();
	var isArray = require_is_array$3();
	var isObject = require_is_object();
	var toObject = require_to_object();
	var lengthOfArrayLike = require_length_of_array_like();
	var doesNotExceedSafeInteger = require_does_not_exceed_safe_integer();
	var createProperty = require_create_property();
	var setArrayLength = require_array_set_length();
	var arraySpeciesCreate = require_array_species_create();
	var arrayMethodHasSpeciesSupport = require_array_method_has_species_support();
	var wellKnownSymbol = require_well_known_symbol();
	var V8_VERSION = require_environment_v8_version();
	var IS_CONCAT_SPREADABLE = wellKnownSymbol("isConcatSpreadable");
	var IS_CONCAT_SPREADABLE_SUPPORT = V8_VERSION >= 51 || !fails(function() {
		var array = [];
		array[IS_CONCAT_SPREADABLE] = false;
		return array.concat()[0] !== array;
	});
	var isConcatSpreadable = function(O) {
		if (!isObject(O)) return false;
		var spreadable = O[IS_CONCAT_SPREADABLE];
		return spreadable !== void 0 ? !!spreadable : isArray(O);
	};
	$({
		target: "Array",
		proto: true,
		arity: 1,
		forced: !IS_CONCAT_SPREADABLE_SUPPORT || !arrayMethodHasSpeciesSupport("concat")
	}, { concat: function concat(arg) {
		var O = toObject(this);
		var A = arraySpeciesCreate(O, 0);
		var n = 0;
		var i, k, length, len, E;
		for (i = -1, length = arguments.length; i < length; i++) {
			E = i === -1 ? O : arguments[i];
			if (isConcatSpreadable(E)) {
				len = lengthOfArrayLike(E);
				doesNotExceedSafeInteger(n + len);
				for (k = 0; k < len; k++, n++) if (k in E) createProperty(A, n, E[k]);
			} else {
				doesNotExceedSafeInteger(n + 1);
				createProperty(A, n++, E);
			}
		}
		setArrayLength(A, n);
		return A;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.async-dispose.js
var require_es_symbol_async_dispose = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("asyncDispose");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.async-iterator.js
var require_es_symbol_async_iterator = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("asyncIterator");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.description.js
var require_es_symbol_description = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.dispose.js
var require_es_symbol_dispose = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("dispose");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.has-instance.js
var require_es_symbol_has_instance = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("hasInstance");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.is-concat-spreadable.js
var require_es_symbol_is_concat_spreadable = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("isConcatSpreadable");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.iterator.js
var require_es_symbol_iterator = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("iterator");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.match.js
var require_es_symbol_match = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("match");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.match-all.js
var require_es_symbol_match_all = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("matchAll");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.replace.js
var require_es_symbol_replace = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("replace");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.search.js
var require_es_symbol_search = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("search");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.species.js
var require_es_symbol_species = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("species");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.split.js
var require_es_symbol_split = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("split");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.to-primitive.js
var require_es_symbol_to_primitive = /* @__PURE__ */ __commonJSMin((() => {
	var defineWellKnownSymbol = require_well_known_symbol_define();
	var defineSymbolToPrimitive = require_symbol_define_to_primitive();
	defineWellKnownSymbol("toPrimitive");
	defineSymbolToPrimitive();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.to-string-tag.js
var require_es_symbol_to_string_tag = /* @__PURE__ */ __commonJSMin((() => {
	var getBuiltIn = require_get_built_in();
	var defineWellKnownSymbol = require_well_known_symbol_define();
	var setToStringTag = require_set_to_string_tag();
	defineWellKnownSymbol("toStringTag");
	setToStringTag(getBuiltIn("Symbol"), "Symbol");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.symbol.unscopables.js
var require_es_symbol_unscopables = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("unscopables");
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.json.to-string-tag.js
var require_es_json_to_string_tag = /* @__PURE__ */ __commonJSMin((() => {
	var globalThis = require_global_this$6();
	require_set_to_string_tag()(globalThis.JSON, "JSON", true);
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.math.to-string-tag.js
var require_es_math_to_string_tag = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/modules/es.reflect.to-string-tag.js
var require_es_reflect_to_string_tag = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/es/symbol/index.js
var require_symbol$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_concat();
	require_es_object_to_string();
	require_es_symbol();
	require_es_symbol_async_dispose();
	require_es_symbol_async_iterator();
	require_es_symbol_description();
	require_es_symbol_dispose();
	require_es_symbol_has_instance();
	require_es_symbol_is_concat_spreadable();
	require_es_symbol_iterator();
	require_es_symbol_match();
	require_es_symbol_match_all();
	require_es_symbol_replace();
	require_es_symbol_search();
	require_es_symbol_species();
	require_es_symbol_split();
	require_es_symbol_to_primitive();
	require_es_symbol_to_string_tag();
	require_es_symbol_unscopables();
	require_es_json_to_string_tag();
	require_es_math_to_string_tag();
	require_es_reflect_to_string_tag();
	module.exports = require_path().Symbol;
}));
//#endregion
//#region node_modules/core-js-pure/stable/symbol/index.js
var require_symbol$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_symbol$4();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.function.metadata.js
var require_esnext_function_metadata = /* @__PURE__ */ __commonJSMin((() => {
	var wellKnownSymbol = require_well_known_symbol();
	var defineProperty = require_object_define_property().f;
	var METADATA = wellKnownSymbol("metadata");
	var FunctionPrototype = Function.prototype;
	if (FunctionPrototype[METADATA] === void 0) defineProperty(FunctionPrototype, METADATA, { value: null });
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.async-dispose.js
var require_esnext_symbol_async_dispose = /* @__PURE__ */ __commonJSMin((() => {
	require_es_symbol_async_dispose();
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.dispose.js
var require_esnext_symbol_dispose = /* @__PURE__ */ __commonJSMin((() => {
	require_es_symbol_dispose();
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.metadata.js
var require_esnext_symbol_metadata = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("metadata");
}));
//#endregion
//#region node_modules/core-js-pure/actual/symbol/index.js
var require_symbol$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_symbol$3();
	require_esnext_function_metadata();
	require_esnext_symbol_async_dispose();
	require_esnext_symbol_dispose();
	require_esnext_symbol_metadata();
	module.exports = parent;
}));
//#endregion
//#region node_modules/core-js-pure/internals/symbol-is-registered.js
var require_symbol_is_registered = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var uncurryThis = require_function_uncurry_this();
	var Symbol = getBuiltIn("Symbol");
	var keyFor = Symbol.keyFor;
	var thisSymbolValue = uncurryThis(Symbol.prototype.valueOf);
	module.exports = Symbol.isRegisteredSymbol || function isRegisteredSymbol(value) {
		try {
			return keyFor(thisSymbolValue(value)) !== void 0;
		} catch (error) {
			return false;
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.is-registered-symbol.js
var require_esnext_symbol_is_registered_symbol = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Symbol",
		stat: true
	}, { isRegisteredSymbol: require_symbol_is_registered() });
}));
//#endregion
//#region node_modules/core-js-pure/internals/symbol-is-well-known.js
var require_symbol_is_well_known = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var shared = require_shared();
	var getBuiltIn = require_get_built_in();
	var uncurryThis = require_function_uncurry_this();
	var isSymbol = require_is_symbol();
	var wellKnownSymbol = require_well_known_symbol();
	var Symbol = getBuiltIn("Symbol");
	var $isWellKnownSymbol = Symbol.isWellKnownSymbol;
	var getOwnPropertyNames = getBuiltIn("Object", "getOwnPropertyNames");
	var thisSymbolValue = uncurryThis(Symbol.prototype.valueOf);
	var WellKnownSymbolsStore = shared("wks");
	for (var i = 0, symbolKeys = getOwnPropertyNames(Symbol), symbolKeysLength = symbolKeys.length; i < symbolKeysLength; i++) try {
		var symbolKey = symbolKeys[i];
		if (isSymbol(Symbol[symbolKey])) wellKnownSymbol(symbolKey);
	} catch (error) {}
	module.exports = function isWellKnownSymbol(value) {
		if ($isWellKnownSymbol && $isWellKnownSymbol(value)) return true;
		try {
			var symbol = thisSymbolValue(value);
			for (var j = 0, keys = getOwnPropertyNames(WellKnownSymbolsStore), keysLength = keys.length; j < keysLength; j++) if (WellKnownSymbolsStore[keys[j]] == symbol) return true;
		} catch (error) {}
		return false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.is-well-known-symbol.js
var require_esnext_symbol_is_well_known_symbol = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Symbol",
		stat: true,
		forced: true
	}, { isWellKnownSymbol: require_symbol_is_well_known() });
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.custom-matcher.js
var require_esnext_symbol_custom_matcher = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("customMatcher");
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.observable.js
var require_esnext_symbol_observable = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("observable");
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.is-registered.js
var require_esnext_symbol_is_registered = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Symbol",
		stat: true,
		name: "isRegisteredSymbol"
	}, { isRegistered: require_symbol_is_registered() });
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.is-well-known.js
var require_esnext_symbol_is_well_known = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Symbol",
		stat: true,
		name: "isWellKnownSymbol",
		forced: true
	}, { isWellKnown: require_symbol_is_well_known() });
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.matcher.js
var require_esnext_symbol_matcher = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("matcher");
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.metadata-key.js
var require_esnext_symbol_metadata_key = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("metadataKey");
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.pattern-match.js
var require_esnext_symbol_pattern_match = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("patternMatch");
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.symbol.replace-all.js
var require_esnext_symbol_replace_all = /* @__PURE__ */ __commonJSMin((() => {
	require_well_known_symbol_define()("replaceAll");
}));
//#endregion
//#region node_modules/core-js-pure/full/symbol/index.js
var require_symbol$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_symbol$2();
	require_esnext_symbol_is_registered_symbol();
	require_esnext_symbol_is_well_known_symbol();
	require_esnext_symbol_custom_matcher();
	require_esnext_symbol_observable();
	require_esnext_symbol_is_registered();
	require_esnext_symbol_is_well_known();
	require_esnext_symbol_matcher();
	require_esnext_symbol_metadata_key();
	require_esnext_symbol_pattern_match();
	require_esnext_symbol_replace_all();
	module.exports = parent;
}));
//#endregion
//#region node_modules/core-js-pure/features/symbol/index.js
var require_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_symbol$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/string-multibyte.js
var require_string_multibyte = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var toString = require_to_string();
	var requireObjectCoercible = require_require_object_coercible();
	var charAt = uncurryThis("".charAt);
	var charCodeAt = uncurryThis("".charCodeAt);
	var stringSlice = uncurryThis("".slice);
	var createMethod = function(CONVERT_TO_STRING) {
		return function($this, pos) {
			var S = toString(requireObjectCoercible($this));
			var position = toIntegerOrInfinity(pos);
			var size = S.length;
			var first, second;
			if (position < 0 || position >= size) return CONVERT_TO_STRING ? "" : void 0;
			first = charCodeAt(S, position);
			return first < 55296 || first > 56319 || position + 1 === size || (second = charCodeAt(S, position + 1)) < 56320 || second > 57343 ? CONVERT_TO_STRING ? charAt(S, position) : first : CONVERT_TO_STRING ? stringSlice(S, position, position + 2) : (first - 55296 << 10) + (second - 56320) + 65536;
		};
	};
	module.exports = {
		codeAt: createMethod(false),
		charAt: createMethod(true)
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.string.iterator.js
var require_es_string_iterator = /* @__PURE__ */ __commonJSMin((() => {
	var charAt = require_string_multibyte().charAt;
	var toString = require_to_string();
	var InternalStateModule = require_internal_state();
	var defineIterator = require_iterator_define();
	var createIterResultObject = require_create_iter_result_object();
	var STRING_ITERATOR = "String Iterator";
	var setInternalState = InternalStateModule.set;
	var getInternalState = InternalStateModule.getterFor(STRING_ITERATOR);
	defineIterator(String, "String", function(iterated) {
		setInternalState(this, {
			type: STRING_ITERATOR,
			string: toString(iterated),
			index: 0
		});
	}, function next() {
		var state = getInternalState(this);
		var string = state.string;
		var index = state.index;
		var point;
		if (index >= string.length) return createIterResultObject(void 0, true);
		point = charAt(string, index);
		state.index += point.length;
		return createIterResultObject(point, false);
	});
}));
//#endregion
//#region node_modules/core-js-pure/es/symbol/iterator.js
var require_iterator$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	require_es_string_iterator();
	require_es_symbol_iterator();
	module.exports = require_well_known_symbol_wrapped().f("iterator");
}));
//#endregion
//#region node_modules/core-js-pure/stable/symbol/iterator.js
var require_iterator$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_iterator$4();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/core-js-pure/actual/symbol/iterator.js
var require_iterator$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_iterator$3();
}));
//#endregion
//#region node_modules/core-js-pure/full/symbol/iterator.js
var require_iterator$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_iterator$2();
}));
//#endregion
//#region node_modules/core-js-pure/features/symbol/iterator.js
var require_iterator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_iterator$1();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/helpers/typeof.js
var require_typeof = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var _Symbol = require_symbol();
	var _Symbol$iterator = require_iterator();
	function _typeof(o) {
		"@babel/helpers - typeof";
		return module.exports = _typeof = "function" == typeof _Symbol && "symbol" == typeof _Symbol$iterator ? function(o) {
			return typeof o;
		} : function(o) {
			return o && "function" == typeof _Symbol && o.constructor === _Symbol && o !== _Symbol.prototype ? "symbol" : typeof o;
		}, module.exports.__esModule = true, module.exports["default"] = module.exports, _typeof(o);
	}
	module.exports = _typeof, module.exports.__esModule = true, module.exports["default"] = module.exports;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.date.to-primitive.js
var require_es_date_to_primitive = /* @__PURE__ */ __commonJSMin((() => {}));
//#endregion
//#region node_modules/core-js-pure/es/symbol/to-primitive.js
var require_to_primitive$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_date_to_primitive();
	require_es_symbol_to_primitive();
	module.exports = require_well_known_symbol_wrapped().f("toPrimitive");
}));
//#endregion
//#region node_modules/core-js-pure/stable/symbol/to-primitive.js
var require_to_primitive$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_to_primitive$4();
}));
//#endregion
//#region node_modules/core-js-pure/actual/symbol/to-primitive.js
var require_to_primitive$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_to_primitive$3();
}));
//#endregion
//#region node_modules/core-js-pure/full/symbol/to-primitive.js
var require_to_primitive$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_to_primitive$2();
}));
//#endregion
//#region node_modules/core-js-pure/features/symbol/to-primitive.js
var require_to_primitive = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_to_primitive$1();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/helpers/toPrimitive.js
var require_toPrimitive = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var _Symbol$toPrimitive = require_to_primitive();
	var _typeof = require_typeof()["default"];
	function toPrimitive(t, r) {
		if ("object" != _typeof(t) || !t) return t;
		var e = t[_Symbol$toPrimitive];
		if (void 0 !== e) {
			var i = e.call(t, r || "default");
			if ("object" != _typeof(i)) return i;
			throw new TypeError("@@toPrimitive must return a primitive value.");
		}
		return ("string" === r ? String : Number)(t);
	}
	module.exports = toPrimitive, module.exports.__esModule = true, module.exports["default"] = module.exports;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/helpers/toPropertyKey.js
var require_toPropertyKey = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var _typeof = require_typeof()["default"];
	var toPrimitive = require_toPrimitive();
	function toPropertyKey(t) {
		var i = toPrimitive(t, "string");
		return "symbol" == _typeof(i) ? i : i + "";
	}
	module.exports = toPropertyKey, module.exports.__esModule = true, module.exports["default"] = module.exports;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/helpers/defineProperty.js
var require_defineProperty = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var _Object$defineProperty = require_define_property();
	var toPropertyKey = require_toPropertyKey();
	function _defineProperty(e, r, t) {
		return (r = toPropertyKey(r)) in e ? _Object$defineProperty(e, r, {
			value: t,
			enumerable: !0,
			configurable: !0,
			writable: !0
		}) : e[r] = t, e;
	}
	module.exports = _defineProperty, module.exports.__esModule = true, module.exports["default"] = module.exports;
}));
//#endregion
//#region node_modules/core-js-pure/internals/copy-constructor-properties.js
var require_copy_constructor_properties = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var hasOwn = require_has_own_property();
	var ownKeys = require_own_keys();
	var getOwnPropertyDescriptorModule = require_object_get_own_property_descriptor();
	var definePropertyModule = require_object_define_property();
	module.exports = function(target, source, exceptions) {
		var keys = ownKeys(source);
		var defineProperty = definePropertyModule.f;
		var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
		for (var i = 0; i < keys.length; i++) {
			var key = keys[i];
			if (!hasOwn(target, key) && !(exceptions && hasOwn(exceptions, key))) defineProperty(target, key, getOwnPropertyDescriptor(source, key));
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/install-error-cause.js
var require_install_error_cause = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	module.exports = function(O, options) {
		if (isObject(options) && "cause" in options) createNonEnumerableProperty(O, "cause", options.cause);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/error-stack-clear.js
var require_error_stack_clear = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var $Error = Error;
	var replace = uncurryThis("".replace);
	var TEST = (function(arg) {
		return String(new $Error(arg).stack);
	})("zxcasd");
	var V8_OR_CHAKRA_STACK_ENTRY = /\n\s*at [^:]*:[^\n]*/;
	var IS_V8_OR_CHAKRA_STACK = V8_OR_CHAKRA_STACK_ENTRY.test(TEST);
	module.exports = function(stack, dropEntries) {
		if (IS_V8_OR_CHAKRA_STACK && typeof stack == "string" && !$Error.prepareStackTrace) while (dropEntries--) stack = replace(stack, V8_OR_CHAKRA_STACK_ENTRY, "");
		return stack;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/error-stack-installable.js
var require_error_stack_installable = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fails = require_fails();
	var createPropertyDescriptor = require_create_property_descriptor();
	module.exports = !fails(function() {
		var error = /* @__PURE__ */ new Error("a");
		if (!("stack" in error)) return true;
		Object.defineProperty(error, "stack", createPropertyDescriptor(1, 7));
		return error.stack !== 7;
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/error-stack-install.js
var require_error_stack_install = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var clearErrorStack = require_error_stack_clear();
	var ERROR_STACK_INSTALLABLE = require_error_stack_installable();
	var captureStackTrace = Error.captureStackTrace;
	module.exports = function(error, C, stack, dropEntries) {
		if (ERROR_STACK_INSTALLABLE) {
			if (captureStackTrace) captureStackTrace(error, C);
			else createNonEnumerableProperty(error, "stack", clearErrorStack(stack, dropEntries));
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/normalize-string-argument.js
var require_normalize_string_argument = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toString = require_to_string();
	module.exports = function(argument, $default) {
		return argument === void 0 ? arguments.length < 2 ? "" : $default : toString(argument);
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.aggregate-error.constructor.js
var require_es_aggregate_error_constructor = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var isPrototypeOf = require_object_is_prototype_of();
	var getPrototypeOf = require_object_get_prototype_of();
	var setPrototypeOf = require_object_set_prototype_of();
	var copyConstructorProperties = require_copy_constructor_properties();
	var create = require_object_create();
	var createNonEnumerableProperty = require_create_non_enumerable_property();
	var createPropertyDescriptor = require_create_property_descriptor();
	var installErrorCause = require_install_error_cause();
	var installErrorStack = require_error_stack_install();
	var iterate = require_iterate();
	var normalizeStringArgument = require_normalize_string_argument();
	var TO_STRING_TAG = require_well_known_symbol()("toStringTag");
	var $Error = Error;
	var push = [].push;
	var $AggregateError = function AggregateError(errors, message) {
		var isInstance = isPrototypeOf(AggregateErrorPrototype, this);
		var that;
		if (setPrototypeOf) that = setPrototypeOf(new $Error(), isInstance ? getPrototypeOf(this) : AggregateErrorPrototype);
		else {
			that = isInstance ? this : create(AggregateErrorPrototype);
			createNonEnumerableProperty(that, TO_STRING_TAG, "Error");
		}
		if (message !== void 0) createNonEnumerableProperty(that, "message", normalizeStringArgument(message));
		installErrorStack(that, $AggregateError, that.stack, 1);
		if (arguments.length > 2) installErrorCause(that, arguments[2]);
		var errorsArray = [];
		iterate(errors, push, { that: errorsArray });
		createNonEnumerableProperty(that, "errors", errorsArray);
		return that;
	};
	if (setPrototypeOf) setPrototypeOf($AggregateError, $Error);
	else copyConstructorProperties($AggregateError, $Error, { name: true });
	var AggregateErrorPrototype = $AggregateError.prototype = create($Error.prototype, {
		constructor: createPropertyDescriptor(1, $AggregateError),
		message: createPropertyDescriptor(1, ""),
		name: createPropertyDescriptor(1, "AggregateError")
	});
	$({
		global: true,
		constructor: true,
		arity: 2
	}, { AggregateError: $AggregateError });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.aggregate-error.js
var require_es_aggregate_error = /* @__PURE__ */ __commonJSMin((() => {
	require_es_aggregate_error_constructor();
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment.js
var require_environment = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var userAgent = require_environment_user_agent();
	var classof = require_classof_raw();
	var userAgentStartsWith = function(string) {
		return userAgent.slice(0, string.length) === string;
	};
	module.exports = (function() {
		if (userAgentStartsWith("Bun/")) return "BUN";
		if (userAgentStartsWith("Cloudflare-Workers")) return "CLOUDFLARE";
		if (userAgentStartsWith("Deno/")) return "DENO";
		if (userAgentStartsWith("Node.js/")) return "NODE";
		if (globalThis.Bun && typeof Bun.version == "string") return "BUN";
		if (globalThis.Deno && typeof Deno.version == "object") return "DENO";
		if (classof(globalThis.process) === "process") return "NODE";
		if (globalThis.window && globalThis.document) return "BROWSER";
		return "REST";
	})();
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-is-node.js
var require_environment_is_node = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_environment() === "NODE";
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-species.js
var require_set_species = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var defineBuiltInAccessor = require_define_built_in_accessor();
	var wellKnownSymbol = require_well_known_symbol();
	var DESCRIPTORS = require_descriptors();
	var SPECIES = wellKnownSymbol("species");
	module.exports = function(CONSTRUCTOR_NAME) {
		var Constructor = getBuiltIn(CONSTRUCTOR_NAME);
		if (DESCRIPTORS && Constructor && !Constructor[SPECIES]) defineBuiltInAccessor(Constructor, SPECIES, {
			configurable: true,
			get: function() {
				return this;
			}
		});
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-constructor.js
var require_a_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isConstructor = require_is_constructor();
	var tryToString = require_try_to_string();
	var $TypeError = TypeError;
	module.exports = function(argument) {
		if (isConstructor(argument)) return argument;
		throw new $TypeError(tryToString(argument) + " is not a constructor");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/species-constructor.js
var require_species_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var anObject = require_an_object();
	var aConstructor = require_a_constructor();
	var isNullOrUndefined = require_is_null_or_undefined();
	var SPECIES = require_well_known_symbol()("species");
	module.exports = function(O, defaultConstructor) {
		var C = anObject(O).constructor;
		var S;
		return C === void 0 || isNullOrUndefined(S = anObject(C)[SPECIES]) ? defaultConstructor : aConstructor(S);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/validate-arguments-length.js
var require_validate_arguments_length = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var $TypeError = TypeError;
	module.exports = function(passed, required) {
		if (passed < required) throw new $TypeError("Not enough arguments");
		return passed;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-is-ios.js
var require_environment_is_ios = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var userAgent = require_environment_user_agent();
	module.exports = /ipad|iphone|ipod/i.test(userAgent) && /applewebkit/i.test(userAgent);
}));
//#endregion
//#region node_modules/core-js-pure/internals/task.js
var require_task = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var apply = require_function_apply();
	var bind = require_function_bind_context();
	var isCallable = require_is_callable();
	var hasOwn = require_has_own_property();
	var fails = require_fails();
	var html = require_html();
	var arraySlice = require_array_slice();
	var createElement = require_document_create_element();
	var validateArgumentsLength = require_validate_arguments_length();
	var IS_IOS = require_environment_is_ios();
	var IS_NODE = require_environment_is_node();
	var set = globalThis.setImmediate;
	var clear = globalThis.clearImmediate;
	var process = globalThis.process;
	var Dispatch = globalThis.Dispatch;
	var Function = globalThis.Function;
	var MessageChannel = globalThis.MessageChannel;
	var String = globalThis.String;
	var counter = 0;
	var queue = {};
	var ONREADYSTATECHANGE = "onreadystatechange";
	var $location;
	var defer;
	var channel;
	var port;
	fails(function() {
		$location = globalThis.location;
	});
	var run = function(id) {
		if (hasOwn(queue, id)) {
			var fn = queue[id];
			delete queue[id];
			fn();
		}
	};
	var runner = function(id) {
		return function() {
			run(id);
		};
	};
	var eventListener = function(event) {
		run(event.data);
	};
	var globalPostMessageDefer = function(id) {
		globalThis.postMessage(String(id), $location.protocol + "//" + $location.host);
	};
	if (!set || !clear) {
		set = function setImmediate(handler) {
			validateArgumentsLength(arguments.length, 1);
			var fn = isCallable(handler) ? handler : Function(handler);
			var args = arraySlice(arguments, 1);
			queue[++counter] = function() {
				apply(fn, void 0, args);
			};
			defer(counter);
			return counter;
		};
		clear = function clearImmediate(id) {
			delete queue[id];
		};
		if (IS_NODE) defer = function(id) {
			process.nextTick(runner(id));
		};
		else if (Dispatch && Dispatch.now) defer = function(id) {
			Dispatch.now(runner(id));
		};
		else if (MessageChannel && !IS_IOS) {
			channel = new MessageChannel();
			port = channel.port2;
			channel.port1.onmessage = eventListener;
			defer = bind(port.postMessage, port);
		} else if (globalThis.addEventListener && isCallable(globalThis.postMessage) && !globalThis.importScripts && $location && $location.protocol !== "file:" && !fails(globalPostMessageDefer)) {
			defer = globalPostMessageDefer;
			globalThis.addEventListener("message", eventListener, false);
		} else if (ONREADYSTATECHANGE in createElement("script")) defer = function(id) {
			html.appendChild(createElement("script"))[ONREADYSTATECHANGE] = function() {
				html.removeChild(this);
				run(id);
			};
		};
		else defer = function(id) {
			setTimeout(runner(id), 0);
		};
	}
	module.exports = {
		set,
		clear
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/safe-get-built-in.js
var require_safe_get_built_in = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var DESCRIPTORS = require_descriptors();
	var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
	module.exports = function(name) {
		if (!DESCRIPTORS) return globalThis[name];
		var descriptor = getOwnPropertyDescriptor(globalThis, name);
		return descriptor && descriptor.value;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/queue.js
var require_queue = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Queue = function() {
		this.head = null;
		this.tail = null;
	};
	Queue.prototype = {
		add: function(item) {
			var entry = {
				item,
				next: null
			};
			var tail = this.tail;
			if (tail) tail.next = entry;
			else this.head = entry;
			this.tail = entry;
		},
		get: function() {
			var entry = this.head;
			if (entry) {
				if ((this.head = entry.next) === null) this.tail = null;
				return entry.item;
			}
		}
	};
	module.exports = Queue;
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-is-ios-pebble.js
var require_environment_is_ios_pebble = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var userAgent = require_environment_user_agent();
	module.exports = /ipad|iphone|ipod/i.test(userAgent) && typeof Pebble != "undefined";
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-is-webos-webkit.js
var require_environment_is_webos_webkit = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var userAgent = require_environment_user_agent();
	module.exports = /web0s(?!.*chrome)/i.test(userAgent);
}));
//#endregion
//#region node_modules/core-js-pure/internals/microtask.js
var require_microtask = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var safeGetBuiltIn = require_safe_get_built_in();
	var bind = require_function_bind_context();
	var macrotask = require_task().set;
	var Queue = require_queue();
	var IS_IOS = require_environment_is_ios();
	var IS_IOS_PEBBLE = require_environment_is_ios_pebble();
	var IS_WEBOS_WEBKIT = require_environment_is_webos_webkit();
	var IS_NODE = require_environment_is_node();
	var MutationObserver = globalThis.MutationObserver || globalThis.WebKitMutationObserver;
	var document = globalThis.document;
	var process = globalThis.process;
	var Promise = globalThis.Promise;
	var microtask = safeGetBuiltIn("queueMicrotask");
	var notify;
	var toggle;
	var node;
	var promise;
	var then;
	if (!microtask) {
		var queue = new Queue();
		var flush = function() {
			var parent, fn;
			if (IS_NODE && (parent = process.domain)) parent.exit();
			while (fn = queue.get()) try {
				fn();
			} catch (error) {
				if (queue.head) notify();
				throw error;
			}
			if (parent) parent.enter();
		};
		if (!IS_IOS && !IS_NODE && !IS_WEBOS_WEBKIT && MutationObserver && document) {
			toggle = true;
			node = document.createTextNode("");
			new MutationObserver(flush).observe(node, { characterData: true });
			notify = function() {
				node.data = toggle = !toggle;
			};
		} else if (!IS_IOS_PEBBLE && Promise && Promise.resolve) {
			promise = Promise.resolve(void 0);
			promise.constructor = Promise;
			then = bind(promise.then, promise);
			notify = function() {
				then(flush);
			};
		} else if (IS_NODE) notify = function() {
			process.nextTick(flush);
		};
		else {
			macrotask = bind(macrotask, globalThis);
			notify = function() {
				macrotask(flush);
			};
		}
		microtask = function(fn) {
			if (!queue.head) notify();
			queue.add(fn);
		};
	}
	module.exports = microtask;
}));
//#endregion
//#region node_modules/core-js-pure/internals/host-report-errors.js
var require_host_report_errors = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(a, b) {
		try {
			arguments.length === 1 ? console.error(a) : console.error(a, b);
		} catch (error) {}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/perform.js
var require_perform = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(exec) {
		try {
			return {
				error: false,
				value: exec()
			};
		} catch (error) {
			return {
				error: true,
				value: error
			};
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/promise-native-constructor.js
var require_promise_native_constructor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_global_this$6().Promise;
}));
//#endregion
//#region node_modules/core-js-pure/internals/promise-constructor-detection.js
var require_promise_constructor_detection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var NativePromiseConstructor = require_promise_native_constructor();
	var isCallable = require_is_callable();
	var isForced = require_is_forced();
	var inspectSource = require_inspect_source();
	var wellKnownSymbol = require_well_known_symbol();
	var ENVIRONMENT = require_environment();
	var IS_PURE = require_is_pure();
	var V8_VERSION = require_environment_v8_version();
	var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
	var SPECIES = wellKnownSymbol("species");
	var SUBCLASSING = false;
	var NATIVE_PROMISE_REJECTION_EVENT = isCallable(globalThis.PromiseRejectionEvent);
	module.exports = {
		CONSTRUCTOR: isForced("Promise", function() {
			var PROMISE_CONSTRUCTOR_SOURCE = inspectSource(NativePromiseConstructor);
			var GLOBAL_CORE_JS_PROMISE = PROMISE_CONSTRUCTOR_SOURCE !== String(NativePromiseConstructor);
			if (!GLOBAL_CORE_JS_PROMISE && V8_VERSION === 66) return true;
			if (IS_PURE && !(NativePromisePrototype["catch"] && NativePromisePrototype["finally"])) return true;
			if (!V8_VERSION || V8_VERSION < 51 || !/native code/.test(PROMISE_CONSTRUCTOR_SOURCE)) {
				var promise = new NativePromiseConstructor(function(resolve) {
					resolve(1);
				});
				var FakePromise = function(exec) {
					exec(function() {}, function() {});
				};
				var constructor = promise.constructor = {};
				constructor[SPECIES] = FakePromise;
				SUBCLASSING = promise.then(function() {}) instanceof FakePromise;
				if (!SUBCLASSING) return true;
			}
			return !GLOBAL_CORE_JS_PROMISE && (ENVIRONMENT === "BROWSER" || ENVIRONMENT === "DENO") && !NATIVE_PROMISE_REJECTION_EVENT;
		}),
		REJECTION_EVENT: NATIVE_PROMISE_REJECTION_EVENT,
		SUBCLASSING
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/new-promise-capability.js
var require_new_promise_capability = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aCallable = require_a_callable();
	var $TypeError = TypeError;
	var PromiseCapability = function(C) {
		var resolve, reject;
		this.promise = new C(function($$resolve, $$reject) {
			if (resolve !== void 0 || reject !== void 0) throw new $TypeError("Bad Promise constructor");
			resolve = $$resolve;
			reject = $$reject;
		});
		this.resolve = aCallable(resolve);
		this.reject = aCallable(reject);
	};
	module.exports.f = function(C) {
		return new PromiseCapability(C);
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.constructor.js
var require_es_promise_constructor = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var IS_PURE = require_is_pure();
	var IS_NODE = require_environment_is_node();
	var globalThis = require_global_this$6();
	var path = require_path();
	var call = require_function_call();
	var defineBuiltIn = require_define_built_in();
	var setPrototypeOf = require_object_set_prototype_of();
	var setToStringTag = require_set_to_string_tag();
	var setSpecies = require_set_species();
	var aCallable = require_a_callable();
	var isCallable = require_is_callable();
	var isObject = require_is_object();
	var anInstance = require_an_instance();
	var speciesConstructor = require_species_constructor();
	var task = require_task().set;
	var microtask = require_microtask();
	var hostReportErrors = require_host_report_errors();
	var perform = require_perform();
	var Queue = require_queue();
	var InternalStateModule = require_internal_state();
	var NativePromiseConstructor = require_promise_native_constructor();
	var PromiseConstructorDetection = require_promise_constructor_detection();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var PROMISE = "Promise";
	var FORCED_PROMISE_CONSTRUCTOR = PromiseConstructorDetection.CONSTRUCTOR;
	var NATIVE_PROMISE_REJECTION_EVENT = PromiseConstructorDetection.REJECTION_EVENT;
	var NATIVE_PROMISE_SUBCLASSING = PromiseConstructorDetection.SUBCLASSING;
	var getInternalPromiseState = InternalStateModule.getterFor(PROMISE);
	var setInternalState = InternalStateModule.set;
	var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
	var PromiseConstructor = NativePromiseConstructor;
	var PromisePrototype = NativePromisePrototype;
	var TypeError = globalThis.TypeError;
	var document = globalThis.document;
	var process = globalThis.process;
	var newPromiseCapability = newPromiseCapabilityModule.f;
	var newGenericPromiseCapability = newPromiseCapability;
	var DISPATCH_EVENT = !!(document && document.createEvent && globalThis.dispatchEvent);
	var UNHANDLED_REJECTION = "unhandledrejection";
	var REJECTION_HANDLED = "rejectionhandled";
	var PENDING = 0;
	var FULFILLED = 1;
	var REJECTED = 2;
	var HANDLED = 1;
	var UNHANDLED = 2;
	var Internal;
	var OwnPromiseCapability;
	var PromiseWrapper;
	var nativeThen;
	var isThenable = function(it) {
		var then;
		return isObject(it) && isCallable(then = it.then) ? then : false;
	};
	var callReaction = function(reaction, state) {
		var value = state.value;
		var ok = state.state === FULFILLED;
		var handler = ok ? reaction.ok : reaction.fail;
		var resolve = reaction.resolve;
		var reject = reaction.reject;
		var domain = reaction.domain;
		var result, then, exited;
		try {
			if (handler) {
				if (!ok) {
					if (state.rejection === UNHANDLED) onHandleUnhandled(state);
					state.rejection = HANDLED;
				}
				if (handler === true) result = value;
				else {
					if (domain) domain.enter();
					result = handler(value);
					if (domain) {
						domain.exit();
						exited = true;
					}
				}
				if (result === reaction.promise) reject(new TypeError("Promise-chain cycle"));
				else if (then = isThenable(result)) call(then, result, resolve, reject);
				else resolve(result);
			} else reject(value);
		} catch (error) {
			if (domain && !exited) domain.exit();
			reject(error);
		}
	};
	var notify = function(state, isReject) {
		if (state.notified) return;
		state.notified = true;
		microtask(function() {
			var reactions = state.reactions;
			var reaction;
			while (reaction = reactions.get()) callReaction(reaction, state);
			state.notified = false;
			if (isReject && !state.rejection) onUnhandled(state);
		});
	};
	var dispatchEvent = function(name, promise, reason) {
		var event, handler;
		if (DISPATCH_EVENT) {
			event = document.createEvent("Event");
			event.promise = promise;
			event.reason = reason;
			event.initEvent(name, false, true);
			globalThis.dispatchEvent(event);
		} else event = {
			promise,
			reason
		};
		if (!NATIVE_PROMISE_REJECTION_EVENT && (handler = globalThis["on" + name])) handler(event);
		else if (name === UNHANDLED_REJECTION) hostReportErrors("Unhandled promise rejection", reason);
	};
	var onUnhandled = function(state) {
		call(task, globalThis, function() {
			var promise = state.facade;
			var value = state.value;
			var IS_UNHANDLED = isUnhandled(state);
			var result;
			if (IS_UNHANDLED) {
				result = perform(function() {
					if (IS_NODE) process.emit("unhandledRejection", value, promise);
					else dispatchEvent(UNHANDLED_REJECTION, promise, value);
				});
				state.rejection = IS_NODE || isUnhandled(state) ? UNHANDLED : HANDLED;
				if (result.error) throw result.value;
			}
		});
	};
	var isUnhandled = function(state) {
		return state.rejection !== HANDLED && !state.parent;
	};
	var onHandleUnhandled = function(state) {
		call(task, globalThis, function() {
			var promise = state.facade;
			if (IS_NODE) process.emit("rejectionHandled", promise);
			else dispatchEvent(REJECTION_HANDLED, promise, state.value);
		});
	};
	var bind = function(fn, state, unwrap) {
		return function(value) {
			fn(state, value, unwrap);
		};
	};
	var internalReject = function(state, value, unwrap) {
		if (state.done) return;
		state.done = true;
		if (unwrap) state = unwrap;
		state.value = value;
		state.state = REJECTED;
		notify(state, true);
	};
	var internalResolve = function(state, value, unwrap) {
		if (state.done) return;
		state.done = true;
		if (unwrap) state = unwrap;
		try {
			if (state.facade === value) throw new TypeError("Promise can't be resolved itself");
			var then = isThenable(value);
			if (then) microtask(function() {
				var wrapper = { done: false };
				try {
					call(then, value, bind(internalResolve, wrapper, state), bind(internalReject, wrapper, state));
				} catch (error) {
					internalReject(wrapper, error, state);
				}
			});
			else {
				state.value = value;
				state.state = FULFILLED;
				notify(state, false);
			}
		} catch (error) {
			internalReject({ done: false }, error, state);
		}
	};
	if (FORCED_PROMISE_CONSTRUCTOR) {
		PromiseConstructor = function Promise(executor) {
			anInstance(this, PromisePrototype);
			aCallable(executor);
			call(Internal, this);
			var state = getInternalPromiseState(this);
			try {
				executor(bind(internalResolve, state), bind(internalReject, state));
			} catch (error) {
				internalReject(state, error);
			}
		};
		PromisePrototype = PromiseConstructor.prototype;
		Internal = function Promise(executor) {
			setInternalState(this, {
				type: PROMISE,
				done: false,
				notified: false,
				parent: false,
				reactions: new Queue(),
				rejection: false,
				state: PENDING,
				value: null
			});
		};
		Internal.prototype = defineBuiltIn(PromisePrototype, "then", function then(onFulfilled, onRejected) {
			var state = getInternalPromiseState(this);
			var reaction = newPromiseCapability(speciesConstructor(this, PromiseConstructor));
			state.parent = true;
			reaction.ok = isCallable(onFulfilled) ? onFulfilled : true;
			reaction.fail = isCallable(onRejected) && onRejected;
			reaction.domain = IS_NODE ? process.domain : void 0;
			if (state.state === PENDING) state.reactions.add(reaction);
			else microtask(function() {
				callReaction(reaction, state);
			});
			return reaction.promise;
		});
		OwnPromiseCapability = function() {
			var promise = new Internal();
			var state = getInternalPromiseState(promise);
			this.promise = promise;
			this.resolve = bind(internalResolve, state);
			this.reject = bind(internalReject, state);
		};
		newPromiseCapabilityModule.f = newPromiseCapability = function(C) {
			return C === PromiseConstructor || C === PromiseWrapper ? new OwnPromiseCapability(C) : newGenericPromiseCapability(C);
		};
		if (!IS_PURE && isCallable(NativePromiseConstructor) && NativePromisePrototype !== Object.prototype) {
			nativeThen = NativePromisePrototype.then;
			if (!NATIVE_PROMISE_SUBCLASSING) defineBuiltIn(NativePromisePrototype, "then", function then(onFulfilled, onRejected) {
				var that = this;
				return new PromiseConstructor(function(resolve, reject) {
					call(nativeThen, that, resolve, reject);
				}).then(onFulfilled, onRejected);
			}, { unsafe: true });
			try {
				delete NativePromisePrototype.constructor;
			} catch (error) {}
			if (setPrototypeOf) setPrototypeOf(NativePromisePrototype, PromisePrototype);
		}
	}
	$({
		global: true,
		constructor: true,
		wrap: true,
		forced: FORCED_PROMISE_CONSTRUCTOR
	}, { Promise: PromiseConstructor });
	PromiseWrapper = path.Promise;
	setToStringTag(PromiseConstructor, PROMISE, false, true);
	setSpecies(PROMISE);
}));
//#endregion
//#region node_modules/core-js-pure/internals/check-correctness-of-iteration.js
var require_check_correctness_of_iteration = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ITERATOR = require_well_known_symbol()("iterator");
	var SAFE_CLOSING = false;
	try {
		var called = 0;
		var iteratorWithReturn = {
			next: function() {
				return { done: !!called++ };
			},
			"return": function() {
				SAFE_CLOSING = true;
			}
		};
		iteratorWithReturn[ITERATOR] = function() {
			return this;
		};
		Array.from(iteratorWithReturn, function() {
			throw 2;
		});
	} catch (error) {}
	module.exports = function(exec, SKIP_CLOSING) {
		try {
			if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
		} catch (error) {
			return false;
		}
		var ITERATION_SUPPORT = false;
		try {
			var object = {};
			object[ITERATOR] = function() {
				return { next: function() {
					return { done: ITERATION_SUPPORT = true };
				} };
			};
			exec(object);
		} catch (error) {}
		return ITERATION_SUPPORT;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/promise-statics-incorrect-iteration.js
var require_promise_statics_incorrect_iteration = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var NativePromiseConstructor = require_promise_native_constructor();
	var checkCorrectnessOfIteration = require_check_correctness_of_iteration();
	module.exports = require_promise_constructor_detection().CONSTRUCTOR || !checkCorrectnessOfIteration(function(iterable) {
		NativePromiseConstructor.all(iterable).then(void 0, function() {});
	});
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.all.js
var require_es_promise_all = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var call = require_function_call();
	var aCallable = require_a_callable();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var perform = require_perform();
	var iterate = require_iterate();
	$({
		target: "Promise",
		stat: true,
		forced: require_promise_statics_incorrect_iteration()
	}, { all: function all(iterable) {
		var C = this;
		var capability = newPromiseCapabilityModule.f(C);
		var resolve = capability.resolve;
		var reject = capability.reject;
		var result = perform(function() {
			var $promiseResolve = aCallable(C.resolve);
			var values = [];
			var counter = 0;
			var remaining = 1;
			iterate(iterable, function(promise) {
				var index = counter++;
				var alreadyCalled = false;
				remaining++;
				call($promiseResolve, C, promise).then(function(value) {
					if (alreadyCalled) return;
					alreadyCalled = true;
					values[index] = value;
					--remaining || resolve(values);
				}, reject);
			});
			--remaining || resolve(values);
		});
		if (result.error) reject(result.value);
		return capability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.catch.js
var require_es_promise_catch = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var IS_PURE = require_is_pure();
	var FORCED_PROMISE_CONSTRUCTOR = require_promise_constructor_detection().CONSTRUCTOR;
	var NativePromiseConstructor = require_promise_native_constructor();
	var getBuiltIn = require_get_built_in();
	var isCallable = require_is_callable();
	var defineBuiltIn = require_define_built_in();
	var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
	$({
		target: "Promise",
		proto: true,
		forced: FORCED_PROMISE_CONSTRUCTOR,
		real: true
	}, { "catch": function(onRejected) {
		return this.then(void 0, onRejected);
	} });
	if (!IS_PURE && isCallable(NativePromiseConstructor)) {
		var method = getBuiltIn("Promise").prototype["catch"];
		if (NativePromisePrototype["catch"] !== method) defineBuiltIn(NativePromisePrototype, "catch", method, { unsafe: true });
	}
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.race.js
var require_es_promise_race = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var call = require_function_call();
	var aCallable = require_a_callable();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var perform = require_perform();
	var iterate = require_iterate();
	$({
		target: "Promise",
		stat: true,
		forced: require_promise_statics_incorrect_iteration()
	}, { race: function race(iterable) {
		var C = this;
		var capability = newPromiseCapabilityModule.f(C);
		var reject = capability.reject;
		var result = perform(function() {
			var $promiseResolve = aCallable(C.resolve);
			iterate(iterable, function(promise) {
				call($promiseResolve, C, promise).then(capability.resolve, reject);
			});
		});
		if (result.error) reject(result.value);
		return capability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.reject.js
var require_es_promise_reject = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var FORCED_PROMISE_CONSTRUCTOR = require_promise_constructor_detection().CONSTRUCTOR;
	$({
		target: "Promise",
		stat: true,
		forced: FORCED_PROMISE_CONSTRUCTOR
	}, { reject: function reject(r) {
		var capability = newPromiseCapabilityModule.f(this);
		var capabilityReject = capability.reject;
		capabilityReject(r);
		return capability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/internals/promise-resolve.js
var require_promise_resolve = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var anObject = require_an_object();
	var isObject = require_is_object();
	var newPromiseCapability = require_new_promise_capability();
	module.exports = function(C, x) {
		anObject(C);
		if (isObject(x) && x.constructor === C) return x;
		var promiseCapability = newPromiseCapability.f(C);
		var resolve = promiseCapability.resolve;
		resolve(x);
		return promiseCapability.promise;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.resolve.js
var require_es_promise_resolve = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var getBuiltIn = require_get_built_in();
	var IS_PURE = require_is_pure();
	var NativePromiseConstructor = require_promise_native_constructor();
	var FORCED_PROMISE_CONSTRUCTOR = require_promise_constructor_detection().CONSTRUCTOR;
	var promiseResolve = require_promise_resolve();
	var PromiseConstructorWrapper = getBuiltIn("Promise");
	var CHECK_WRAPPER = IS_PURE && !FORCED_PROMISE_CONSTRUCTOR;
	$({
		target: "Promise",
		stat: true,
		forced: IS_PURE || FORCED_PROMISE_CONSTRUCTOR
	}, { resolve: function resolve(x) {
		return promiseResolve(CHECK_WRAPPER && this === PromiseConstructorWrapper ? NativePromiseConstructor : this, x);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.js
var require_es_promise = /* @__PURE__ */ __commonJSMin((() => {
	require_es_promise_constructor();
	require_es_promise_all();
	require_es_promise_catch();
	require_es_promise_race();
	require_es_promise_reject();
	require_es_promise_resolve();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.all-settled.js
var require_es_promise_all_settled = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var call = require_function_call();
	var aCallable = require_a_callable();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var perform = require_perform();
	var iterate = require_iterate();
	$({
		target: "Promise",
		stat: true,
		forced: require_promise_statics_incorrect_iteration()
	}, { allSettled: function allSettled(iterable) {
		var C = this;
		var capability = newPromiseCapabilityModule.f(C);
		var resolve = capability.resolve;
		var reject = capability.reject;
		var result = perform(function() {
			var promiseResolve = aCallable(C.resolve);
			var values = [];
			var counter = 0;
			var remaining = 1;
			iterate(iterable, function(promise) {
				var index = counter++;
				var alreadyCalled = false;
				remaining++;
				call(promiseResolve, C, promise).then(function(value) {
					if (alreadyCalled) return;
					alreadyCalled = true;
					values[index] = {
						status: "fulfilled",
						value
					};
					--remaining || resolve(values);
				}, function(error) {
					if (alreadyCalled) return;
					alreadyCalled = true;
					values[index] = {
						status: "rejected",
						reason: error
					};
					--remaining || resolve(values);
				});
			});
			--remaining || resolve(values);
		});
		if (result.error) reject(result.value);
		return capability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.any.js
var require_es_promise_any = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var call = require_function_call();
	var aCallable = require_a_callable();
	var getBuiltIn = require_get_built_in();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var perform = require_perform();
	var iterate = require_iterate();
	var PROMISE_STATICS_INCORRECT_ITERATION = require_promise_statics_incorrect_iteration();
	var PROMISE_ANY_ERROR = "No one promise resolved";
	$({
		target: "Promise",
		stat: true,
		forced: PROMISE_STATICS_INCORRECT_ITERATION
	}, { any: function any(iterable) {
		var C = this;
		var AggregateError = getBuiltIn("AggregateError");
		var capability = newPromiseCapabilityModule.f(C);
		var resolve = capability.resolve;
		var reject = capability.reject;
		var result = perform(function() {
			var promiseResolve = aCallable(C.resolve);
			var errors = [];
			var counter = 0;
			var remaining = 1;
			var alreadyResolved = false;
			iterate(iterable, function(promise) {
				var index = counter++;
				var alreadyRejected = false;
				remaining++;
				call(promiseResolve, C, promise).then(function(value) {
					if (alreadyRejected || alreadyResolved) return;
					alreadyResolved = true;
					resolve(value);
				}, function(error) {
					if (alreadyRejected || alreadyResolved) return;
					alreadyRejected = true;
					errors[index] = error;
					--remaining || reject(new AggregateError(errors, PROMISE_ANY_ERROR));
				});
			});
			--remaining || reject(new AggregateError(errors, PROMISE_ANY_ERROR));
		});
		if (result.error) reject(result.value);
		return capability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.try.js
var require_es_promise_try = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	var apply = require_function_apply();
	var slice = require_array_slice();
	var promiseResolve = require_promise_resolve();
	var newPromiseCapabilityModule = require_new_promise_capability();
	var aCallable = require_a_callable();
	var perform = require_perform();
	var fails = require_fails();
	var Promise = globalThis.Promise;
	var ACCEPT_ARGUMENTS = false;
	$({
		target: "Promise",
		stat: true,
		forced: !Promise || !Promise["try"] || fails(function() {
			var p = Promise.resolve();
			return Promise["try"](function(argument) {
				ACCEPT_ARGUMENTS = argument === 8;
				return p;
			}, 8) !== p;
		}) || !ACCEPT_ARGUMENTS
	}, { "try": function(callbackfn) {
		var args = arguments.length > 1 ? slice(arguments, 1) : [];
		var result = perform(function() {
			return apply(aCallable(callbackfn), void 0, args);
		});
		if (!result.error) return promiseResolve(this, result.value);
		var promiseCapability = newPromiseCapabilityModule.f(this);
		var reject = promiseCapability.reject;
		reject(result.value);
		return promiseCapability.promise;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.with-resolvers.js
var require_es_promise_with_resolvers = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var newPromiseCapabilityModule = require_new_promise_capability();
	$({
		target: "Promise",
		stat: true
	}, { withResolvers: function withResolvers() {
		var promiseCapability = newPromiseCapabilityModule.f(this);
		return {
			promise: promiseCapability.promise,
			resolve: promiseCapability.resolve,
			reject: promiseCapability.reject
		};
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.promise.finally.js
var require_es_promise_finally = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var IS_PURE = require_is_pure();
	var NativePromiseConstructor = require_promise_native_constructor();
	var fails = require_fails();
	var getBuiltIn = require_get_built_in();
	var isCallable = require_is_callable();
	var speciesConstructor = require_species_constructor();
	var promiseResolve = require_promise_resolve();
	var defineBuiltIn = require_define_built_in();
	var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
	$({
		target: "Promise",
		proto: true,
		real: true,
		forced: !!NativePromiseConstructor && fails(function() {
			NativePromisePrototype["finally"].call({ then: function() {} }, function() {});
		})
	}, { "finally": function(onFinally) {
		var C = speciesConstructor(this, getBuiltIn("Promise"));
		var isFunction = isCallable(onFinally);
		return this.then(isFunction ? function(x) {
			return promiseResolve(C, onFinally()).then(function() {
				return x;
			});
		} : onFinally, isFunction ? function(e) {
			return promiseResolve(C, onFinally()).then(function() {
				throw e;
			});
		} : onFinally);
	} });
	if (!IS_PURE && isCallable(NativePromiseConstructor)) {
		var method = getBuiltIn("Promise").prototype["finally"];
		if (NativePromisePrototype["finally"] !== method) defineBuiltIn(NativePromisePrototype, "finally", method, { unsafe: true });
	}
}));
//#endregion
//#region node_modules/core-js-pure/es/promise/index.js
var require_promise$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_aggregate_error();
	require_es_array_iterator();
	require_es_object_to_string();
	require_es_promise();
	require_es_promise_all_settled();
	require_es_promise_any();
	require_es_promise_try();
	require_es_promise_with_resolvers();
	require_es_promise_finally();
	require_es_string_iterator();
	module.exports = require_path().Promise;
}));
//#endregion
//#region node_modules/core-js-pure/stable/promise/index.js
var require_promise$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_promise$2();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/promise.js
var require_promise = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_promise$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.is-array.js
var require_es_array_is_array = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Array",
		stat: true
	}, { isArray: require_is_array$3() });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/is-array.js
var require_is_array$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_is_array();
	module.exports = require_path().Array.isArray;
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/is-array.js
var require_is_array$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_is_array$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/array/is-array.js
var require_is_array = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_is_array$1();
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/concat.js
var require_concat$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_concat();
	module.exports = require_get_built_in_prototype_method()("Array", "concat");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/concat.js
var require_concat$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_concat$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.concat;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.concat ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/concat.js
var require_concat$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_concat$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/concat.js
var require_concat = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_concat$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/CoreManager.js
var require_CoreManager = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _forEach = _interopRequireDefault(require_for_each());
	var _concat = _interopRequireDefault(require_concat());
	/** Based on https://github.com/react-native-async-storage/async-storage/blob/main/packages/default-storage-backend/src/types.ts */
	var config = {
		IS_NODE: typeof process !== "undefined" && !!process.versions && !!process.versions.node && !process.versions.electron,
		REQUEST_ATTEMPT_LIMIT: 5,
		REQUEST_BATCH_SIZE: 20,
		REQUEST_HEADERS: {},
		SERVER_URL: "https://api.parse.com/1",
		LIVEQUERY_SERVER_URL: null,
		ENCRYPTED_KEY: null,
		VERSION: "js8.6.0",
		APPLICATION_ID: null,
		JAVASCRIPT_KEY: null,
		MAINTENANCE_KEY: null,
		MASTER_KEY: null,
		USE_MASTER_KEY: false,
		PERFORM_USER_REWRITE: true,
		FORCE_REVOCABLE_SESSION: false,
		ENCRYPTED_USER: false,
		IDEMPOTENCY: false,
		ALLOW_CUSTOM_OBJECT_ID: false,
		PARSE_ERRORS: [],
		NODE_LOGGING: false
	};
	function requireMethods(name, methods, controller) {
		(0, _forEach.default)(methods).call(methods, (func) => {
			if (typeof controller[func] !== "function") {
				var _context;
				throw new Error((0, _concat.default)(_context = "".concat(name, " must implement ")).call(_context, func, "()"));
			}
		});
	}
	exports.default = {
		get: function(key) {
			if (Object.hasOwn(config, key)) return config[key];
			throw new Error("Configuration key not found: " + key);
		},
		set: function(key, value) {
			config[key] = value;
		},
		setIfNeeded: function(key, value) {
			if (!Object.hasOwn(config, key)) config[key] = value;
			return config[key];
		},
		setAnalyticsController(controller) {
			requireMethods("AnalyticsController", ["track"], controller);
			config["AnalyticsController"] = controller;
		},
		getAnalyticsController() {
			return config["AnalyticsController"];
		},
		setCloudController(controller) {
			requireMethods("CloudController", [
				"run",
				"getJobsData",
				"startJob"
			], controller);
			config["CloudController"] = controller;
		},
		getCloudController() {
			return config["CloudController"];
		},
		setConfigController(controller) {
			requireMethods("ConfigController", [
				"current",
				"get",
				"save"
			], controller);
			config["ConfigController"] = controller;
		},
		getConfigController() {
			return config["ConfigController"];
		},
		setCryptoController(controller) {
			requireMethods("CryptoController", ["encrypt", "decrypt"], controller);
			config["CryptoController"] = controller;
		},
		getCryptoController() {
			return config["CryptoController"];
		},
		setEventEmitter(eventEmitter) {
			config["EventEmitter"] = eventEmitter;
		},
		getEventEmitter() {
			return config["EventEmitter"];
		},
		setFileController(controller) {
			requireMethods("FileController", ["saveFile", "saveBase64"], controller);
			config["FileController"] = controller;
		},
		setEventuallyQueue(controller) {
			requireMethods("EventuallyQueue", [
				"poll",
				"save",
				"destroy"
			], controller);
			config["EventuallyQueue"] = controller;
		},
		getEventuallyQueue() {
			return config["EventuallyQueue"];
		},
		getFileController() {
			return config["FileController"];
		},
		setInstallationController(controller) {
			requireMethods("InstallationController", [
				"currentInstallationId",
				"currentInstallation",
				"updateInstallationOnDisk"
			], controller);
			config["InstallationController"] = controller;
		},
		getInstallationController() {
			return config["InstallationController"];
		},
		setLiveQuery(liveQuery) {
			config["LiveQuery"] = liveQuery;
		},
		getLiveQuery() {
			return config["LiveQuery"];
		},
		setObjectController(controller) {
			requireMethods("ObjectController", [
				"save",
				"fetch",
				"destroy"
			], controller);
			config["ObjectController"] = controller;
		},
		getObjectController() {
			return config["ObjectController"];
		},
		setObjectStateController(controller) {
			requireMethods("ObjectStateController", [
				"getState",
				"initializeState",
				"removeState",
				"getServerData",
				"setServerData",
				"getPendingOps",
				"setPendingOp",
				"pushPendingState",
				"popPendingState",
				"mergeFirstPendingState",
				"getObjectCache",
				"estimateAttribute",
				"estimateAttributes",
				"commitServerChanges",
				"enqueueTask",
				"clearAllState"
			], controller);
			config["ObjectStateController"] = controller;
		},
		getObjectStateController() {
			return config["ObjectStateController"];
		},
		setPushController(controller) {
			requireMethods("PushController", ["send"], controller);
			config["PushController"] = controller;
		},
		getPushController() {
			return config["PushController"];
		},
		setQueryController(controller) {
			requireMethods("QueryController", ["find", "aggregate"], controller);
			config["QueryController"] = controller;
		},
		getQueryController() {
			return config["QueryController"];
		},
		setRESTController(controller) {
			requireMethods("RESTController", ["request", "ajax"], controller);
			config["RESTController"] = controller;
		},
		getRESTController() {
			return config["RESTController"];
		},
		setSchemaController(controller) {
			requireMethods("SchemaController", [
				"get",
				"create",
				"update",
				"delete",
				"send",
				"purge"
			], controller);
			config["SchemaController"] = controller;
		},
		getSchemaController() {
			return config["SchemaController"];
		},
		setSessionController(controller) {
			requireMethods("SessionController", ["getSession"], controller);
			config["SessionController"] = controller;
		},
		getSessionController() {
			return config["SessionController"];
		},
		setStorageController(controller) {
			if (controller.async) requireMethods("An async StorageController", [
				"getItemAsync",
				"setItemAsync",
				"removeItemAsync",
				"getAllKeysAsync"
			], controller);
			else requireMethods("A synchronous StorageController", [
				"getItem",
				"setItem",
				"removeItem",
				"getAllKeys"
			], controller);
			config["StorageController"] = controller;
		},
		setLocalDatastoreController(controller) {
			requireMethods("LocalDatastoreController", [
				"pinWithName",
				"fromPinWithName",
				"unPinWithName",
				"getAllContents",
				"clear"
			], controller);
			config["LocalDatastoreController"] = controller;
		},
		getLocalDatastoreController() {
			return config["LocalDatastoreController"];
		},
		setLocalDatastore(store) {
			config["LocalDatastore"] = store;
		},
		getLocalDatastore() {
			return config["LocalDatastore"];
		},
		getStorageController() {
			return config["StorageController"];
		},
		setAsyncStorage(storage) {
			config["AsyncStorage"] = storage;
		},
		getAsyncStorage() {
			return config["AsyncStorage"];
		},
		setWebSocketController(controller) {
			config["WebSocketController"] = controller;
		},
		getWebSocketController() {
			return config["WebSocketController"];
		},
		setUserController(controller) {
			requireMethods("UserController", [
				"setCurrentUser",
				"currentUser",
				"currentUserAsync",
				"signUp",
				"logIn",
				"become",
				"logOut",
				"me",
				"requestPasswordReset",
				"upgradeToRevocableSession",
				"requestEmailVerification",
				"verifyPassword",
				"linkWith"
			], controller);
			config["UserController"] = controller;
		},
		getUserController() {
			return config["UserController"];
		},
		setLiveQueryController(controller) {
			requireMethods("LiveQueryController", [
				"setDefaultLiveQueryClient",
				"getDefaultLiveQueryClient",
				"_clearCachedDefaultClient"
			], controller);
			config["LiveQueryController"] = controller;
		},
		getLiveQueryController() {
			return config["LiveQueryController"];
		},
		setHooksController(controller) {
			requireMethods("HooksController", [
				"create",
				"get",
				"update",
				"remove"
			], controller);
			config["HooksController"] = controller;
		},
		getHooksController() {
			return config["HooksController"];
		},
		setParseOp(op) {
			config["ParseOp"] = op;
		},
		getParseOp() {
			return config["ParseOp"];
		},
		setParseObject(object) {
			config["ParseObject"] = object;
		},
		getParseObject() {
			return config["ParseObject"];
		},
		setParseQuery(query) {
			config["ParseQuery"] = query;
		},
		getParseQuery() {
			return config["ParseQuery"];
		},
		setParseRole(role) {
			config["ParseRole"] = role;
		},
		getParseRole() {
			return config["ParseRole"];
		},
		setParseUser(user) {
			config["ParseUser"] = user;
		},
		getParseUser() {
			return config["ParseUser"];
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.slice.js
var require_es_array_slice = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var isArray = require_is_array$3();
	var isConstructor = require_is_constructor();
	var isObject = require_is_object();
	var toAbsoluteIndex = require_to_absolute_index();
	var lengthOfArrayLike = require_length_of_array_like();
	var toIndexedObject = require_to_indexed_object();
	var createProperty = require_create_property();
	var setArrayLength = require_array_set_length();
	var wellKnownSymbol = require_well_known_symbol();
	var arrayMethodHasSpeciesSupport = require_array_method_has_species_support();
	var nativeSlice = require_array_slice();
	var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport("slice");
	var SPECIES = wellKnownSymbol("species");
	var $Array = Array;
	var max = Math.max;
	$({
		target: "Array",
		proto: true,
		forced: !HAS_SPECIES_SUPPORT
	}, { slice: function slice(start, end) {
		var O = toIndexedObject(this);
		var length = lengthOfArrayLike(O);
		var k = toAbsoluteIndex(start, length);
		var fin = toAbsoluteIndex(end === void 0 ? length : end, length);
		var Constructor, result, n;
		if (isArray(O)) {
			Constructor = O.constructor;
			if (isConstructor(Constructor) && (Constructor === $Array || isArray(Constructor.prototype))) Constructor = void 0;
			else if (isObject(Constructor)) {
				Constructor = Constructor[SPECIES];
				if (Constructor === null) Constructor = void 0;
			}
			if (Constructor === $Array || Constructor === void 0) return nativeSlice(O, k, fin);
		}
		result = new (Constructor === void 0 ? $Array : Constructor)(max(fin - k, 0));
		for (n = 0; k < fin; k++, n++) if (k in O) createProperty(result, n, O[k]);
		setArrayLength(result, n);
		return result;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/slice.js
var require_slice$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_slice();
	module.exports = require_get_built_in_prototype_method()("Array", "slice");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/slice.js
var require_slice$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_slice$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.slice;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.slice ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/slice.js
var require_slice$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_slice$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/slice.js
var require_slice = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_slice$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/string-repeat.js
var require_string_repeat = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var toString = require_to_string();
	var requireObjectCoercible = require_require_object_coercible();
	var $RangeError = RangeError;
	var floor = Math.floor;
	module.exports = function repeat(count) {
		var str = toString(requireObjectCoercible(this));
		var result = "";
		var n = toIntegerOrInfinity(count);
		if (n < 0 || n === Infinity) throw new $RangeError("Wrong number of repetitions");
		for (; n > 0; (n = floor(n / 2)) && (str += str)) if (n % 2) result += str;
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/string-pad.js
var require_string_pad = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var toLength = require_to_length();
	var toString = require_to_string();
	var $repeat = require_string_repeat();
	var requireObjectCoercible = require_require_object_coercible();
	var repeat = uncurryThis($repeat);
	var stringSlice = uncurryThis("".slice);
	var ceil = Math.ceil;
	var createMethod = function(IS_END) {
		return function($this, maxLength, fillString) {
			var S = toString(requireObjectCoercible($this));
			var intMaxLength = toLength(maxLength);
			var stringLength = S.length;
			if (intMaxLength <= stringLength) return S;
			var fillStr = fillString === void 0 ? " " : toString(fillString);
			var fillLen, stringFiller;
			if (fillStr === "") return S;
			fillLen = intMaxLength - stringLength;
			stringFiller = repeat(fillStr, ceil(fillLen / fillStr.length));
			if (stringFiller.length > fillLen) stringFiller = stringSlice(stringFiller, 0, fillLen);
			return IS_END ? S + stringFiller : stringFiller + S;
		};
	};
	module.exports = {
		start: createMethod(false),
		end: createMethod(true)
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/date-to-iso-string.js
var require_date_to_iso_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var fails = require_fails();
	var padStart = require_string_pad().start;
	var $RangeError = RangeError;
	var $isFinite = isFinite;
	var abs = Math.abs;
	var DatePrototype = Date.prototype;
	var nativeDateToISOString = DatePrototype.toISOString;
	var thisTimeValue = uncurryThis(DatePrototype.getTime);
	var getUTCDate = uncurryThis(DatePrototype.getUTCDate);
	var getUTCFullYear = uncurryThis(DatePrototype.getUTCFullYear);
	var getUTCHours = uncurryThis(DatePrototype.getUTCHours);
	var getUTCMilliseconds = uncurryThis(DatePrototype.getUTCMilliseconds);
	var getUTCMinutes = uncurryThis(DatePrototype.getUTCMinutes);
	var getUTCMonth = uncurryThis(DatePrototype.getUTCMonth);
	var getUTCSeconds = uncurryThis(DatePrototype.getUTCSeconds);
	module.exports = fails(function() {
		return nativeDateToISOString.call(/* @__PURE__ */ new Date(-5e13 - 1)) !== "0385-07-25T07:06:39.999Z";
	}) || !fails(function() {
		nativeDateToISOString.call(/* @__PURE__ */ new Date(NaN));
	}) ? function toISOString() {
		if (!$isFinite(thisTimeValue(this))) throw new $RangeError("Invalid time value");
		var date = this;
		var year = getUTCFullYear(date);
		var milliseconds = getUTCMilliseconds(date);
		var sign = year < 0 ? "-" : year > 9999 ? "+" : "";
		return sign + padStart(abs(year), sign ? 6 : 4, 0) + "-" + padStart(getUTCMonth(date) + 1, 2, 0) + "-" + padStart(getUTCDate(date), 2, 0) + "T" + padStart(getUTCHours(date), 2, 0) + ":" + padStart(getUTCMinutes(date), 2, 0) + ":" + padStart(getUTCSeconds(date), 2, 0) + "." + padStart(milliseconds, 3, 0) + "Z";
	} : nativeDateToISOString;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.date.to-json.js
var require_es_date_to_json = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var call = require_function_call();
	var toObject = require_to_object();
	var toPrimitive = require_to_primitive$5();
	var toISOString = require_date_to_iso_string();
	var classof = require_classof_raw();
	$({
		target: "Date",
		proto: true,
		forced: require_fails()(function() {
			return (/* @__PURE__ */ new Date(NaN)).toJSON() !== null || call(Date.prototype.toJSON, { toISOString: function() {
				return 1;
			} }) !== 1;
		})
	}, { toJSON: function toJSON(key) {
		var O = toObject(this);
		var pv = toPrimitive(O, "number");
		return typeof pv == "number" && !isFinite(pv) ? null : !("toISOString" in O) && classof(O) === "Date" ? call(toISOString, O) : O.toISOString();
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/json/stringify.js
var require_stringify$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_date_to_json();
	require_es_json_stringify();
	var path = require_path();
	var apply = require_function_apply();
	if (!path.JSON) path.JSON = { stringify: JSON.stringify };
	module.exports = function stringify(it, replacer, space) {
		return apply(path.JSON.stringify, null, arguments);
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/json/stringify.js
var require_stringify$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_stringify$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/json/stringify.js
var require_stringify = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_stringify$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseError.js
var require_ParseError = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty2 = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty2(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _forEach = _interopRequireDefault(require_for_each());
	var _defineProperty2 = _interopRequireDefault(require_define_property$3());
	var _defineProperty3 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	/**
	* Constructs a new Parse.Error object with the given code and message.
	*
	* Parse.CoreManager.set('PARSE_ERRORS', [{ code, message }]) can be use to override error messages.
	*
	* @alias Parse.Error
	*/
	var ParseError = class extends Error {
		/**
		* @param {number} code An error code constant from <code>Parse.Error</code>.
		* @param {string} message A detailed description of the error.
		*/
		constructor(code, message) {
			var _context;
			super(message);
			(0, _defineProperty3.default)(this, "code", void 0);
			(0, _defineProperty3.default)(this, "message", void 0);
			(0, _defineProperty3.default)(this, "object", void 0);
			(0, _defineProperty3.default)(this, "errors", void 0);
			this.code = code;
			let customMessage = message;
			(0, _forEach.default)(_context = _CoreManager.default.get("PARSE_ERRORS")).call(_context, (error) => {
				if (error.code === code && error.code) customMessage = error.message;
			});
			(0, _defineProperty2.default)(this, "message", {
				enumerable: true,
				value: customMessage
			});
		}
		toString() {
			return "ParseError: " + this.code + " " + this.message;
		}
	};
	(0, _defineProperty3.default)(ParseError, "OTHER_CAUSE", -1);
	/**
	* Error code indicating that something has gone wrong with the server.
	*
	* @property {number} INTERNAL_SERVER_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INTERNAL_SERVER_ERROR", 1);
	/**
	* Error code indicating the connection to the Parse servers failed.
	*
	* @property {number} CONNECTION_FAILED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "CONNECTION_FAILED", 100);
	/**
	* Error code indicating the specified object doesn't exist.
	*
	* @property {number} OBJECT_NOT_FOUND
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "OBJECT_NOT_FOUND", 101);
	/**
	* Error code indicating you tried to query with a datatype that doesn't
	* support it, like exact matching an array or object.
	*
	* @property {number} INVALID_QUERY
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_QUERY", 102);
	/**
	* Error code indicating a missing or invalid classname. Classnames are
	* case-sensitive. They must start with a letter, and a-zA-Z0-9_ are the
	* only valid characters.
	*
	* @property {number} INVALID_CLASS_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_CLASS_NAME", 103);
	/**
	* Error code indicating an unspecified object id.
	*
	* @property {number} MISSING_OBJECT_ID
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MISSING_OBJECT_ID", 104);
	/**
	* Error code indicating an invalid key name. Keys are case-sensitive. They
	* must start with a letter, and a-zA-Z0-9_ are the only valid characters.
	*
	* @property {number} INVALID_KEY_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_KEY_NAME", 105);
	/**
	* Error code indicating a malformed pointer. You should not see this unless
	* you have been mucking about changing internal Parse code.
	*
	* @property {number} INVALID_POINTER
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_POINTER", 106);
	/**
	* Error code indicating that badly formed JSON was received upstream. This
	* either indicates you have done something unusual with modifying how
	* things encode to JSON, or the network is failing badly.
	*
	* @property {number} INVALID_JSON
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_JSON", 107);
	/**
	* Error code indicating that the feature you tried to access is only
	* available internally for testing purposes.
	*
	* @property {number} COMMAND_UNAVAILABLE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "COMMAND_UNAVAILABLE", 108);
	/**
	* You must call Parse.initialize before using the Parse library.
	*
	* @property {number} NOT_INITIALIZED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "NOT_INITIALIZED", 109);
	/**
	* Error code indicating that a field was set to an inconsistent type.
	*
	* @property {number} INCORRECT_TYPE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INCORRECT_TYPE", 111);
	/**
	* Error code indicating an invalid channel name. A channel name is either
	* an empty string (the broadcast channel) or contains only a-zA-Z0-9_
	* characters and starts with a letter.
	*
	* @property {number} INVALID_CHANNEL_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_CHANNEL_NAME", 112);
	/**
	* Error code indicating that push is misconfigured.
	*
	* @property {number} PUSH_MISCONFIGURED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "PUSH_MISCONFIGURED", 115);
	/**
	* Error code indicating that the object is too large.
	*
	* @property {number} OBJECT_TOO_LARGE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "OBJECT_TOO_LARGE", 116);
	/**
	* Error code indicating that the operation isn't allowed for clients.
	*
	* @property {number} OPERATION_FORBIDDEN
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "OPERATION_FORBIDDEN", 119);
	/**
	* Error code indicating the result was not found in the cache.
	*
	* @property {number} CACHE_MISS
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "CACHE_MISS", 120);
	/**
	* Error code indicating that an invalid key was used in a nested
	* JSONObject.
	*
	* @property {number} INVALID_NESTED_KEY
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_NESTED_KEY", 121);
	/**
	* Error code indicating that an invalid filename was used for ParseFile.
	* A valid file name contains only a-zA-Z0-9_. characters and is between 1
	* and 128 characters.
	*
	* @property {number} INVALID_FILE_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_FILE_NAME", 122);
	/**
	* Error code indicating an invalid ACL was provided.
	*
	* @property {number} INVALID_ACL
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_ACL", 123);
	/**
	* Error code indicating that the request timed out on the server. Typically
	* this indicates that the request is too expensive to run.
	*
	* @property {number} TIMEOUT
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "TIMEOUT", 124);
	/**
	* Error code indicating that the email address was invalid.
	*
	* @property {number} INVALID_EMAIL_ADDRESS
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_EMAIL_ADDRESS", 125);
	/**
	* Error code indicating a missing content type.
	*
	* @property {number} MISSING_CONTENT_TYPE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MISSING_CONTENT_TYPE", 126);
	/**
	* Error code indicating a missing content length.
	*
	* @property {number} MISSING_CONTENT_LENGTH
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MISSING_CONTENT_LENGTH", 127);
	/**
	* Error code indicating an invalid content length.
	*
	* @property {number} INVALID_CONTENT_LENGTH
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_CONTENT_LENGTH", 128);
	/**
	* Error code indicating a file that was too large.
	*
	* @property {number} FILE_TOO_LARGE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "FILE_TOO_LARGE", 129);
	/**
	* Error code indicating an error saving a file.
	*
	* @property {number} FILE_SAVE_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "FILE_SAVE_ERROR", 130);
	/**
	* Error code indicating that a unique field was given a value that is
	* already taken.
	*
	* @property {number} DUPLICATE_VALUE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "DUPLICATE_VALUE", 137);
	/**
	* Error code indicating that a role's name is invalid.
	*
	* @property {number} INVALID_ROLE_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_ROLE_NAME", 139);
	/**
	* Error code indicating that an application quota was exceeded.  Upgrade to
	* resolve.
	*
	* @property {number} EXCEEDED_QUOTA
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "EXCEEDED_QUOTA", 140);
	/**
	* Error code indicating that a Cloud Code script failed.
	*
	* @property {number} SCRIPT_FAILED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "SCRIPT_FAILED", 141);
	/**
	* Error code indicating that a Cloud Code validation failed.
	*
	* @property {number} VALIDATION_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "VALIDATION_ERROR", 142);
	/**
	* Error code indicating that invalid image data was provided.
	*
	* @property {number} INVALID_IMAGE_DATA
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_IMAGE_DATA", 143);
	/**
	* Error code indicating an unsaved file.
	*
	* @property {number} UNSAVED_FILE_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "UNSAVED_FILE_ERROR", 151);
	/**
	* Error code indicating an invalid push time.
	*
	* @property {number} INVALID_PUSH_TIME_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_PUSH_TIME_ERROR", 152);
	/**
	* Error code indicating an error deleting a file.
	*
	* @property {number} FILE_DELETE_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "FILE_DELETE_ERROR", 153);
	/**
	* Error code indicating an error deleting an unnamed file.
	*
	* @property {number} FILE_DELETE_UNNAMED_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "FILE_DELETE_UNNAMED_ERROR", 161);
	/**
	* Error code indicating that the application has exceeded its request
	* limit.
	*
	* @property {number} REQUEST_LIMIT_EXCEEDED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "REQUEST_LIMIT_EXCEEDED", 155);
	/**
	* Error code indicating that the request was a duplicate and has been discarded due to
	* idempotency rules.
	*
	* @property {number} DUPLICATE_REQUEST
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "DUPLICATE_REQUEST", 159);
	/**
	* Error code indicating an invalid event name.
	*
	* @property {number} INVALID_EVENT_NAME
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_EVENT_NAME", 160);
	/**
	* Error code indicating that a field had an invalid value.
	*
	* @property {number} INVALID_VALUE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_VALUE", 162);
	/**
	* Error code indicating that the username is missing or empty.
	*
	* @property {number} USERNAME_MISSING
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "USERNAME_MISSING", 200);
	/**
	* Error code indicating that the password is missing or empty.
	*
	* @property {number} PASSWORD_MISSING
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "PASSWORD_MISSING", 201);
	/**
	* Error code indicating that the username has already been taken.
	*
	* @property {number} USERNAME_TAKEN
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "USERNAME_TAKEN", 202);
	/**
	* Error code indicating that the email has already been taken.
	*
	* @property {number} EMAIL_TAKEN
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "EMAIL_TAKEN", 203);
	/**
	* Error code indicating that the email is missing, but must be specified.
	*
	* @property {number} EMAIL_MISSING
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "EMAIL_MISSING", 204);
	/**
	* Error code indicating that a user with the specified email was not found.
	*
	* @property {number} EMAIL_NOT_FOUND
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "EMAIL_NOT_FOUND", 205);
	/**
	* Error code indicating that a user object without a valid session could
	* not be altered.
	*
	* @property {number} SESSION_MISSING
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "SESSION_MISSING", 206);
	/**
	* Error code indicating that a user can only be created through signup.
	*
	* @property {number} MUST_CREATE_USER_THROUGH_SIGNUP
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MUST_CREATE_USER_THROUGH_SIGNUP", 207);
	/**
	* Error code indicating that an an account being linked is already linked
	* to another user.
	*
	* @property {number} ACCOUNT_ALREADY_LINKED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "ACCOUNT_ALREADY_LINKED", 208);
	/**
	* Error code indicating that the current session token is invalid.
	*
	* @property {number} INVALID_SESSION_TOKEN
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_SESSION_TOKEN", 209);
	/**
	* Error code indicating an error enabling or verifying MFA
	*
	* @property {number} MFA_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MFA_ERROR", 210);
	/**
	* Error code indicating that a valid MFA token must be provided
	*
	* @property {number} MFA_TOKEN_REQUIRED
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "MFA_TOKEN_REQUIRED", 211);
	/**
	* Error code indicating that a user cannot be linked to an account because
	* that account's id could not be found.
	*
	* @property {number} LINKED_ID_MISSING
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "LINKED_ID_MISSING", 250);
	/**
	* Error code indicating that a user with a linked (e.g. Facebook) account
	* has an invalid session.
	*
	* @property {number} INVALID_LINKED_SESSION
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_LINKED_SESSION", 251);
	/**
	* Error code indicating that a service being linked (e.g. Facebook or
	* Twitter) is unsupported.
	*
	* @property {number} UNSUPPORTED_SERVICE
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "UNSUPPORTED_SERVICE", 252);
	/**
	* Error code indicating an invalid operation occured on schema
	*
	* @property {number} INVALID_SCHEMA_OPERATION
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "INVALID_SCHEMA_OPERATION", 255);
	/**
	* Error code indicating that there were multiple errors. Aggregate errors
	* have an "errors" property, which is an array of error objects with more
	* detail about each error that occurred.
	*
	* @property {number} AGGREGATE_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "AGGREGATE_ERROR", 600);
	/**
	* Error code indicating the client was unable to read an input file.
	*
	* @property {number} FILE_READ_ERROR
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "FILE_READ_ERROR", 601);
	/**
	* Error code indicating a real error code is unavailable because
	* we had to use an XDomainRequest object to allow CORS requests in
	* Internet Explorer, which strips the body from HTTP responses that have
	* a non-2XX status code.
	*
	* @property {number} X_DOMAIN_REQUEST
	* @static
	*/
	(0, _defineProperty3.default)(ParseError, "X_DOMAIN_REQUEST", 602);
	exports.default = ParseError;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseFile.js
var require_ParseFile = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys2 = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty2 = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.b64Digit = b64Digit;
	exports.default = void 0;
	var _isArray = _interopRequireDefault(require_is_array());
	var _slice = _interopRequireDefault(require_slice());
	var _keys = _interopRequireDefault(require_keys$4());
	var _forEach = _interopRequireDefault(require_for_each());
	var _promise = _interopRequireDefault(require_promise());
	var _stringify = _interopRequireDefault(require_stringify());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseError = _interopRequireDefault(require_ParseError());
	function ownKeys(e, r) {
		var t = _Object$keys2(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context6, _context7;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty2(_context6 = ownKeys(Object(t), !0)).call(_context6, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty2(_context7 = ownKeys(Object(t))).call(_context7, function(r) {
				_Object$defineProperty(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var NodeReadable;
	function b64Digit(number) {
		if (number < 26) return String.fromCharCode(65 + number);
		if (number < 52) return String.fromCharCode(97 + (number - 26));
		if (number < 62) return String.fromCharCode(48 + (number - 52));
		if (number === 62) return "+";
		if (number === 63) return "/";
		throw new TypeError("Tried to encode large digit " + number + " in base64.");
	}
	/**
	* A Parse.File is a local representation of a file that is saved to the Parse
	* cloud.
	*
	* @alias Parse.File
	*/
	var ParseFile = class ParseFile {
		/**
		* @param name {String} The file's name. This will be prefixed by a unique
		*     value once the file has finished saving. The file name must begin with
		*     an alphanumeric character, and consist of alphanumeric characters,
		*     periods, spaces, underscores, or dashes.
		* @param data {Array} The data for the file, as either:
		*     1. an Array of byte value Numbers or Uint8Array.
		*     2. an Object like { base64: "..." } with a base64-encoded String.
		*     3. an Object like { uri: "..." } with a uri String.
		*     4. a File object selected with a file upload control.
		*     5. (Node.js only) a Buffer. Uploaded as raw binary data instead of
		*        base64-encoding, reducing memory usage. Falls back to base64
		*        JSON encoding if metadata or tags are set.
		*     6. (Node.js only) a Readable stream, or a Web ReadableStream.
		*        Streamed as raw binary data directly into the upload request.
		*        Supports metadata, tags, and directory when Parse Server >= 9.5.0.
		*        For example:
		* <pre>
		* var fileUploadControl = $("#profilePhotoFileUpload")[0];
		* if (fileUploadControl.files.length > 0) {
		*   var file = fileUploadControl.files[0];
		*   var name = "photo.jpg";
		*   var parseFile = new Parse.File(name, file);
		*   parseFile.save().then(function() {
		*     // The file has been saved to Parse.
		*   }, function(error) {
		*     // The file either could not be read, or could not be saved to Parse.
		*   });
		* }</pre>
		* @param type {String} Optional Content-Type header to use for the file. If
		*     this is omitted, the content type will be inferred from the name's
		*     extension.
		* @param metadata {object} Optional key value pairs to be stored with file object
		* @param tags {object} Optional key value pairs to be stored with file object
		*/
		constructor(name, data, type, metadata, tags) {
			(0, _defineProperty2.default)(this, "_name", void 0);
			(0, _defineProperty2.default)(this, "_url", void 0);
			(0, _defineProperty2.default)(this, "_source", void 0);
			(0, _defineProperty2.default)(this, "_previousSave", void 0);
			(0, _defineProperty2.default)(this, "_data", void 0);
			(0, _defineProperty2.default)(this, "_requestTask", void 0);
			(0, _defineProperty2.default)(this, "_metadata", void 0);
			(0, _defineProperty2.default)(this, "_tags", void 0);
			(0, _defineProperty2.default)(this, "_directory", void 0);
			const specifiedType = type || "";
			this._name = name;
			this._metadata = metadata || {};
			this._tags = tags || {};
			if (data !== void 0) {
				if (typeof Buffer !== "undefined" && Buffer.isBuffer(data)) this._source = {
					format: "buffer",
					buffer: data,
					type: specifiedType
				};
				else if (data !== null && typeof data === "object" && typeof data.pipe === "function" && typeof data.read === "function") this._source = {
					format: "stream",
					stream: data,
					type: specifiedType
				};
				else if (typeof ReadableStream !== "undefined" && data instanceof ReadableStream) this._source = {
					format: "stream",
					stream: data,
					type: specifiedType
				};
				else if ((0, _isArray.default)(data) || data instanceof Uint8Array) {
					this._data = ParseFile.encodeBase64(data);
					this._source = {
						format: "base64",
						base64: this._data,
						type: specifiedType
					};
				} else if (typeof Blob !== "undefined" && data instanceof Blob) this._source = {
					format: "file",
					file: data,
					type: specifiedType
				};
				else if (data && typeof data.uri === "string" && data.uri !== void 0) this._source = {
					format: "uri",
					uri: data.uri,
					type: specifiedType
				};
				else if (data && typeof data.base64 === "string") {
					var _context, _context2, _context3;
					const base64 = (0, _slice.default)(_context = data.base64.split(",")).call(_context, -1)[0];
					const dataType = specifiedType || (0, _slice.default)(_context2 = (0, _slice.default)(_context3 = data.base64.split(";")).call(_context3, 0, 1)[0].split(":")).call(_context2, 1, 2)[0] || "text/plain";
					this._data = base64;
					this._source = {
						format: "base64",
						base64,
						type: dataType
					};
				} else throw new TypeError("Cannot create a Parse.File with that data.");
			}
		}
		/**
		* Return the data for the file, downloading it if not already present.
		* Data is present if initialized with Byte Array, Base64 or Saved with Uri.
		* Data is cleared if saved with File object selected with a file upload control
		*
		* @param {object} options
		* @param {function} [options.progress] callback for download progress
		* <pre>
		* const parseFile = new Parse.File(name, file);
		* parseFile.getData({
		*   progress: (progressValue, loaded, total) => {
		*     if (progressValue !== null) {
		*       // Update the UI using progressValue
		*     }
		*   }
		* });
		* </pre>
		* @returns {Promise} Promise that is resolve with base64 data
		*/
		async getData(options) {
			var _this$_source;
			options = options || {};
			if (this._data) return this._data;
			if (((_this$_source = this._source) === null || _this$_source === void 0 ? void 0 : _this$_source.format) === "buffer") {
				this._data = this._source.buffer.toString("base64");
				return this._data;
			}
			if (!this._url) throw new Error("Cannot retrieve data for unsaved ParseFile.");
			options.requestTask = (task) => this._requestTask = task;
			const result = await _CoreManager.default.getFileController().download(this._url, options);
			this._data = result.base64;
			return this._data;
		}
		/**
		* Gets the name of the file. Before save is called, this is the filename
		* given by the user. After save is called, that name gets prefixed with a
		* unique identifier.
		*
		* @returns {string}
		*/
		name() {
			return this._name;
		}
		/**
		* Gets the url of the file. It is only available after you save the file or
		* after you get the file from a Parse.Object.
		*
		* @param {object} options An object to specify url options
		* @param {boolean} [options.forceSecure] force the url to be secure
		* @returns {string | undefined}
		*/
		url(options) {
			options = options || {};
			if (!this._url) return;
			if (options.forceSecure) return this._url.replace(/^http:\/\//i, "https://");
			else return this._url;
		}
		/**
		* Gets the metadata of the file.
		*
		* @returns {object}
		*/
		metadata() {
			return this._metadata;
		}
		/**
		* Gets the tags of the file.
		*
		* @returns {object}
		*/
		tags() {
			return this._tags;
		}
		/**
		* Gets the directory of the file.
		* Requires Parse Server >= 9.4.0.
		*
		* @returns {string | undefined}
		*/
		directory() {
			return this._directory;
		}
		/**
		* Saves the file to the Parse cloud.
		*
		* In Node.js, files created with Buffer or ReadableStream are uploaded as
		* raw binary data, avoiding base64 encoding overhead. If metadata
		* or tags are set on a Buffer-backed file, the upload falls back to base64
		* JSON encoding. Stream-backed files support metadata, tags, and directory
		* when Parse Server >= 9.5.0.
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*     behalf of a specific user.
		*   <li>progress: callback for upload progress. For example:
		* <pre>
		* let parseFile = new Parse.File(name, file);
		* parseFile.save({
		*   progress: (progressValue, loaded, total) => {
		*     if (progressValue !== null) {
		*       // Update the UI using progressValue
		*     }
		*   }
		* });
		* </pre>
		*   <li>maxUploadSize: Overrides the server's maxUploadSize for this upload.
		*     Requires the master key. Only supported for Buffer and Stream source
		*     types; files created from base64 strings, number arrays, Blobs, or URIs
		*     do not support this option. Requires Parse Server >= 9.5.0.
		* </ul>
		* @returns {Promise | undefined} Promise that is resolved when the save finishes.
		*/
		save(options) {
			options = options || {};
			options.requestTask = (task) => this._requestTask = task;
			options.metadata = this._metadata;
			options.tags = this._tags;
			options.directory = this._directory;
			const controller = _CoreManager.default.getFileController();
			if (!this._previousSave) {
				if (this._source.format === "buffer" || this._source.format === "stream") {
					if (this._source.format === "stream" && !controller.saveBinary) throw new Error("Cannot save a stream-based file without saveBinary support on the FileController.");
					const hasFileData = this._metadata && (0, _keys.default)(this._metadata).length > 0 || this._tags && (0, _keys.default)(this._tags).length > 0 || !!this._directory;
					if (controller.saveBinary && (this._source.format === "stream" || !hasFileData || options.maxUploadSize)) this._previousSave = controller.saveBinary(this._name, this._source, options).then((res) => {
						this._name = res.name;
						this._url = res.url;
						this._data = null;
						this._requestTask = null;
						return this;
					});
					else if (this._source.format === "buffer") {
						const base64Source = {
							format: "base64",
							base64: this._source.buffer.toString("base64"),
							type: this._source.type
						};
						this._previousSave = controller.saveBase64(this._name, base64Source, options).then((res) => {
							this._name = res.name;
							this._url = res.url;
							this._requestTask = null;
							return this;
						});
					}
				} else if (this._source.format === "file") this._previousSave = controller.saveFile(this._name, this._source, options).then((res) => {
					this._name = res.name;
					this._url = res.url;
					this._data = null;
					this._requestTask = null;
					return this;
				});
				else if (this._source.format === "uri") this._previousSave = controller.download(this._source.uri, options).then((result) => {
					if (!(result && result.base64)) return {};
					const newSource = {
						format: "base64",
						base64: result.base64,
						type: result.contentType
					};
					this._data = result.base64;
					this._requestTask = null;
					return controller.saveBase64(this._name, newSource, options);
				}).then((res) => {
					this._name = res.name;
					this._url = res.url;
					this._requestTask = null;
					return this;
				});
				else this._previousSave = controller.saveBase64(this._name, this._source, options).then((res) => {
					this._name = res.name;
					this._url = res.url;
					this._requestTask = null;
					return this;
				});
			}
			if (this._previousSave) return this._previousSave;
		}
		/**
		* Aborts the request if it has already been sent.
		*/
		cancel() {
			if (this._requestTask && typeof this._requestTask.abort === "function") {
				this._requestTask._aborted = true;
				this._requestTask.abort();
			}
			this._requestTask = null;
		}
		/**
		* Deletes the file from the Parse cloud.
		* In Cloud Code and Node only with Master Key.
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		* <pre>
		* @returns {Promise} Promise that is resolved when the delete finishes.
		*/
		destroy() {
			let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			if (!this._name) throw new _ParseError.default(_ParseError.default.FILE_DELETE_UNNAMED_ERROR, "Cannot delete an unnamed file.");
			const destroyOptions = { useMasterKey: true };
			if (Object.hasOwn(options, "useMasterKey")) destroyOptions.useMasterKey = !!options.useMasterKey;
			return _CoreManager.default.getFileController().deleteFile(this._name, destroyOptions).then(() => {
				this._data = void 0;
				this._requestTask = null;
				return this;
			});
		}
		toJSON() {
			return {
				__type: "File",
				name: this._name,
				url: this._url
			};
		}
		equals(other) {
			if (this === other) return true;
			return other instanceof ParseFile && this.name() === other.name() && this.url() === other.url() && typeof this.url() !== "undefined";
		}
		/**
		* Sets metadata to be saved with file object. Overwrites existing metadata.
		* When used with a stream-based file, requires Parse Server >= 9.5.0.
		*
		* @param {object} metadata Key value pairs to be stored with file object
		*/
		setMetadata(metadata) {
			if (metadata && typeof metadata === "object") {
				var _context4;
				(0, _forEach.default)(_context4 = (0, _keys.default)(metadata)).call(_context4, (key) => {
					this.addMetadata(key, metadata[key]);
				});
			}
		}
		/**
		* Sets metadata to be saved with file object. Adds to existing metadata.
		* When used with a stream-based file, requires Parse Server >= 9.5.0.
		*
		* @param {string} key key to store the metadata
		* @param {*} value metadata
		*/
		addMetadata(key, value) {
			if (typeof key === "string") this._metadata[key] = value;
		}
		/**
		* Sets tags to be saved with file object. Overwrites existing tags.
		* When used with a stream-based file, requires Parse Server >= 9.5.0.
		*
		* @param {object} tags Key value pairs to be stored with file object
		*/
		setTags(tags) {
			if (tags && typeof tags === "object") {
				var _context5;
				(0, _forEach.default)(_context5 = (0, _keys.default)(tags)).call(_context5, (key) => {
					this.addTag(key, tags[key]);
				});
			}
		}
		/**
		* Sets tags to be saved with file object. Adds to existing tags.
		* When used with a stream-based file, requires Parse Server >= 9.5.0.
		*
		* @param {string} key key to store tags
		* @param {*} value tag
		*/
		addTag(key, value) {
			if (typeof key === "string") this._tags[key] = value;
		}
		/**
		* Sets the directory where the file will be stored.
		* Requires the Master Key when saving.
		* Requires Parse Server >= 9.4.0; when used with a stream-based file, requires Parse Server >= 9.5.0.
		*
		* @param {string} directory the directory path
		*/
		setDirectory(directory) {
			if (typeof directory === "string" && directory.length > 0) this._directory = directory;
		}
		static fromJSON(obj) {
			if (obj.__type !== "File") throw new TypeError("JSON object does not represent a ParseFile");
			const file = new ParseFile(obj.name);
			file._url = obj.url;
			return file;
		}
		static encodeBase64(bytes) {
			const chunks = [];
			chunks.length = Math.ceil(bytes.length / 3);
			for (let i = 0; i < chunks.length; i++) {
				const b1 = bytes[i * 3];
				const b2 = bytes[i * 3 + 1] || 0;
				const b3 = bytes[i * 3 + 2] || 0;
				const has2 = i * 3 + 1 < bytes.length;
				const has3 = i * 3 + 2 < bytes.length;
				chunks[i] = [
					b64Digit(b1 >> 2 & 63),
					b64Digit(b1 << 4 & 48 | b2 >> 4 & 15),
					has2 ? b64Digit(b2 << 2 & 60 | b3 >> 6 & 3) : "=",
					has3 ? b64Digit(b3 & 63) : "="
				].join("");
			}
			return chunks.join("");
		}
	};
	var DefaultController = {
		saveFile: async function(name, source, options) {
			if (source.format !== "file") throw new Error("saveFile can only be used with File-type sources.");
			const [first, second] = (await new _promise.default((res, rej) => {
				const reader = new FileReader();
				reader.onload = () => res(reader.result);
				reader.onerror = (error) => rej(error);
				reader.readAsDataURL(source.file);
			})).split(",");
			const newSource = {
				format: "base64",
				base64: second ? second : first,
				type: source.type || (source.file ? source.file.type : void 0)
			};
			return await DefaultController.saveBase64(name, newSource, options);
		},
		saveBase64: function(name, source) {
			let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			if (source.format !== "base64") throw new Error("saveBase64 can only be used with Base64-type sources.");
			const data = {
				base64: source.base64,
				fileData: _objectSpread({
					metadata: _objectSpread({}, options.metadata),
					tags: _objectSpread({}, options.tags)
				}, options.directory ? { directory: options.directory } : {})
			};
			delete options.metadata;
			delete options.tags;
			delete options.directory;
			if (source.type) data._ContentType = source.type;
			return _CoreManager.default.getRESTController().request("POST", "files/" + name, data, options);
		},
		saveBinary: async function(name, source) {
			let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			if (source.format !== "buffer" && source.format !== "stream") throw new Error("saveBinary can only be used with Buffer or Stream sources.");
			const headers = {
				"X-Parse-Application-ID": _CoreManager.default.get("APPLICATION_ID"),
				"X-Parse-Upload-Mode": "stream"
			};
			headers["Content-Type"] = (source.type || "application/octet-stream").replace(/[\r\n]/g, "");
			if (options.directory) headers["X-Parse-File-Directory"] = options.directory.replace(/[\r\n]/g, "");
			if (options.metadata && (0, _keys.default)(options.metadata).length > 0) headers["X-Parse-File-Metadata"] = (0, _stringify.default)(options.metadata);
			if (options.tags && (0, _keys.default)(options.tags).length > 0) headers["X-Parse-File-Tags"] = (0, _stringify.default)(options.tags);
			if (options.maxUploadSize) headers["X-Parse-File-Max-Upload-Size"] = options.maxUploadSize.replace(/[\r\n]/g, "");
			const jsKey = _CoreManager.default.get("JAVASCRIPT_KEY");
			if (jsKey) headers["X-Parse-JavaScript-Key"] = jsKey;
			let useMasterKey = options.useMasterKey;
			if (typeof useMasterKey === "undefined") useMasterKey = _CoreManager.default.get("USE_MASTER_KEY");
			if (useMasterKey) {
				if (_CoreManager.default.get("MASTER_KEY")) {
					delete headers["X-Parse-JavaScript-Key"];
					headers["X-Parse-Master-Key"] = _CoreManager.default.get("MASTER_KEY");
				} else throw new Error("Cannot use the Master Key, it has not been provided.");
			}
			if (options.sessionToken) headers["X-Parse-Session-Token"] = options.sessionToken;
			else {
				const userController = _CoreManager.default.getUserController();
				if (userController) {
					const user = await userController.currentUserAsync();
					if (user) {
						const token = user.getSessionToken();
						if (token) headers["X-Parse-Session-Token"] = token;
					}
				}
			}
			let body;
			if (source.format === "buffer") body = source.buffer;
			else if (source.format === "stream") {
				const stream = source.stream;
				if (typeof stream.pipe === "function" && typeof stream.read === "function") body = NodeReadable.toWeb(stream);
				else body = stream;
			}
			let url = _CoreManager.default.get("SERVER_URL");
			if (url[url.length - 1] !== "/") url += "/";
			url += "files/" + encodeURIComponent(name);
			return _CoreManager.default.getRESTController().ajax("POST", url, body, headers, options).then((_ref) => {
				let { response } = _ref;
				return response;
			});
		},
		download: async function(uri, options) {
			const controller = new AbortController();
			options.requestTask(controller);
			const { signal } = controller;
			try {
				const response = await fetch(uri, { signal });
				const reader = response.body.getReader();
				const length = +response.headers.get("Content-Length") || 0;
				const contentType = response.headers.get("Content-Type");
				if (length === 0) {
					var _options$progress;
					(_options$progress = options.progress) === null || _options$progress === void 0 || _options$progress.call(options, null, null, null);
					return {
						base64: "",
						contentType
					};
				}
				let recieved = 0;
				const chunks = [];
				while (true) {
					var _options$progress2;
					const { done, value } = await reader.read();
					if (done) break;
					chunks.push(value);
					recieved += (value === null || value === void 0 ? void 0 : value.length) || 0;
					(_options$progress2 = options.progress) === null || _options$progress2 === void 0 || _options$progress2.call(options, recieved / length, recieved, length);
				}
				const body = new Uint8Array(recieved);
				let offset = 0;
				for (const chunk of chunks) {
					body.set(chunk, offset);
					offset += chunk.length;
				}
				return {
					base64: ParseFile.encodeBase64(body),
					contentType
				};
			} catch (error) {
				if (error.name === "AbortError") return {};
				else throw error;
			}
		},
		deleteFile: function(name, options) {
			const headers = { "X-Parse-Application-ID": _CoreManager.default.get("APPLICATION_ID") };
			if (options.useMasterKey) headers["X-Parse-Master-Key"] = _CoreManager.default.get("MASTER_KEY");
			let url = _CoreManager.default.get("SERVER_URL");
			if (url[url.length - 1] !== "/") url += "/";
			url += "files/" + name;
			return _CoreManager.default.getRESTController().ajax("DELETE", url, "", headers).catch((response) => {
				if (!response || response.toString() === "SyntaxError: Unexpected end of JSON input") return _promise.default.resolve();
				else return _CoreManager.default.getRESTController().handleError(response);
			});
		}
	};
	_CoreManager.default.setFileController(DefaultController);
	exports.default = ParseFile;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseGeoPoint.js
var require_ParseGeoPoint = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _isArray = _interopRequireDefault(require_is_array());
	var _promise = _interopRequireDefault(require_promise());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	exports.default = class ParseGeoPoint {
		/**
		* @param {(number[] | object | number)} arg1 Either a list of coordinate pairs, an object with `latitude`, `longitude`, or the latitude or the point.
		* @param {number} arg2 The longitude of the GeoPoint
		*/
		constructor(arg1, arg2) {
			(0, _defineProperty2.default)(this, "_latitude", void 0);
			(0, _defineProperty2.default)(this, "_longitude", void 0);
			if ((0, _isArray.default)(arg1)) {
				ParseGeoPoint._validate(arg1[0], arg1[1]);
				this._latitude = arg1[0];
				this._longitude = arg1[1];
			} else if (typeof arg1 === "object") {
				ParseGeoPoint._validate(arg1.latitude, arg1.longitude);
				this._latitude = arg1.latitude;
				this._longitude = arg1.longitude;
			} else if (arg1 !== void 0 && arg2 !== void 0) {
				ParseGeoPoint._validate(arg1, arg2);
				this._latitude = arg1;
				this._longitude = arg2;
			} else {
				this._latitude = 0;
				this._longitude = 0;
			}
		}
		/**
		* North-south portion of the coordinate, in range [-90, 90].
		* Throws an exception if set out of range in a modern browser.
		*
		* @property {number} latitude
		* @returns {number}
		*/
		get latitude() {
			return this._latitude;
		}
		set latitude(val) {
			ParseGeoPoint._validate(val, this.longitude);
			this._latitude = val;
		}
		/**
		* East-west portion of the coordinate, in range [-180, 180].
		* Throws if set out of range in a modern browser.
		*
		* @property {number} longitude
		* @returns {number}
		*/
		get longitude() {
			return this._longitude;
		}
		set longitude(val) {
			ParseGeoPoint._validate(this.latitude, val);
			this._longitude = val;
		}
		/**
		* Returns a JSON representation of the GeoPoint, suitable for Parse.
		*
		* @returns {object}
		*/
		toJSON() {
			ParseGeoPoint._validate(this._latitude, this._longitude);
			return {
				__type: "GeoPoint",
				latitude: this._latitude,
				longitude: this._longitude
			};
		}
		equals(other) {
			return other instanceof ParseGeoPoint && this.latitude === other.latitude && this.longitude === other.longitude;
		}
		/**
		* Returns the distance from this GeoPoint to another in radians.
		*
		* @param {Parse.GeoPoint} point the other Parse.GeoPoint.
		* @returns {number}
		*/
		radiansTo(point) {
			const d2r = Math.PI / 180;
			const lat1rad = this.latitude * d2r;
			const long1rad = this.longitude * d2r;
			const lat2rad = point.latitude * d2r;
			const long2rad = point.longitude * d2r;
			const sinDeltaLatDiv2 = Math.sin((lat1rad - lat2rad) / 2);
			const sinDeltaLongDiv2 = Math.sin((long1rad - long2rad) / 2);
			let a = sinDeltaLatDiv2 * sinDeltaLatDiv2 + Math.cos(lat1rad) * Math.cos(lat2rad) * sinDeltaLongDiv2 * sinDeltaLongDiv2;
			a = Math.min(1, a);
			return 2 * Math.asin(Math.sqrt(a));
		}
		/**
		* Returns the distance from this GeoPoint to another in kilometers.
		*
		* @param {Parse.GeoPoint} point the other Parse.GeoPoint.
		* @returns {number}
		*/
		kilometersTo(point) {
			return this.radiansTo(point) * 6371;
		}
		/**
		* Returns the distance from this GeoPoint to another in miles.
		*
		* @param {Parse.GeoPoint} point the other Parse.GeoPoint.
		* @returns {number}
		*/
		milesTo(point) {
			return this.radiansTo(point) * 3958.8;
		}
		static _validate(latitude, longitude) {
			if (isNaN(latitude) || isNaN(longitude) || typeof latitude !== "number" || typeof longitude !== "number") throw new TypeError("GeoPoint latitude and longitude must be valid numbers");
			if (latitude < -90) throw new TypeError("GeoPoint latitude out of bounds: " + latitude + " < -90.0.");
			if (latitude > 90) throw new TypeError("GeoPoint latitude out of bounds: " + latitude + " > 90.0.");
			if (longitude < -180) throw new TypeError("GeoPoint longitude out of bounds: " + longitude + " < -180.0.");
			if (longitude > 180) throw new TypeError("GeoPoint longitude out of bounds: " + longitude + " > 180.0.");
		}
		/**
		* Creates a GeoPoint with the user's current location, if available.
		*
		* @param {object} options The options.
		* @param {boolean} [options.enableHighAccuracy] A boolean value that indicates the application would like to receive the best possible results.
		*  If true and if the device is able to provide a more accurate position, it will do so.
		*  Note that this can result in slower response times or increased power consumption (with a GPS chip on a mobile device for example).
		*  On the other hand, if false, the device can take the liberty to save resources by responding more quickly and/or using less power. Default: false.
		* @param {number} [options.timeout] A positive long value representing the maximum length of time (in milliseconds) the device is allowed to take in order to return a position.
		*  The default value is Infinity, meaning that getCurrentPosition() won't return until the position is available.
		* @param {number} [options.maximumAge] A positive long value indicating the maximum age in milliseconds of a possible cached position that is acceptable to return.
		*  If set to 0, it means that the device cannot use a cached position and must attempt to retrieve the real current position.
		*  If set to Infinity the device must return a cached position regardless of its age. Default: 0.
		* @static
		* @returns {Promise<Parse.GeoPoint>} User's current location
		*/
		static current(options) {
			return new _promise.default((resolve, reject) => {
				navigator.geolocation.getCurrentPosition((location) => {
					resolve(new ParseGeoPoint(location.coords.latitude, location.coords.longitude));
				}, reject, options);
			});
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/ParsePolygon.js
var require_ParsePolygon = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _isArray = _interopRequireDefault(require_is_array());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	exports.default = class ParsePolygon {
		/**
		* @param {(Coordinates | Parse.GeoPoint[])} coordinates An Array of coordinate pairs
		*/
		constructor(coordinates) {
			(0, _defineProperty2.default)(this, "_coordinates", void 0);
			this._coordinates = ParsePolygon._validate(coordinates);
		}
		/**
		* Coordinates value for this Polygon.
		* Throws an exception if not valid type.
		*
		* @property {(Coordinates | Parse.GeoPoint[])} coordinates list of coordinates
		* @returns {Coordinates}
		*/
		get coordinates() {
			return this._coordinates;
		}
		set coordinates(coords) {
			this._coordinates = ParsePolygon._validate(coords);
		}
		/**
		* Returns a JSON representation of the Polygon, suitable for Parse.
		*
		* @returns {object}
		*/
		toJSON() {
			ParsePolygon._validate(this._coordinates);
			return {
				__type: "Polygon",
				coordinates: this._coordinates
			};
		}
		/**
		* Checks if two polygons are equal
		*
		* @param {(Parse.Polygon | object)} other
		* @returns {boolean}
		*/
		equals(other) {
			if (!(other instanceof ParsePolygon) || this.coordinates.length !== other.coordinates.length) return false;
			let isEqual = true;
			for (let i = 1; i < this._coordinates.length; i += 1) if (this._coordinates[i][0] != other.coordinates[i][0] || this._coordinates[i][1] != other.coordinates[i][1]) {
				isEqual = false;
				break;
			}
			return isEqual;
		}
		/**
		*
		* @param {Parse.GeoPoint} point
		* @returns {boolean} Returns if the point is contained in the polygon
		*/
		containsPoint(point) {
			let minX = this._coordinates[0][0];
			let maxX = this._coordinates[0][0];
			let minY = this._coordinates[0][1];
			let maxY = this._coordinates[0][1];
			for (let i = 1; i < this._coordinates.length; i += 1) {
				const p = this._coordinates[i];
				minX = Math.min(p[0], minX);
				maxX = Math.max(p[0], maxX);
				minY = Math.min(p[1], minY);
				maxY = Math.max(p[1], maxY);
			}
			if (point.latitude < minX || point.latitude > maxX || point.longitude < minY || point.longitude > maxY) return false;
			let inside = false;
			for (let i = 0, j = this._coordinates.length - 1; i < this._coordinates.length; j = i++) {
				const startX = this._coordinates[i][0];
				const startY = this._coordinates[i][1];
				const endX = this._coordinates[j][0];
				const endY = this._coordinates[j][1];
				if (startY > point.longitude != endY > point.longitude && point.latitude < (endX - startX) * (point.longitude - startY) / (endY - startY) + startX) inside = !inside;
			}
			return inside;
		}
		/**
		* Validates that the list of coordinates can form a valid polygon
		*
		* @param {Array} coords the list of coordinates to validate as a polygon
		* @throws {TypeError}
		* @returns {number[][]} Array of coordinates if validated.
		*/
		static _validate(coords) {
			if (!(0, _isArray.default)(coords)) throw new TypeError("Coordinates must be an Array");
			if (coords.length < 3) throw new TypeError("Polygon must have at least 3 GeoPoints or Points");
			const points = [];
			for (let i = 0; i < coords.length; i += 1) {
				const coord = coords[i];
				let geoPoint;
				if (coord instanceof _ParseGeoPoint.default) geoPoint = coord;
				else if ((0, _isArray.default)(coord) && coord.length === 2) geoPoint = new _ParseGeoPoint.default(coord[0], coord[1]);
				else throw new TypeError("Coordinates must be an Array of GeoPoints or Points");
				points.push([geoPoint.latitude, geoPoint.longitude]);
			}
			return points;
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseRelation.js
var require_ParseRelation = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _isArray = _interopRequireDefault(require_is_array());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	/**
	* Creates a new Relation for the given parent object and key. This
	* constructor should rarely be used directly, but rather created by
	* Parse.Object.relation.
	*
	* <p>
	* A class that is used to access all of the children of a many-to-many
	* relationship.  Each instance of Parse.Relation is associated with a
	* particular parent object and key.
	* </p>
	*
	* @alias Parse.Relation
	*/
	var ParseRelation = class {
		/**
		* @param {Parse.Object} parent The parent of this relation.
		* @param {string} key The key for this relation on the parent.
		*/
		constructor(parent, key) {
			(0, _defineProperty2.default)(this, "parent", void 0);
			(0, _defineProperty2.default)(this, "key", void 0);
			(0, _defineProperty2.default)(this, "targetClassName", void 0);
			this.parent = parent;
			this.key = key;
			this.targetClassName = null;
		}
		_ensureParentAndKey(parent, key) {
			this.key = this.key || key;
			if (this.key !== key) throw new Error("Internal Error. Relation retrieved from two different keys.");
			if (this.parent) {
				if (this.parent.className !== parent.className) throw new Error("Internal Error. Relation retrieved from two different Objects.");
				if (this.parent.id) {
					if (this.parent.id !== parent.id) throw new Error("Internal Error. Relation retrieved from two different Objects.");
				} else if (parent.id) this.parent = parent;
			} else this.parent = parent;
		}
		/**
		* Adds a Parse.Object or an array of Parse.Objects to the relation.
		*
		* @param {(Parse.Object|Array)} objects The item or items to add.
		* @returns {Parse.Object} The parent of the relation.
		*/
		add(objects) {
			if (!(0, _isArray.default)(objects)) objects = [objects];
			const { RelationOp } = _CoreManager.default.getParseOp();
			const change = new RelationOp(objects, []);
			const parent = this.parent;
			if (!parent) throw new Error("Cannot add to a Relation without a parent");
			if (objects.length === 0) return parent;
			parent.set(this.key, change);
			this.targetClassName = change._targetClassName;
			return parent;
		}
		/**
		* Removes a Parse.Object or an array of Parse.Objects from this relation.
		*
		* @param {(Parse.Object|Array)} objects The item or items to remove.
		*/
		remove(objects) {
			if (!(0, _isArray.default)(objects)) objects = [objects];
			const { RelationOp } = _CoreManager.default.getParseOp();
			const change = new RelationOp([], objects);
			if (!this.parent) throw new Error("Cannot remove from a Relation without a parent");
			if (objects.length === 0) return;
			this.parent.set(this.key, change);
			this.targetClassName = change._targetClassName;
		}
		/**
		* Returns a JSON version of the object suitable for saving to disk.
		*
		* @returns {object} JSON representation of Relation
		*/
		toJSON() {
			return {
				__type: "Relation",
				className: this.targetClassName
			};
		}
		/**
		* Returns a Parse.Query that is limited to objects in this
		* relation.
		*
		* @returns {Parse.Query} Relation Query
		*/
		query() {
			let query;
			const parent = this.parent;
			if (!parent) throw new Error("Cannot construct a query for a Relation without a parent");
			const ParseQuery = _CoreManager.default.getParseQuery();
			if (!this.targetClassName) {
				query = new ParseQuery(parent.className);
				query._extraOptions.redirectClassNameForKey = this.key;
			} else query = new ParseQuery(this.targetClassName);
			query._addCondition("$relatedTo", "object", {
				__type: "Pointer",
				className: parent.className,
				objectId: parent.id
			});
			query._addCondition("$relatedTo", "key", this.key);
			return query;
		}
	};
	exports.default = ParseRelation;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.includes.js
var require_es_array_includes = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $includes = require_array_includes().includes;
	var fails = require_fails();
	var addToUnscopables = require_add_to_unscopables();
	var BROKEN_ON_SPARSE = fails(function() {
		return !Array(1).includes();
	});
	var BROKEN_ON_SPARSE_WITH_FROM_INDEX = fails(function() {
		return [, 1].includes(void 0, 1);
	});
	$({
		target: "Array",
		proto: true,
		forced: BROKEN_ON_SPARSE || BROKEN_ON_SPARSE_WITH_FROM_INDEX
	}, { includes: function includes(el) {
		return $includes(this, el, arguments.length > 1 ? arguments[1] : void 0);
	} });
	addToUnscopables("includes");
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/includes.js
var require_includes$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_includes();
	module.exports = require_get_built_in_prototype_method()("Array", "includes");
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-regexp.js
var require_is_regexp = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	var classof = require_classof_raw();
	var MATCH = require_well_known_symbol()("match");
	module.exports = function(it) {
		var isRegExp;
		return isObject(it) && ((isRegExp = it[MATCH]) !== void 0 ? !!isRegExp : classof(it) === "RegExp");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/not-a-regexp.js
var require_not_a_regexp = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isRegExp = require_is_regexp();
	var $TypeError = TypeError;
	module.exports = function(it) {
		if (isRegExp(it)) throw new $TypeError("The method doesn't accept regular expressions");
		return it;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/correct-is-regexp-logic.js
var require_correct_is_regexp_logic = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var MATCH = require_well_known_symbol()("match");
	module.exports = function(METHOD_NAME) {
		var regexp = /./;
		try {
			"/./"[METHOD_NAME](regexp);
		} catch (error1) {
			try {
				regexp[MATCH] = false;
				return "/./"[METHOD_NAME](regexp);
			} catch (error2) {}
		}
		return false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.string.includes.js
var require_es_string_includes = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this();
	var notARegExp = require_not_a_regexp();
	var requireObjectCoercible = require_require_object_coercible();
	var toString = require_to_string();
	var correctIsRegExpLogic = require_correct_is_regexp_logic();
	var stringIndexOf = uncurryThis("".indexOf);
	$({
		target: "String",
		proto: true,
		forced: !correctIsRegExpLogic("includes")
	}, { includes: function includes(searchString) {
		return !!~stringIndexOf(toString(requireObjectCoercible(this)), toString(notARegExp(searchString)), arguments.length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/string/virtual/includes.js
var require_includes$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_string_includes();
	module.exports = require_get_built_in_prototype_method()("String", "includes");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/includes.js
var require_includes$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var arrayMethod = require_includes$4();
	var stringMethod = require_includes$3();
	var ArrayPrototype = Array.prototype;
	var StringPrototype = String.prototype;
	module.exports = function(it) {
		var own = it.includes;
		if (it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.includes) return arrayMethod;
		if (typeof it == "string" || it === StringPrototype || isPrototypeOf(StringPrototype, it) && own === StringPrototype.includes) return stringMethod;
		return own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/includes.js
var require_includes$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_includes$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/includes.js
var require_includes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_includes$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.some.js
var require_es_array_some = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $some = require_array_iteration().some;
	$({
		target: "Array",
		proto: true,
		forced: !require_array_method_is_strict()("some")
	}, { some: function some(callbackfn) {
		return $some(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/some.js
var require_some$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_some();
	module.exports = require_get_built_in_prototype_method()("Array", "some");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/some.js
var require_some$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_some$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.some;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.some ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/some.js
var require_some$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_some$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/some.js
var require_some = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_some$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/isDangerousKey.js
var require_isDangerousKey = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.isDangerousKey = isDangerousKey;
	var _includes = _interopRequireDefault(require_includes());
	var _some = _interopRequireDefault(require_some());
	/**
	* Check if a property name or path is potentially dangerous for prototype pollution
	* Dangerous keys include: __proto__, constructor, prototype
	* @param key - The property name or dotted path to check
	* @returns true if the key is dangerous, false otherwise
	*/
	function isDangerousKey(key) {
		const dangerousKeys = [
			"__proto__",
			"constructor",
			"prototype"
		];
		if ((0, _includes.default)(dangerousKeys).call(dangerousKeys, key)) return true;
		if ((0, _includes.default)(key).call(key, ".")) {
			const parts = key.split(".");
			return (0, _some.default)(parts).call(parts, (part) => (0, _includes.default)(dangerousKeys).call(dangerousKeys, part));
		}
		return false;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/decode.js
var require_decode = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = decode;
	var _isArray = _interopRequireDefault(require_is_array());
	var _forEach = _interopRequireDefault(require_for_each());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	var _ParsePolygon = _interopRequireDefault(require_ParsePolygon());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var _isDangerousKey = require_isDangerousKey();
	function decode(value) {
		if (value === null || typeof value !== "object" || value instanceof Date) return value;
		if ((0, _isArray.default)(value)) {
			const dup = [];
			(0, _forEach.default)(value).call(value, (v, i) => {
				dup[i] = decode(v);
			});
			return dup;
		}
		if (typeof value.__op === "string") {
			const { opFromJSON } = _CoreManager.default.getParseOp();
			return opFromJSON(value);
		}
		const ParseObject = _CoreManager.default.getParseObject();
		if (value.__type === "Pointer" && value.className) return ParseObject.fromJSON(value);
		if (value.__type === "Object" && value.className) return ParseObject.fromJSON(value);
		if (value.__type === "Relation") {
			const relation = new _ParseRelation.default(null, null);
			relation.targetClassName = value.className;
			return relation;
		}
		if (value.__type === "Date") return new Date(value.iso);
		if (value.__type === "File") return _ParseFile.default.fromJSON(value);
		if (value.__type === "GeoPoint") return new _ParseGeoPoint.default({
			latitude: value.latitude,
			longitude: value.longitude
		});
		if (value.__type === "Polygon") return new _ParsePolygon.default(value.coordinates);
		const copy = {};
		for (const k in value) if (Object.prototype.hasOwnProperty.call(value, k)) {
			if ((0, _isDangerousKey.isDangerousKey)(k)) continue;
			copy[k] = decode(value[k]);
		}
		return copy;
	}
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.index-of.js
var require_es_array_index_of = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this_clause();
	var $indexOf = require_array_includes().indexOf;
	var arrayMethodIsStrict = require_array_method_is_strict();
	var nativeIndexOf = uncurryThis([].indexOf);
	var NEGATIVE_ZERO = !!nativeIndexOf && 1 / nativeIndexOf([1], 1, -0) < 0;
	$({
		target: "Array",
		proto: true,
		forced: NEGATIVE_ZERO || !arrayMethodIsStrict("indexOf")
	}, { indexOf: function indexOf(searchElement) {
		var fromIndex = arguments.length > 1 ? arguments[1] : void 0;
		return NEGATIVE_ZERO ? nativeIndexOf(this, searchElement, fromIndex) || 0 : $indexOf(this, searchElement, fromIndex);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/index-of.js
var require_index_of$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_index_of();
	module.exports = require_get_built_in_prototype_method()("Array", "indexOf");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/index-of.js
var require_index_of$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_index_of$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.indexOf;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.indexOf ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/index-of.js
var require_index_of$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_index_of$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/index-of.js
var require_index_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_index_of$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.string.starts-with.js
var require_es_string_starts_with = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this_clause();
	var getOwnPropertyDescriptor = require_object_get_own_property_descriptor().f;
	var toLength = require_to_length();
	var toString = require_to_string();
	var notARegExp = require_not_a_regexp();
	var requireObjectCoercible = require_require_object_coercible();
	var correctIsRegExpLogic = require_correct_is_regexp_logic();
	var IS_PURE = require_is_pure();
	var stringSlice = uncurryThis("".slice);
	var min = Math.min;
	var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic("startsWith");
	$({
		target: "String",
		proto: true,
		forced: !(!IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function() {
			var descriptor = getOwnPropertyDescriptor(String.prototype, "startsWith");
			return descriptor && !descriptor.writable;
		}()) && !CORRECT_IS_REGEXP_LOGIC
	}, { startsWith: function startsWith(searchString) {
		var that = toString(requireObjectCoercible(this));
		notARegExp(searchString);
		var search = toString(searchString);
		var index = toLength(min(arguments.length > 1 ? arguments[1] : void 0, that.length));
		return stringSlice(that, index, index + search.length) === search;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/string/virtual/starts-with.js
var require_starts_with$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_string_starts_with();
	module.exports = require_get_built_in_prototype_method()("String", "startsWith");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/starts-with.js
var require_starts_with$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_starts_with$3();
	var StringPrototype = String.prototype;
	module.exports = function(it) {
		var own = it.startsWith;
		return typeof it == "string" || it === StringPrototype || isPrototypeOf(StringPrototype, it) && own === StringPrototype.startsWith ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/starts-with.js
var require_starts_with$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_starts_with$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/starts-with.js
var require_starts_with = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_starts_with$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.map.js
var require_es_array_map = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $map = require_array_iteration().map;
	$({
		target: "Array",
		proto: true,
		forced: !require_array_method_has_species_support()("map")
	}, { map: function map(callbackfn) {
		return $map(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/map.js
var require_map$6 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_map();
	module.exports = require_get_built_in_prototype_method()("Array", "map");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/map.js
var require_map$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_map$6();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.map;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.map ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/map.js
var require_map$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_map$5();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/map.js
var require_map$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_map$4();
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseACL.js
var require_ParseACL = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _keys = _interopRequireDefault(require_keys$4());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var PUBLIC_KEY = "*";
	exports.default = class ParseACL {
		/**
		* @param {(Parse.User | object | null)} arg1 The user to initialize the ACL for
		*/
		constructor(arg1) {
			(0, _defineProperty2.default)(this, "permissionsById", void 0);
			this.permissionsById = {};
			if (arg1 && typeof arg1 === "object") {
				if (arg1 instanceof _CoreManager.default.getParseUser()) {
					this.setReadAccess(arg1, true);
					this.setWriteAccess(arg1, true);
				} else for (const userId in arg1) {
					const accessList = arg1[userId];
					this.permissionsById[userId] = {};
					for (const permission in accessList) {
						const allowed = accessList[permission];
						if (permission !== "read" && permission !== "write") throw new TypeError("Tried to create an ACL with an invalid permission type.");
						if (typeof allowed !== "boolean") throw new TypeError("Tried to create an ACL with an invalid permission value.");
						this.permissionsById[userId][permission] = allowed;
					}
				}
			} else if (typeof arg1 === "function") throw new TypeError("ParseACL constructed with a function. Did you forget ()?");
		}
		/**
		* Returns a JSON-encoded version of the ACL.
		*
		* @returns {object}
		*/
		toJSON() {
			const permissions = {};
			for (const p in this.permissionsById) permissions[p] = this.permissionsById[p];
			return permissions;
		}
		/**
		* Returns whether this ACL is equal to another object
		*
		* @param {ParseACL} other The other object's ACL to compare to
		* @returns {boolean}
		*/
		equals(other) {
			if (!(other instanceof ParseACL)) return false;
			const users = (0, _keys.default)(this.permissionsById);
			const otherUsers = (0, _keys.default)(other.permissionsById);
			if (users.length !== otherUsers.length) return false;
			for (const u in this.permissionsById) {
				if (!other.permissionsById[u]) return false;
				if (this.permissionsById[u].read !== other.permissionsById[u].read) return false;
				if (this.permissionsById[u].write !== other.permissionsById[u].write) return false;
			}
			return true;
		}
		_setAccess(accessType, userId, allowed) {
			const ParseRole = _CoreManager.default.getParseRole();
			const ParseUser = _CoreManager.default.getParseUser();
			if (userId instanceof ParseUser) userId = userId.id;
			else if (userId instanceof ParseRole) {
				const name = userId.getName();
				if (!name) throw new TypeError("Role must have a name");
				userId = "role:" + name;
			}
			if (typeof userId !== "string") throw new TypeError("userId must be a string.");
			if (typeof allowed !== "boolean") throw new TypeError("allowed must be either true or false.");
			let permissions = this.permissionsById[userId];
			if (!permissions) {
				if (!allowed) return;
				else {
					permissions = {};
					this.permissionsById[userId] = permissions;
				}
			}
			if (allowed) this.permissionsById[userId][accessType] = true;
			else {
				delete permissions[accessType];
				if ((0, _keys.default)(permissions).length === 0) delete this.permissionsById[userId];
			}
		}
		_getAccess(accessType, userId) {
			const ParseRole = _CoreManager.default.getParseRole();
			const ParseUser = _CoreManager.default.getParseUser();
			if (userId instanceof ParseUser) {
				userId = userId.id;
				if (!userId) throw new Error("Cannot get access for a ParseUser without an ID");
			} else if (userId instanceof ParseRole) {
				const name = userId.getName();
				if (!name) throw new TypeError("Role must have a name");
				userId = "role:" + name;
			}
			const permissions = this.permissionsById[userId];
			if (!permissions) return false;
			return !!permissions[accessType];
		}
		/**
		* Sets whether the given user is allowed to read this object.
		*
		* @param userId An instance of Parse.User or its objectId.
		* @param {boolean} allowed Whether that user should have read access.
		*/
		setReadAccess(userId, allowed) {
			this._setAccess("read", userId, allowed);
		}
		/**
		* Get whether the given user id is *explicitly* allowed to read this object.
		* Even if this returns false, the user may still be able to access it if
		* getPublicReadAccess returns true or a role that the user belongs to has
		* write access.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role.
		* @returns {boolean}
		*/
		getReadAccess(userId) {
			return this._getAccess("read", userId);
		}
		/**
		* Sets whether the given user id is allowed to write this object.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role..
		* @param {boolean} allowed Whether that user should have write access.
		*/
		setWriteAccess(userId, allowed) {
			this._setAccess("write", userId, allowed);
		}
		/**
		* Gets whether the given user id is *explicitly* allowed to write this object.
		* Even if this returns false, the user may still be able to write it if
		* getPublicWriteAccess returns true or a role that the user belongs to has
		* write access.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role.
		* @returns {boolean}
		*/
		getWriteAccess(userId) {
			return this._getAccess("write", userId);
		}
		/**
		* Sets whether the public is allowed to read this object.
		*
		* @param {boolean} allowed
		*/
		setPublicReadAccess(allowed) {
			this.setReadAccess(PUBLIC_KEY, allowed);
		}
		/**
		* Gets whether the public is allowed to read this object.
		*
		* @returns {boolean}
		*/
		getPublicReadAccess() {
			return this.getReadAccess(PUBLIC_KEY);
		}
		/**
		* Sets whether the public is allowed to write this object.
		*
		* @param {boolean} allowed
		*/
		setPublicWriteAccess(allowed) {
			this.setWriteAccess(PUBLIC_KEY, allowed);
		}
		/**
		* Gets whether the public is allowed to write this object.
		*
		* @returns {boolean}
		*/
		getPublicWriteAccess() {
			return this.getWriteAccess(PUBLIC_KEY);
		}
		/**
		* Gets whether users belonging to the given role are allowed
		* to read this object. Even if this returns false, the role may
		* still be able to write it if a parent role has read access.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @returns {boolean} true if the role has read access. false otherwise.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		getRoleReadAccess(role) {
			const ParseRole = _CoreManager.default.getParseRole();
			if (role instanceof ParseRole) role = role.getName();
			if (typeof role !== "string") throw new TypeError("role must be a ParseRole or a String");
			return this.getReadAccess("role:" + role);
		}
		/**
		* Gets whether users belonging to the given role are allowed
		* to write this object. Even if this returns false, the role may
		* still be able to write it if a parent role has write access.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @returns {boolean} true if the role has write access. false otherwise.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		getRoleWriteAccess(role) {
			const ParseRole = _CoreManager.default.getParseRole();
			if (role instanceof ParseRole) role = role.getName();
			if (typeof role !== "string") throw new TypeError("role must be a ParseRole or a String");
			return this.getWriteAccess("role:" + role);
		}
		/**
		* Sets whether users belonging to the given role are allowed
		* to read this object.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @param {boolean} allowed Whether the given role can read this object.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		setRoleReadAccess(role, allowed) {
			const ParseRole = _CoreManager.default.getParseRole();
			if (role instanceof ParseRole) role = role.getName();
			if (typeof role !== "string") throw new TypeError("role must be a ParseRole or a String");
			this.setReadAccess("role:" + role, allowed);
		}
		/**
		* Sets whether users belonging to the given role are allowed
		* to write this object.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @param {boolean} allowed Whether the given role can write this object.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		setRoleWriteAccess(role, allowed) {
			const ParseRole = _CoreManager.default.getParseRole();
			if (role instanceof ParseRole) role = role.getName();
			if (typeof role !== "string") throw new TypeError("role must be a ParseRole or a String");
			this.setWriteAccess("role:" + role, allowed);
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/encode.js
var require_encode = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = _default;
	var _indexOf = _interopRequireDefault(require_index_of());
	var _keys = _interopRequireDefault(require_keys$4());
	var _startsWith = _interopRequireDefault(require_starts_with());
	var _concat = _interopRequireDefault(require_concat());
	var _isArray = _interopRequireDefault(require_is_array());
	var _map = _interopRequireDefault(require_map$3());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseACL = _interopRequireDefault(require_ParseACL());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	var _ParsePolygon = _interopRequireDefault(require_ParsePolygon());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var _isDangerousKey = require_isDangerousKey();
	function encode(value, disallowObjects, forcePointers, seen, offline) {
		if (value instanceof _CoreManager.default.getParseObject()) {
			if (disallowObjects) throw new Error("Parse Objects not allowed here");
			const seenEntry = value.id ? value.className + ":" + value.id : value;
			if (forcePointers || !seen || (0, _indexOf.default)(seen).call(seen, seenEntry) > -1 || value.dirty() || (0, _keys.default)(value._getServerData()).length < 1) {
				var _context;
				if (offline && (0, _startsWith.default)(_context = value._getId()).call(_context, "local")) return value.toOfflinePointer();
				return value.toPointer();
			}
			seen = (0, _concat.default)(seen).call(seen, seenEntry);
			return value._toFullJSON(seen, offline);
		}
		const { Op } = _CoreManager.default.getParseOp();
		if (value instanceof Op || value instanceof _ParseACL.default || value instanceof _ParseGeoPoint.default || value instanceof _ParsePolygon.default || value instanceof _ParseRelation.default) return value.toJSON();
		if (value instanceof _ParseFile.default) {
			if (!value.url()) throw new Error("Tried to encode an unsaved file.");
			return value.toJSON();
		}
		if (Object.prototype.toString.call(value) === "[object Date]") {
			if (isNaN(value)) throw new Error("Tried to encode an invalid date.");
			return {
				__type: "Date",
				iso: value.toJSON()
			};
		}
		if (Object.prototype.toString.call(value) === "[object RegExp]" && typeof value.source === "string") return value.source;
		if ((0, _isArray.default)(value)) return (0, _map.default)(value).call(value, (v) => {
			return encode(v, disallowObjects, forcePointers, seen, offline);
		});
		if (value && typeof value === "object") {
			const output = {};
			for (const k in value) if (Object.prototype.hasOwnProperty.call(value, k)) {
				if ((0, _isDangerousKey.isDangerousKey)(k)) continue;
				output[k] = encode(value[k], disallowObjects, forcePointers, seen, offline);
			}
			return output;
		}
		return value;
	}
	function _default(value, disallowObjects, forcePointers, seen, offline) {
		return encode(value, !!disallowObjects, !!forcePointers, seen || [], offline);
	}
}));
//#endregion
//#region browser-external:crypto
var require_browser_external_crypto = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Object.create(new Proxy({}, { get(_, key) {
		if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") console.warn(`Module "crypto" has been externalized for browser compatibility. Cannot access "crypto.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
	} }));
}));
//#endregion
//#region node_modules/crypto-js/core.js
var require_core = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory();
		else if (typeof define === "function" && define.amd) define([], factory);
		else root.CryptoJS = factory();
	})(exports, function() {
		/**
		* CryptoJS core components.
		*/
		var CryptoJS = CryptoJS || function(Math, undefined) {
			var crypto;
			if (typeof window !== "undefined" && window.crypto) crypto = window.crypto;
			if (typeof self !== "undefined" && self.crypto) crypto = self.crypto;
			if (typeof globalThis !== "undefined" && globalThis.crypto) crypto = globalThis.crypto;
			if (!crypto && typeof window !== "undefined" && window.msCrypto) crypto = window.msCrypto;
			if (!crypto && typeof global !== "undefined" && global.crypto) crypto = global.crypto;
			if (!crypto && typeof __require === "function") try {
				crypto = require_browser_external_crypto();
			} catch (err) {}
			var cryptoSecureRandomInt = function() {
				if (crypto) {
					if (typeof crypto.getRandomValues === "function") try {
						return crypto.getRandomValues(/* @__PURE__ */ new Uint32Array(1))[0];
					} catch (err) {}
					if (typeof crypto.randomBytes === "function") try {
						return crypto.randomBytes(4).readInt32LE();
					} catch (err) {}
				}
				throw new Error("Native crypto module could not be used to get secure random number.");
			};
			var create = Object.create || function() {
				function F() {}
				return function(obj) {
					var subtype;
					F.prototype = obj;
					subtype = new F();
					F.prototype = null;
					return subtype;
				};
			}();
			/**
			* CryptoJS namespace.
			*/
			var C = {};
			/**
			* Library namespace.
			*/
			var C_lib = C.lib = {};
			/**
			* Base object for prototypal inheritance.
			*/
			var Base = C_lib.Base = function() {
				return {
					/**
					* Creates a new object that inherits from this object.
					*
					* @param {Object} overrides Properties to copy into the new object.
					*
					* @return {Object} The new object.
					*
					* @static
					*
					* @example
					*
					*     var MyType = CryptoJS.lib.Base.extend({
					*         field: 'value',
					*
					*         method: function () {
					*         }
					*     });
					*/
					extend: function(overrides) {
						var subtype = create(this);
						if (overrides) subtype.mixIn(overrides);
						if (!subtype.hasOwnProperty("init") || this.init === subtype.init) subtype.init = function() {
							subtype.$super.init.apply(this, arguments);
						};
						subtype.init.prototype = subtype;
						subtype.$super = this;
						return subtype;
					},
					/**
					* Extends this object and runs the init method.
					* Arguments to create() will be passed to init().
					*
					* @return {Object} The new object.
					*
					* @static
					*
					* @example
					*
					*     var instance = MyType.create();
					*/
					create: function() {
						var instance = this.extend();
						instance.init.apply(instance, arguments);
						return instance;
					},
					/**
					* Initializes a newly created object.
					* Override this method to add some logic when your objects are created.
					*
					* @example
					*
					*     var MyType = CryptoJS.lib.Base.extend({
					*         init: function () {
					*             // ...
					*         }
					*     });
					*/
					init: function() {},
					/**
					* Copies properties into this object.
					*
					* @param {Object} properties The properties to mix in.
					*
					* @example
					*
					*     MyType.mixIn({
					*         field: 'value'
					*     });
					*/
					mixIn: function(properties) {
						for (var propertyName in properties) if (properties.hasOwnProperty(propertyName)) this[propertyName] = properties[propertyName];
						if (properties.hasOwnProperty("toString")) this.toString = properties.toString;
					},
					/**
					* Creates a copy of this object.
					*
					* @return {Object} The clone.
					*
					* @example
					*
					*     var clone = instance.clone();
					*/
					clone: function() {
						return this.init.prototype.extend(this);
					}
				};
			}();
			/**
			* An array of 32-bit words.
			*
			* @property {Array} words The array of 32-bit words.
			* @property {number} sigBytes The number of significant bytes in this word array.
			*/
			var WordArray = C_lib.WordArray = Base.extend({
				/**
				* Initializes a newly created word array.
				*
				* @param {Array} words (Optional) An array of 32-bit words.
				* @param {number} sigBytes (Optional) The number of significant bytes in the words.
				*
				* @example
				*
				*     var wordArray = CryptoJS.lib.WordArray.create();
				*     var wordArray = CryptoJS.lib.WordArray.create([0x00010203, 0x04050607]);
				*     var wordArray = CryptoJS.lib.WordArray.create([0x00010203, 0x04050607], 6);
				*/
				init: function(words, sigBytes) {
					words = this.words = words || [];
					if (sigBytes != undefined) this.sigBytes = sigBytes;
					else this.sigBytes = words.length * 4;
				},
				/**
				* Converts this word array to a string.
				*
				* @param {Encoder} encoder (Optional) The encoding strategy to use. Default: CryptoJS.enc.Hex
				*
				* @return {string} The stringified word array.
				*
				* @example
				*
				*     var string = wordArray + '';
				*     var string = wordArray.toString();
				*     var string = wordArray.toString(CryptoJS.enc.Utf8);
				*/
				toString: function(encoder) {
					return (encoder || Hex).stringify(this);
				},
				/**
				* Concatenates a word array to this word array.
				*
				* @param {WordArray} wordArray The word array to append.
				*
				* @return {WordArray} This word array.
				*
				* @example
				*
				*     wordArray1.concat(wordArray2);
				*/
				concat: function(wordArray) {
					var thisWords = this.words;
					var thatWords = wordArray.words;
					var thisSigBytes = this.sigBytes;
					var thatSigBytes = wordArray.sigBytes;
					this.clamp();
					if (thisSigBytes % 4) for (var i = 0; i < thatSigBytes; i++) {
						var thatByte = thatWords[i >>> 2] >>> 24 - i % 4 * 8 & 255;
						thisWords[thisSigBytes + i >>> 2] |= thatByte << 24 - (thisSigBytes + i) % 4 * 8;
					}
					else for (var j = 0; j < thatSigBytes; j += 4) thisWords[thisSigBytes + j >>> 2] = thatWords[j >>> 2];
					this.sigBytes += thatSigBytes;
					return this;
				},
				/**
				* Removes insignificant bits.
				*
				* @example
				*
				*     wordArray.clamp();
				*/
				clamp: function() {
					var words = this.words;
					var sigBytes = this.sigBytes;
					words[sigBytes >>> 2] &= 4294967295 << 32 - sigBytes % 4 * 8;
					words.length = Math.ceil(sigBytes / 4);
				},
				/**
				* Creates a copy of this word array.
				*
				* @return {WordArray} The clone.
				*
				* @example
				*
				*     var clone = wordArray.clone();
				*/
				clone: function() {
					var clone = Base.clone.call(this);
					clone.words = this.words.slice(0);
					return clone;
				},
				/**
				* Creates a word array filled with random bytes.
				*
				* @param {number} nBytes The number of random bytes to generate.
				*
				* @return {WordArray} The random word array.
				*
				* @static
				*
				* @example
				*
				*     var wordArray = CryptoJS.lib.WordArray.random(16);
				*/
				random: function(nBytes) {
					var words = [];
					for (var i = 0; i < nBytes; i += 4) words.push(cryptoSecureRandomInt());
					return new WordArray.init(words, nBytes);
				}
			});
			/**
			* Encoder namespace.
			*/
			var C_enc = C.enc = {};
			/**
			* Hex encoding strategy.
			*/
			var Hex = C_enc.Hex = {
				/**
				* Converts a word array to a hex string.
				*
				* @param {WordArray} wordArray The word array.
				*
				* @return {string} The hex string.
				*
				* @static
				*
				* @example
				*
				*     var hexString = CryptoJS.enc.Hex.stringify(wordArray);
				*/
				stringify: function(wordArray) {
					var words = wordArray.words;
					var sigBytes = wordArray.sigBytes;
					var hexChars = [];
					for (var i = 0; i < sigBytes; i++) {
						var bite = words[i >>> 2] >>> 24 - i % 4 * 8 & 255;
						hexChars.push((bite >>> 4).toString(16));
						hexChars.push((bite & 15).toString(16));
					}
					return hexChars.join("");
				},
				/**
				* Converts a hex string to a word array.
				*
				* @param {string} hexStr The hex string.
				*
				* @return {WordArray} The word array.
				*
				* @static
				*
				* @example
				*
				*     var wordArray = CryptoJS.enc.Hex.parse(hexString);
				*/
				parse: function(hexStr) {
					var hexStrLength = hexStr.length;
					var words = [];
					for (var i = 0; i < hexStrLength; i += 2) words[i >>> 3] |= parseInt(hexStr.substr(i, 2), 16) << 24 - i % 8 * 4;
					return new WordArray.init(words, hexStrLength / 2);
				}
			};
			/**
			* Latin1 encoding strategy.
			*/
			var Latin1 = C_enc.Latin1 = {
				/**
				* Converts a word array to a Latin1 string.
				*
				* @param {WordArray} wordArray The word array.
				*
				* @return {string} The Latin1 string.
				*
				* @static
				*
				* @example
				*
				*     var latin1String = CryptoJS.enc.Latin1.stringify(wordArray);
				*/
				stringify: function(wordArray) {
					var words = wordArray.words;
					var sigBytes = wordArray.sigBytes;
					var latin1Chars = [];
					for (var i = 0; i < sigBytes; i++) {
						var bite = words[i >>> 2] >>> 24 - i % 4 * 8 & 255;
						latin1Chars.push(String.fromCharCode(bite));
					}
					return latin1Chars.join("");
				},
				/**
				* Converts a Latin1 string to a word array.
				*
				* @param {string} latin1Str The Latin1 string.
				*
				* @return {WordArray} The word array.
				*
				* @static
				*
				* @example
				*
				*     var wordArray = CryptoJS.enc.Latin1.parse(latin1String);
				*/
				parse: function(latin1Str) {
					var latin1StrLength = latin1Str.length;
					var words = [];
					for (var i = 0; i < latin1StrLength; i++) words[i >>> 2] |= (latin1Str.charCodeAt(i) & 255) << 24 - i % 4 * 8;
					return new WordArray.init(words, latin1StrLength);
				}
			};
			/**
			* UTF-8 encoding strategy.
			*/
			var Utf8 = C_enc.Utf8 = {
				/**
				* Converts a word array to a UTF-8 string.
				*
				* @param {WordArray} wordArray The word array.
				*
				* @return {string} The UTF-8 string.
				*
				* @static
				*
				* @example
				*
				*     var utf8String = CryptoJS.enc.Utf8.stringify(wordArray);
				*/
				stringify: function(wordArray) {
					try {
						return decodeURIComponent(escape(Latin1.stringify(wordArray)));
					} catch (e) {
						throw new Error("Malformed UTF-8 data");
					}
				},
				/**
				* Converts a UTF-8 string to a word array.
				*
				* @param {string} utf8Str The UTF-8 string.
				*
				* @return {WordArray} The word array.
				*
				* @static
				*
				* @example
				*
				*     var wordArray = CryptoJS.enc.Utf8.parse(utf8String);
				*/
				parse: function(utf8Str) {
					return Latin1.parse(unescape(encodeURIComponent(utf8Str)));
				}
			};
			/**
			* Abstract buffered block algorithm template.
			*
			* The property blockSize must be implemented in a concrete subtype.
			*
			* @property {number} _minBufferSize The number of blocks that should be kept unprocessed in the buffer. Default: 0
			*/
			var BufferedBlockAlgorithm = C_lib.BufferedBlockAlgorithm = Base.extend({
				/**
				* Resets this block algorithm's data buffer to its initial state.
				*
				* @example
				*
				*     bufferedBlockAlgorithm.reset();
				*/
				reset: function() {
					this._data = new WordArray.init();
					this._nDataBytes = 0;
				},
				/**
				* Adds new data to this block algorithm's buffer.
				*
				* @param {WordArray|string} data The data to append. Strings are converted to a WordArray using UTF-8.
				*
				* @example
				*
				*     bufferedBlockAlgorithm._append('data');
				*     bufferedBlockAlgorithm._append(wordArray);
				*/
				_append: function(data) {
					if (typeof data == "string") data = Utf8.parse(data);
					this._data.concat(data);
					this._nDataBytes += data.sigBytes;
				},
				/**
				* Processes available data blocks.
				*
				* This method invokes _doProcessBlock(offset), which must be implemented by a concrete subtype.
				*
				* @param {boolean} doFlush Whether all blocks and partial blocks should be processed.
				*
				* @return {WordArray} The processed data.
				*
				* @example
				*
				*     var processedData = bufferedBlockAlgorithm._process();
				*     var processedData = bufferedBlockAlgorithm._process(!!'flush');
				*/
				_process: function(doFlush) {
					var processedWords;
					var data = this._data;
					var dataWords = data.words;
					var dataSigBytes = data.sigBytes;
					var blockSize = this.blockSize;
					var nBlocksReady = dataSigBytes / (blockSize * 4);
					if (doFlush) nBlocksReady = Math.ceil(nBlocksReady);
					else nBlocksReady = Math.max((nBlocksReady | 0) - this._minBufferSize, 0);
					var nWordsReady = nBlocksReady * blockSize;
					var nBytesReady = Math.min(nWordsReady * 4, dataSigBytes);
					if (nWordsReady) {
						for (var offset = 0; offset < nWordsReady; offset += blockSize) this._doProcessBlock(dataWords, offset);
						processedWords = dataWords.splice(0, nWordsReady);
						data.sigBytes -= nBytesReady;
					}
					return new WordArray.init(processedWords, nBytesReady);
				},
				/**
				* Creates a copy of this object.
				*
				* @return {Object} The clone.
				*
				* @example
				*
				*     var clone = bufferedBlockAlgorithm.clone();
				*/
				clone: function() {
					var clone = Base.clone.call(this);
					clone._data = this._data.clone();
					return clone;
				},
				_minBufferSize: 0
			});
			C_lib.Hasher = BufferedBlockAlgorithm.extend({
				/**
				* Configuration options.
				*/
				cfg: Base.extend(),
				/**
				* Initializes a newly created hasher.
				*
				* @param {Object} cfg (Optional) The configuration options to use for this hash computation.
				*
				* @example
				*
				*     var hasher = CryptoJS.algo.SHA256.create();
				*/
				init: function(cfg) {
					this.cfg = this.cfg.extend(cfg);
					this.reset();
				},
				/**
				* Resets this hasher to its initial state.
				*
				* @example
				*
				*     hasher.reset();
				*/
				reset: function() {
					BufferedBlockAlgorithm.reset.call(this);
					this._doReset();
				},
				/**
				* Updates this hasher with a message.
				*
				* @param {WordArray|string} messageUpdate The message to append.
				*
				* @return {Hasher} This hasher.
				*
				* @example
				*
				*     hasher.update('message');
				*     hasher.update(wordArray);
				*/
				update: function(messageUpdate) {
					this._append(messageUpdate);
					this._process();
					return this;
				},
				/**
				* Finalizes the hash computation.
				* Note that the finalize operation is effectively a destructive, read-once operation.
				*
				* @param {WordArray|string} messageUpdate (Optional) A final message update.
				*
				* @return {WordArray} The hash.
				*
				* @example
				*
				*     var hash = hasher.finalize();
				*     var hash = hasher.finalize('message');
				*     var hash = hasher.finalize(wordArray);
				*/
				finalize: function(messageUpdate) {
					if (messageUpdate) this._append(messageUpdate);
					return this._doFinalize();
				},
				blockSize: 16,
				/**
				* Creates a shortcut function to a hasher's object interface.
				*
				* @param {Hasher} hasher The hasher to create a helper for.
				*
				* @return {Function} The shortcut function.
				*
				* @static
				*
				* @example
				*
				*     var SHA256 = CryptoJS.lib.Hasher._createHelper(CryptoJS.algo.SHA256);
				*/
				_createHelper: function(hasher) {
					return function(message, cfg) {
						return new hasher.init(cfg).finalize(message);
					};
				},
				/**
				* Creates a shortcut function to the HMAC's object interface.
				*
				* @param {Hasher} hasher The hasher to use in this HMAC helper.
				*
				* @return {Function} The shortcut function.
				*
				* @static
				*
				* @example
				*
				*     var HmacSHA256 = CryptoJS.lib.Hasher._createHmacHelper(CryptoJS.algo.SHA256);
				*/
				_createHmacHelper: function(hasher) {
					return function(message, key) {
						return new C_algo.HMAC.init(hasher, key).finalize(message);
					};
				}
			});
			/**
			* Algorithm namespace.
			*/
			var C_algo = C.algo = {};
			return C;
		}(Math);
		return CryptoJS;
	});
}));
//#endregion
//#region node_modules/crypto-js/enc-base64.js
var require_enc_base64 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory(require_core());
		else if (typeof define === "function" && define.amd) define(["./core"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function() {
			var C = CryptoJS;
			var WordArray = C.lib.WordArray;
			var C_enc = C.enc;
			C_enc.Base64 = {
				/**
				* Converts a word array to a Base64 string.
				*
				* @param {WordArray} wordArray The word array.
				*
				* @return {string} The Base64 string.
				*
				* @static
				*
				* @example
				*
				*     var base64String = CryptoJS.enc.Base64.stringify(wordArray);
				*/
				stringify: function(wordArray) {
					var words = wordArray.words;
					var sigBytes = wordArray.sigBytes;
					var map = this._map;
					wordArray.clamp();
					var base64Chars = [];
					for (var i = 0; i < sigBytes; i += 3) {
						var byte1 = words[i >>> 2] >>> 24 - i % 4 * 8 & 255;
						var byte2 = words[i + 1 >>> 2] >>> 24 - (i + 1) % 4 * 8 & 255;
						var byte3 = words[i + 2 >>> 2] >>> 24 - (i + 2) % 4 * 8 & 255;
						var triplet = byte1 << 16 | byte2 << 8 | byte3;
						for (var j = 0; j < 4 && i + j * .75 < sigBytes; j++) base64Chars.push(map.charAt(triplet >>> 6 * (3 - j) & 63));
					}
					var paddingChar = map.charAt(64);
					if (paddingChar) while (base64Chars.length % 4) base64Chars.push(paddingChar);
					return base64Chars.join("");
				},
				/**
				* Converts a Base64 string to a word array.
				*
				* @param {string} base64Str The Base64 string.
				*
				* @return {WordArray} The word array.
				*
				* @static
				*
				* @example
				*
				*     var wordArray = CryptoJS.enc.Base64.parse(base64String);
				*/
				parse: function(base64Str) {
					var base64StrLength = base64Str.length;
					var map = this._map;
					var reverseMap = this._reverseMap;
					if (!reverseMap) {
						reverseMap = this._reverseMap = [];
						for (var j = 0; j < map.length; j++) reverseMap[map.charCodeAt(j)] = j;
					}
					var paddingChar = map.charAt(64);
					if (paddingChar) {
						var paddingIndex = base64Str.indexOf(paddingChar);
						if (paddingIndex !== -1) base64StrLength = paddingIndex;
					}
					return parseLoop(base64Str, base64StrLength, reverseMap);
				},
				_map: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/="
			};
			function parseLoop(base64Str, base64StrLength, reverseMap) {
				var words = [];
				var nBytes = 0;
				for (var i = 0; i < base64StrLength; i++) if (i % 4) {
					var bitsCombined = reverseMap[base64Str.charCodeAt(i - 1)] << i % 4 * 2 | reverseMap[base64Str.charCodeAt(i)] >>> 6 - i % 4 * 2;
					words[nBytes >>> 2] |= bitsCombined << 24 - nBytes % 4 * 8;
					nBytes++;
				}
				return WordArray.create(words, nBytes);
			}
		})();
		return CryptoJS.enc.Base64;
	});
}));
//#endregion
//#region node_modules/crypto-js/md5.js
var require_md5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory(require_core());
		else if (typeof define === "function" && define.amd) define(["./core"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function(Math) {
			var C = CryptoJS;
			var C_lib = C.lib;
			var WordArray = C_lib.WordArray;
			var Hasher = C_lib.Hasher;
			var C_algo = C.algo;
			var T = [];
			(function() {
				for (var i = 0; i < 64; i++) T[i] = Math.abs(Math.sin(i + 1)) * 4294967296 | 0;
			})();
			/**
			* MD5 hash algorithm.
			*/
			var MD5 = C_algo.MD5 = Hasher.extend({
				_doReset: function() {
					this._hash = new WordArray.init([
						1732584193,
						4023233417,
						2562383102,
						271733878
					]);
				},
				_doProcessBlock: function(M, offset) {
					for (var i = 0; i < 16; i++) {
						var offset_i = offset + i;
						var M_offset_i = M[offset_i];
						M[offset_i] = (M_offset_i << 8 | M_offset_i >>> 24) & 16711935 | (M_offset_i << 24 | M_offset_i >>> 8) & 4278255360;
					}
					var H = this._hash.words;
					var M_offset_0 = M[offset + 0];
					var M_offset_1 = M[offset + 1];
					var M_offset_2 = M[offset + 2];
					var M_offset_3 = M[offset + 3];
					var M_offset_4 = M[offset + 4];
					var M_offset_5 = M[offset + 5];
					var M_offset_6 = M[offset + 6];
					var M_offset_7 = M[offset + 7];
					var M_offset_8 = M[offset + 8];
					var M_offset_9 = M[offset + 9];
					var M_offset_10 = M[offset + 10];
					var M_offset_11 = M[offset + 11];
					var M_offset_12 = M[offset + 12];
					var M_offset_13 = M[offset + 13];
					var M_offset_14 = M[offset + 14];
					var M_offset_15 = M[offset + 15];
					var a = H[0];
					var b = H[1];
					var c = H[2];
					var d = H[3];
					a = FF(a, b, c, d, M_offset_0, 7, T[0]);
					d = FF(d, a, b, c, M_offset_1, 12, T[1]);
					c = FF(c, d, a, b, M_offset_2, 17, T[2]);
					b = FF(b, c, d, a, M_offset_3, 22, T[3]);
					a = FF(a, b, c, d, M_offset_4, 7, T[4]);
					d = FF(d, a, b, c, M_offset_5, 12, T[5]);
					c = FF(c, d, a, b, M_offset_6, 17, T[6]);
					b = FF(b, c, d, a, M_offset_7, 22, T[7]);
					a = FF(a, b, c, d, M_offset_8, 7, T[8]);
					d = FF(d, a, b, c, M_offset_9, 12, T[9]);
					c = FF(c, d, a, b, M_offset_10, 17, T[10]);
					b = FF(b, c, d, a, M_offset_11, 22, T[11]);
					a = FF(a, b, c, d, M_offset_12, 7, T[12]);
					d = FF(d, a, b, c, M_offset_13, 12, T[13]);
					c = FF(c, d, a, b, M_offset_14, 17, T[14]);
					b = FF(b, c, d, a, M_offset_15, 22, T[15]);
					a = GG(a, b, c, d, M_offset_1, 5, T[16]);
					d = GG(d, a, b, c, M_offset_6, 9, T[17]);
					c = GG(c, d, a, b, M_offset_11, 14, T[18]);
					b = GG(b, c, d, a, M_offset_0, 20, T[19]);
					a = GG(a, b, c, d, M_offset_5, 5, T[20]);
					d = GG(d, a, b, c, M_offset_10, 9, T[21]);
					c = GG(c, d, a, b, M_offset_15, 14, T[22]);
					b = GG(b, c, d, a, M_offset_4, 20, T[23]);
					a = GG(a, b, c, d, M_offset_9, 5, T[24]);
					d = GG(d, a, b, c, M_offset_14, 9, T[25]);
					c = GG(c, d, a, b, M_offset_3, 14, T[26]);
					b = GG(b, c, d, a, M_offset_8, 20, T[27]);
					a = GG(a, b, c, d, M_offset_13, 5, T[28]);
					d = GG(d, a, b, c, M_offset_2, 9, T[29]);
					c = GG(c, d, a, b, M_offset_7, 14, T[30]);
					b = GG(b, c, d, a, M_offset_12, 20, T[31]);
					a = HH(a, b, c, d, M_offset_5, 4, T[32]);
					d = HH(d, a, b, c, M_offset_8, 11, T[33]);
					c = HH(c, d, a, b, M_offset_11, 16, T[34]);
					b = HH(b, c, d, a, M_offset_14, 23, T[35]);
					a = HH(a, b, c, d, M_offset_1, 4, T[36]);
					d = HH(d, a, b, c, M_offset_4, 11, T[37]);
					c = HH(c, d, a, b, M_offset_7, 16, T[38]);
					b = HH(b, c, d, a, M_offset_10, 23, T[39]);
					a = HH(a, b, c, d, M_offset_13, 4, T[40]);
					d = HH(d, a, b, c, M_offset_0, 11, T[41]);
					c = HH(c, d, a, b, M_offset_3, 16, T[42]);
					b = HH(b, c, d, a, M_offset_6, 23, T[43]);
					a = HH(a, b, c, d, M_offset_9, 4, T[44]);
					d = HH(d, a, b, c, M_offset_12, 11, T[45]);
					c = HH(c, d, a, b, M_offset_15, 16, T[46]);
					b = HH(b, c, d, a, M_offset_2, 23, T[47]);
					a = II(a, b, c, d, M_offset_0, 6, T[48]);
					d = II(d, a, b, c, M_offset_7, 10, T[49]);
					c = II(c, d, a, b, M_offset_14, 15, T[50]);
					b = II(b, c, d, a, M_offset_5, 21, T[51]);
					a = II(a, b, c, d, M_offset_12, 6, T[52]);
					d = II(d, a, b, c, M_offset_3, 10, T[53]);
					c = II(c, d, a, b, M_offset_10, 15, T[54]);
					b = II(b, c, d, a, M_offset_1, 21, T[55]);
					a = II(a, b, c, d, M_offset_8, 6, T[56]);
					d = II(d, a, b, c, M_offset_15, 10, T[57]);
					c = II(c, d, a, b, M_offset_6, 15, T[58]);
					b = II(b, c, d, a, M_offset_13, 21, T[59]);
					a = II(a, b, c, d, M_offset_4, 6, T[60]);
					d = II(d, a, b, c, M_offset_11, 10, T[61]);
					c = II(c, d, a, b, M_offset_2, 15, T[62]);
					b = II(b, c, d, a, M_offset_9, 21, T[63]);
					H[0] = H[0] + a | 0;
					H[1] = H[1] + b | 0;
					H[2] = H[2] + c | 0;
					H[3] = H[3] + d | 0;
				},
				_doFinalize: function() {
					var data = this._data;
					var dataWords = data.words;
					var nBitsTotal = this._nDataBytes * 8;
					var nBitsLeft = data.sigBytes * 8;
					dataWords[nBitsLeft >>> 5] |= 128 << 24 - nBitsLeft % 32;
					var nBitsTotalH = Math.floor(nBitsTotal / 4294967296);
					var nBitsTotalL = nBitsTotal;
					dataWords[(nBitsLeft + 64 >>> 9 << 4) + 15] = (nBitsTotalH << 8 | nBitsTotalH >>> 24) & 16711935 | (nBitsTotalH << 24 | nBitsTotalH >>> 8) & 4278255360;
					dataWords[(nBitsLeft + 64 >>> 9 << 4) + 14] = (nBitsTotalL << 8 | nBitsTotalL >>> 24) & 16711935 | (nBitsTotalL << 24 | nBitsTotalL >>> 8) & 4278255360;
					data.sigBytes = (dataWords.length + 1) * 4;
					this._process();
					var hash = this._hash;
					var H = hash.words;
					for (var i = 0; i < 4; i++) {
						var H_i = H[i];
						H[i] = (H_i << 8 | H_i >>> 24) & 16711935 | (H_i << 24 | H_i >>> 8) & 4278255360;
					}
					return hash;
				},
				clone: function() {
					var clone = Hasher.clone.call(this);
					clone._hash = this._hash.clone();
					return clone;
				}
			});
			function FF(a, b, c, d, x, s, t) {
				var n = a + (b & c | ~b & d) + x + t;
				return (n << s | n >>> 32 - s) + b;
			}
			function GG(a, b, c, d, x, s, t) {
				var n = a + (b & d | c & ~d) + x + t;
				return (n << s | n >>> 32 - s) + b;
			}
			function HH(a, b, c, d, x, s, t) {
				var n = a + (b ^ c ^ d) + x + t;
				return (n << s | n >>> 32 - s) + b;
			}
			function II(a, b, c, d, x, s, t) {
				var n = a + (c ^ (b | ~d)) + x + t;
				return (n << s | n >>> 32 - s) + b;
			}
			/**
			* Shortcut function to the hasher's object interface.
			*
			* @param {WordArray|string} message The message to hash.
			*
			* @return {WordArray} The hash.
			*
			* @static
			*
			* @example
			*
			*     var hash = CryptoJS.MD5('message');
			*     var hash = CryptoJS.MD5(wordArray);
			*/
			C.MD5 = Hasher._createHelper(MD5);
			/**
			* Shortcut function to the HMAC's object interface.
			*
			* @param {WordArray|string} message The message to hash.
			* @param {WordArray|string} key The secret key.
			*
			* @return {WordArray} The HMAC.
			*
			* @static
			*
			* @example
			*
			*     var hmac = CryptoJS.HmacMD5(message, key);
			*/
			C.HmacMD5 = Hasher._createHmacHelper(MD5);
		})(Math);
		return CryptoJS.MD5;
	});
}));
//#endregion
//#region node_modules/crypto-js/sha1.js
var require_sha1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory(require_core());
		else if (typeof define === "function" && define.amd) define(["./core"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function() {
			var C = CryptoJS;
			var C_lib = C.lib;
			var WordArray = C_lib.WordArray;
			var Hasher = C_lib.Hasher;
			var C_algo = C.algo;
			var W = [];
			/**
			* SHA-1 hash algorithm.
			*/
			var SHA1 = C_algo.SHA1 = Hasher.extend({
				_doReset: function() {
					this._hash = new WordArray.init([
						1732584193,
						4023233417,
						2562383102,
						271733878,
						3285377520
					]);
				},
				_doProcessBlock: function(M, offset) {
					var H = this._hash.words;
					var a = H[0];
					var b = H[1];
					var c = H[2];
					var d = H[3];
					var e = H[4];
					for (var i = 0; i < 80; i++) {
						if (i < 16) W[i] = M[offset + i] | 0;
						else {
							var n = W[i - 3] ^ W[i - 8] ^ W[i - 14] ^ W[i - 16];
							W[i] = n << 1 | n >>> 31;
						}
						var t = (a << 5 | a >>> 27) + e + W[i];
						if (i < 20) t += (b & c | ~b & d) + 1518500249;
						else if (i < 40) t += (b ^ c ^ d) + 1859775393;
						else if (i < 60) t += (b & c | b & d | c & d) - 1894007588;
						else t += (b ^ c ^ d) - 899497514;
						e = d;
						d = c;
						c = b << 30 | b >>> 2;
						b = a;
						a = t;
					}
					H[0] = H[0] + a | 0;
					H[1] = H[1] + b | 0;
					H[2] = H[2] + c | 0;
					H[3] = H[3] + d | 0;
					H[4] = H[4] + e | 0;
				},
				_doFinalize: function() {
					var data = this._data;
					var dataWords = data.words;
					var nBitsTotal = this._nDataBytes * 8;
					var nBitsLeft = data.sigBytes * 8;
					dataWords[nBitsLeft >>> 5] |= 128 << 24 - nBitsLeft % 32;
					dataWords[(nBitsLeft + 64 >>> 9 << 4) + 14] = Math.floor(nBitsTotal / 4294967296);
					dataWords[(nBitsLeft + 64 >>> 9 << 4) + 15] = nBitsTotal;
					data.sigBytes = dataWords.length * 4;
					this._process();
					return this._hash;
				},
				clone: function() {
					var clone = Hasher.clone.call(this);
					clone._hash = this._hash.clone();
					return clone;
				}
			});
			/**
			* Shortcut function to the hasher's object interface.
			*
			* @param {WordArray|string} message The message to hash.
			*
			* @return {WordArray} The hash.
			*
			* @static
			*
			* @example
			*
			*     var hash = CryptoJS.SHA1('message');
			*     var hash = CryptoJS.SHA1(wordArray);
			*/
			C.SHA1 = Hasher._createHelper(SHA1);
			/**
			* Shortcut function to the HMAC's object interface.
			*
			* @param {WordArray|string} message The message to hash.
			* @param {WordArray|string} key The secret key.
			*
			* @return {WordArray} The HMAC.
			*
			* @static
			*
			* @example
			*
			*     var hmac = CryptoJS.HmacSHA1(message, key);
			*/
			C.HmacSHA1 = Hasher._createHmacHelper(SHA1);
		})();
		return CryptoJS.SHA1;
	});
}));
//#endregion
//#region node_modules/crypto-js/hmac.js
var require_hmac = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory(require_core());
		else if (typeof define === "function" && define.amd) define(["./core"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function() {
			var C = CryptoJS;
			var Base = C.lib.Base;
			var Utf8 = C.enc.Utf8;
			var C_algo = C.algo;
			C_algo.HMAC = Base.extend({
				/**
				* Initializes a newly created HMAC.
				*
				* @param {Hasher} hasher The hash algorithm to use.
				* @param {WordArray|string} key The secret key.
				*
				* @example
				*
				*     var hmacHasher = CryptoJS.algo.HMAC.create(CryptoJS.algo.SHA256, key);
				*/
				init: function(hasher, key) {
					hasher = this._hasher = new hasher.init();
					if (typeof key == "string") key = Utf8.parse(key);
					var hasherBlockSize = hasher.blockSize;
					var hasherBlockSizeBytes = hasherBlockSize * 4;
					if (key.sigBytes > hasherBlockSizeBytes) key = hasher.finalize(key);
					key.clamp();
					var oKey = this._oKey = key.clone();
					var iKey = this._iKey = key.clone();
					var oKeyWords = oKey.words;
					var iKeyWords = iKey.words;
					for (var i = 0; i < hasherBlockSize; i++) {
						oKeyWords[i] ^= 1549556828;
						iKeyWords[i] ^= 909522486;
					}
					oKey.sigBytes = iKey.sigBytes = hasherBlockSizeBytes;
					this.reset();
				},
				/**
				* Resets this HMAC to its initial state.
				*
				* @example
				*
				*     hmacHasher.reset();
				*/
				reset: function() {
					var hasher = this._hasher;
					hasher.reset();
					hasher.update(this._iKey);
				},
				/**
				* Updates this HMAC with a message.
				*
				* @param {WordArray|string} messageUpdate The message to append.
				*
				* @return {HMAC} This HMAC instance.
				*
				* @example
				*
				*     hmacHasher.update('message');
				*     hmacHasher.update(wordArray);
				*/
				update: function(messageUpdate) {
					this._hasher.update(messageUpdate);
					return this;
				},
				/**
				* Finalizes the HMAC computation.
				* Note that the finalize operation is effectively a destructive, read-once operation.
				*
				* @param {WordArray|string} messageUpdate (Optional) A final message update.
				*
				* @return {WordArray} The HMAC.
				*
				* @example
				*
				*     var hmac = hmacHasher.finalize();
				*     var hmac = hmacHasher.finalize('message');
				*     var hmac = hmacHasher.finalize(wordArray);
				*/
				finalize: function(messageUpdate) {
					var hasher = this._hasher;
					var innerHash = hasher.finalize(messageUpdate);
					hasher.reset();
					return hasher.finalize(this._oKey.clone().concat(innerHash));
				}
			});
		})();
	});
}));
//#endregion
//#region node_modules/crypto-js/evpkdf.js
var require_evpkdf = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory, undef) {
		if (typeof exports === "object") module.exports = exports = factory(require_core(), require_sha1(), require_hmac());
		else if (typeof define === "function" && define.amd) define([
			"./core",
			"./sha1",
			"./hmac"
		], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function() {
			var C = CryptoJS;
			var C_lib = C.lib;
			var Base = C_lib.Base;
			var WordArray = C_lib.WordArray;
			var C_algo = C.algo;
			var MD5 = C_algo.MD5;
			/**
			* This key derivation function is meant to conform with EVP_BytesToKey.
			* www.openssl.org/docs/crypto/EVP_BytesToKey.html
			*/
			var EvpKDF = C_algo.EvpKDF = Base.extend({
				/**
				* Configuration options.
				*
				* @property {number} keySize The key size in words to generate. Default: 4 (128 bits)
				* @property {Hasher} hasher The hash algorithm to use. Default: MD5
				* @property {number} iterations The number of iterations to perform. Default: 1
				*/
				cfg: Base.extend({
					keySize: 4,
					hasher: MD5,
					iterations: 1
				}),
				/**
				* Initializes a newly created key derivation function.
				*
				* @param {Object} cfg (Optional) The configuration options to use for the derivation.
				*
				* @example
				*
				*     var kdf = CryptoJS.algo.EvpKDF.create();
				*     var kdf = CryptoJS.algo.EvpKDF.create({ keySize: 8 });
				*     var kdf = CryptoJS.algo.EvpKDF.create({ keySize: 8, iterations: 1000 });
				*/
				init: function(cfg) {
					this.cfg = this.cfg.extend(cfg);
				},
				/**
				* Derives a key from a password.
				*
				* @param {WordArray|string} password The password.
				* @param {WordArray|string} salt A salt.
				*
				* @return {WordArray} The derived key.
				*
				* @example
				*
				*     var key = kdf.compute(password, salt);
				*/
				compute: function(password, salt) {
					var block;
					var cfg = this.cfg;
					var hasher = cfg.hasher.create();
					var derivedKey = WordArray.create();
					var derivedKeyWords = derivedKey.words;
					var keySize = cfg.keySize;
					var iterations = cfg.iterations;
					while (derivedKeyWords.length < keySize) {
						if (block) hasher.update(block);
						block = hasher.update(password).finalize(salt);
						hasher.reset();
						for (var i = 1; i < iterations; i++) {
							block = hasher.finalize(block);
							hasher.reset();
						}
						derivedKey.concat(block);
					}
					derivedKey.sigBytes = keySize * 4;
					return derivedKey;
				}
			});
			/**
			* Derives a key from a password.
			*
			* @param {WordArray|string} password The password.
			* @param {WordArray|string} salt A salt.
			* @param {Object} cfg (Optional) The configuration options to use for this computation.
			*
			* @return {WordArray} The derived key.
			*
			* @static
			*
			* @example
			*
			*     var key = CryptoJS.EvpKDF(password, salt);
			*     var key = CryptoJS.EvpKDF(password, salt, { keySize: 8 });
			*     var key = CryptoJS.EvpKDF(password, salt, { keySize: 8, iterations: 1000 });
			*/
			C.EvpKDF = function(password, salt, cfg) {
				return EvpKDF.create(cfg).compute(password, salt);
			};
		})();
		return CryptoJS.EvpKDF;
	});
}));
//#endregion
//#region node_modules/crypto-js/cipher-core.js
var require_cipher_core = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory, undef) {
		if (typeof exports === "object") module.exports = exports = factory(require_core(), require_evpkdf());
		else if (typeof define === "function" && define.amd) define(["./core", "./evpkdf"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		/**
		* Cipher core components.
		*/
		CryptoJS.lib.Cipher || function(undefined) {
			var C = CryptoJS;
			var C_lib = C.lib;
			var Base = C_lib.Base;
			var WordArray = C_lib.WordArray;
			var BufferedBlockAlgorithm = C_lib.BufferedBlockAlgorithm;
			var C_enc = C.enc;
			C_enc.Utf8;
			var Base64 = C_enc.Base64;
			var EvpKDF = C.algo.EvpKDF;
			/**
			* Abstract base cipher template.
			*
			* @property {number} keySize This cipher's key size. Default: 4 (128 bits)
			* @property {number} ivSize This cipher's IV size. Default: 4 (128 bits)
			* @property {number} _ENC_XFORM_MODE A constant representing encryption mode.
			* @property {number} _DEC_XFORM_MODE A constant representing decryption mode.
			*/
			var Cipher = C_lib.Cipher = BufferedBlockAlgorithm.extend({
				/**
				* Configuration options.
				*
				* @property {WordArray} iv The IV to use for this operation.
				*/
				cfg: Base.extend(),
				/**
				* Creates this cipher in encryption mode.
				*
				* @param {WordArray} key The key.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {Cipher} A cipher instance.
				*
				* @static
				*
				* @example
				*
				*     var cipher = CryptoJS.algo.AES.createEncryptor(keyWordArray, { iv: ivWordArray });
				*/
				createEncryptor: function(key, cfg) {
					return this.create(this._ENC_XFORM_MODE, key, cfg);
				},
				/**
				* Creates this cipher in decryption mode.
				*
				* @param {WordArray} key The key.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {Cipher} A cipher instance.
				*
				* @static
				*
				* @example
				*
				*     var cipher = CryptoJS.algo.AES.createDecryptor(keyWordArray, { iv: ivWordArray });
				*/
				createDecryptor: function(key, cfg) {
					return this.create(this._DEC_XFORM_MODE, key, cfg);
				},
				/**
				* Initializes a newly created cipher.
				*
				* @param {number} xformMode Either the encryption or decryption transormation mode constant.
				* @param {WordArray} key The key.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @example
				*
				*     var cipher = CryptoJS.algo.AES.create(CryptoJS.algo.AES._ENC_XFORM_MODE, keyWordArray, { iv: ivWordArray });
				*/
				init: function(xformMode, key, cfg) {
					this.cfg = this.cfg.extend(cfg);
					this._xformMode = xformMode;
					this._key = key;
					this.reset();
				},
				/**
				* Resets this cipher to its initial state.
				*
				* @example
				*
				*     cipher.reset();
				*/
				reset: function() {
					BufferedBlockAlgorithm.reset.call(this);
					this._doReset();
				},
				/**
				* Adds data to be encrypted or decrypted.
				*
				* @param {WordArray|string} dataUpdate The data to encrypt or decrypt.
				*
				* @return {WordArray} The data after processing.
				*
				* @example
				*
				*     var encrypted = cipher.process('data');
				*     var encrypted = cipher.process(wordArray);
				*/
				process: function(dataUpdate) {
					this._append(dataUpdate);
					return this._process();
				},
				/**
				* Finalizes the encryption or decryption process.
				* Note that the finalize operation is effectively a destructive, read-once operation.
				*
				* @param {WordArray|string} dataUpdate The final data to encrypt or decrypt.
				*
				* @return {WordArray} The data after final processing.
				*
				* @example
				*
				*     var encrypted = cipher.finalize();
				*     var encrypted = cipher.finalize('data');
				*     var encrypted = cipher.finalize(wordArray);
				*/
				finalize: function(dataUpdate) {
					if (dataUpdate) this._append(dataUpdate);
					return this._doFinalize();
				},
				keySize: 4,
				ivSize: 4,
				_ENC_XFORM_MODE: 1,
				_DEC_XFORM_MODE: 2,
				/**
				* Creates shortcut functions to a cipher's object interface.
				*
				* @param {Cipher} cipher The cipher to create a helper for.
				*
				* @return {Object} An object with encrypt and decrypt shortcut functions.
				*
				* @static
				*
				* @example
				*
				*     var AES = CryptoJS.lib.Cipher._createHelper(CryptoJS.algo.AES);
				*/
				_createHelper: function() {
					function selectCipherStrategy(key) {
						if (typeof key == "string") return PasswordBasedCipher;
						else return SerializableCipher;
					}
					return function(cipher) {
						return {
							encrypt: function(message, key, cfg) {
								return selectCipherStrategy(key).encrypt(cipher, message, key, cfg);
							},
							decrypt: function(ciphertext, key, cfg) {
								return selectCipherStrategy(key).decrypt(cipher, ciphertext, key, cfg);
							}
						};
					};
				}()
			});
			C_lib.StreamCipher = Cipher.extend({
				_doFinalize: function() {
					return this._process(true);
				},
				blockSize: 1
			});
			/**
			* Mode namespace.
			*/
			var C_mode = C.mode = {};
			/**
			* Abstract base block cipher mode template.
			*/
			var BlockCipherMode = C_lib.BlockCipherMode = Base.extend({
				/**
				* Creates this mode for encryption.
				*
				* @param {Cipher} cipher A block cipher instance.
				* @param {Array} iv The IV words.
				*
				* @static
				*
				* @example
				*
				*     var mode = CryptoJS.mode.CBC.createEncryptor(cipher, iv.words);
				*/
				createEncryptor: function(cipher, iv) {
					return this.Encryptor.create(cipher, iv);
				},
				/**
				* Creates this mode for decryption.
				*
				* @param {Cipher} cipher A block cipher instance.
				* @param {Array} iv The IV words.
				*
				* @static
				*
				* @example
				*
				*     var mode = CryptoJS.mode.CBC.createDecryptor(cipher, iv.words);
				*/
				createDecryptor: function(cipher, iv) {
					return this.Decryptor.create(cipher, iv);
				},
				/**
				* Initializes a newly created mode.
				*
				* @param {Cipher} cipher A block cipher instance.
				* @param {Array} iv The IV words.
				*
				* @example
				*
				*     var mode = CryptoJS.mode.CBC.Encryptor.create(cipher, iv.words);
				*/
				init: function(cipher, iv) {
					this._cipher = cipher;
					this._iv = iv;
				}
			});
			/**
			* Cipher Block Chaining mode.
			*/
			var CBC = C_mode.CBC = function() {
				/**
				* Abstract base CBC mode.
				*/
				var CBC = BlockCipherMode.extend();
				/**
				* CBC encryptor.
				*/
				CBC.Encryptor = CBC.extend({ 
				/**
				* Processes the data block at offset.
				*
				* @param {Array} words The data words to operate on.
				* @param {number} offset The offset where the block starts.
				*
				* @example
				*
				*     mode.processBlock(data.words, offset);
				*/
processBlock: function(words, offset) {
					var cipher = this._cipher;
					var blockSize = cipher.blockSize;
					xorBlock.call(this, words, offset, blockSize);
					cipher.encryptBlock(words, offset);
					this._prevBlock = words.slice(offset, offset + blockSize);
				} });
				/**
				* CBC decryptor.
				*/
				CBC.Decryptor = CBC.extend({ 
				/**
				* Processes the data block at offset.
				*
				* @param {Array} words The data words to operate on.
				* @param {number} offset The offset where the block starts.
				*
				* @example
				*
				*     mode.processBlock(data.words, offset);
				*/
processBlock: function(words, offset) {
					var cipher = this._cipher;
					var blockSize = cipher.blockSize;
					var thisBlock = words.slice(offset, offset + blockSize);
					cipher.decryptBlock(words, offset);
					xorBlock.call(this, words, offset, blockSize);
					this._prevBlock = thisBlock;
				} });
				function xorBlock(words, offset, blockSize) {
					var block;
					var iv = this._iv;
					if (iv) {
						block = iv;
						this._iv = undefined;
					} else block = this._prevBlock;
					for (var i = 0; i < blockSize; i++) words[offset + i] ^= block[i];
				}
				return CBC;
			}();
			/**
			* Padding namespace.
			*/
			var C_pad = C.pad = {};
			/**
			* PKCS #5/7 padding strategy.
			*/
			var Pkcs7 = C_pad.Pkcs7 = {
				/**
				* Pads data using the algorithm defined in PKCS #5/7.
				*
				* @param {WordArray} data The data to pad.
				* @param {number} blockSize The multiple that the data should be padded to.
				*
				* @static
				*
				* @example
				*
				*     CryptoJS.pad.Pkcs7.pad(wordArray, 4);
				*/
				pad: function(data, blockSize) {
					var blockSizeBytes = blockSize * 4;
					var nPaddingBytes = blockSizeBytes - data.sigBytes % blockSizeBytes;
					var paddingWord = nPaddingBytes << 24 | nPaddingBytes << 16 | nPaddingBytes << 8 | nPaddingBytes;
					var paddingWords = [];
					for (var i = 0; i < nPaddingBytes; i += 4) paddingWords.push(paddingWord);
					var padding = WordArray.create(paddingWords, nPaddingBytes);
					data.concat(padding);
				},
				/**
				* Unpads data that had been padded using the algorithm defined in PKCS #5/7.
				*
				* @param {WordArray} data The data to unpad.
				*
				* @static
				*
				* @example
				*
				*     CryptoJS.pad.Pkcs7.unpad(wordArray);
				*/
				unpad: function(data) {
					var nPaddingBytes = data.words[data.sigBytes - 1 >>> 2] & 255;
					data.sigBytes -= nPaddingBytes;
				}
			};
			C_lib.BlockCipher = Cipher.extend({
				/**
				* Configuration options.
				*
				* @property {Mode} mode The block mode to use. Default: CBC
				* @property {Padding} padding The padding strategy to use. Default: Pkcs7
				*/
				cfg: Cipher.cfg.extend({
					mode: CBC,
					padding: Pkcs7
				}),
				reset: function() {
					var modeCreator;
					Cipher.reset.call(this);
					var cfg = this.cfg;
					var iv = cfg.iv;
					var mode = cfg.mode;
					if (this._xformMode == this._ENC_XFORM_MODE) modeCreator = mode.createEncryptor;
					else {
						modeCreator = mode.createDecryptor;
						this._minBufferSize = 1;
					}
					if (this._mode && this._mode.__creator == modeCreator) this._mode.init(this, iv && iv.words);
					else {
						this._mode = modeCreator.call(mode, this, iv && iv.words);
						this._mode.__creator = modeCreator;
					}
				},
				_doProcessBlock: function(words, offset) {
					this._mode.processBlock(words, offset);
				},
				_doFinalize: function() {
					var finalProcessedBlocks;
					var padding = this.cfg.padding;
					if (this._xformMode == this._ENC_XFORM_MODE) {
						padding.pad(this._data, this.blockSize);
						finalProcessedBlocks = this._process(true);
					} else {
						finalProcessedBlocks = this._process(true);
						padding.unpad(finalProcessedBlocks);
					}
					return finalProcessedBlocks;
				},
				blockSize: 4
			});
			/**
			* A collection of cipher parameters.
			*
			* @property {WordArray} ciphertext The raw ciphertext.
			* @property {WordArray} key The key to this ciphertext.
			* @property {WordArray} iv The IV used in the ciphering operation.
			* @property {WordArray} salt The salt used with a key derivation function.
			* @property {Cipher} algorithm The cipher algorithm.
			* @property {Mode} mode The block mode used in the ciphering operation.
			* @property {Padding} padding The padding scheme used in the ciphering operation.
			* @property {number} blockSize The block size of the cipher.
			* @property {Format} formatter The default formatting strategy to convert this cipher params object to a string.
			*/
			var CipherParams = C_lib.CipherParams = Base.extend({
				/**
				* Initializes a newly created cipher params object.
				*
				* @param {Object} cipherParams An object with any of the possible cipher parameters.
				*
				* @example
				*
				*     var cipherParams = CryptoJS.lib.CipherParams.create({
				*         ciphertext: ciphertextWordArray,
				*         key: keyWordArray,
				*         iv: ivWordArray,
				*         salt: saltWordArray,
				*         algorithm: CryptoJS.algo.AES,
				*         mode: CryptoJS.mode.CBC,
				*         padding: CryptoJS.pad.PKCS7,
				*         blockSize: 4,
				*         formatter: CryptoJS.format.OpenSSL
				*     });
				*/
				init: function(cipherParams) {
					this.mixIn(cipherParams);
				},
				/**
				* Converts this cipher params object to a string.
				*
				* @param {Format} formatter (Optional) The formatting strategy to use.
				*
				* @return {string} The stringified cipher params.
				*
				* @throws Error If neither the formatter nor the default formatter is set.
				*
				* @example
				*
				*     var string = cipherParams + '';
				*     var string = cipherParams.toString();
				*     var string = cipherParams.toString(CryptoJS.format.OpenSSL);
				*/
				toString: function(formatter) {
					return (formatter || this.formatter).stringify(this);
				}
			});
			/**
			* Format namespace.
			*/
			var C_format = C.format = {};
			/**
			* OpenSSL formatting strategy.
			*/
			var OpenSSLFormatter = C_format.OpenSSL = {
				/**
				* Converts a cipher params object to an OpenSSL-compatible string.
				*
				* @param {CipherParams} cipherParams The cipher params object.
				*
				* @return {string} The OpenSSL-compatible string.
				*
				* @static
				*
				* @example
				*
				*     var openSSLString = CryptoJS.format.OpenSSL.stringify(cipherParams);
				*/
				stringify: function(cipherParams) {
					var wordArray;
					var ciphertext = cipherParams.ciphertext;
					var salt = cipherParams.salt;
					if (salt) wordArray = WordArray.create([1398893684, 1701076831]).concat(salt).concat(ciphertext);
					else wordArray = ciphertext;
					return wordArray.toString(Base64);
				},
				/**
				* Converts an OpenSSL-compatible string to a cipher params object.
				*
				* @param {string} openSSLStr The OpenSSL-compatible string.
				*
				* @return {CipherParams} The cipher params object.
				*
				* @static
				*
				* @example
				*
				*     var cipherParams = CryptoJS.format.OpenSSL.parse(openSSLString);
				*/
				parse: function(openSSLStr) {
					var salt;
					var ciphertext = Base64.parse(openSSLStr);
					var ciphertextWords = ciphertext.words;
					if (ciphertextWords[0] == 1398893684 && ciphertextWords[1] == 1701076831) {
						salt = WordArray.create(ciphertextWords.slice(2, 4));
						ciphertextWords.splice(0, 4);
						ciphertext.sigBytes -= 16;
					}
					return CipherParams.create({
						ciphertext,
						salt
					});
				}
			};
			/**
			* A cipher wrapper that returns ciphertext as a serializable cipher params object.
			*/
			var SerializableCipher = C_lib.SerializableCipher = Base.extend({
				/**
				* Configuration options.
				*
				* @property {Formatter} format The formatting strategy to convert cipher param objects to and from a string. Default: OpenSSL
				*/
				cfg: Base.extend({ format: OpenSSLFormatter }),
				/**
				* Encrypts a message.
				*
				* @param {Cipher} cipher The cipher algorithm to use.
				* @param {WordArray|string} message The message to encrypt.
				* @param {WordArray} key The key.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {CipherParams} A cipher params object.
				*
				* @static
				*
				* @example
				*
				*     var ciphertextParams = CryptoJS.lib.SerializableCipher.encrypt(CryptoJS.algo.AES, message, key);
				*     var ciphertextParams = CryptoJS.lib.SerializableCipher.encrypt(CryptoJS.algo.AES, message, key, { iv: iv });
				*     var ciphertextParams = CryptoJS.lib.SerializableCipher.encrypt(CryptoJS.algo.AES, message, key, { iv: iv, format: CryptoJS.format.OpenSSL });
				*/
				encrypt: function(cipher, message, key, cfg) {
					cfg = this.cfg.extend(cfg);
					var encryptor = cipher.createEncryptor(key, cfg);
					var ciphertext = encryptor.finalize(message);
					var cipherCfg = encryptor.cfg;
					return CipherParams.create({
						ciphertext,
						key,
						iv: cipherCfg.iv,
						algorithm: cipher,
						mode: cipherCfg.mode,
						padding: cipherCfg.padding,
						blockSize: cipher.blockSize,
						formatter: cfg.format
					});
				},
				/**
				* Decrypts serialized ciphertext.
				*
				* @param {Cipher} cipher The cipher algorithm to use.
				* @param {CipherParams|string} ciphertext The ciphertext to decrypt.
				* @param {WordArray} key The key.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {WordArray} The plaintext.
				*
				* @static
				*
				* @example
				*
				*     var plaintext = CryptoJS.lib.SerializableCipher.decrypt(CryptoJS.algo.AES, formattedCiphertext, key, { iv: iv, format: CryptoJS.format.OpenSSL });
				*     var plaintext = CryptoJS.lib.SerializableCipher.decrypt(CryptoJS.algo.AES, ciphertextParams, key, { iv: iv, format: CryptoJS.format.OpenSSL });
				*/
				decrypt: function(cipher, ciphertext, key, cfg) {
					cfg = this.cfg.extend(cfg);
					ciphertext = this._parse(ciphertext, cfg.format);
					return cipher.createDecryptor(key, cfg).finalize(ciphertext.ciphertext);
				},
				/**
				* Converts serialized ciphertext to CipherParams,
				* else assumed CipherParams already and returns ciphertext unchanged.
				*
				* @param {CipherParams|string} ciphertext The ciphertext.
				* @param {Formatter} format The formatting strategy to use to parse serialized ciphertext.
				*
				* @return {CipherParams} The unserialized ciphertext.
				*
				* @static
				*
				* @example
				*
				*     var ciphertextParams = CryptoJS.lib.SerializableCipher._parse(ciphertextStringOrParams, format);
				*/
				_parse: function(ciphertext, format) {
					if (typeof ciphertext == "string") return format.parse(ciphertext, this);
					else return ciphertext;
				}
			});
			/**
			* Key derivation function namespace.
			*/
			var C_kdf = C.kdf = {};
			/**
			* OpenSSL key derivation function.
			*/
			var OpenSSLKdf = C_kdf.OpenSSL = { 
			/**
			* Derives a key and IV from a password.
			*
			* @param {string} password The password to derive from.
			* @param {number} keySize The size in words of the key to generate.
			* @param {number} ivSize The size in words of the IV to generate.
			* @param {WordArray|string} salt (Optional) A 64-bit salt to use. If omitted, a salt will be generated randomly.
			*
			* @return {CipherParams} A cipher params object with the key, IV, and salt.
			*
			* @static
			*
			* @example
			*
			*     var derivedParams = CryptoJS.kdf.OpenSSL.execute('Password', 256/32, 128/32);
			*     var derivedParams = CryptoJS.kdf.OpenSSL.execute('Password', 256/32, 128/32, 'saltsalt');
			*/
execute: function(password, keySize, ivSize, salt, hasher) {
				if (!salt) salt = WordArray.random(8);
				if (!hasher) var key = EvpKDF.create({ keySize: keySize + ivSize }).compute(password, salt);
				else var key = EvpKDF.create({
					keySize: keySize + ivSize,
					hasher
				}).compute(password, salt);
				var iv = WordArray.create(key.words.slice(keySize), ivSize * 4);
				key.sigBytes = keySize * 4;
				return CipherParams.create({
					key,
					iv,
					salt
				});
			} };
			/**
			* A serializable cipher wrapper that derives the key from a password,
			* and returns ciphertext as a serializable cipher params object.
			*/
			var PasswordBasedCipher = C_lib.PasswordBasedCipher = SerializableCipher.extend({
				/**
				* Configuration options.
				*
				* @property {KDF} kdf The key derivation function to use to generate a key and IV from a password. Default: OpenSSL
				*/
				cfg: SerializableCipher.cfg.extend({ kdf: OpenSSLKdf }),
				/**
				* Encrypts a message using a password.
				*
				* @param {Cipher} cipher The cipher algorithm to use.
				* @param {WordArray|string} message The message to encrypt.
				* @param {string} password The password.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {CipherParams} A cipher params object.
				*
				* @static
				*
				* @example
				*
				*     var ciphertextParams = CryptoJS.lib.PasswordBasedCipher.encrypt(CryptoJS.algo.AES, message, 'password');
				*     var ciphertextParams = CryptoJS.lib.PasswordBasedCipher.encrypt(CryptoJS.algo.AES, message, 'password', { format: CryptoJS.format.OpenSSL });
				*/
				encrypt: function(cipher, message, password, cfg) {
					cfg = this.cfg.extend(cfg);
					var derivedParams = cfg.kdf.execute(password, cipher.keySize, cipher.ivSize, cfg.salt, cfg.hasher);
					cfg.iv = derivedParams.iv;
					var ciphertext = SerializableCipher.encrypt.call(this, cipher, message, derivedParams.key, cfg);
					ciphertext.mixIn(derivedParams);
					return ciphertext;
				},
				/**
				* Decrypts serialized ciphertext using a password.
				*
				* @param {Cipher} cipher The cipher algorithm to use.
				* @param {CipherParams|string} ciphertext The ciphertext to decrypt.
				* @param {string} password The password.
				* @param {Object} cfg (Optional) The configuration options to use for this operation.
				*
				* @return {WordArray} The plaintext.
				*
				* @static
				*
				* @example
				*
				*     var plaintext = CryptoJS.lib.PasswordBasedCipher.decrypt(CryptoJS.algo.AES, formattedCiphertext, 'password', { format: CryptoJS.format.OpenSSL });
				*     var plaintext = CryptoJS.lib.PasswordBasedCipher.decrypt(CryptoJS.algo.AES, ciphertextParams, 'password', { format: CryptoJS.format.OpenSSL });
				*/
				decrypt: function(cipher, ciphertext, password, cfg) {
					cfg = this.cfg.extend(cfg);
					ciphertext = this._parse(ciphertext, cfg.format);
					var derivedParams = cfg.kdf.execute(password, cipher.keySize, cipher.ivSize, ciphertext.salt, cfg.hasher);
					cfg.iv = derivedParams.iv;
					return SerializableCipher.decrypt.call(this, cipher, ciphertext, derivedParams.key, cfg);
				}
			});
		}();
	});
}));
//#endregion
//#region node_modules/crypto-js/aes.js
var require_aes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory, undef) {
		if (typeof exports === "object") module.exports = exports = factory(require_core(), require_enc_base64(), require_md5(), require_evpkdf(), require_cipher_core());
		else if (typeof define === "function" && define.amd) define([
			"./core",
			"./enc-base64",
			"./md5",
			"./evpkdf",
			"./cipher-core"
		], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		(function() {
			var C = CryptoJS;
			var BlockCipher = C.lib.BlockCipher;
			var C_algo = C.algo;
			var SBOX = [];
			var INV_SBOX = [];
			var SUB_MIX_0 = [];
			var SUB_MIX_1 = [];
			var SUB_MIX_2 = [];
			var SUB_MIX_3 = [];
			var INV_SUB_MIX_0 = [];
			var INV_SUB_MIX_1 = [];
			var INV_SUB_MIX_2 = [];
			var INV_SUB_MIX_3 = [];
			(function() {
				var d = [];
				for (var i = 0; i < 256; i++) if (i < 128) d[i] = i << 1;
				else d[i] = i << 1 ^ 283;
				var x = 0;
				var xi = 0;
				for (var i = 0; i < 256; i++) {
					var sx = xi ^ xi << 1 ^ xi << 2 ^ xi << 3 ^ xi << 4;
					sx = sx >>> 8 ^ sx & 255 ^ 99;
					SBOX[x] = sx;
					INV_SBOX[sx] = x;
					var x2 = d[x];
					var x4 = d[x2];
					var x8 = d[x4];
					var t = d[sx] * 257 ^ sx * 16843008;
					SUB_MIX_0[x] = t << 24 | t >>> 8;
					SUB_MIX_1[x] = t << 16 | t >>> 16;
					SUB_MIX_2[x] = t << 8 | t >>> 24;
					SUB_MIX_3[x] = t;
					var t = x8 * 16843009 ^ x4 * 65537 ^ x2 * 257 ^ x * 16843008;
					INV_SUB_MIX_0[sx] = t << 24 | t >>> 8;
					INV_SUB_MIX_1[sx] = t << 16 | t >>> 16;
					INV_SUB_MIX_2[sx] = t << 8 | t >>> 24;
					INV_SUB_MIX_3[sx] = t;
					if (!x) x = xi = 1;
					else {
						x = x2 ^ d[d[d[x8 ^ x2]]];
						xi ^= d[d[xi]];
					}
				}
			})();
			var RCON = [
				0,
				1,
				2,
				4,
				8,
				16,
				32,
				64,
				128,
				27,
				54
			];
			/**
			* AES block cipher algorithm.
			*/
			var AES = C_algo.AES = BlockCipher.extend({
				_doReset: function() {
					var t;
					if (this._nRounds && this._keyPriorReset === this._key) return;
					var key = this._keyPriorReset = this._key;
					var keyWords = key.words;
					var keySize = key.sigBytes / 4;
					var ksRows = ((this._nRounds = keySize + 6) + 1) * 4;
					var keySchedule = this._keySchedule = [];
					for (var ksRow = 0; ksRow < ksRows; ksRow++) if (ksRow < keySize) keySchedule[ksRow] = keyWords[ksRow];
					else {
						t = keySchedule[ksRow - 1];
						if (!(ksRow % keySize)) {
							t = t << 8 | t >>> 24;
							t = SBOX[t >>> 24] << 24 | SBOX[t >>> 16 & 255] << 16 | SBOX[t >>> 8 & 255] << 8 | SBOX[t & 255];
							t ^= RCON[ksRow / keySize | 0] << 24;
						} else if (keySize > 6 && ksRow % keySize == 4) t = SBOX[t >>> 24] << 24 | SBOX[t >>> 16 & 255] << 16 | SBOX[t >>> 8 & 255] << 8 | SBOX[t & 255];
						keySchedule[ksRow] = keySchedule[ksRow - keySize] ^ t;
					}
					var invKeySchedule = this._invKeySchedule = [];
					for (var invKsRow = 0; invKsRow < ksRows; invKsRow++) {
						var ksRow = ksRows - invKsRow;
						if (invKsRow % 4) var t = keySchedule[ksRow];
						else var t = keySchedule[ksRow - 4];
						if (invKsRow < 4 || ksRow <= 4) invKeySchedule[invKsRow] = t;
						else invKeySchedule[invKsRow] = INV_SUB_MIX_0[SBOX[t >>> 24]] ^ INV_SUB_MIX_1[SBOX[t >>> 16 & 255]] ^ INV_SUB_MIX_2[SBOX[t >>> 8 & 255]] ^ INV_SUB_MIX_3[SBOX[t & 255]];
					}
				},
				encryptBlock: function(M, offset) {
					this._doCryptBlock(M, offset, this._keySchedule, SUB_MIX_0, SUB_MIX_1, SUB_MIX_2, SUB_MIX_3, SBOX);
				},
				decryptBlock: function(M, offset) {
					var t = M[offset + 1];
					M[offset + 1] = M[offset + 3];
					M[offset + 3] = t;
					this._doCryptBlock(M, offset, this._invKeySchedule, INV_SUB_MIX_0, INV_SUB_MIX_1, INV_SUB_MIX_2, INV_SUB_MIX_3, INV_SBOX);
					var t = M[offset + 1];
					M[offset + 1] = M[offset + 3];
					M[offset + 3] = t;
				},
				_doCryptBlock: function(M, offset, keySchedule, SUB_MIX_0, SUB_MIX_1, SUB_MIX_2, SUB_MIX_3, SBOX) {
					var nRounds = this._nRounds;
					var s0 = M[offset] ^ keySchedule[0];
					var s1 = M[offset + 1] ^ keySchedule[1];
					var s2 = M[offset + 2] ^ keySchedule[2];
					var s3 = M[offset + 3] ^ keySchedule[3];
					var ksRow = 4;
					for (var round = 1; round < nRounds; round++) {
						var t0 = SUB_MIX_0[s0 >>> 24] ^ SUB_MIX_1[s1 >>> 16 & 255] ^ SUB_MIX_2[s2 >>> 8 & 255] ^ SUB_MIX_3[s3 & 255] ^ keySchedule[ksRow++];
						var t1 = SUB_MIX_0[s1 >>> 24] ^ SUB_MIX_1[s2 >>> 16 & 255] ^ SUB_MIX_2[s3 >>> 8 & 255] ^ SUB_MIX_3[s0 & 255] ^ keySchedule[ksRow++];
						var t2 = SUB_MIX_0[s2 >>> 24] ^ SUB_MIX_1[s3 >>> 16 & 255] ^ SUB_MIX_2[s0 >>> 8 & 255] ^ SUB_MIX_3[s1 & 255] ^ keySchedule[ksRow++];
						var t3 = SUB_MIX_0[s3 >>> 24] ^ SUB_MIX_1[s0 >>> 16 & 255] ^ SUB_MIX_2[s1 >>> 8 & 255] ^ SUB_MIX_3[s2 & 255] ^ keySchedule[ksRow++];
						s0 = t0;
						s1 = t1;
						s2 = t2;
						s3 = t3;
					}
					var t0 = (SBOX[s0 >>> 24] << 24 | SBOX[s1 >>> 16 & 255] << 16 | SBOX[s2 >>> 8 & 255] << 8 | SBOX[s3 & 255]) ^ keySchedule[ksRow++];
					var t1 = (SBOX[s1 >>> 24] << 24 | SBOX[s2 >>> 16 & 255] << 16 | SBOX[s3 >>> 8 & 255] << 8 | SBOX[s0 & 255]) ^ keySchedule[ksRow++];
					var t2 = (SBOX[s2 >>> 24] << 24 | SBOX[s3 >>> 16 & 255] << 16 | SBOX[s0 >>> 8 & 255] << 8 | SBOX[s1 & 255]) ^ keySchedule[ksRow++];
					var t3 = (SBOX[s3 >>> 24] << 24 | SBOX[s0 >>> 16 & 255] << 16 | SBOX[s1 >>> 8 & 255] << 8 | SBOX[s2 & 255]) ^ keySchedule[ksRow++];
					M[offset] = t0;
					M[offset + 1] = t1;
					M[offset + 2] = t2;
					M[offset + 3] = t3;
				},
				keySize: 8
			});
			/**
			* Shortcut functions to the cipher's object interface.
			*
			* @example
			*
			*     var ciphertext = CryptoJS.AES.encrypt(message, key, cfg);
			*     var plaintext  = CryptoJS.AES.decrypt(ciphertext, key, cfg);
			*/
			C.AES = BlockCipher._createHelper(AES);
		})();
		return CryptoJS.AES;
	});
}));
//#endregion
//#region node_modules/crypto-js/enc-utf8.js
var require_enc_utf8 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function(root, factory) {
		if (typeof exports === "object") module.exports = exports = factory(require_core());
		else if (typeof define === "function" && define.amd) define(["./core"], factory);
		else factory(root.CryptoJS);
	})(exports, function(CryptoJS) {
		return CryptoJS.enc.Utf8;
	});
}));
//#endregion
//#region node_modules/parse/lib/browser/CryptoController.js
var require_CryptoController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var AES;
	var ENC;
	AES = require_aes();
	ENC = require_enc_utf8();
	exports.default = {
		encrypt(obj, secretKey) {
			return AES.encrypt((0, _stringify.default)(obj), secretKey).toString();
		},
		decrypt(encryptedText, secretKey) {
			return AES.decrypt(encryptedText, secretKey).toString(ENC);
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/delete-property-or-throw.js
var require_delete_property_or_throw = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var tryToString = require_try_to_string();
	var $TypeError = TypeError;
	module.exports = function(O, P) {
		if (!delete O[P]) throw new $TypeError("Cannot delete property " + tryToString(P) + " of " + tryToString(O));
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.splice.js
var require_es_array_splice = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var toObject = require_to_object();
	var toAbsoluteIndex = require_to_absolute_index();
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var lengthOfArrayLike = require_length_of_array_like();
	var setArrayLength = require_array_set_length();
	var doesNotExceedSafeInteger = require_does_not_exceed_safe_integer();
	var arraySpeciesCreate = require_array_species_create();
	var createProperty = require_create_property();
	var deletePropertyOrThrow = require_delete_property_or_throw();
	var HAS_SPECIES_SUPPORT = require_array_method_has_species_support()("splice");
	var max = Math.max;
	var min = Math.min;
	$({
		target: "Array",
		proto: true,
		forced: !HAS_SPECIES_SUPPORT
	}, { splice: function splice(start, deleteCount) {
		var O = toObject(this);
		var len = lengthOfArrayLike(O);
		var actualStart = toAbsoluteIndex(start, len);
		var argumentsLength = arguments.length;
		var insertCount, actualDeleteCount, A, k, from, to;
		if (argumentsLength === 0) insertCount = actualDeleteCount = 0;
		else if (argumentsLength === 1) {
			insertCount = 0;
			actualDeleteCount = len - actualStart;
		} else {
			insertCount = argumentsLength - 2;
			actualDeleteCount = min(max(toIntegerOrInfinity(deleteCount), 0), len - actualStart);
		}
		doesNotExceedSafeInteger(len + insertCount - actualDeleteCount);
		A = arraySpeciesCreate(O, actualDeleteCount);
		for (k = 0; k < actualDeleteCount; k++) {
			from = actualStart + k;
			if (from in O) createProperty(A, k, O[from]);
		}
		setArrayLength(A, actualDeleteCount);
		if (insertCount < actualDeleteCount) {
			for (k = actualStart; k < len - actualDeleteCount; k++) {
				from = k + actualDeleteCount;
				to = k + insertCount;
				if (from in O) O[to] = O[from];
				else deletePropertyOrThrow(O, to);
			}
			for (k = len; k > len - actualDeleteCount + insertCount; k--) deletePropertyOrThrow(O, k - 1);
		} else if (insertCount > actualDeleteCount) for (k = len - actualDeleteCount; k > actualStart; k--) {
			from = k + actualDeleteCount - 1;
			to = k + insertCount - 1;
			if (from in O) O[to] = O[from];
			else deletePropertyOrThrow(O, to);
		}
		for (k = 0; k < insertCount; k++) O[k + actualStart] = arguments[k + 2];
		setArrayLength(O, len - actualDeleteCount + insertCount);
		return A;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/splice.js
var require_splice$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_splice();
	module.exports = require_get_built_in_prototype_method()("Array", "splice");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/splice.js
var require_splice$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_splice$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.splice;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.splice ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/splice.js
var require_splice$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_splice$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/splice.js
var require_splice = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_splice$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.find-index.js
var require_es_array_find_index = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $findIndex = require_array_iteration().findIndex;
	var addToUnscopables = require_add_to_unscopables();
	var FIND_INDEX = "findIndex";
	var SKIPS_HOLES = true;
	if (FIND_INDEX in []) Array(1)[FIND_INDEX](function() {
		SKIPS_HOLES = false;
	});
	$({
		target: "Array",
		proto: true,
		forced: SKIPS_HOLES
	}, { findIndex: function findIndex(callbackfn) {
		return $findIndex(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
	addToUnscopables(FIND_INDEX);
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/find-index.js
var require_find_index$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_find_index();
	module.exports = require_get_built_in_prototype_method()("Array", "findIndex");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/find-index.js
var require_find_index$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_find_index$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.findIndex;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.findIndex ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/find-index.js
var require_find_index$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_find_index$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/find-index.js
var require_find_index = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_find_index$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/schedulers-fix.js
var require_schedulers_fix = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var apply = require_function_apply();
	var isCallable = require_is_callable();
	var ENVIRONMENT = require_environment();
	var USER_AGENT = require_environment_user_agent();
	var arraySlice = require_array_slice();
	var validateArgumentsLength = require_validate_arguments_length();
	var Function = globalThis.Function;
	var WRAP = /MSIE .\./.test(USER_AGENT) || ENVIRONMENT === "BUN" && (function() {
		var version = globalThis.Bun.version.split(".");
		return version.length < 3 || version[0] === "0" && (version[1] < 3 || version[1] === "3" && version[2] === "0");
	})();
	module.exports = function(scheduler, hasTimeArg) {
		var firstParamIndex = hasTimeArg ? 2 : 1;
		return WRAP ? function(handler, timeout) {
			var boundArgs = validateArgumentsLength(arguments.length, 1) > firstParamIndex;
			var fn = isCallable(handler) ? handler : Function(handler);
			var params = boundArgs ? arraySlice(arguments, firstParamIndex) : [];
			var callback = boundArgs ? function() {
				apply(fn, this, params);
			} : fn;
			return hasTimeArg ? scheduler(callback, timeout) : scheduler(callback);
		} : scheduler;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/web.set-interval.js
var require_web_set_interval = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	var setInterval = require_schedulers_fix()(globalThis.setInterval, true);
	$({
		global: true,
		bind: true,
		forced: globalThis.setInterval !== setInterval
	}, { setInterval });
}));
//#endregion
//#region node_modules/core-js-pure/modules/web.set-timeout.js
var require_web_set_timeout = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	var setTimeout = require_schedulers_fix()(globalThis.setTimeout, true);
	$({
		global: true,
		bind: true,
		forced: globalThis.setTimeout !== setTimeout
	}, { setTimeout });
}));
//#endregion
//#region node_modules/core-js-pure/modules/web.timers.js
var require_web_timers = /* @__PURE__ */ __commonJSMin((() => {
	require_web_set_interval();
	require_web_set_timeout();
}));
//#endregion
//#region node_modules/core-js-pure/stable/set-interval.js
var require_set_interval$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_web_timers();
	module.exports = require_path().setInterval;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/set-interval.js
var require_set_interval = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_set_interval$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.find.js
var require_es_array_find = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $find = require_array_iteration().find;
	var addToUnscopables = require_add_to_unscopables();
	var FIND = "find";
	var SKIPS_HOLES = true;
	if (FIND in []) Array(1)[FIND](function() {
		SKIPS_HOLES = false;
	});
	$({
		target: "Array",
		proto: true,
		forced: SKIPS_HOLES
	}, { find: function find(callbackfn) {
		return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
	addToUnscopables(FIND);
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/find.js
var require_find$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_find();
	module.exports = require_get_built_in_prototype_method()("Array", "find");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/find.js
var require_find$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_find$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.find;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.find ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/find.js
var require_find$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_find$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/find.js
var require_find = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_find$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.create.js
var require_es_object_create = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var DESCRIPTORS = require_descriptors();
	var create = require_object_create();
	$({
		target: "Object",
		stat: true,
		sham: !DESCRIPTORS
	}, { create });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/create.js
var require_create$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_create();
	var Object = require_path().Object;
	module.exports = function create(P, D) {
		return Object.create(P, D);
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/create.js
var require_create$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_create$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/create.js
var require_create = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_create$1();
}));
//#endregion
//#region node_modules/core-js-pure/es/symbol/for.js
var require_for$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_symbol();
	module.exports = require_path().Symbol["for"];
}));
//#endregion
//#region node_modules/core-js-pure/stable/symbol/for.js
var require_for$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_for$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/symbol/for.js
var require_for = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_for$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.freeze.js
var require_es_object_freeze = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var FREEZING = require_freezing();
	var fails = require_fails();
	var isObject = require_is_object();
	var onFreeze = require_internal_metadata().onFreeze;
	var $freeze = Object.freeze;
	$({
		target: "Object",
		stat: true,
		forced: fails(function() {
			$freeze(1);
		}),
		sham: !FREEZING
	}, { freeze: function freeze(it) {
		return $freeze && isObject(it) ? $freeze(onFreeze(it)) : it;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/freeze.js
var require_freeze$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_freeze();
	module.exports = require_path().Object.freeze;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/freeze.js
var require_freeze$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_freeze$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/freeze.js
var require_freeze = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_freeze$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.get-prototype-of.js
var require_es_object_get_prototype_of = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var fails = require_fails();
	var toObject = require_to_object();
	var nativeGetPrototypeOf = require_object_get_prototype_of();
	var CORRECT_PROTOTYPE_GETTER = require_correct_prototype_getter();
	$({
		target: "Object",
		stat: true,
		forced: fails(function() {
			nativeGetPrototypeOf(1);
		}),
		sham: !CORRECT_PROTOTYPE_GETTER
	}, { getPrototypeOf: function getPrototypeOf(it) {
		return nativeGetPrototypeOf(toObject(it));
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/get-prototype-of.js
var require_get_prototype_of$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_get_prototype_of();
	module.exports = require_path().Object.getPrototypeOf;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/get-prototype-of.js
var require_get_prototype_of$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_prototype_of$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/get-prototype-of.js
var require_get_prototype_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_get_prototype_of$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/canBeSerialized.js
var require_canBeSerialized = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = canBeSerialized;
	var _isArray = _interopRequireDefault(require_is_array());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	function canBeSerialized(obj) {
		if (!(obj instanceof _CoreManager.default.getParseObject())) return true;
		const attributes = obj.attributes;
		for (const attr in attributes) {
			const val = attributes[attr];
			if (!canBeSerializedHelper(val)) return false;
		}
		return true;
	}
	function canBeSerializedHelper(value) {
		if (typeof value !== "object") return true;
		if (value instanceof _ParseRelation.default) return true;
		if (value instanceof _CoreManager.default.getParseObject()) return !!value.id;
		if (value instanceof _ParseFile.default) {
			if (value.url()) return true;
			return false;
		}
		if ((0, _isArray.default)(value)) {
			for (let i = 0; i < value.length; i++) if (!canBeSerializedHelper(value[i])) return false;
			return true;
		}
		for (const k in value) if (!canBeSerializedHelper(value[k])) return false;
		return true;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/escape.js
var require_escape = /* @__PURE__ */ __commonJSMin(((exports) => {
	require_define_property$3()(exports, "__esModule", { value: true });
	exports.default = escape;
	var encoded = {
		"&": "&amp;",
		"<": "&lt;",
		">": "&gt;",
		"/": "&#x2F;",
		"'": "&#x27;",
		"\"": "&quot;"
	};
	function escape(str) {
		return str.replace(/[&<>\/'"]/g, function(char) {
			return encoded[char];
		});
	}
}));
//#endregion
//#region node_modules/core-js-pure/internals/whitespaces.js
var require_whitespaces = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = "	\n\v\f\r \xA0              　\u2028\u2029﻿";
}));
//#endregion
//#region node_modules/core-js-pure/internals/string-trim.js
var require_string_trim = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var requireObjectCoercible = require_require_object_coercible();
	var toString = require_to_string();
	var whitespaces = require_whitespaces();
	var replace = uncurryThis("".replace);
	var ltrim = RegExp("^[" + whitespaces + "]+");
	var rtrim = RegExp("(^|[^" + whitespaces + "])[" + whitespaces + "]+$");
	var createMethod = function(TYPE) {
		return function($this) {
			var string = toString(requireObjectCoercible($this));
			if (TYPE & 1) string = replace(string, ltrim, "");
			if (TYPE & 2) string = replace(string, rtrim, "$1");
			return string;
		};
	};
	module.exports = {
		start: createMethod(1),
		end: createMethod(2),
		trim: createMethod(3)
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/number-parse-int.js
var require_number_parse_int = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var globalThis = require_global_this$6();
	var fails = require_fails();
	var uncurryThis = require_function_uncurry_this();
	var toString = require_to_string();
	var trim = require_string_trim().trim;
	var whitespaces = require_whitespaces();
	var $parseInt = globalThis.parseInt;
	var Symbol = globalThis.Symbol;
	var ITERATOR = Symbol && Symbol.iterator;
	var hex = /^[+-]?0x/i;
	var exec = uncurryThis(hex.exec);
	module.exports = $parseInt(whitespaces + "08") !== 8 || $parseInt(whitespaces + "0x16") !== 22 || ITERATOR && !fails(function() {
		$parseInt(Object(ITERATOR));
	}) ? function parseInt(string, radix) {
		var S = trim(toString(string));
		return $parseInt(S, radix >>> 0 || (exec(hex, S) ? 16 : 10));
	} : $parseInt;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.parse-int.js
var require_es_parse_int = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $parseInt = require_number_parse_int();
	$({
		global: true,
		forced: parseInt !== $parseInt
	}, { parseInt: $parseInt });
}));
//#endregion
//#region node_modules/core-js-pure/es/parse-int.js
var require_parse_int$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_parse_int();
	module.exports = require_path().parseInt;
}));
//#endregion
//#region node_modules/core-js-pure/stable/parse-int.js
var require_parse_int$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_parse_int$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/parse-int.js
var require_parse_int = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_parse_int$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/parseDate.js
var require_parseDate = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = parseDate;
	var _parseInt2 = _interopRequireDefault(require_parse_int());
	function parseDate(iso8601) {
		const match = (/* @__PURE__ */ new RegExp("^([0-9]{1,4})-([0-9]{1,2})-([0-9]{1,2})T([0-9]{1,2}):([0-9]{1,2}):([0-9]{1,2})(.([0-9]+))?Z$")).exec(iso8601);
		if (!match) return null;
		const year = (0, _parseInt2.default)(match[1]) || 0;
		const month = ((0, _parseInt2.default)(match[2]) || 1) - 1;
		const day = (0, _parseInt2.default)(match[3]) || 0;
		const hour = (0, _parseInt2.default)(match[4]) || 0;
		const minute = (0, _parseInt2.default)(match[5]) || 0;
		const second = (0, _parseInt2.default)(match[6]) || 0;
		const milli = (0, _parseInt2.default)(match[8]) || 0;
		return new Date(Date.UTC(year, month, day, hour, minute, second, milli));
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/promiseUtils.js
var require_promiseUtils = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.continueWhile = continueWhile;
	exports.resolvingPromise = resolvingPromise;
	exports.when = when;
	var _promise = _interopRequireDefault(require_promise());
	var _isArray = _interopRequireDefault(require_is_array());
	function resolvingPromise() {
		let res;
		let rej;
		const defer = new _promise.default((resolve, reject) => {
			res = resolve;
			rej = reject;
		});
		defer.resolve = res;
		defer.reject = rej;
		return defer;
	}
	function when(promises) {
		let objects;
		const arrayArgument = (0, _isArray.default)(promises);
		if (arrayArgument) objects = promises;
		else objects = arguments;
		let total = objects.length;
		let hadError = false;
		const results = [];
		const returnValue = arrayArgument ? [results] : results;
		const errors = [];
		results.length = objects.length;
		errors.length = objects.length;
		if (total === 0) return _promise.default.resolve(returnValue);
		const promise = resolvingPromise();
		const resolveOne = function() {
			total--;
			if (total <= 0) {
				if (hadError) promise.reject(errors);
				else promise.resolve(returnValue);
			}
		};
		const chain = function(object, index) {
			if (object && typeof object.then === "function") object.then(function(result) {
				results[index] = result;
				resolveOne();
			}, function(error) {
				errors[index] = error;
				hadError = true;
				resolveOne();
			});
			else {
				results[index] = object;
				resolveOne();
			}
		};
		for (let i = 0; i < objects.length; i++) chain(objects[i], i);
		return promise;
	}
	function continueWhile(test, emitter) {
		if (test()) return emitter().then(() => {
			return continueWhile(test, emitter);
		});
		return _promise.default.resolve();
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/LocalDatastoreUtils.js
var require_LocalDatastoreUtils = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.PIN_PREFIX = exports.OBJECT_PREFIX = exports.DEFAULT_PIN = void 0;
	exports.isLocalDatastoreKey = isLocalDatastoreKey;
	var _startsWith = _interopRequireDefault(require_starts_with());
	var DEFAULT_PIN = exports.DEFAULT_PIN = "_default";
	var PIN_PREFIX = exports.PIN_PREFIX = "parsePin_";
	var OBJECT_PREFIX = exports.OBJECT_PREFIX = "Parse_LDS_";
	function isLocalDatastoreKey(key) {
		return !!(key && (key === DEFAULT_PIN || (0, _startsWith.default)(key).call(key, PIN_PREFIX) || (0, _startsWith.default)(key).call(key, OBJECT_PREFIX)));
	}
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.global-this.js
var require_es_global_this = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var globalThis = require_global_this$6();
	$({
		global: true,
		forced: globalThis.globalThis !== globalThis
	}, { globalThis });
}));
//#endregion
//#region node_modules/core-js-pure/modules/esnext.global-this.js
var require_esnext_global_this = /* @__PURE__ */ __commonJSMin((() => {
	require_es_global_this();
}));
//#endregion
//#region node_modules/core-js-pure/es/global-this.js
var require_global_this$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_global_this();
	module.exports = require_global_this$6();
}));
//#endregion
//#region node_modules/core-js-pure/stable/global-this.js
var require_global_this$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_global_this$5();
}));
//#endregion
//#region node_modules/core-js-pure/actual/global-this.js
var require_global_this$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_global_this$4();
}));
//#endregion
//#region node_modules/core-js-pure/full/global-this.js
var require_global_this$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_esnext_global_this();
	module.exports = require_global_this$3();
}));
//#endregion
//#region node_modules/core-js-pure/features/global-this.js
var require_global_this$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_global_this$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js/global-this.js
var require_global_this = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_global_this$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/uuid.js
var require_uuid = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _globalThis2 = _interopRequireDefault(require_global_this());
	var uuid = () => _globalThis2.default.crypto.randomUUID();
	exports.default = uuid;
}));
//#endregion
//#region node_modules/parse/lib/browser/arrayContainsObject.js
var require_arrayContainsObject = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = arrayContainsObject;
	var _indexOf = _interopRequireDefault(require_index_of());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	function arrayContainsObject(array, object) {
		if ((0, _indexOf.default)(array).call(array, object) > -1) return true;
		const ParseObject = _CoreManager.default.getParseObject();
		for (let i = 0; i < array.length; i++) if (array[i] instanceof ParseObject && array[i].className === object.className && array[i]._getId() === object._getId()) return true;
		return false;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/unique.js
var require_unique = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = unique;
	var _forEach = _interopRequireDefault(require_for_each());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _arrayContainsObject = _interopRequireDefault(require_arrayContainsObject());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	function unique(arr) {
		const uniques = [];
		(0, _forEach.default)(arr).call(arr, (value) => {
			if (value instanceof _CoreManager.default.getParseObject()) {
				if (!(0, _arrayContainsObject.default)(uniques, value)) uniques.push(value);
			} else if ((0, _indexOf.default)(uniques).call(uniques, value) < 0) uniques.push(value);
		});
		return uniques;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseOp.js
var require_ParseOp = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.UnsetOp = exports.SetOp = exports.RemoveOp = exports.RelationOp = exports.Op = exports.IncrementOp = exports.AddUniqueOp = exports.AddOp = void 0;
	exports.opFromJSON = opFromJSON;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _isArray = _interopRequireDefault(require_is_array());
	var _concat = _interopRequireDefault(require_concat());
	var _forEach = _interopRequireDefault(require_for_each());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _splice = _interopRequireDefault(require_splice());
	var _map = _interopRequireDefault(require_map$3());
	var _arrayContainsObject = _interopRequireDefault(require_arrayContainsObject());
	var _decode = _interopRequireDefault(require_decode());
	var _encode = _interopRequireDefault(require_encode());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var _unique = _interopRequireDefault(require_unique());
	function opFromJSON(json) {
		if (!json || !json.__op) return null;
		switch (json.__op) {
			case "Delete": return new UnsetOp();
			case "Increment": return new IncrementOp(json.amount);
			case "Add": return new AddOp((0, _decode.default)(json.objects));
			case "AddUnique": return new AddUniqueOp((0, _decode.default)(json.objects));
			case "Remove": return new RemoveOp((0, _decode.default)(json.objects));
			case "AddRelation": {
				const toAdd = (0, _decode.default)(json.objects);
				if (!(0, _isArray.default)(toAdd)) return new RelationOp([], []);
				return new RelationOp(toAdd, []);
			}
			case "RemoveRelation": {
				const toRemove = (0, _decode.default)(json.objects);
				if (!(0, _isArray.default)(toRemove)) return new RelationOp([], []);
				return new RelationOp([], toRemove);
			}
			case "Batch": {
				let toAdd = [];
				let toRemove = [];
				for (let i = 0; i < json.ops.length; i++) if (json.ops[i].__op === "AddRelation") toAdd = (0, _concat.default)(toAdd).call(toAdd, (0, _decode.default)(json.ops[i].objects));
				else if (json.ops[i].__op === "RemoveRelation") toRemove = (0, _concat.default)(toRemove).call(toRemove, (0, _decode.default)(json.ops[i].objects));
				return new RelationOp(toAdd, toRemove);
			}
		}
		return null;
	}
	var Op = class {
		applyTo() {}
		mergeWith() {}
		toJSON() {}
	};
	exports.Op = Op;
	var SetOp = class SetOp extends Op {
		constructor(value) {
			super();
			(0, _defineProperty2.default)(this, "_value", void 0);
			this._value = value;
		}
		applyTo() {
			return this._value;
		}
		mergeWith() {
			return new SetOp(this._value);
		}
		toJSON(offline) {
			return (0, _encode.default)(this._value, false, true, void 0, offline);
		}
	};
	exports.SetOp = SetOp;
	var UnsetOp = class UnsetOp extends Op {
		applyTo() {}
		mergeWith() {
			return new UnsetOp();
		}
		toJSON() {
			return { __op: "Delete" };
		}
	};
	exports.UnsetOp = UnsetOp;
	var IncrementOp = class IncrementOp extends Op {
		constructor(amount) {
			super();
			(0, _defineProperty2.default)(this, "_amount", void 0);
			if (typeof amount !== "number") throw new TypeError("Increment Op must be initialized with a numeric amount.");
			this._amount = amount;
		}
		applyTo(value) {
			if (typeof value === "undefined") return this._amount;
			if (typeof value !== "number") throw new TypeError("Cannot increment a non-numeric value.");
			return this._amount + value;
		}
		mergeWith(previous) {
			if (!previous) return this;
			if (previous instanceof SetOp) return new SetOp(this.applyTo(previous._value));
			if (previous instanceof UnsetOp) return new SetOp(this._amount);
			if (previous instanceof IncrementOp) return new IncrementOp(this.applyTo(previous._amount));
			throw new Error("Cannot merge Increment Op with the previous Op");
		}
		toJSON() {
			return {
				__op: "Increment",
				amount: this._amount
			};
		}
	};
	exports.IncrementOp = IncrementOp;
	var AddOp = class AddOp extends Op {
		constructor(value) {
			super();
			(0, _defineProperty2.default)(this, "_value", void 0);
			this._value = (0, _isArray.default)(value) ? value : [value];
		}
		applyTo(value) {
			if (value == null) return this._value;
			if ((0, _isArray.default)(value)) return (0, _concat.default)(value).call(value, this._value);
			throw new Error("Cannot add elements to a non-array value");
		}
		mergeWith(previous) {
			if (!previous) return this;
			if (previous instanceof SetOp) return new SetOp(this.applyTo(previous._value));
			if (previous instanceof UnsetOp) return new SetOp(this._value);
			if (previous instanceof AddOp) return new AddOp(this.applyTo(previous._value));
			throw new Error("Cannot merge Add Op with the previous Op");
		}
		toJSON() {
			return {
				__op: "Add",
				objects: (0, _encode.default)(this._value, false, true)
			};
		}
	};
	exports.AddOp = AddOp;
	var AddUniqueOp = class AddUniqueOp extends Op {
		constructor(value) {
			super();
			(0, _defineProperty2.default)(this, "_value", void 0);
			this._value = (0, _unique.default)((0, _isArray.default)(value) ? value : [value]);
		}
		applyTo(value) {
			if (value == null) return this._value || [];
			if ((0, _isArray.default)(value)) {
				var _context;
				const ParseObject = _CoreManager.default.getParseObject();
				const toAdd = [];
				(0, _forEach.default)(_context = this._value).call(_context, (v) => {
					if (v instanceof ParseObject) {
						if (!(0, _arrayContainsObject.default)(value, v)) toAdd.push(v);
					} else if ((0, _indexOf.default)(value).call(value, v) < 0) toAdd.push(v);
				});
				return (0, _concat.default)(value).call(value, toAdd);
			}
			throw new Error("Cannot add elements to a non-array value");
		}
		mergeWith(previous) {
			if (!previous) return this;
			if (previous instanceof SetOp) return new SetOp(this.applyTo(previous._value));
			if (previous instanceof UnsetOp) return new SetOp(this._value);
			if (previous instanceof AddUniqueOp) return new AddUniqueOp(this.applyTo(previous._value));
			throw new Error("Cannot merge AddUnique Op with the previous Op");
		}
		toJSON() {
			return {
				__op: "AddUnique",
				objects: (0, _encode.default)(this._value, false, true)
			};
		}
	};
	exports.AddUniqueOp = AddUniqueOp;
	var RemoveOp = class RemoveOp extends Op {
		constructor(value) {
			super();
			(0, _defineProperty2.default)(this, "_value", void 0);
			this._value = (0, _unique.default)((0, _isArray.default)(value) ? value : [value]);
		}
		applyTo(value) {
			if (value == null) return [];
			if ((0, _isArray.default)(value)) {
				const ParseObject = _CoreManager.default.getParseObject();
				const removed = (0, _concat.default)(value).call(value, []);
				for (let i = 0; i < this._value.length; i++) {
					let index = (0, _indexOf.default)(removed).call(removed, this._value[i]);
					while (index > -1) {
						(0, _splice.default)(removed).call(removed, index, 1);
						index = (0, _indexOf.default)(removed).call(removed, this._value[i]);
					}
					if (this._value[i] instanceof ParseObject && this._value[i].id) {
						for (let j = 0; j < removed.length; j++) if (removed[j] instanceof ParseObject && this._value[i].id === removed[j].id) {
							(0, _splice.default)(removed).call(removed, j, 1);
							j--;
						}
					}
				}
				return removed;
			}
			throw new Error("Cannot remove elements from a non-array value");
		}
		mergeWith(previous) {
			if (!previous) return this;
			if (previous instanceof SetOp) return new SetOp(this.applyTo(previous._value));
			if (previous instanceof UnsetOp) return new UnsetOp();
			if (previous instanceof RemoveOp) {
				var _context2;
				const ParseObject = _CoreManager.default.getParseObject();
				const uniques = (0, _concat.default)(_context2 = previous._value).call(_context2, []);
				for (let i = 0; i < this._value.length; i++) if (this._value[i] instanceof ParseObject) {
					if (!(0, _arrayContainsObject.default)(uniques, this._value[i])) uniques.push(this._value[i]);
				} else if ((0, _indexOf.default)(uniques).call(uniques, this._value[i]) < 0) uniques.push(this._value[i]);
				return new RemoveOp(uniques);
			}
			throw new Error("Cannot merge Remove Op with the previous Op");
		}
		toJSON() {
			return {
				__op: "Remove",
				objects: (0, _encode.default)(this._value, false, true)
			};
		}
	};
	exports.RemoveOp = RemoveOp;
	var RelationOp = class RelationOp extends Op {
		constructor(adds, removes) {
			super();
			(0, _defineProperty2.default)(this, "_targetClassName", void 0);
			(0, _defineProperty2.default)(this, "relationsToAdd", void 0);
			(0, _defineProperty2.default)(this, "relationsToRemove", void 0);
			this._targetClassName = null;
			if ((0, _isArray.default)(adds)) this.relationsToAdd = (0, _unique.default)((0, _map.default)(adds).call(adds, this._extractId, this));
			if ((0, _isArray.default)(removes)) this.relationsToRemove = (0, _unique.default)((0, _map.default)(removes).call(removes, this._extractId, this));
		}
		_extractId(obj) {
			if (typeof obj === "string") return obj;
			if (!obj.id) throw new Error("You cannot add or remove an unsaved Parse Object from a relation");
			if (!this._targetClassName) this._targetClassName = obj.className;
			if (this._targetClassName !== obj.className) throw new Error("Tried to create a Relation with 2 different object types: " + this._targetClassName + " and " + obj.className + ".");
			return obj.id;
		}
		applyTo(value, parent, key) {
			if (!value) {
				if (!parent || !key) throw new Error("Cannot apply a RelationOp without either a previous value, or an object and a key");
				const relation = new _ParseRelation.default(parent, key);
				relation.targetClassName = this._targetClassName;
				return relation;
			}
			if (value instanceof _ParseRelation.default) {
				if (this._targetClassName) {
					if (value.targetClassName) {
						if (this._targetClassName !== value.targetClassName) throw new Error("Related object must be a " + value.targetClassName + ", but a " + this._targetClassName + " was passed in.");
					} else value.targetClassName = this._targetClassName;
				}
				return value;
			} else throw new Error("Relation cannot be applied to a non-relation field");
		}
		mergeWith(previous) {
			if (!previous) return this;
			else if (previous instanceof UnsetOp) throw new Error("You cannot modify a relation after deleting it.");
			else if (previous instanceof SetOp && previous._value instanceof _ParseRelation.default) return this;
			else if (previous instanceof RelationOp) {
				var _context3, _context4, _context5, _context6, _context7, _context8;
				if (previous._targetClassName && previous._targetClassName !== this._targetClassName) throw new Error("Related object must be of class " + previous._targetClassName + ", but " + (this._targetClassName || "null") + " was passed in.");
				const newAdd = (0, _concat.default)(_context3 = previous.relationsToAdd).call(_context3, []);
				(0, _forEach.default)(_context4 = this.relationsToRemove).call(_context4, (r) => {
					const index = (0, _indexOf.default)(newAdd).call(newAdd, r);
					if (index > -1) (0, _splice.default)(newAdd).call(newAdd, index, 1);
				});
				(0, _forEach.default)(_context5 = this.relationsToAdd).call(_context5, (r) => {
					if ((0, _indexOf.default)(newAdd).call(newAdd, r) < 0) newAdd.push(r);
				});
				const newRemove = (0, _concat.default)(_context6 = previous.relationsToRemove).call(_context6, []);
				(0, _forEach.default)(_context7 = this.relationsToAdd).call(_context7, (r) => {
					const index = (0, _indexOf.default)(newRemove).call(newRemove, r);
					if (index > -1) (0, _splice.default)(newRemove).call(newRemove, index, 1);
				});
				(0, _forEach.default)(_context8 = this.relationsToRemove).call(_context8, (r) => {
					if ((0, _indexOf.default)(newRemove).call(newRemove, r) < 0) newRemove.push(r);
				});
				const newRelation = new RelationOp(newAdd, newRemove);
				newRelation._targetClassName = this._targetClassName;
				return newRelation;
			}
			throw new Error("Cannot merge Relation Op with the previous Op");
		}
		toJSON() {
			const idToPointer = (id) => {
				return {
					__type: "Pointer",
					className: this._targetClassName,
					objectId: id
				};
			};
			let pointers = null;
			let adds = null;
			let removes = null;
			if (this.relationsToAdd.length > 0) {
				var _context9;
				pointers = (0, _map.default)(_context9 = this.relationsToAdd).call(_context9, idToPointer);
				adds = {
					__op: "AddRelation",
					objects: pointers
				};
			}
			if (this.relationsToRemove.length > 0) {
				var _context0;
				pointers = (0, _map.default)(_context0 = this.relationsToRemove).call(_context0, idToPointer);
				removes = {
					__op: "RemoveRelation",
					objects: pointers
				};
			}
			if (adds && removes) return {
				__op: "Batch",
				ops: [adds, removes]
			};
			return adds || removes || {};
		}
	};
	exports.RelationOp = RelationOp;
	_CoreManager.default.setParseOp({
		Op,
		opFromJSON,
		SetOp,
		UnsetOp,
		IncrementOp,
		AddOp,
		RelationOp,
		RemoveOp,
		AddUniqueOp
	});
}));
//#endregion
//#region node_modules/parse/lib/browser/TaskQueue.js
var require_TaskQueue = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _promiseUtils = require_promiseUtils();
	var TaskQueue = class {
		constructor() {
			(0, _defineProperty2.default)(this, "queue", void 0);
			this.queue = [];
		}
		enqueue(task) {
			const taskComplete = (0, _promiseUtils.resolvingPromise)();
			this.queue.push({
				task,
				_completion: taskComplete
			});
			if (this.queue.length === 1) task().then(() => {
				this._dequeue();
				taskComplete.resolve();
			}, (error) => {
				this._dequeue();
				taskComplete.reject(error);
			});
			return taskComplete;
		}
		_dequeue() {
			this.queue.shift();
			if (this.queue.length) {
				const next = this.queue[0];
				next.task().then(() => {
					this._dequeue();
					next._completion.resolve();
				}, (error) => {
					this._dequeue();
					next._completion.reject(error);
				});
			}
		}
	};
	exports.default = TaskQueue;
}));
//#endregion
//#region node_modules/parse/lib/browser/ObjectStateMutations.js
var require_ObjectStateMutations = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.commitServerChanges = commitServerChanges;
	exports.defaultState = defaultState;
	exports.estimateAttribute = estimateAttribute;
	exports.estimateAttributes = estimateAttributes;
	exports.mergeFirstPendingState = mergeFirstPendingState;
	exports.popPendingState = popPendingState;
	exports.pushPendingState = pushPendingState;
	exports.setPendingOp = setPendingOp;
	exports.setServerData = setServerData;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _create = _interopRequireDefault(require_create());
	var _includes = _interopRequireDefault(require_includes());
	var _isArray = _interopRequireDefault(require_is_array());
	var _stringify = _interopRequireDefault(require_stringify());
	var _encode = _interopRequireDefault(require_encode());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var _TaskQueue = _interopRequireDefault(require_TaskQueue());
	var _ParseOp = require_ParseOp();
	var _isDangerousKey = require_isDangerousKey();
	function ownKeys(e, r) {
		var t = _Object$keys(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context, _context2;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty(_context = ownKeys(Object(t), !0)).call(_context, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty(_context2 = ownKeys(Object(t))).call(_context2, function(r) {
				_Object$defineProperty(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	function defaultState() {
		return {
			serverData: (0, _create.default)(null),
			pendingOps: [(0, _create.default)(null)],
			objectCache: (0, _create.default)(null),
			tasks: new _TaskQueue.default(),
			existed: false
		};
	}
	function setServerData(serverData, attributes) {
		for (const attr in attributes) {
			if (!Object.prototype.hasOwnProperty.call(attributes, attr)) continue;
			if ((0, _isDangerousKey.isDangerousKey)(attr)) continue;
			if (typeof attributes[attr] !== "undefined") serverData[attr] = attributes[attr];
			else delete serverData[attr];
		}
	}
	function setPendingOp(pendingOps, attr, op) {
		if ((0, _isDangerousKey.isDangerousKey)(attr)) return;
		const last = pendingOps.length - 1;
		if (op) pendingOps[last][attr] = op;
		else delete pendingOps[last][attr];
	}
	function pushPendingState(pendingOps) {
		pendingOps.push((0, _create.default)(null));
	}
	function popPendingState(pendingOps) {
		const first = pendingOps.shift();
		if (!pendingOps.length) pendingOps[0] = (0, _create.default)(null);
		return first;
	}
	function mergeFirstPendingState(pendingOps) {
		const first = popPendingState(pendingOps);
		const next = pendingOps[0];
		for (const attr in first) {
			if (!Object.prototype.hasOwnProperty.call(first, attr)) continue;
			if ((0, _isDangerousKey.isDangerousKey)(attr)) continue;
			if (next[attr] && first[attr]) {
				const merged = next[attr].mergeWith(first[attr]);
				if (merged) next[attr] = merged;
			} else next[attr] = first[attr];
		}
	}
	function estimateAttribute(serverData, pendingOps, object, attr) {
		if ((0, _isDangerousKey.isDangerousKey)(attr)) return;
		let value = serverData[attr];
		for (let i = 0; i < pendingOps.length; i++) if (pendingOps[i][attr]) {
			if (pendingOps[i][attr] instanceof _ParseOp.RelationOp) {
				if (object.id) value = pendingOps[i][attr].applyTo(value, object, attr);
			} else value = pendingOps[i][attr].applyTo(value);
		}
		return value;
	}
	function estimateAttributes(serverData, pendingOps, object) {
		const data = (0, _create.default)(null);
		for (var attr in serverData) data[attr] = serverData[attr];
		for (let i = 0; i < pendingOps.length; i++) for (attr in pendingOps[i]) {
			if (!Object.prototype.hasOwnProperty.call(pendingOps[i], attr)) continue;
			if ((0, _isDangerousKey.isDangerousKey)(attr)) continue;
			if (pendingOps[i][attr] instanceof _ParseOp.RelationOp) {
				if (object.id) data[attr] = pendingOps[i][attr].applyTo(data[attr], object, attr);
			} else if ((0, _includes.default)(attr).call(attr, ".")) {
				const fields = attr.split(".");
				const last = fields[fields.length - 1];
				let object = data;
				for (let i = 0; i < fields.length - 1; i++) {
					const key = fields[i];
					if (!(key in object)) {
						const nextKey = fields[i + 1];
						if (!isNaN(nextKey)) object[key] = [];
						else object[key] = (0, _create.default)(null);
					} else if ((0, _isArray.default)(object[key])) object[key] = [...object[key]];
					else object[key] = _objectSpread({}, object[key]);
					object = object[key];
				}
				object[last] = pendingOps[i][attr].applyTo(object[last]);
			} else data[attr] = pendingOps[i][attr].applyTo(data[attr]);
		}
		return data;
	}
	/**
	* Allows setting properties/variables deep in an object.
	* Converts a.b into { a: { b: value } } for dot notation on Objects
	* Converts a.0.b into { a: [{ b: value }] } for dot notation on Arrays
	*
	* @param obj The object to assign the value to
	* @param key The key to assign. If it's in a deeper path, then use dot notation (`prop1.prop2.prop3`)
	* Note that intermediate object(s) in the nested path are automatically created if they don't exist.
	* @param value The value to assign. If it's an `undefined` then the key is deleted.
	*/
	function nestedSet(obj, key, value) {
		const paths = key.split(".");
		for (let i = 0; i < paths.length - 1; i++) {
			const path = paths[i];
			if (!(path in obj)) {
				const nextPath = paths[i + 1];
				if (!isNaN(nextPath)) obj[path] = [];
				else obj[path] = (0, _create.default)(null);
			}
			obj = obj[path];
		}
		if (typeof value === "undefined") delete obj[paths[paths.length - 1]];
		else obj[paths[paths.length - 1]] = value;
	}
	function commitServerChanges(serverData, objectCache, changes) {
		const ParseObject = _CoreManager.default.getParseObject();
		for (const attr in changes) {
			if (!Object.prototype.hasOwnProperty.call(changes, attr)) continue;
			if ((0, _isDangerousKey.isDangerousKey)(attr)) continue;
			const val = changes[attr];
			nestedSet(serverData, attr, val);
			if (val && typeof val === "object" && !(val instanceof ParseObject) && !(val instanceof _ParseFile.default) && !(val instanceof _ParseRelation.default)) {
				const json = (0, _encode.default)(val, false, true);
				objectCache[attr] = (0, _stringify.default)(json);
			}
		}
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/SingleInstanceStateController.js
var require_SingleInstanceStateController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _WeakMap = require_weak_map();
	var _Object$defineProperty = require_define_property$3();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.clearAllState = clearAllState;
	exports.commitServerChanges = commitServerChanges;
	exports.duplicateState = duplicateState;
	exports.enqueueTask = enqueueTask;
	exports.estimateAttribute = estimateAttribute;
	exports.estimateAttributes = estimateAttributes;
	exports.getObjectCache = getObjectCache;
	exports.getPendingOps = getPendingOps;
	exports.getServerData = getServerData;
	exports.getState = getState;
	exports.initializeState = initializeState;
	exports.mergeFirstPendingState = mergeFirstPendingState;
	exports.popPendingState = popPendingState;
	exports.pushPendingState = pushPendingState;
	exports.removeState = removeState;
	exports.setPendingOp = setPendingOp;
	exports.setServerData = setServerData;
	var _create = _interopRequireDefault(require_create());
	var ObjectStateMutations = _interopRequireWildcard(require_ObjectStateMutations());
	function _interopRequireWildcard(e, t) {
		if ("function" == typeof _WeakMap) var r = new _WeakMap(), n = new _WeakMap();
		return (_interopRequireWildcard = function(e, t) {
			if (!t && e && e.__esModule) return e;
			var o, i, f = {
				__proto__: null,
				default: e
			};
			if (null === e || "object" != typeof e && "function" != typeof e) return f;
			if (o = t ? n : r) {
				if (o.has(e)) return o.get(e);
				o.set(e, f);
			}
			for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = _Object$defineProperty) && _Object$getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
			return f;
		})(e, t);
	}
	var objectState = (0, _create.default)(null);
	function getState(obj) {
		const classData = objectState[obj.className];
		if (classData) return classData[obj.id] || null;
		return null;
	}
	function initializeState(obj, initial) {
		let state = getState(obj);
		if (state) return state;
		if (!objectState[obj.className]) objectState[obj.className] = (0, _create.default)(null);
		if (!initial) initial = ObjectStateMutations.defaultState();
		state = objectState[obj.className][obj.id] = initial;
		return state;
	}
	function removeState(obj) {
		const state = getState(obj);
		if (state === null) return null;
		delete objectState[obj.className][obj.id];
		return state;
	}
	function getServerData(obj) {
		const state = getState(obj);
		if (state) return state.serverData;
		return {};
	}
	function setServerData(obj, attributes) {
		const serverData = initializeState(obj).serverData;
		ObjectStateMutations.setServerData(serverData, attributes);
	}
	function getPendingOps(obj) {
		const state = getState(obj);
		if (state) return state.pendingOps;
		return [{}];
	}
	function setPendingOp(obj, attr, op) {
		const pendingOps = initializeState(obj).pendingOps;
		ObjectStateMutations.setPendingOp(pendingOps, attr, op);
	}
	function pushPendingState(obj) {
		const pendingOps = initializeState(obj).pendingOps;
		ObjectStateMutations.pushPendingState(pendingOps);
	}
	function popPendingState(obj) {
		const pendingOps = initializeState(obj).pendingOps;
		return ObjectStateMutations.popPendingState(pendingOps);
	}
	function mergeFirstPendingState(obj) {
		const pendingOps = getPendingOps(obj);
		ObjectStateMutations.mergeFirstPendingState(pendingOps);
	}
	function getObjectCache(obj) {
		const state = getState(obj);
		if (state) return state.objectCache;
		return {};
	}
	function estimateAttribute(obj, attr) {
		const serverData = getServerData(obj);
		const pendingOps = getPendingOps(obj);
		return ObjectStateMutations.estimateAttribute(serverData, pendingOps, obj, attr);
	}
	function estimateAttributes(obj) {
		const serverData = getServerData(obj);
		const pendingOps = getPendingOps(obj);
		return ObjectStateMutations.estimateAttributes(serverData, pendingOps, obj);
	}
	function commitServerChanges(obj, changes) {
		const state = initializeState(obj);
		ObjectStateMutations.commitServerChanges(state.serverData, state.objectCache, changes);
	}
	function enqueueTask(obj, task) {
		return initializeState(obj).tasks.enqueue(task);
	}
	function clearAllState() {
		objectState = (0, _create.default)(null);
	}
	function duplicateState(source, dest) {
		dest.id = source.id;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/UniqueInstanceStateController.js
var require_UniqueInstanceStateController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _WeakMap2 = require_weak_map();
	var _Object$defineProperty = require_define_property$3();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.clearAllState = clearAllState;
	exports.commitServerChanges = commitServerChanges;
	exports.duplicateState = duplicateState;
	exports.enqueueTask = enqueueTask;
	exports.estimateAttribute = estimateAttribute;
	exports.estimateAttributes = estimateAttributes;
	exports.getObjectCache = getObjectCache;
	exports.getPendingOps = getPendingOps;
	exports.getServerData = getServerData;
	exports.getState = getState;
	exports.initializeState = initializeState;
	exports.mergeFirstPendingState = mergeFirstPendingState;
	exports.popPendingState = popPendingState;
	exports.pushPendingState = pushPendingState;
	exports.removeState = removeState;
	exports.setPendingOp = setPendingOp;
	exports.setServerData = setServerData;
	var _weakMap = _interopRequireDefault(require_weak_map());
	var ObjectStateMutations = _interopRequireWildcard(require_ObjectStateMutations());
	var _TaskQueue = _interopRequireDefault(require_TaskQueue());
	function _interopRequireWildcard(e, t) {
		if ("function" == typeof _WeakMap2) var r = new _WeakMap2(), n = new _WeakMap2();
		return (_interopRequireWildcard = function(e, t) {
			if (!t && e && e.__esModule) return e;
			var o, i, f = {
				__proto__: null,
				default: e
			};
			if (null === e || "object" != typeof e && "function" != typeof e) return f;
			if (o = t ? n : r) {
				if (o.has(e)) return o.get(e);
				o.set(e, f);
			}
			for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = _Object$defineProperty) && _Object$getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
			return f;
		})(e, t);
	}
	var objectState = new _weakMap.default();
	function getState(obj) {
		return objectState.get(obj) || null;
	}
	function initializeState(obj, initial) {
		let state = getState(obj);
		if (state) return state;
		if (!initial) initial = {
			serverData: {},
			pendingOps: [{}],
			objectCache: {},
			tasks: new _TaskQueue.default(),
			existed: false
		};
		state = initial;
		objectState.set(obj, state);
		return state;
	}
	function removeState(obj) {
		const state = getState(obj);
		if (state === null) return null;
		objectState.delete(obj);
		return state;
	}
	function getServerData(obj) {
		const state = getState(obj);
		if (state) return state.serverData;
		return {};
	}
	function setServerData(obj, attributes) {
		const serverData = initializeState(obj).serverData;
		ObjectStateMutations.setServerData(serverData, attributes);
	}
	function getPendingOps(obj) {
		const state = getState(obj);
		if (state) return state.pendingOps;
		return [{}];
	}
	function setPendingOp(obj, attr, op) {
		const pendingOps = initializeState(obj).pendingOps;
		ObjectStateMutations.setPendingOp(pendingOps, attr, op);
	}
	function pushPendingState(obj) {
		const pendingOps = initializeState(obj).pendingOps;
		ObjectStateMutations.pushPendingState(pendingOps);
	}
	function popPendingState(obj) {
		const pendingOps = initializeState(obj).pendingOps;
		return ObjectStateMutations.popPendingState(pendingOps);
	}
	function mergeFirstPendingState(obj) {
		const pendingOps = getPendingOps(obj);
		ObjectStateMutations.mergeFirstPendingState(pendingOps);
	}
	function getObjectCache(obj) {
		const state = getState(obj);
		if (state) return state.objectCache;
		return {};
	}
	function estimateAttribute(obj, attr) {
		const serverData = getServerData(obj);
		const pendingOps = getPendingOps(obj);
		return ObjectStateMutations.estimateAttribute(serverData, pendingOps, obj, attr);
	}
	function estimateAttributes(obj) {
		const serverData = getServerData(obj);
		const pendingOps = getPendingOps(obj);
		return ObjectStateMutations.estimateAttributes(serverData, pendingOps, obj);
	}
	function commitServerChanges(obj, changes) {
		const state = initializeState(obj);
		ObjectStateMutations.commitServerChanges(state.serverData, state.objectCache, changes);
	}
	function enqueueTask(obj, task) {
		return initializeState(obj).tasks.enqueue(task);
	}
	function duplicateState(source, dest) {
		const oldState = initializeState(source);
		const newState = initializeState(dest);
		for (const key in oldState.serverData) newState.serverData[key] = oldState.serverData[key];
		for (let index = 0; index < oldState.pendingOps.length; index++) for (const key in oldState.pendingOps[index]) newState.pendingOps[index][key] = oldState.pendingOps[index][key];
		for (const key in oldState.objectCache) newState.objectCache[key] = oldState.objectCache[key];
		newState.existed = oldState.existed;
	}
	function clearAllState() {
		objectState = new _weakMap.default();
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/unsavedChildren.js
var require_unsavedChildren = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = unsavedChildren;
	var _concat = _interopRequireDefault(require_concat());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _isArray = _interopRequireDefault(require_is_array());
	var _forEach = _interopRequireDefault(require_for_each());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	/**
	* Return an array of unsaved children, which are either Parse Objects or Files.
	* If it encounters any dirty Objects without Ids, it will throw an exception.
	*
	* @param {Parse.Object} obj
	* @param {boolean} allowDeepUnsaved
	* @returns {Array}
	*/
	function unsavedChildren(obj, allowDeepUnsaved) {
		const encountered = {
			objects: {},
			files: []
		};
		const identifier = obj.className + ":" + obj._getId();
		encountered.objects[identifier] = obj.dirty() ? obj : true;
		const attributes = obj.attributes;
		for (const attr in attributes) if (typeof attributes[attr] === "object") traverse(attributes[attr], encountered, false, !!allowDeepUnsaved);
		const unsaved = [];
		for (const id in encountered.objects) if (id !== identifier && encountered.objects[id] !== true) unsaved.push(encountered.objects[id]);
		return (0, _concat.default)(unsaved).call(unsaved, encountered.files);
	}
	function traverse(obj, encountered, shouldThrow, allowDeepUnsaved) {
		if (obj instanceof _CoreManager.default.getParseObject()) {
			if (!obj.id && shouldThrow) throw new Error("Cannot create a pointer to an unsaved Object.");
			const identifier = obj.className + ":" + obj._getId();
			if (!encountered.objects[identifier]) {
				encountered.objects[identifier] = obj.dirty() ? obj : true;
				const attributes = obj.attributes;
				for (const attr in attributes) if (typeof attributes[attr] === "object") traverse(attributes[attr], encountered, !allowDeepUnsaved, allowDeepUnsaved);
			}
			return;
		}
		if (obj instanceof _ParseFile.default) {
			var _context;
			if (!obj.url() && (0, _indexOf.default)(_context = encountered.files).call(_context, obj) < 0) encountered.files.push(obj);
			return;
		}
		if (obj instanceof _ParseRelation.default) return;
		if ((0, _isArray.default)(obj)) (0, _forEach.default)(obj).call(obj, (el) => {
			if (typeof el === "object") traverse(el, encountered, shouldThrow, allowDeepUnsaved);
		});
		for (const k in obj) if (typeof obj[k] === "object") traverse(obj[k], encountered, shouldThrow, allowDeepUnsaved);
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseObject.js
var require_ParseObject = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys2 = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty2 = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty2 = require_define_property$3();
	var _WeakMap = require_weak_map();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty2(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _create = _interopRequireDefault(require_create());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _for = _interopRequireDefault(require_for());
	var _concat = _interopRequireDefault(require_concat());
	var _stringify = _interopRequireDefault(require_stringify());
	var _freeze = _interopRequireDefault(require_freeze());
	var _keys = _interopRequireDefault(require_keys$4());
	var _forEach = _interopRequireDefault(require_for_each());
	var _includes = _interopRequireDefault(require_includes());
	var _getPrototypeOf = _interopRequireDefault(require_get_prototype_of());
	var _some = _interopRequireDefault(require_some());
	var _promise = _interopRequireDefault(require_promise());
	var _isArray = _interopRequireDefault(require_is_array());
	var _defineProperty3 = _interopRequireDefault(require_define_property$3());
	var _find = _interopRequireDefault(require_find());
	var _map = _interopRequireDefault(require_map$3());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _canBeSerialized = _interopRequireDefault(require_canBeSerialized());
	var _decode = _interopRequireDefault(require_decode());
	var _encode = _interopRequireDefault(require_encode());
	var _escape = _interopRequireDefault(require_escape());
	var _ParseACL = _interopRequireDefault(require_ParseACL());
	var _parseDate = _interopRequireDefault(require_parseDate());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _promiseUtils = require_promiseUtils();
	var _LocalDatastoreUtils = require_LocalDatastoreUtils();
	var _uuid = _interopRequireDefault(require_uuid());
	var _ParseOp = require_ParseOp();
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var SingleInstanceStateController = _interopRequireWildcard(require_SingleInstanceStateController());
	var _unique = _interopRequireDefault(require_unique());
	var UniqueInstanceStateController = _interopRequireWildcard(require_UniqueInstanceStateController());
	var _unsavedChildren = _interopRequireDefault(require_unsavedChildren());
	function _interopRequireWildcard(e, t) {
		if ("function" == typeof _WeakMap) var r = new _WeakMap(), n = new _WeakMap();
		return (_interopRequireWildcard = function(e, t) {
			if (!t && e && e.__esModule) return e;
			var o, i, f = {
				__proto__: null,
				default: e
			};
			if (null === e || "object" != typeof e && "function" != typeof e) return f;
			if (o = t ? n : r) {
				if (o.has(e)) return o.get(e);
				o.set(e, f);
			}
			for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = _Object$defineProperty2) && _Object$getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
			return f;
		})(e, t);
	}
	function ownKeys(e, r) {
		var t = _Object$keys2(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context4, _context5;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty2(_context4 = ownKeys(Object(t), !0)).call(_context4, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty2(_context5 = ownKeys(Object(t))).call(_context5, function(r) {
				_Object$defineProperty2(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	/** Options for destroyAll batch operation */
	/** Options for saveAll batch operation */
	/** Options for fetchAll batch operation */
	var classMap = (0, _create.default)(null);
	var objectCount = 0;
	var singleInstance = !_CoreManager.default.get("IS_NODE");
	if (singleInstance) _CoreManager.default.setObjectStateController(SingleInstanceStateController);
	else _CoreManager.default.setObjectStateController(UniqueInstanceStateController);
	function getServerUrlPath() {
		let serverUrl = _CoreManager.default.get("SERVER_URL");
		if (serverUrl[serverUrl.length - 1] !== "/") serverUrl += "/";
		const url = serverUrl.replace(/https?:\/\//, "");
		return url.substr((0, _indexOf.default)(url).call(url, "/"));
	}
	/**
	* Creates a new model with defined attributes.
	*
	* <p>You won't normally call this method directly.  It is recommended that
	* you use a subclass of <code>Parse.Object</code> instead, created by calling
	* <code>extend</code>.</p>
	*
	* <p>However, if you don't want to use a subclass, or aren't sure which
	* subclass is appropriate, you can use this form:<pre>
	*     var object = new Parse.Object("ClassName");
	* </pre>
	* That is basically equivalent to:<pre>
	*     var MyClass = Parse.Object.extend("ClassName");
	*     var object = new MyClass();
	* </pre></p>
	*
	* @alias Parse.Object
	*/
	var ParseObject = class ParseObject {
		/**
		* @param {string} className The class name for the object
		* @param {object} attributes The initial set of data to store in the object.
		* @param {object} options The options for this object instance.
		* @param {boolean} [options.ignoreValidation] Set to `true` ignore any attribute validation errors.
		*/
		constructor(className, attributes, options) {
			/**
			* The ID of this object, unique within its class.
			*
			* @property {string} id
			*/
			(0, _defineProperty2.default)(this, "id", void 0);
			(0, _defineProperty2.default)(this, "_localId", void 0);
			(0, _defineProperty2.default)(this, "_objCount", void 0);
			(0, _defineProperty2.default)(this, "className", void 0);
			if (typeof this.initialize === "function") this.initialize.apply(this, arguments);
			let toSet = null;
			this._objCount = objectCount++;
			if (typeof className === "string") {
				this.className = className;
				if (attributes && typeof attributes === "object") toSet = attributes;
			} else if (className && typeof className === "object") {
				this.className = className.className;
				toSet = {};
				for (const attr in className) if (attr !== "className") toSet[attr] = className[attr];
				if (attributes && typeof attributes === "object") options = attributes;
			}
			if (toSet) try {
				this.set(toSet, options);
			} catch (_) {
				throw new Error("Can't create an invalid Parse Object");
			}
			if (_CoreManager.default.get("NODE_LOGGING")) this[(0, _for.default)("nodejs.util.inspect.custom")] = function() {
				var _context, _context2;
				return (0, _concat.default)(_context = (0, _concat.default)(_context2 = "ParseObject: className: ".concat(this.className, ", id: ")).call(_context2, this.id, "\nAttributes: ")).call(_context, (0, _stringify.default)(this.attributes, null, 2));
			};
		}
		get attributes() {
			const stateController = _CoreManager.default.getObjectStateController();
			return (0, _freeze.default)(stateController.estimateAttributes(this._getStateIdentifier()));
		}
		/**
		* The first time this object was saved on the server.
		*
		* @property {Date} createdAt
		* @returns {Date}
		*/
		get createdAt() {
			return this._getServerData().createdAt;
		}
		/**
		* The last time this object was updated on the server.
		*
		* @property {Date} updatedAt
		* @returns {Date}
		*/
		get updatedAt() {
			return this._getServerData().updatedAt;
		}
		/**
		* Returns a local or server Id used uniquely identify this object
		*
		* @returns {string}
		*/
		_getId() {
			if (typeof this.id === "string") return this.id;
			if (typeof this._localId === "string") return this._localId;
			const localId = "local" + (0, _uuid.default)();
			this._localId = localId;
			return localId;
		}
		/**
		* Returns a unique identifier used to pull data from the State Controller.
		*
		* @returns {Parse.Object|object}
		*/
		_getStateIdentifier() {
			if (singleInstance) {
				let id = this.id;
				if (!id) id = this._getId();
				return {
					id,
					className: this.className
				};
			} else return this;
		}
		_getServerData() {
			return _CoreManager.default.getObjectStateController().getServerData(this._getStateIdentifier());
		}
		_clearServerData() {
			const serverData = this._getServerData();
			const unset = {};
			for (const attr in serverData) unset[attr] = void 0;
			_CoreManager.default.getObjectStateController().setServerData(this._getStateIdentifier(), unset);
		}
		_getPendingOps() {
			return _CoreManager.default.getObjectStateController().getPendingOps(this._getStateIdentifier());
		}
		/**
		* @param {Array<string>} [keysToClear] - if specified, only ops matching
		* these fields will be cleared
		*/
		_clearPendingOps(keysToClear) {
			const pending = this._getPendingOps();
			const latest = pending[pending.length - 1];
			const keys = keysToClear || (0, _keys.default)(latest);
			(0, _forEach.default)(keys).call(keys, (key) => {
				delete latest[key];
			});
		}
		_getDirtyObjectAttributes() {
			const attributes = this.attributes;
			const objectCache = _CoreManager.default.getObjectStateController().getObjectCache(this._getStateIdentifier());
			const dirty = {};
			for (const attr in attributes) {
				const val = attributes[attr];
				if (val && typeof val === "object" && !(val instanceof ParseObject) && !(val instanceof _ParseFile.default) && !(val instanceof _ParseRelation.default)) try {
					const json = (0, _encode.default)(val, false, true);
					const stringified = (0, _stringify.default)(json);
					if (objectCache[attr] !== stringified) dirty[attr] = val;
				} catch (_) {
					dirty[attr] = val;
				}
			}
			return dirty;
		}
		_toFullJSON(seen, offline) {
			const json = this.toJSON(seen, offline);
			json.__type = "Object";
			json.className = this.className;
			return json;
		}
		_getSaveJSON() {
			const pending = this._getPendingOps();
			const dirtyObjects = this._getDirtyObjectAttributes();
			const json = {};
			for (var attr in dirtyObjects) {
				let isDotNotation = false;
				for (let i = 0; i < pending.length; i += 1) for (const field in pending[i]) if ((0, _includes.default)(field).call(field, ".")) {
					if (field.split(".")[0] === attr) {
						isDotNotation = true;
						break;
					}
				}
				if (!isDotNotation) json[attr] = new _ParseOp.SetOp(dirtyObjects[attr]).toJSON();
			}
			for (attr in pending[0]) json[attr] = pending[0][attr].toJSON();
			return json;
		}
		_getSaveParams() {
			let method = this.id ? "PUT" : "POST";
			const body = this._getSaveJSON();
			let path = "classes/" + this.className;
			if (_CoreManager.default.get("ALLOW_CUSTOM_OBJECT_ID")) {
				if (!this.createdAt) {
					method = "POST";
					body.objectId = this.id;
				} else {
					method = "PUT";
					path += "/" + this.id;
				}
			} else if (this.id) path += "/" + this.id;
			else if (this.className === "_User") path = "users";
			return {
				method,
				body,
				path
			};
		}
		_finishFetch(serverData) {
			if (!this.id && serverData.objectId) this.id = serverData.objectId;
			const stateController = _CoreManager.default.getObjectStateController();
			stateController.initializeState(this._getStateIdentifier());
			const decoded = {};
			for (const attr in serverData) if (attr === "ACL") decoded[attr] = new _ParseACL.default(serverData[attr]);
			else if (attr !== "objectId") {
				decoded[attr] = (0, _decode.default)(serverData[attr]);
				if (decoded[attr] instanceof _ParseRelation.default) decoded[attr]._ensureParentAndKey(this, attr);
			}
			if (decoded.createdAt && typeof decoded.createdAt === "string") decoded.createdAt = (0, _parseDate.default)(decoded.createdAt);
			if (decoded.updatedAt && typeof decoded.updatedAt === "string") decoded.updatedAt = (0, _parseDate.default)(decoded.updatedAt);
			if (!decoded.updatedAt && decoded.createdAt) decoded.updatedAt = decoded.createdAt;
			stateController.commitServerChanges(this._getStateIdentifier(), decoded);
		}
		_setExisted(existed) {
			const state = _CoreManager.default.getObjectStateController().getState(this._getStateIdentifier());
			if (state) state.existed = existed;
		}
		_migrateId(serverId) {
			if (this._localId && serverId) {
				if (singleInstance) {
					const stateController = _CoreManager.default.getObjectStateController();
					const oldState = stateController.removeState(this._getStateIdentifier());
					this.id = serverId;
					delete this._localId;
					if (oldState) stateController.initializeState(this._getStateIdentifier(), oldState);
				} else {
					this.id = serverId;
					delete this._localId;
				}
			}
		}
		_handleSaveResponse(response, status) {
			const changes = {};
			const stateController = _CoreManager.default.getObjectStateController();
			const pending = stateController.popPendingState(this._getStateIdentifier());
			for (var attr in pending) if (pending[attr] instanceof _ParseOp.RelationOp) changes[attr] = pending[attr].applyTo(void 0, this, attr);
			else if (!(attr in response)) changes[attr] = pending[attr].applyTo(void 0);
			for (attr in response) if ((attr === "createdAt" || attr === "updatedAt") && typeof response[attr] === "string") changes[attr] = (0, _parseDate.default)(response[attr]);
			else if (attr === "ACL") changes[attr] = new _ParseACL.default(response[attr]);
			else if (attr !== "objectId") {
				const val = (0, _decode.default)(response[attr]);
				if (val && (0, _getPrototypeOf.default)(val) === Object.prototype) changes[attr] = _objectSpread(_objectSpread({}, this.attributes[attr]), val);
				else changes[attr] = val;
				if (changes[attr] instanceof _ParseOp.UnsetOp) changes[attr] = void 0;
			}
			if (changes.createdAt && !changes.updatedAt) changes.updatedAt = changes.createdAt;
			this._migrateId(response.objectId);
			if (status !== 201) this._setExisted(true);
			stateController.commitServerChanges(this._getStateIdentifier(), changes);
		}
		_handleSaveError() {
			_CoreManager.default.getObjectStateController().mergeFirstPendingState(this._getStateIdentifier());
		}
		static _getClassMap() {
			return classMap;
		}
		static _getRequestOptions() {
			let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			const requestOptions = {};
			if (Object.prototype.toString.call(options) !== "[object Object]") throw new Error("request options must be of type Object");
			const { hasOwn } = Object;
			if (hasOwn(options, "useMasterKey")) requestOptions.useMasterKey = !!options.useMasterKey;
			if (hasOwn(options, "useMaintenanceKey")) requestOptions.useMaintenanceKey = !!options.useMaintenanceKey;
			if (hasOwn(options, "sessionToken") && typeof options.sessionToken === "string") requestOptions.sessionToken = options.sessionToken;
			if (hasOwn(options, "installationId") && typeof options.installationId === "string") requestOptions.installationId = options.installationId;
			if (hasOwn(options, "transaction") && typeof options.transaction === "boolean") requestOptions.transaction = options.transaction;
			if (hasOwn(options, "batchSize") && typeof options.batchSize === "number") requestOptions.batchSize = options.batchSize;
			if (hasOwn(options, "context") && typeof options.context === "object") requestOptions.context = options.context;
			if (hasOwn(options, "include")) requestOptions.include = ParseObject.handleIncludeOptions(options);
			if (hasOwn(options, "usePost")) requestOptions.usePost = options.usePost;
			if (hasOwn(options, "json")) requestOptions.json = options.json;
			return requestOptions;
		}
		initialize() {}
		/**
		* Returns a JSON version of the object suitable for saving to Parse.
		*
		* @param seen
		* @param offline
		* @returns {object}
		*/
		toJSON(seen, offline) {
			const seenEntry = this.id ? this.className + ":" + this.id : this;
			seen = seen || [seenEntry];
			const json = {};
			const attrs = this.attributes;
			for (const attr in attrs) if ((attr === "createdAt" || attr === "updatedAt") && attrs[attr].toJSON) json[attr] = attrs[attr].toJSON();
			else json[attr] = (0, _encode.default)(attrs[attr], false, false, seen, offline);
			const pending = this._getPendingOps();
			for (const attr in pending[0]) if ((0, _indexOf.default)(attr).call(attr, ".") < 0) json[attr] = pending[0][attr].toJSON(offline);
			if (this.id) json.objectId = this.id;
			return json;
		}
		/**
		* Determines whether this ParseObject is equal to another ParseObject
		*
		* @param {object} other - An other object ot compare
		* @returns {boolean}
		*/
		equals(other) {
			if (this === other) return true;
			return other instanceof ParseObject && this.className === other.className && this.id === other.id && typeof this.id !== "undefined";
		}
		/**
		* Returns true if this object has been modified since its last
		* save/refresh.  If an attribute is specified, it returns true only if that
		* particular attribute has been modified since the last save/refresh.
		*
		* @param {string} attr An attribute name (optional).
		* @returns {boolean}
		*/
		dirty(attr) {
			if (!this.id) return true;
			const pendingOps = this._getPendingOps();
			const dirtyObjects = this._getDirtyObjectAttributes();
			if (attr) {
				if (Object.hasOwn(dirtyObjects, attr)) return true;
				for (let i = 0; i < pendingOps.length; i++) if (Object.hasOwn(pendingOps[i], attr)) return true;
				return false;
			}
			if ((0, _keys.default)(pendingOps[0]).length !== 0) return true;
			if ((0, _keys.default)(dirtyObjects).length !== 0) return true;
			return false;
		}
		/**
		* Returns an array of keys that have been modified since last save/refresh
		*
		* @returns {string[]}
		*/
		dirtyKeys() {
			const pendingOps = this._getPendingOps();
			const keys = {};
			for (let i = 0; i < pendingOps.length; i++) for (const attr in pendingOps[i]) keys[attr] = true;
			const dirtyObjects = this._getDirtyObjectAttributes();
			for (const attr in dirtyObjects) keys[attr] = true;
			return (0, _keys.default)(keys);
		}
		/**
		* Returns true if the object has been fetched.
		*
		* @returns {boolean}
		*/
		isDataAvailable() {
			const serverData = this._getServerData();
			return !!(0, _keys.default)(serverData).length;
		}
		/**
		* Gets a Pointer referencing this Object.
		*
		* @returns {Pointer}
		*/
		toPointer() {
			if (!this.id) throw new Error("Cannot create a pointer to an unsaved ParseObject");
			return {
				__type: "Pointer",
				className: this.className,
				objectId: this.id
			};
		}
		/**
		* Gets a Pointer referencing this Object.
		*
		* @returns {Pointer}
		*/
		toOfflinePointer() {
			if (!this._localId) throw new Error("Cannot create a offline pointer to a saved ParseObject");
			return {
				__type: "Object",
				className: this.className,
				_localId: this._localId
			};
		}
		/**
		* Gets the value of an attribute.
		*
		* @param {string} attr The string name of an attribute.
		* @returns {*}
		*/
		get(attr) {
			return this.attributes[attr];
		}
		/**
		* Gets a relation on the given class for the attribute.
		*
		* @param {string} attr The attribute to get the relation for.
		* @returns {Parse.Relation}
		*/
		relation(attr) {
			const value = this.get(attr);
			if (value) {
				if (!(value instanceof _ParseRelation.default)) throw new Error("Called relation() on non-relation field " + attr);
				value._ensureParentAndKey(this, attr);
				return value;
			}
			return new _ParseRelation.default(this, attr);
		}
		/**
		* Gets the HTML-escaped value of an attribute.
		*
		* @param {string} attr The string name of an attribute.
		* @returns {string}
		*/
		escape(attr) {
			let val = this.attributes[attr];
			if (val == null) return "";
			if (typeof val !== "string") {
				if (typeof val.toString !== "function") return "";
				val = val.toString();
			}
			return (0, _escape.default)(val);
		}
		/**
		* Returns <code>true</code> if the attribute contains a value that is not
		* null or undefined.
		*
		* @param {string} attr The string name of the attribute.
		* @returns {boolean}
		*/
		has(attr) {
			const attributes = this.attributes;
			if (Object.hasOwn(attributes, attr)) return attributes[attr] != null;
			return false;
		}
		/**
		* Sets a hash of model attributes on the object.
		*
		* <p>You can call it with an object containing keys and values, with one
		* key and value, or dot notation.  For example:<pre>
		*   gameTurn.set({
		*     player: player1,
		*     diceRoll: 2
		*   }, {
		*     error: function(gameTurnAgain, error) {
		*       // The set failed validation.
		*     }
		*   });
		*
		*   game.set("currentPlayer", player2, {
		*     error: function(gameTurnAgain, error) {
		*       // The set failed validation.
		*     }
		*   });
		*
		*   game.set("finished", true);</pre></p>
		*
		*   game.set("player.score", 10);</pre></p>
		*
		* @param {(string|object)} key The key to set.
		* @param {(string|object)} value The value to give it.
		* @param {object} options A set of options for the set.
		*     The only supported option is <code>error</code>.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		set(key, value, options) {
			let changes = {};
			const newOps = {};
			if (key && typeof key === "object") {
				changes = key;
				options = value;
			} else if (typeof key === "string") changes[key] = value;
			else return this;
			options = options || {};
			let readonly = [];
			if (typeof this.constructor.readOnlyAttributes === "function") readonly = (0, _concat.default)(readonly).call(readonly, this.constructor.readOnlyAttributes());
			for (const k in changes) {
				if (k === "createdAt" || k === "updatedAt") continue;
				if ((0, _indexOf.default)(readonly).call(readonly, k) > -1) throw new Error("Cannot modify readonly attribute: " + k);
				if (options.unset) newOps[k] = new _ParseOp.UnsetOp();
				else if (changes[k] instanceof _ParseOp.Op) newOps[k] = changes[k];
				else if (changes[k] && typeof changes[k] === "object" && typeof changes[k].__op === "string") newOps[k] = (0, _ParseOp.opFromJSON)(changes[k]);
				else if (k === "objectId" || k === "id") {
					if (typeof changes[k] === "string") this.id = changes[k];
				} else if (k === "ACL" && typeof changes[k] === "object" && !(changes[k] instanceof _ParseACL.default)) newOps[k] = new _ParseOp.SetOp(new _ParseACL.default(changes[k]));
				else if (changes[k] instanceof _ParseRelation.default) {
					const relation = new _ParseRelation.default(this, k);
					relation.targetClassName = changes[k].targetClassName;
					newOps[k] = new _ParseOp.SetOp(relation);
				} else newOps[k] = new _ParseOp.SetOp(changes[k]);
			}
			const currentAttributes = this.attributes;
			const newValues = {};
			for (const attr in newOps) if (newOps[attr] instanceof _ParseOp.RelationOp) newValues[attr] = newOps[attr].applyTo(currentAttributes[attr], this, attr);
			else if (!(newOps[attr] instanceof _ParseOp.UnsetOp)) newValues[attr] = newOps[attr].applyTo(currentAttributes[attr]);
			if (!options.ignoreValidation) {
				const validationError = this.validate(newValues);
				if (validationError) throw validationError;
			}
			const pendingOps = this._getPendingOps();
			const last = pendingOps.length - 1;
			const stateController = _CoreManager.default.getObjectStateController();
			for (const attr in newOps) {
				const nextOp = newOps[attr].mergeWith(pendingOps[last][attr]);
				stateController.setPendingOp(this._getStateIdentifier(), attr, nextOp);
			}
			return this;
		}
		/**
		* Remove an attribute from the model. This is a noop if the attribute doesn't
		* exist.
		*
		* @param {string} attr The string name of an attribute.
		* @param options
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		unset(attr, options) {
			options = options || {};
			options.unset = true;
			return this.set(attr, null, options);
		}
		/**
		* Atomically increments the value of the given attribute the next time the
		* object is saved. If no amount is specified, 1 is used by default.
		*
		* @param attr {String} The key.
		* @param amount {Number} The amount to increment by (optional).
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		increment(attr, amount) {
			if (typeof amount === "undefined") amount = 1;
			if (typeof amount !== "number") throw new Error("Cannot increment by a non-numeric amount.");
			return this.set(attr, new _ParseOp.IncrementOp(amount));
		}
		/**
		* Atomically decrements the value of the given attribute the next time the
		* object is saved. If no amount is specified, 1 is used by default.
		*
		* @param attr {String} The key.
		* @param amount {Number} The amount to decrement by (optional).
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		decrement(attr, amount) {
			if (typeof amount === "undefined") amount = 1;
			if (typeof amount !== "number") throw new Error("Cannot decrement by a non-numeric amount.");
			return this.set(attr, new _ParseOp.IncrementOp(amount * -1));
		}
		/**
		* Atomically add an object to the end of the array associated with a given
		* key.
		*
		* @param attr {String} The key.
		* @param item {} The item to add.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		add(attr, item) {
			return this.set(attr, new _ParseOp.AddOp([item]));
		}
		/**
		* Atomically add the objects to the end of the array associated with a given
		* key.
		*
		* @param attr {String} The key.
		* @param items {Object[]} The items to add.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		addAll(attr, items) {
			return this.set(attr, new _ParseOp.AddOp(items));
		}
		/**
		* Atomically add an object to the array associated with a given key, only
		* if it is not already present in the array. The position of the insert is
		* not guaranteed.
		*
		* @param attr {String} The key.
		* @param item {} The object to add.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		addUnique(attr, item) {
			return this.set(attr, new _ParseOp.AddUniqueOp([item]));
		}
		/**
		* Atomically add the objects to the array associated with a given key, only
		* if it is not already present in the array. The position of the insert is
		* not guaranteed.
		*
		* @param attr {String} The key.
		* @param items {Object[]} The objects to add.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		addAllUnique(attr, items) {
			return this.set(attr, new _ParseOp.AddUniqueOp(items));
		}
		/**
		* Atomically remove all instances of an object from the array associated
		* with a given key.
		*
		* @param attr {String} The key.
		* @param item {} The object to remove.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		remove(attr, item) {
			return this.set(attr, new _ParseOp.RemoveOp([item]));
		}
		/**
		* Atomically remove all instances of the objects from the array associated
		* with a given key.
		*
		* @param attr {String} The key.
		* @param items {Object[]} The object to remove.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		removeAll(attr, items) {
			return this.set(attr, new _ParseOp.RemoveOp(items));
		}
		/**
		* Returns an instance of a subclass of Parse.Op describing what kind of
		* modification has been performed on this field since the last time it was
		* saved. For example, after calling object.increment("x"), calling
		* object.op("x") would return an instance of Parse.Op.Increment.
		*
		* @param attr {String} The key.
		* @returns {Parse.Op | undefined} The operation, or undefined if none.
		*/
		op(attr) {
			const pending = this._getPendingOps();
			for (let i = 0; i < pending.length; i += 1) if (pending[i][attr]) return pending[i][attr];
		}
		/**
		* Creates a new model with identical attributes to this one.
		*
		* @returns {Parse.Object}
		*/
		clone() {
			const clone = new this.constructor(this.className);
			let attributes = this.attributes;
			if (typeof this.constructor.readOnlyAttributes === "function") {
				const readonly = this.constructor.readOnlyAttributes() || [];
				const copy = {};
				for (const a in attributes) if ((0, _indexOf.default)(readonly).call(readonly, a) < 0) copy[a] = attributes[a];
				attributes = copy;
			}
			if (clone.set) clone.set(attributes);
			return clone;
		}
		/**
		* Creates a new instance of this object. Not to be confused with clone()
		*
		* @returns {Parse.Object}
		*/
		newInstance() {
			const clone = new this.constructor(this.className);
			clone.id = this.id;
			if (singleInstance) return clone;
			const stateController = _CoreManager.default.getObjectStateController();
			if (stateController) stateController.duplicateState(this._getStateIdentifier(), clone._getStateIdentifier());
			return clone;
		}
		/**
		* Returns true if this object has never been saved to Parse.
		*
		* @returns {boolean}
		*/
		isNew() {
			return !this.id;
		}
		/**
		* Returns true if this object was created by the Parse server when the
		* object might have already been there (e.g. in the case of a Facebook
		* login)
		*
		* @returns {boolean}
		*/
		existed() {
			if (!this.id) return false;
			const state = _CoreManager.default.getObjectStateController().getState(this._getStateIdentifier());
			if (state) return state.existed;
			return false;
		}
		/**
		* Returns true if this object exists on the Server
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise<boolean>} A boolean promise that is fulfilled if object exists.
		*/
		async exists(options) {
			if (!this.id) return false;
			try {
				await new (_CoreManager.default.getParseQuery())(this.className).get(this.id, options);
				return true;
			} catch (e) {
				if (e.code === _ParseError.default.OBJECT_NOT_FOUND) return false;
				throw e;
			}
		}
		/**
		* Checks if the model is currently in a valid state.
		*
		* @returns {boolean}
		*/
		isValid() {
			return !this.validate(this.attributes);
		}
		/**
		* You should not call this function directly unless you subclass
		* <code>Parse.Object</code>, in which case you can override this method
		* to provide additional validation on <code>set</code> and
		* <code>save</code>.  Your implementation should return
		*
		* @param {object} attrs The current data to validate.
		* @returns {Parse.Error|boolean} False if the data is valid.  An error object otherwise.
		* @see Parse.Object#set
		*/
		validate(attrs) {
			if (Object.hasOwn(attrs, "ACL") && !(attrs.ACL instanceof _ParseACL.default)) return new _ParseError.default(_ParseError.default.OTHER_CAUSE, "ACL must be a Parse ACL.");
			for (const key in attrs) if (!/^[A-Za-z_][0-9A-Za-z_.]*$/.test(key)) return new _ParseError.default(_ParseError.default.INVALID_KEY_NAME, "Invalid key name: ".concat(key));
			return false;
		}
		/**
		* Returns the ACL for this object.
		*
		* @returns {Parse.ACL|null} An instance of Parse.ACL.
		* @see Parse.Object#get
		*/
		getACL() {
			const acl = this.get("ACL");
			if (acl instanceof _ParseACL.default) return acl;
			return null;
		}
		/**
		* Sets the ACL to be used for this object.
		*
		* @param {Parse.ACL} acl An instance of Parse.ACL.
		* @param {object} options
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		* @see Parse.Object#set
		*/
		setACL(acl, options) {
			return this.set("ACL", acl, options);
		}
		/**
		* Clears any (or specific) changes to this object made since the last call to save()
		*
		* @param {string} [keys] - specify which fields to revert
		*/
		revert() {
			let keysToRevert;
			for (var _len = arguments.length, keys = new Array(_len), _key = 0; _key < _len; _key++) keys[_key] = arguments[_key];
			if (keys.length) {
				keysToRevert = [];
				for (const key of keys) if (typeof key === "string") keysToRevert.push(key);
				else throw new Error("Parse.Object#revert expects either no, or a list of string, arguments.");
			}
			this._clearPendingOps(keysToRevert);
		}
		/**
		* Clears all attributes on a model
		*
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		clear() {
			const attributes = this.attributes;
			const erasable = {};
			let readonly = ["createdAt", "updatedAt"];
			if (typeof this.constructor.readOnlyAttributes === "function") readonly = (0, _concat.default)(readonly).call(readonly, this.constructor.readOnlyAttributes());
			for (const attr in attributes) if ((0, _indexOf.default)(readonly).call(readonly, attr) < 0) erasable[attr] = true;
			return this.set(erasable, { unset: true });
		}
		/**
		* Fetch the model from the server. If the server's representation of the
		* model differs from its current attributes, they will be overriden.
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>include: The name(s) of the key(s) to include. Can be a string, an array of strings,
		*       or an array of array of strings.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the fetch
		*     completes.
		*/
		fetch(options) {
			const fetchOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().fetch(this, true, fetchOptions);
		}
		/**
		* Fetch the model from the server. If the server's representation of the
		* model differs from its current attributes, they will be overriden.
		*
		* Includes nested Parse.Objects for the provided key. You can use dot
		* notation to specify which fields in the included object are also fetched.
		*
		* @param {string | Array<string | Array<string>>} keys The name(s) of the key(s) to include.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the fetch
		*     completes.
		*/
		fetchWithInclude(keys, options) {
			options = options || {};
			options.include = keys;
			return this.fetch(options);
		}
		/**
		* Saves this object to the server at some unspecified time in the future,
		* even if Parse is currently inaccessible.
		*
		* Use this when you may not have a solid network connection, and don't need to know when the save completes.
		* If there is some problem with the object such that it can't be saved, it will be silently discarded.
		*
		* Objects saved with this method will be stored locally in an on-disk cache until they can be delivered to Parse.
		* They will be sent immediately if possible. Otherwise, they will be sent the next time a network connection is
		* available. Objects saved this way will persist even after the app is closed, in which case they will be sent the
		* next time the app is opened.
		*
		* @param {object} [options]
		* Used to pass option parameters to method if arg1 and arg2 were both passed as strings.
		* Valid options are:
		* <ul>
		* <li>sessionToken: A valid session token, used for making a request on
		* behalf of a specific user.
		* <li>cascadeSave: If `false`, nested objects will not be saved (default is `true`).
		* <li>context: A dictionary that is accessible in Cloud Code `beforeSave` and `afterSave` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the save
		* completes.
		*/
		async saveEventually(options) {
			try {
				await this.save(null, options);
			} catch (e) {
				if (e.code === _ParseError.default.CONNECTION_FAILED) {
					await _CoreManager.default.getEventuallyQueue().save(this, options);
					_CoreManager.default.getEventuallyQueue().poll();
				}
			}
			return this;
		}
		/**
		* Set a hash of model attributes, and save the model to the server.
		* updatedAt will be updated when the request returns.
		* You can either call it as:<pre>
		* object.save();</pre>
		* or<pre>
		* object.save(attrs);</pre>
		* or<pre>
		* object.save(null, options);</pre>
		* or<pre>
		* object.save(attrs, options);</pre>
		* or<pre>
		* object.save(key, value);</pre>
		* or<pre>
		* object.save(key, value, options);</pre>
		*
		* Example 1: <pre>
		* gameTurn.save({
		* player: "Jake Cutter",
		* diceRoll: 2
		* }).then(function(gameTurnAgain) {
		* // The save was successful.
		* }, function(error) {
		* // The save failed.  Error is an instance of Parse.Error.
		* });</pre>
		*
		* Example 2: <pre>
		* gameTurn.save("player", "Jake Cutter");</pre>
		*
		* @param {string | object | null} [arg1]
		* Valid options are:<ul>
		* <li>`Object` - Key/value pairs to update on the object.</li>
		* <li>`String` Key - Key of attribute to update (requires arg2 to also be string)</li>
		* <li>`null` - Passing null for arg1 allows you to save the object with options passed in arg2.</li>
		* </ul>
		* @param {string | object} [arg2]
		* <ul>
		* <li>`String` Value - If arg1 was passed as a key, arg2 is the value that should be set on that key.</li>
		* <li>`Object` Options - Valid options are:
		* <ul>
		* <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		* be used for this request.
		* <li>sessionToken: A valid session token, used for making a request on
		* behalf of a specific user.
		* <li>cascadeSave: If `false`, nested objects will not be saved (default is `true`).
		* <li>context: A dictionary that is accessible in Cloud Code `beforeSave` and `afterSave` triggers.
		* </ul>
		* </li>
		* </ul>
		* @param {object} [arg3]
		* Used to pass option parameters to method if arg1 and arg2 were both passed as strings.
		* Valid options are:
		* <ul>
		* <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		* be used for this request.
		* <li>sessionToken: A valid session token, used for making a request on
		* behalf of a specific user.
		* <li>cascadeSave: If `false`, nested objects will not be saved (default is `true`).
		* <li>context: A dictionary that is accessible in Cloud Code `beforeSave` and `afterSave` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the save
		* completes.
		*/
		async save(arg1, arg2, arg3) {
			let attrs;
			let options;
			if (typeof arg1 === "object" || typeof arg1 === "undefined") {
				attrs = arg1;
				if (typeof arg2 === "object") options = arg2;
			} else {
				attrs = {};
				attrs[arg1] = arg2;
				options = arg3;
			}
			options = options || {};
			if (attrs) this.set(attrs, options);
			const saveOptions = ParseObject._getRequestOptions(options);
			const controller = _CoreManager.default.getObjectController();
			const unsaved = options.cascadeSave !== false ? (0, _unsavedChildren.default)(this) : null;
			if (unsaved && unsaved.length && options.transaction === true && (0, _some.default)(unsaved).call(unsaved, (el) => el instanceof ParseObject)) {
				const unsavedFiles = [];
				const unsavedObjects = [];
				(0, _forEach.default)(unsaved).call(unsaved, (el) => {
					if (el instanceof _ParseFile.default) unsavedFiles.push(el);
					else unsavedObjects.push(el);
				});
				unsavedObjects.push(this);
				return (unsavedFiles.length ? controller.save(unsavedFiles, saveOptions) : _promise.default.resolve()).then(() => controller.save(unsavedObjects, saveOptions)).then((savedOjbects) => savedOjbects.pop());
			}
			await controller.save(unsaved, saveOptions);
			return controller.save(this, saveOptions);
		}
		/**
		* Deletes this object from the server at some unspecified time in the future,
		* even if Parse is currently inaccessible.
		*
		* Use this when you may not have a solid network connection,
		* and don't need to know when the delete completes. If there is some problem with the object
		* such that it can't be deleted, the request will be silently discarded.
		*
		* Delete instructions made with this method will be stored locally in an on-disk cache until they can be transmitted
		* to Parse. They will be sent immediately if possible. Otherwise, they will be sent the next time a network connection
		* is available. Delete requests will persist even after the app is closed, in which case they will be sent the
		* next time the app is opened.
		*
		* @param {object} [options]
		* Valid options are:<ul>
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeDelete` and `afterDelete` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the destroy
		*     completes.
		*/
		async destroyEventually(options) {
			try {
				await this.destroy(options);
			} catch (e) {
				if (e.code === _ParseError.default.CONNECTION_FAILED) {
					await _CoreManager.default.getEventuallyQueue().destroy(this, options);
					_CoreManager.default.getEventuallyQueue().poll();
				}
			}
			return this;
		}
		/**
		* Destroy this model on the server if it was already persisted.
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeDelete` and `afterDelete` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the destroy
		*     completes.
		*/
		destroy(options) {
			if (!this.id) return _promise.default.resolve(void 0);
			const destroyOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().destroy(this, destroyOptions);
		}
		/**
		* Asynchronously stores the object and every object it points to in the local datastore,
		* recursively, using a default pin name: _default.
		*
		* If those other objects have not been fetched from Parse, they will not be stored.
		* However, if they have changed data, all the changes will be retained.
		*
		* <pre>
		* await object.pin();
		* </pre>
		*
		* To retrieve object:
		* <code>query.fromLocalDatastore()</code> or <code>query.fromPin()</code>
		*
		* @returns {Promise} A promise that is fulfilled when the pin completes.
		*/
		pin() {
			return ParseObject.pinAllWithName(_LocalDatastoreUtils.DEFAULT_PIN, [this]);
		}
		/**
		* Asynchronously removes the object and every object it points to in the local datastore,
		* recursively, using a default pin name: _default.
		*
		* <pre>
		* await object.unPin();
		* </pre>
		*
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		*/
		unPin() {
			return ParseObject.unPinAllWithName(_LocalDatastoreUtils.DEFAULT_PIN, [this]);
		}
		/**
		* Asynchronously returns if the object is pinned
		*
		* <pre>
		* const isPinned = await object.isPinned();
		* </pre>
		*
		* @returns {Promise<boolean>} A boolean promise that is fulfilled if object is pinned.
		*/
		async isPinned() {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			const objectKey = localDatastore.getKeyForObject(this);
			return (await localDatastore.fromPinWithName(objectKey)).length > 0;
		}
		/**
		* Asynchronously stores the objects and every object they point to in the local datastore, recursively.
		*
		* If those other objects have not been fetched from Parse, they will not be stored.
		* However, if they have changed data, all the changes will be retained.
		*
		* <pre>
		* await object.pinWithName(name);
		* </pre>
		*
		* To retrieve object:
		* <code>query.fromLocalDatastore()</code> or <code>query.fromPinWithName(name)</code>
		*
		* @param {string} name Name of Pin.
		* @returns {Promise} A promise that is fulfilled when the pin completes.
		*/
		pinWithName(name) {
			return ParseObject.pinAllWithName(name, [this]);
		}
		/**
		* Asynchronously removes the object and every object it points to in the local datastore, recursively.
		*
		* <pre>
		* await object.unPinWithName(name);
		* </pre>
		*
		* @param {string} name Name of Pin.
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		*/
		unPinWithName(name) {
			return ParseObject.unPinAllWithName(name, [this]);
		}
		/**
		* Asynchronously loads data from the local datastore into this object.
		*
		* <pre>
		* await object.fetchFromLocalDatastore();
		* </pre>
		*
		* You can create an unfetched pointer with <code>Parse.Object.createWithoutData()</code>
		* and then call <code>fetchFromLocalDatastore()</code> on it.
		*
		* @returns {Promise} A promise that is fulfilled when the fetch completes.
		*/
		async fetchFromLocalDatastore() {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) throw new Error("Parse.enableLocalDatastore() must be called first");
			const objectKey = localDatastore.getKeyForObject(this);
			const pinned = await localDatastore._serializeObject(objectKey);
			if (!pinned) throw new Error("Cannot fetch an unsaved ParseObject");
			const result = ParseObject.fromJSON(pinned);
			this._finishFetch(result.toJSON());
			return this;
		}
		static _clearAllState() {
			_CoreManager.default.getObjectStateController().clearAllState();
		}
		/**
		* Fetches the given list of Parse.Object.
		* If any error is encountered, stops and calls the error handler.
		*
		* <pre>
		*   Parse.Object.fetchAll([object1, object2, ...])
		*    .then((list) => {
		*      // All the objects were fetched.
		*    }, (error) => {
		*      // An error occurred while fetching one of the objects.
		*    });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>include: The name(s) of the key(s) to include. Can be a string, an array of strings,
		*       or an array of array of strings.
		* </ul>
		* @static
		* @returns {Parse.Object[]}
		*/
		static fetchAll(list, options) {
			const fetchOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().fetch(list, true, fetchOptions);
		}
		/**
		* Fetches the given list of Parse.Object.
		*
		* Includes nested Parse.Objects for the provided key. You can use dot
		* notation to specify which fields in the included object are also fetched.
		*
		* If any error is encountered, stops and calls the error handler.
		*
		* <pre>
		*   Parse.Object.fetchAllWithInclude([object1, object2, ...], [pointer1, pointer2, ...])
		*    .then((list) => {
		*      // All the objects were fetched.
		*    }, (error) => {
		*      // An error occurred while fetching one of the objects.
		*    });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {string | Array<string | Array<string>>} keys The name(s) of the key(s) to include.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @static
		* @returns {Parse.Object[]}
		*/
		static fetchAllWithInclude(list, keys, options) {
			options = options || {};
			options.include = keys;
			return ParseObject.fetchAll(list, options);
		}
		/**
		* Fetches the given list of Parse.Object if needed.
		* If any error is encountered, stops and calls the error handler.
		*
		* Includes nested Parse.Objects for the provided key. You can use dot
		* notation to specify which fields in the included object are also fetched.
		*
		* If any error is encountered, stops and calls the error handler.
		*
		* <pre>
		*   Parse.Object.fetchAllIfNeededWithInclude([object1, object2, ...], [pointer1, pointer2, ...])
		*    .then((list) => {
		*      // All the objects were fetched.
		*    }, (error) => {
		*      // An error occurred while fetching one of the objects.
		*    });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {string | Array<string | Array<string>>} keys The name(s) of the key(s) to include.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @static
		* @returns {Parse.Object[]}
		*/
		static fetchAllIfNeededWithInclude(list, keys, options) {
			options = options || {};
			options.include = keys;
			return ParseObject.fetchAllIfNeeded(list, options);
		}
		/**
		* Fetches the given list of Parse.Object if needed.
		* If any error is encountered, stops and calls the error handler.
		*
		* <pre>
		*   Parse.Object.fetchAllIfNeeded([object1, ...])
		*    .then((list) => {
		*      // Objects were fetched and updated.
		*    }, (error) => {
		*      // An error occurred while fetching one of the objects.
		*    });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>include: The name(s) of the key(s) to include. Can be a string, an array of strings,
		*       or an array of array of strings.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		* </ul>
		* @static
		* @returns {Parse.Object[]}
		*/
		static fetchAllIfNeeded(list, options) {
			const fetchOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().fetch(list, false, fetchOptions);
		}
		static handleIncludeOptions(options) {
			let include = [];
			if ((0, _isArray.default)(options.include)) {
				var _context3;
				(0, _forEach.default)(_context3 = options.include).call(_context3, (key) => {
					if ((0, _isArray.default)(key)) include = (0, _concat.default)(include).call(include, key);
					else include.push(key);
				});
			} else include.push(options.include);
			return include;
		}
		/**
		* Destroy the given list of models on the server if it was already persisted.
		*
		* <p>Unlike saveAll, if an error occurs while deleting an individual model,
		* this method will continue trying to delete the rest of the models if
		* possible, except in the case of a fatal error like a connection error.
		*
		* <p>In particular, the Parse.Error object returned in the case of error may
		* be one of two types:
		*
		* <ul>
		* <li>A Parse.Error.AGGREGATE_ERROR. This object's "errors" property is an
		* array of other Parse.Error objects. Each error object in this array
		* has an "object" property that references the object that could not be
		* deleted (for instance, because that object could not be found).</li>
		* <li>A non-aggregate Parse.Error. This indicates a serious error that
		* caused the delete operation to be aborted partway through (for
		* instance, a connection failure in the middle of the delete).</li>
		* </ul>
		*
		* <pre>
		* Parse.Object.destroyAll([object1, object2, ...])
		* .then((list) => {
		* // All the objects were deleted.
		* }, (error) => {
		* // An error occurred while deleting one or more of the objects.
		* // If this is an aggregate error, then we can inspect each error
		* // object individually to determine the reason why a particular
		* // object was not deleted.
		* if (error.code === Parse.Error.AGGREGATE_ERROR) {
		* for (var i = 0; i < error.errors.length; i++) {
		* console.log("Couldn't delete " + error.errors[i].object.id +
		* "due to " + error.errors[i].message);
		* }
		* } else {
		* console.log("Delete aborted because of " + error.message);
		* }
		* });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeDelete` and `afterDelete` triggers.
		*   <li>transaction: Set to true to enable transactions
		*   <li>batchSize: How many objects to yield in each batch (default: 20)
		* </ul>
		* @static
		* @returns {Promise} A promise that is fulfilled when the destroyAll
		* completes.
		*/
		static destroyAll(list, options) {
			const destroyOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().destroy(list, destroyOptions);
		}
		/**
		* Saves the given list of Parse.Object.
		* If any error is encountered, stops and calls the error handler.
		*
		* <pre>
		* Parse.Object.saveAll([object1, object2, ...])
		* .then((list) => {
		* // All the objects were saved.
		* }, (error) => {
		* // An error occurred while saving one of the objects.
		* });
		* </pre>
		*
		* @param {Array} list A list of <code>Parse.Object</code>.
		* @param {object} options
		* Valid options are:
		* <ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*        be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>cascadeSave: If `false`, nested objects will not be saved (default is `true`).
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeSave` and `afterSave` triggers.
		*   <li>transaction: Set to true to enable transactions
		*   <li>batchSize: How many objects to yield in each batch (default: 20)
		* </ul>
		* @static
		* @returns {Parse.Object[]}
		*/
		static saveAll(list, options) {
			const saveOptions = ParseObject._getRequestOptions(options);
			return _CoreManager.default.getObjectController().save(list, saveOptions);
		}
		/**
		* Creates a reference to a subclass of Parse.Object with the given id. This
		* does not exist on Parse.Object, only on subclasses.
		*
		* <p>A shortcut for: <pre>
		*  var Foo = Parse.Object.extend("Foo");
		*  var pointerToFoo = new Foo();
		*  pointerToFoo.id = "myObjectId";
		* </pre>
		*
		* @param {string} id The ID of the object to create a reference to.
		* @static
		* @returns {Parse.Object} A Parse.Object reference.
		*/
		static createWithoutData(id) {
			const obj = new this();
			obj.id = id;
			return obj;
		}
		/**
		* Creates a new instance of a Parse Object from a JSON representation.
		*
		* @param {object} json The JSON map of the Object's data
		* @param {boolean} override In single instance mode, all old server data
		*   is overwritten if this is set to true
		* @param {boolean} dirty Whether the Parse.Object should set JSON keys to dirty
		* @static
		* @returns {Parse.Object} A Parse.Object reference
		*/
		static fromJSON(json, override, dirty) {
			if (!json.className) throw new Error("Cannot create an object without a className");
			const constructor = classMap[json.className];
			const o = constructor ? new constructor(json.className) : new ParseObject(json.className);
			const otherAttributes = {};
			for (const attr in json) if (attr !== "className" && attr !== "__type") {
				otherAttributes[attr] = json[attr];
				if (dirty) o.set(attr, json[attr]);
			}
			if (override) {
				if (otherAttributes.objectId) o.id = otherAttributes.objectId;
				let preserved = null;
				if (typeof o._preserveFieldsOnFetch === "function") preserved = o._preserveFieldsOnFetch();
				o._clearServerData();
				if (preserved) o._finishFetch(preserved);
			}
			o._finishFetch(otherAttributes);
			if (json.objectId) o._setExisted(true);
			return o;
		}
		/**
		* Registers a subclass of Parse.Object with a specific class name.
		* When objects of that class are retrieved from a query, they will be
		* instantiated with this subclass.
		* This is only necessary when using ES6 subclassing.
		*
		* @param {string} className The class name of the subclass
		* @param {Function} constructor The subclass
		*/
		static registerSubclass(className, constructor) {
			if (typeof className !== "string") throw new TypeError("The first argument must be a valid class name.");
			if (typeof constructor === "undefined") throw new TypeError("You must supply a subclass constructor.");
			if (typeof constructor !== "function") throw new TypeError("You must register the subclass constructor. Did you attempt to register an instance of the subclass?");
			classMap[className] = constructor;
			if (!constructor.className) constructor.className = className;
		}
		/**
		* Unegisters a subclass of Parse.Object with a specific class name.
		*
		* @param {string} className The class name of the subclass
		*/
		static unregisterSubclass(className) {
			if (typeof className !== "string") throw new TypeError("The first argument must be a valid class name.");
			delete classMap[className];
		}
		/**
		* Creates a new subclass of Parse.Object for the given Parse class name.
		*
		* <p>Every extension of a Parse class will inherit from the most recent
		* previous extension of that class. When a Parse.Object is automatically
		* created by parsing JSON, it will use the most recent extension of that
		* class.</p>
		*
		* <p>You should call either:<pre>
		*     var MyClass = Parse.Object.extend("MyClass", {
		*         <i>Instance methods</i>,
		*         initialize: function(attrs, options) {
		*             this.someInstanceProperty = [],
		*             <i>Other instance properties</i>
		*         }
		*     }, {
		*         <i>Class properties</i>
		*     });</pre>
		* or, for Backbone compatibility:<pre>
		*     var MyClass = Parse.Object.extend({
		*         className: "MyClass",
		*         <i>Instance methods</i>,
		*         initialize: function(attrs, options) {
		*             this.someInstanceProperty = [],
		*             <i>Other instance properties</i>
		*         }
		*     }, {
		*         <i>Class properties</i>
		*     });</pre></p>
		*
		* @param {string} className The name of the Parse class backing this model.
		* @param {object} [protoProps] Instance properties to add to instances of the
		*     class returned from this method.
		* @param {object} [classProps] Class properties to add the class returned from
		*     this method.
		* @returns {Parse.Object} A new subclass of Parse.Object.
		*/
		static extend(className, protoProps, classProps) {
			if (typeof className !== "string") {
				if (className && typeof className.className === "string") return ParseObject.extend(className.className, className, protoProps);
				else throw new Error("Parse.Object.extend's first argument should be the className.");
			}
			let adjustedClassName = className;
			if (adjustedClassName === "User" && _CoreManager.default.get("PERFORM_USER_REWRITE")) adjustedClassName = "_User";
			let parentProto = ParseObject.prototype;
			if (Object.hasOwn(this, "__super__") && this.__super__) parentProto = this.prototype;
			let ParseObjectSubclass = function(attributes, options) {
				this.className = adjustedClassName;
				this._objCount = objectCount++;
				if (typeof this.initialize === "function") this.initialize.apply(this, arguments);
				if (this._initializers) for (const initializer of this._initializers) initializer.apply(this, arguments);
				if (attributes && typeof attributes === "object") try {
					this.set(attributes || {}, options);
				} catch (_) {
					throw new Error("Can't create an invalid Parse Object");
				}
			};
			if (classMap[adjustedClassName]) ParseObjectSubclass = classMap[adjustedClassName];
			else {
				ParseObjectSubclass.extend = function(name, protoProps, classProps) {
					if (typeof name === "string") return ParseObject.extend.call(ParseObjectSubclass, name, protoProps, classProps);
					return ParseObject.extend.call(ParseObjectSubclass, adjustedClassName, name, protoProps);
				};
				ParseObjectSubclass.createWithoutData = ParseObject.createWithoutData;
				ParseObjectSubclass.className = adjustedClassName;
				ParseObjectSubclass.__super__ = parentProto;
				ParseObjectSubclass.prototype = (0, _create.default)(parentProto, { constructor: {
					value: ParseObjectSubclass,
					enumerable: false,
					writable: true,
					configurable: true
				} });
			}
			if (protoProps) for (const prop in protoProps) {
				if (prop === "initialize") {
					(0, _defineProperty3.default)(ParseObjectSubclass.prototype, "_initializers", {
						value: [...ParseObjectSubclass.prototype._initializers || [], protoProps[prop]],
						enumerable: false,
						writable: true,
						configurable: true
					});
					continue;
				}
				if (prop !== "className") (0, _defineProperty3.default)(ParseObjectSubclass.prototype, prop, {
					value: protoProps[prop],
					enumerable: false,
					writable: true,
					configurable: true
				});
			}
			if (classProps) {
				for (const prop in classProps) if (prop !== "className") (0, _defineProperty3.default)(ParseObjectSubclass, prop, {
					value: classProps[prop],
					enumerable: false,
					writable: true,
					configurable: true
				});
			}
			classMap[adjustedClassName] = ParseObjectSubclass;
			return ParseObjectSubclass;
		}
		/**
		* Enable single instance objects, where any local objects with the same Id
		* share the same attributes, and stay synchronized with each other.
		* This is disabled by default in server environments, since it can lead to
		* security issues.
		*
		* @static
		*/
		static enableSingleInstance() {
			singleInstance = true;
			_CoreManager.default.setObjectStateController(SingleInstanceStateController);
		}
		/**
		* Disable single instance objects, where any local objects with the same Id
		* share the same attributes, and stay synchronized with each other.
		* When disabled, you can have two instances of the same object in memory
		* without them sharing attributes.
		*
		* @static
		*/
		static disableSingleInstance() {
			singleInstance = false;
			_CoreManager.default.setObjectStateController(UniqueInstanceStateController);
		}
		/**
		* Asynchronously stores the objects and every object they point to in the local datastore,
		* recursively, using a default pin name: _default.
		*
		* If those other objects have not been fetched from Parse, they will not be stored.
		* However, if they have changed data, all the changes will be retained.
		*
		* <pre>
		* await Parse.Object.pinAll([...]);
		* </pre>
		*
		* To retrieve object:
		* <code>query.fromLocalDatastore()</code> or <code>query.fromPin()</code>
		*
		* @param {Array} objects A list of <code>Parse.Object</code>.
		* @returns {Promise} A promise that is fulfilled when the pin completes.
		* @static
		*/
		static pinAll(objects) {
			if (!_CoreManager.default.getLocalDatastore().isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return ParseObject.pinAllWithName(_LocalDatastoreUtils.DEFAULT_PIN, objects);
		}
		/**
		* Asynchronously stores the objects and every object they point to in the local datastore, recursively.
		*
		* If those other objects have not been fetched from Parse, they will not be stored.
		* However, if they have changed data, all the changes will be retained.
		*
		* <pre>
		* await Parse.Object.pinAllWithName(name, [obj1, obj2, ...]);
		* </pre>
		*
		* To retrieve object:
		* <code>query.fromLocalDatastore()</code> or <code>query.fromPinWithName(name)</code>
		*
		* @param {string} name Name of Pin.
		* @param {Array} objects A list of <code>Parse.Object</code>.
		* @returns {Promise} A promise that is fulfilled when the pin completes.
		* @static
		*/
		static pinAllWithName(name, objects) {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return localDatastore._handlePinAllWithName(name, objects);
		}
		/**
		* Asynchronously removes the objects and every object they point to in the local datastore,
		* recursively, using a default pin name: _default.
		*
		* <pre>
		* await Parse.Object.unPinAll([...]);
		* </pre>
		*
		* @param {Array} objects A list of <code>Parse.Object</code>.
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		* @static
		*/
		static unPinAll(objects) {
			if (!_CoreManager.default.getLocalDatastore().isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return ParseObject.unPinAllWithName(_LocalDatastoreUtils.DEFAULT_PIN, objects);
		}
		/**
		* Asynchronously removes the objects and every object they point to in the local datastore, recursively.
		*
		* <pre>
		* await Parse.Object.unPinAllWithName(name, [obj1, obj2, ...]);
		* </pre>
		*
		* @param {string} name Name of Pin.
		* @param {Array} objects A list of <code>Parse.Object</code>.
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		* @static
		*/
		static unPinAllWithName(name, objects) {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return localDatastore._handleUnPinAllWithName(name, objects);
		}
		/**
		* Asynchronously removes all objects in the local datastore using a default pin name: _default.
		*
		* <pre>
		* await Parse.Object.unPinAllObjects();
		* </pre>
		*
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		* @static
		*/
		static unPinAllObjects() {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return localDatastore.unPinWithName(_LocalDatastoreUtils.DEFAULT_PIN);
		}
		/**
		* Asynchronously removes all objects with the specified pin name.
		* Deletes the pin name also.
		*
		* <pre>
		* await Parse.Object.unPinAllObjectsWithName(name);
		* </pre>
		*
		* @param {string} name Name of Pin.
		* @returns {Promise} A promise that is fulfilled when the unPin completes.
		* @static
		*/
		static unPinAllObjectsWithName(name) {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if (!localDatastore.isEnabled) return _promise.default.reject("Parse.enableLocalDatastore() must be called first");
			return localDatastore.unPinWithName(_LocalDatastoreUtils.PIN_PREFIX + name);
		}
	};
	var DefaultController = {
		fetch(target, forceFetch, options) {
			const localDatastore = _CoreManager.default.getLocalDatastore();
			if ((0, _isArray.default)(target)) {
				if (target.length < 1) return _promise.default.resolve([]);
				const objs = [];
				const ids = [];
				let className = null;
				const results = [];
				let error = null;
				(0, _forEach.default)(target).call(target, (el) => {
					if (error) return;
					if (!className) className = el.className;
					if (className !== el.className) error = new _ParseError.default(_ParseError.default.INVALID_CLASS_NAME, "All objects should be of the same class");
					if (!el.id) error = new _ParseError.default(_ParseError.default.MISSING_OBJECT_ID, "All objects must have an ID");
					if (forceFetch || !el.isDataAvailable()) {
						ids.push(el.id);
						objs.push(el);
					}
					results.push(el);
				});
				if (error) return _promise.default.reject(error);
				const query = new (_CoreManager.default.getParseQuery())(className);
				query.containedIn("objectId", ids);
				if (options && options.include) query.include(options.include);
				query._limit = ids.length;
				return (0, _find.default)(query).call(query, options).then(async (objects) => {
					const idMap = {};
					(0, _forEach.default)(objects).call(objects, (o) => {
						idMap[o.id] = o;
					});
					for (let i = 0; i < objs.length; i++) {
						const obj = objs[i];
						if (!obj || !obj.id || !idMap[obj.id]) {
							if (forceFetch) return _promise.default.reject(new _ParseError.default(_ParseError.default.OBJECT_NOT_FOUND, "All objects must exist on the server."));
						}
					}
					if (!singleInstance) for (let i = 0; i < results.length; i++) {
						const obj = results[i];
						if (obj && obj.id && idMap[obj.id]) {
							const id = obj.id;
							obj._finishFetch(idMap[id].toJSON());
							results[i] = idMap[id];
						}
					}
					for (const object of results) await localDatastore._updateObjectIfPinned(object);
					return _promise.default.resolve(results);
				});
			} else if (target instanceof ParseObject) {
				if (!target.id) return _promise.default.reject(new _ParseError.default(_ParseError.default.MISSING_OBJECT_ID, "Object does not have an ID"));
				const RESTController = _CoreManager.default.getRESTController();
				const params = {};
				if (options && options.include) params.include = options.include.join();
				return RESTController.request("GET", "classes/" + target.className + "/" + target._getId(), params, options).then(async (response) => {
					target._clearPendingOps();
					target._clearServerData();
					target._finishFetch(response);
					await localDatastore._updateObjectIfPinned(target);
					return target;
				});
			}
			return _promise.default.resolve(void 0);
		},
		async destroy(target, options) {
			if (options && options.batchSize && options.transaction) throw new _ParseError.default(_ParseError.default.OTHER_CAUSE, "You cannot use both transaction and batchSize options simultaneously.");
			let batchSize = options && options.batchSize ? options.batchSize : _CoreManager.default.get("REQUEST_BATCH_SIZE");
			const localDatastore = _CoreManager.default.getLocalDatastore();
			const RESTController = _CoreManager.default.getRESTController();
			if ((0, _isArray.default)(target)) {
				if (options && options.transaction && target.length > 1) batchSize = target.length;
				if (target.length < 1) return _promise.default.resolve([]);
				const batches = [[]];
				(0, _forEach.default)(target).call(target, (obj) => {
					if (!obj.id) return;
					batches[batches.length - 1].push(obj);
					if (batches[batches.length - 1].length >= batchSize) batches.push([]);
				});
				if (batches[batches.length - 1].length === 0) batches.pop();
				let deleteCompleted = _promise.default.resolve();
				const errors = [];
				(0, _forEach.default)(batches).call(batches, (batch) => {
					const requests = (0, _map.default)(batch).call(batch, (obj) => {
						return {
							method: "DELETE",
							path: getServerUrlPath() + "classes/" + obj.className + "/" + obj._getId(),
							body: {}
						};
					});
					const body = options && options.transaction && requests.length > 1 ? {
						requests,
						transaction: true
					} : { requests };
					deleteCompleted = deleteCompleted.then(() => {
						return RESTController.request("POST", "batch", body, options).then((results) => {
							for (let i = 0; i < results.length; i++) if (results[i] && Object.hasOwn(results[i], "error")) {
								const err = new _ParseError.default(results[i].error.code, results[i].error.error);
								err.object = batch[i];
								errors.push(err);
							}
						});
					});
				});
				return deleteCompleted.then(async () => {
					if (errors.length) {
						const aggregate = new _ParseError.default(_ParseError.default.AGGREGATE_ERROR);
						aggregate.errors = errors;
						return _promise.default.reject(aggregate);
					}
					for (const object of target) await localDatastore._destroyObjectIfPinned(object);
					return _promise.default.resolve(target);
				});
			} else if (target instanceof ParseObject) return RESTController.request("DELETE", "classes/" + target.className + "/" + target._getId(), {}, options).then(async () => {
				await localDatastore._destroyObjectIfPinned(target);
				return _promise.default.resolve(target);
			});
			return _promise.default.resolve(target);
		},
		save(target, options) {
			if (options && options.batchSize && options.transaction) return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "You cannot use both transaction and batchSize options simultaneously."));
			let batchSize = options && options.batchSize ? options.batchSize : _CoreManager.default.get("REQUEST_BATCH_SIZE");
			const localDatastore = _CoreManager.default.getLocalDatastore();
			const mapIdForPin = {};
			const RESTController = _CoreManager.default.getRESTController();
			const stateController = _CoreManager.default.getObjectStateController();
			const allowCustomObjectId = _CoreManager.default.get("ALLOW_CUSTOM_OBJECT_ID");
			options = options || {};
			options.returnStatus = options.returnStatus || true;
			if ((0, _isArray.default)(target)) {
				if (target.length < 1) return _promise.default.resolve([]);
				let unsaved = (0, _concat.default)(target).call(target);
				for (let i = 0; i < target.length; i++) {
					const target_i = target[i];
					if (target_i instanceof ParseObject) unsaved = (0, _concat.default)(unsaved).call(unsaved, (0, _unsavedChildren.default)(target_i, true));
				}
				unsaved = (0, _unique.default)(unsaved);
				const filesSaved = [];
				let pending = [];
				(0, _forEach.default)(unsaved).call(unsaved, (el) => {
					if (el instanceof _ParseFile.default) filesSaved.push(el.save(options));
					else if (el instanceof ParseObject) pending.push(el);
				});
				if (options && options.transaction && pending.length > 1) {
					if ((0, _some.default)(pending).call(pending, (el) => !(0, _canBeSerialized.default)(el))) return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Tried to save a transactional batch containing an object with unserializable attributes."));
					batchSize = pending.length;
				}
				return _promise.default.all(filesSaved).then(() => {
					let objectError = null;
					return (0, _promiseUtils.continueWhile)(() => {
						return pending.length > 0;
					}, () => {
						const batch = [];
						const nextPending = [];
						(0, _forEach.default)(pending).call(pending, (el) => {
							if (allowCustomObjectId && Object.hasOwn(el, "id") && !el.id) throw new _ParseError.default(_ParseError.default.MISSING_OBJECT_ID, "objectId must not be empty or null");
							if (batch.length < batchSize && (0, _canBeSerialized.default)(el)) batch.push(el);
							else nextPending.push(el);
						});
						pending = nextPending;
						if (batch.length < 1) return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Tried to save a batch with a cycle."));
						const batchReturned = (0, _promiseUtils.resolvingPromise)();
						const batchReady = [];
						const batchTasks = [];
						(0, _forEach.default)(batch).call(batch, (obj, index) => {
							const ready = (0, _promiseUtils.resolvingPromise)();
							batchReady.push(ready);
							stateController.pushPendingState(obj._getStateIdentifier());
							batchTasks.push(stateController.enqueueTask(obj._getStateIdentifier(), function() {
								ready.resolve();
								return batchReturned.then((responses) => {
									if (Object.hasOwn(responses[index], "success")) {
										const objectId = responses[index].success.objectId;
										const status = responses[index]._status;
										delete responses[index]._status;
										delete responses[index]._headers;
										mapIdForPin[objectId] = obj._localId;
										obj._handleSaveResponse(responses[index].success, status);
									} else {
										if (!objectError && Object.hasOwn(responses[index], "error")) {
											const serverError = responses[index].error;
											objectError = new _ParseError.default(serverError.code, serverError.error);
											pending = [];
										}
										obj._handleSaveError();
									}
								});
							}));
						});
						(0, _promiseUtils.when)(batchReady).then(() => {
							const requests = (0, _map.default)(batch).call(batch, (obj) => {
								const params = obj._getSaveParams();
								params.path = getServerUrlPath() + params.path;
								return params;
							});
							const body = options && options.transaction && requests.length > 1 ? {
								requests,
								transaction: true
							} : { requests };
							return RESTController.request("POST", "batch", body, options);
						}).then(batchReturned.resolve, (error) => {
							batchReturned.reject(new _ParseError.default(_ParseError.default.INCORRECT_TYPE, error.message));
						});
						return (0, _promiseUtils.when)(batchTasks);
					}).then(async () => {
						if (objectError) return _promise.default.reject(objectError);
						for (const object of target) if (object instanceof ParseObject) {
							await localDatastore._updateLocalIdForObject(mapIdForPin[object.id], object);
							await localDatastore._updateObjectIfPinned(object);
						}
						return _promise.default.resolve(target);
					});
				});
			} else if (target instanceof ParseObject) {
				if (allowCustomObjectId && Object.hasOwn(target, "id") && !target.id) throw new _ParseError.default(_ParseError.default.MISSING_OBJECT_ID, "objectId must not be empty or null");
				target._getId();
				const localId = target._localId;
				const targetCopy = target;
				const task = function() {
					const params = targetCopy._getSaveParams();
					return RESTController.request(params.method, params.path, params.body, options).then((response) => {
						const status = response._status;
						delete response._status;
						delete response._headers;
						targetCopy._handleSaveResponse(response, status);
					}, (error) => {
						targetCopy._handleSaveError();
						return _promise.default.reject(error);
					});
				};
				stateController.pushPendingState(target._getStateIdentifier());
				return stateController.enqueueTask(target._getStateIdentifier(), task).then(async () => {
					await localDatastore._updateLocalIdForObject(localId, target);
					await localDatastore._updateObjectIfPinned(target);
					return target;
				}, (error) => {
					return _promise.default.reject(error);
				});
			}
			return _promise.default.resolve(void 0);
		}
	};
	_CoreManager.default.setParseObject(ParseObject);
	_CoreManager.default.setObjectController(DefaultController);
	exports.default = ParseObject;
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/keys.js
var require_keys$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	module.exports = require_get_built_in_prototype_method()("Array", "keys");
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/virtual/keys.js
var require_keys$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_keys$3();
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/keys.js
var require_keys$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_web_dom_collections_iterator();
	var classof = require_classof();
	var hasOwn = require_has_own_property();
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_keys$2();
	var ArrayPrototype = Array.prototype;
	var DOMIterables = {
		DOMTokenList: true,
		NodeList: true
	};
	module.exports = function(it) {
		var own = it.keys;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.keys || hasOwn(DOMIterables, classof(it)) ? method : own;
	};
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/keys.js
var require_keys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_keys$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-sort.js
var require_array_sort = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var arraySlice = require_array_slice();
	var floor = Math.floor;
	var sort = function(array, comparefn) {
		var length = array.length;
		if (length < 8) {
			var i = 1;
			var element, j;
			while (i < length) {
				j = i;
				element = array[i];
				while (j && comparefn(array[j - 1], element) > 0) array[j] = array[--j];
				if (j !== i++) array[j] = element;
			}
		} else {
			var middle = floor(length / 2);
			var left = sort(arraySlice(array, 0, middle), comparefn);
			var right = sort(arraySlice(array, middle), comparefn);
			var llength = left.length;
			var rlength = right.length;
			var lindex = 0;
			var rindex = 0;
			while (lindex < llength || rindex < rlength) array[lindex + rindex] = lindex < llength && rindex < rlength ? comparefn(left[lindex], right[rindex]) <= 0 ? left[lindex++] : right[rindex++] : lindex < llength ? left[lindex++] : right[rindex++];
		}
		return array;
	};
	module.exports = sort;
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-ff-version.js
var require_environment_ff_version = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var firefox = require_environment_user_agent().match(/firefox\/(\d+)/i);
	module.exports = !!firefox && +firefox[1];
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-is-ie-or-edge.js
var require_environment_is_ie_or_edge = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var UA = require_environment_user_agent();
	module.exports = /MSIE|Trident/.test(UA);
}));
//#endregion
//#region node_modules/core-js-pure/internals/environment-webkit-version.js
var require_environment_webkit_version = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var webkit = require_environment_user_agent().match(/AppleWebKit\/(\d+)\./);
	module.exports = !!webkit && +webkit[1];
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.sort.js
var require_es_array_sort = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this();
	var aCallable = require_a_callable();
	var toObject = require_to_object();
	var lengthOfArrayLike = require_length_of_array_like();
	var deletePropertyOrThrow = require_delete_property_or_throw();
	var toString = require_to_string();
	var fails = require_fails();
	var internalSort = require_array_sort();
	var arrayMethodIsStrict = require_array_method_is_strict();
	var FF = require_environment_ff_version();
	var IE_OR_EDGE = require_environment_is_ie_or_edge();
	var V8 = require_environment_v8_version();
	var WEBKIT = require_environment_webkit_version();
	var test = [];
	var nativeSort = uncurryThis(test.sort);
	var push = uncurryThis(test.push);
	var FAILS_ON_UNDEFINED = fails(function() {
		test.sort(void 0);
	});
	var FAILS_ON_NULL = fails(function() {
		test.sort(null);
	});
	var STRICT_METHOD = arrayMethodIsStrict("sort");
	var STABLE_SORT = !fails(function() {
		if (V8) return V8 < 70;
		if (FF && FF > 3) return;
		if (IE_OR_EDGE) return true;
		if (WEBKIT) return WEBKIT < 603;
		var result = "";
		var code, chr, value, index;
		for (code = 65; code < 76; code++) {
			chr = String.fromCharCode(code);
			switch (code) {
				case 66:
				case 69:
				case 70:
				case 72:
					value = 3;
					break;
				case 68:
				case 71:
					value = 4;
					break;
				default: value = 2;
			}
			for (index = 0; index < 47; index++) test.push({
				k: chr + index,
				v: value
			});
		}
		test.sort(function(a, b) {
			return b.v - a.v;
		});
		for (index = 0; index < test.length; index++) {
			chr = test[index].k.charAt(0);
			if (result.charAt(result.length - 1) !== chr) result += chr;
		}
		return result !== "DGBEFHACIJK";
	});
	var FORCED = FAILS_ON_UNDEFINED || !FAILS_ON_NULL || !STRICT_METHOD || !STABLE_SORT;
	var getSortCompare = function(comparefn) {
		return function(x, y) {
			if (y === void 0) return -1;
			if (x === void 0) return 1;
			if (comparefn !== void 0) return +comparefn(x, y) || 0;
			var xString = toString(x);
			var yString = toString(y);
			return xString === yString ? 0 : xString > yString ? 1 : -1;
		};
	};
	$({
		target: "Array",
		proto: true,
		forced: FORCED
	}, { sort: function sort(comparefn) {
		if (comparefn !== void 0) aCallable(comparefn);
		var array = toObject(this);
		if (STABLE_SORT) return comparefn === void 0 ? nativeSort(array) : nativeSort(array, comparefn);
		var items = [];
		var arrayLength = lengthOfArrayLike(array);
		var itemsLength, index;
		for (index = 0; index < arrayLength; index++) if (index in array) push(items, array[index]);
		internalSort(items, getSortCompare(comparefn));
		itemsLength = lengthOfArrayLike(items);
		index = 0;
		while (index < itemsLength) array[index] = items[index++];
		while (index < arrayLength) deletePropertyOrThrow(array, index++);
		return array;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/sort.js
var require_sort$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_sort();
	module.exports = require_get_built_in_prototype_method()("Array", "sort");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/sort.js
var require_sort$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_sort$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.sort;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.sort ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/sort.js
var require_sort$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_sort$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/sort.js
var require_sort = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_sort$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-to-array.js
var require_object_to_array = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var fails = require_fails();
	var uncurryThis = require_function_uncurry_this();
	var objectGetPrototypeOf = require_object_get_prototype_of();
	var objectKeys = require_object_keys();
	var toIndexedObject = require_to_indexed_object();
	var $propertyIsEnumerable = require_object_property_is_enumerable().f;
	var propertyIsEnumerable = uncurryThis($propertyIsEnumerable);
	var push = uncurryThis([].push);
	var IE_BUG = DESCRIPTORS && fails(function() {
		var O = Object.create(null);
		O[2] = 2;
		return !propertyIsEnumerable(O, 2);
	});
	var createMethod = function(TO_ENTRIES) {
		return function(it) {
			var O = toIndexedObject(it);
			var keys = objectKeys(O);
			var IE_WORKAROUND = IE_BUG && objectGetPrototypeOf(O) === null;
			var length = keys.length;
			var i = 0;
			var result = [];
			var key;
			while (length > i) {
				key = keys[i++];
				if (!DESCRIPTORS || (IE_WORKAROUND ? key in O : propertyIsEnumerable(O, key))) push(result, TO_ENTRIES ? [key, O[key]] : O[key]);
			}
			return result;
		};
	};
	module.exports = {
		entries: createMethod(true),
		values: createMethod(false)
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.entries.js
var require_es_object_entries = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $entries = require_object_to_array().entries;
	$({
		target: "Object",
		stat: true
	}, { entries: function entries(O) {
		return $entries(O);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/entries.js
var require_entries$6 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_entries();
	module.exports = require_path().Object.entries;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/entries.js
var require_entries$5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_entries$6();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/entries.js
var require_entries$4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_entries$5();
}));
//#endregion
//#region node_modules/core-js-pure/internals/is-integral-number.js
var require_is_integral_number = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isObject = require_is_object();
	var floor = Math.floor;
	module.exports = Number.isInteger || function isInteger(it) {
		return !isObject(it) && isFinite(it) && floor(it) === it;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.number.is-integer.js
var require_es_number_is_integer = /* @__PURE__ */ __commonJSMin((() => {
	require_export()({
		target: "Number",
		stat: true
	}, { isInteger: require_is_integral_number() });
}));
//#endregion
//#region node_modules/core-js-pure/es/number/is-integer.js
var require_is_integer$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_number_is_integer();
	module.exports = require_path().Number.isInteger;
}));
//#endregion
//#region node_modules/core-js-pure/stable/number/is-integer.js
var require_is_integer$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_is_integer$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/number/is-integer.js
var require_is_integer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_is_integer$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/equals.js
var require_equals = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = equals;
	var _isArray = _interopRequireDefault(require_is_array());
	var _keys = _interopRequireDefault(require_keys$4());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseACL = _interopRequireDefault(require_ParseACL());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	function equals(a, b) {
		const toString = Object.prototype.toString;
		if (toString.call(a) === "[object Date]" || toString.call(b) === "[object Date]") {
			const dateA = new Date(a);
			const dateB = new Date(b);
			return +dateA === +dateB;
		}
		if (typeof a !== typeof b) return false;
		if (!a || typeof a !== "object") return a === b;
		if ((0, _isArray.default)(a) || (0, _isArray.default)(b)) {
			if (!(0, _isArray.default)(a) || !(0, _isArray.default)(b)) return false;
			if (a.length !== b.length) return false;
			for (let i = 0; i < a.length; i += 1) if (!equals(a[i], b[i])) return false;
			return true;
		}
		const ParseObject = _CoreManager.default.getParseObject();
		if (a instanceof _ParseACL.default || a instanceof _ParseFile.default || a instanceof _ParseGeoPoint.default || a instanceof ParseObject) return a.equals(b);
		if (b instanceof ParseObject) {
			if (a.__type === "Object" || a.__type === "Pointer") return a.objectId === b.id && a.className === b.className;
		}
		if ((0, _keys.default)(a).length !== (0, _keys.default)(b).length) return false;
		for (const k in a) if (!equals(a[k], b[k])) return false;
		return true;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/OfflineQuery.js
var require_OfflineQuery = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _isArray = _interopRequireDefault(require_is_array());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _filter = _interopRequireDefault(require_filter());
	var _slice = _interopRequireDefault(require_slice());
	var _isInteger = _interopRequireDefault(require_is_integer());
	var _concat = _interopRequireDefault(require_concat());
	var _map = _interopRequireDefault(require_map$3());
	var _forEach = _interopRequireDefault(require_for_each());
	var _keys = _interopRequireDefault(require_keys$4());
	var _equals = _interopRequireDefault(require_equals());
	var _decode = _interopRequireDefault(require_decode());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParsePolygon = _interopRequireDefault(require_ParsePolygon());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	/**
	* contains -- Determines if an object is contained in a list with special handling for Parse pointers.
	*
	* @param haystack
	* @param needle
	* @private
	* @returns {boolean}
	*/
	function contains(haystack, needle) {
		if (needle && needle.__type && (needle.__type === "Pointer" || needle.__type === "Object")) {
			for (const i in haystack) {
				const ptr = haystack[i];
				if (typeof ptr === "string" && ptr === needle.objectId) return true;
				if (ptr.className === needle.className && ptr.objectId === needle.objectId) return true;
			}
			return false;
		}
		if ((0, _isArray.default)(needle)) {
			for (const need of needle) if (contains(haystack, need)) return true;
		}
		return (0, _indexOf.default)(haystack).call(haystack, needle) > -1;
	}
	function transformObject(object) {
		if (object._toFullJSON) return object._toFullJSON();
		return object;
	}
	/**
	* matchesQuery -- Determines if an object would be returned by a Parse Query
	* It's a lightweight, where-clause only implementation of a full query engine.
	* Since we find queries that match objects, rather than objects that match
	* queries, we can avoid building a full-blown query tool.
	*
	* @param className
	* @param object
	* @param objects
	* @param query
	* @private
	* @returns {boolean}
	*/
	function matchesQuery(className, object, objects, query) {
		if (object.className !== className) return false;
		let obj = object;
		let q = query;
		if (object.toJSON) obj = object.toJSON();
		if (query.toJSON) q = query.toJSON().where;
		obj.className = className;
		for (const field in q) if (!matchesKeyConstraints(className, obj, objects, field, q[field])) return false;
		return true;
	}
	function equalObjectsGeneric(obj, compareTo, eqlFn) {
		if ((0, _isArray.default)(obj)) {
			for (let i = 0; i < obj.length; i++) if (eqlFn(obj[i], compareTo)) return true;
			return false;
		}
		return eqlFn(obj, compareTo);
	}
	/**
	* @typedef RelativeTimeToDateResult
	* @property {string} status The conversion status, `error` if conversion failed or
	* `success` if conversion succeeded.
	* @property {string} info The error message if conversion failed, or the relative
	* time indication (`past`, `present`, `future`) if conversion succeeded.
	* @property {Date|undefined} result The converted date, or `undefined` if conversion
	* failed.
	*/
	/**
	* Converts human readable relative date string, for example, 'in 10 days' to a date
	* relative to now.
	*
	* @param {string} text The text to convert.
	* @param {Date} [now] The date from which add or subtract. Default is now.
	* @returns {RelativeTimeToDateResult}
	*/
	function relativeTimeToDate(text) {
		let now = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : /* @__PURE__ */ new Date();
		text = text.toLowerCase();
		let parts = text.split(" ");
		parts = (0, _filter.default)(parts).call(parts, (part) => part !== "");
		const future = parts[0] === "in";
		const past = parts[parts.length - 1] === "ago";
		if (!future && !past && text !== "now") return {
			status: "error",
			info: "Time should either start with 'in' or end with 'ago'"
		};
		if (future && past) return {
			status: "error",
			info: "Time cannot have both 'in' and 'ago'"
		};
		if (future) parts = (0, _slice.default)(parts).call(parts, 1);
		else parts = (0, _slice.default)(parts).call(parts, 0, parts.length - 1);
		if (parts.length % 2 !== 0 && text !== "now") return {
			status: "error",
			info: "Invalid time string. Dangling unit or number."
		};
		const pairs = [];
		while (parts.length) pairs.push([parts.shift(), parts.shift()]);
		let seconds = 0;
		for (const [num, interval] of pairs) {
			const val = Number(num);
			if (!(0, _isInteger.default)(val)) return {
				status: "error",
				info: "'".concat(num, "' is not an integer.")
			};
			switch (interval) {
				case "yr":
				case "yrs":
				case "year":
				case "years":
					seconds += val * 31536e3;
					break;
				case "wk":
				case "wks":
				case "week":
				case "weeks":
					seconds += val * 604800;
					break;
				case "d":
				case "day":
				case "days":
					seconds += val * 86400;
					break;
				case "hr":
				case "hrs":
				case "hour":
				case "hours":
					seconds += val * 3600;
					break;
				case "min":
				case "mins":
				case "minute":
				case "minutes":
					seconds += val * 60;
					break;
				case "sec":
				case "secs":
				case "second":
				case "seconds":
					seconds += val;
					break;
				default: return {
					status: "error",
					info: "Invalid interval: '".concat(interval, "'")
				};
			}
		}
		const milliseconds = seconds * 1e3;
		if (future) return {
			status: "success",
			info: "future",
			result: new Date(now.valueOf() + milliseconds)
		};
		else if (past) return {
			status: "success",
			info: "past",
			result: new Date(now.valueOf() - milliseconds)
		};
		else return {
			status: "success",
			info: "present",
			result: new Date(now.valueOf())
		};
	}
	/**
	* Determines whether an object matches a single key's constraints
	*
	* @param className
	* @param object
	* @param objects
	* @param key
	* @param constraints
	* @private
	* @returns {boolean}
	*/
	function matchesKeyConstraints(className, object, objects, key, constraints) {
		if (constraints === null) return false;
		if ((0, _indexOf.default)(key).call(key, ".") >= 0) {
			const keyComponents = key.split(".");
			const subObjectKey = keyComponents[0];
			const keyRemainder = (0, _slice.default)(keyComponents).call(keyComponents, 1).join(".");
			return matchesKeyConstraints(className, object[subObjectKey] || {}, objects, keyRemainder, constraints);
		}
		let i;
		if (key === "$or") {
			for (i = 0; i < constraints.length; i++) if (matchesQuery(className, object, objects, constraints[i])) return true;
			return false;
		}
		if (key === "$and") {
			for (i = 0; i < constraints.length; i++) if (!matchesQuery(className, object, objects, constraints[i])) return false;
			return true;
		}
		if (key === "$nor") {
			for (i = 0; i < constraints.length; i++) if (matchesQuery(className, object, objects, constraints[i])) return false;
			return true;
		}
		if (key === "$relatedTo") return false;
		if (!/^[A-Za-z][0-9A-Za-z_]*$/.test(key)) throw new _ParseError.default(_ParseError.default.INVALID_KEY_NAME, "Invalid Key: ".concat(key));
		if (typeof constraints !== "object") {
			if ((0, _isArray.default)(object[key])) {
				var _context;
				return (0, _indexOf.default)(_context = object[key]).call(_context, constraints) > -1;
			}
			return object[key] === constraints;
		}
		let compareTo;
		if (constraints.__type) {
			if (constraints.__type === "Pointer") return equalObjectsGeneric(object[key], constraints, function(obj, ptr) {
				return typeof obj !== "undefined" && ptr.className === obj.className && ptr.objectId === obj.objectId;
			});
			return equalObjectsGeneric((0, _decode.default)(object[key]), (0, _decode.default)(constraints), _equals.default);
		}
		for (const condition in constraints) {
			var _compareTo, _compareTo2;
			compareTo = constraints[condition];
			if ((_compareTo = compareTo) !== null && _compareTo !== void 0 && _compareTo.__type) compareTo = (0, _decode.default)(compareTo);
			if ((_compareTo2 = compareTo) !== null && _compareTo2 !== void 0 && _compareTo2["$relativeTime"]) {
				const parserResult = relativeTimeToDate(compareTo["$relativeTime"]);
				if (parserResult.status !== "success") {
					var _context2;
					throw new _ParseError.default(_ParseError.default.INVALID_JSON, (0, _concat.default)(_context2 = "bad $relativeTime (".concat(key, ") value. ")).call(_context2, parserResult.info));
				}
				compareTo = parserResult.result;
			}
			if (toString.call(compareTo) === "[object Date]" || typeof compareTo === "string" && new Date(compareTo).toString() !== "Invalid Date") object[key] = new Date(object[key].iso ? object[key].iso : object[key]);
			switch (condition) {
				case "$lt":
					if (object[key] >= compareTo) return false;
					break;
				case "$lte":
					if (object[key] > compareTo) return false;
					break;
				case "$gt":
					if (object[key] <= compareTo) return false;
					break;
				case "$gte":
					if (object[key] < compareTo) return false;
					break;
				case "$ne":
					if ((0, _equals.default)(object[key], compareTo)) return false;
					break;
				case "$in":
					if (!contains(compareTo, object[key])) return false;
					break;
				case "$nin":
					if (contains(compareTo, object[key])) return false;
					break;
				case "$all":
					for (i = 0; i < compareTo.length; i++) {
						var _context3;
						if ((0, _indexOf.default)(_context3 = object[key]).call(_context3, compareTo[i]) < 0) return false;
					}
					break;
				case "$exists": {
					const propertyExists = typeof object[key] !== "undefined";
					const existenceIsRequired = constraints["$exists"];
					if (typeof constraints["$exists"] !== "boolean") break;
					if (!propertyExists && existenceIsRequired || propertyExists && !existenceIsRequired) return false;
					break;
				}
				case "$regex": {
					if (typeof compareTo === "object") return compareTo.test(object[key]);
					let expString = "";
					let escapeEnd = -2;
					let escapeStart = (0, _indexOf.default)(compareTo).call(compareTo, "\\Q");
					while (escapeStart > -1) {
						expString += compareTo.substring(escapeEnd + 2, escapeStart);
						escapeEnd = (0, _indexOf.default)(compareTo).call(compareTo, "\\E", escapeStart);
						if (escapeEnd > -1) expString += compareTo.substring(escapeStart + 2, escapeEnd).replace(/\\\\\\\\E/g, "\\E").replace(/\W/g, "\\$&");
						escapeStart = (0, _indexOf.default)(compareTo).call(compareTo, "\\Q", escapeEnd);
					}
					expString += compareTo.substring(Math.max(escapeStart, escapeEnd + 2));
					let modifiers = constraints.$options || "";
					modifiers = modifiers.replace("x", "").replace("s", "");
					if (!new RegExp(expString, modifiers).test(object[key])) return false;
					break;
				}
				case "$nearSphere":
					if (!compareTo || !object[key]) return false;
					return compareTo.radiansTo(object[key]) <= (constraints.$maxDistance || Infinity);
				case "$within": {
					if (!compareTo || !object[key]) return false;
					const southWest = compareTo.$box[0];
					const northEast = compareTo.$box[1];
					if (southWest.latitude > northEast.latitude || southWest.longitude > northEast.longitude) return false;
					return object[key].latitude > southWest.latitude && object[key].latitude < northEast.latitude && object[key].longitude > southWest.longitude && object[key].longitude < northEast.longitude;
				}
				case "$options": break;
				case "$maxDistance": break;
				case "$select": {
					const subQueryObjects = (0, _filter.default)(objects).call(objects, (obj, _index, arr) => {
						return matchesQuery(compareTo.query.className, obj, arr, compareTo.query.where);
					});
					for (let i = 0; i < subQueryObjects.length; i += 1) {
						const subObject = transformObject(subQueryObjects[i]);
						return (0, _equals.default)(object[key], subObject[compareTo.key]);
					}
					return false;
				}
				case "$dontSelect": {
					const subQueryObjects = (0, _filter.default)(objects).call(objects, (obj, _index, arr) => {
						return matchesQuery(compareTo.query.className, obj, arr, compareTo.query.where);
					});
					for (let i = 0; i < subQueryObjects.length; i += 1) {
						const subObject = transformObject(subQueryObjects[i]);
						return !(0, _equals.default)(object[key], subObject[compareTo.key]);
					}
					return false;
				}
				case "$inQuery": {
					const subQueryObjects = (0, _filter.default)(objects).call(objects, (obj, _index, arr) => {
						return matchesQuery(compareTo.className, obj, arr, compareTo.where);
					});
					for (let i = 0; i < subQueryObjects.length; i += 1) {
						const subObject = transformObject(subQueryObjects[i]);
						if (object[key].className === subObject.className && object[key].objectId === subObject.objectId) return true;
					}
					return false;
				}
				case "$notInQuery": {
					const subQueryObjects = (0, _filter.default)(objects).call(objects, (obj, _index, arr) => {
						return matchesQuery(compareTo.className, obj, arr, compareTo.where);
					});
					for (let i = 0; i < subQueryObjects.length; i += 1) {
						const subObject = transformObject(subQueryObjects[i]);
						if (object[key].className === subObject.className && object[key].objectId === subObject.objectId) return false;
					}
					return true;
				}
				case "$containedBy":
					for (const value of object[key]) if (!contains(compareTo, value)) return false;
					return true;
				case "$geoWithin":
					if (compareTo.$polygon) {
						var _context4;
						const points = (0, _map.default)(_context4 = compareTo.$polygon).call(_context4, (geoPoint) => [geoPoint.latitude, geoPoint.longitude]);
						return new _ParsePolygon.default(points).containsPoint(object[key]);
					}
					if (compareTo.$centerSphere) {
						const [WGS84Point, maxDistance] = compareTo.$centerSphere;
						const centerPoint = new _ParseGeoPoint.default({
							latitude: WGS84Point[1],
							longitude: WGS84Point[0]
						});
						return new _ParseGeoPoint.default(object[key]).radiansTo(centerPoint) <= maxDistance;
					}
					return false;
				case "$geoIntersects": {
					const polygon = new _ParsePolygon.default(object[key].coordinates);
					const point = new _ParseGeoPoint.default(compareTo.$point);
					return polygon.containsPoint(point);
				}
				default: return false;
			}
		}
		return true;
	}
	function validateQuery(query) {
		var _context5;
		let q = query;
		if (query.toJSON) q = query.toJSON().where;
		const specialQuerykeys = [
			"$and",
			"$or",
			"$nor",
			"_rperm",
			"_wperm",
			"_perishable_token",
			"_email_verify_token",
			"_email_verify_token_expires_at",
			"_account_lockout_expires_at",
			"_failed_login_count"
		];
		(0, _forEach.default)(_context5 = (0, _keys.default)(q)).call(_context5, (key) => {
			if (q && q[key] && q[key].$regex) {
				if (typeof q[key].$options === "string") {
					if (!q[key].$options.match(/^[imxs]+$/)) throw new _ParseError.default(_ParseError.default.INVALID_QUERY, "Bad $options value for query: ".concat(q[key].$options));
				}
			}
			if ((0, _indexOf.default)(specialQuerykeys).call(specialQuerykeys, key) < 0 && !key.match(/^[a-zA-Z][a-zA-Z0-9_\.]*$/)) throw new _ParseError.default(_ParseError.default.INVALID_KEY_NAME, "Invalid key name: ".concat(key));
		});
	}
	exports.default = {
		matchesQuery,
		validateQuery
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseQuery.js
var require_ParseQuery = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _forEach = _interopRequireDefault(require_for_each());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _keys = _interopRequireDefault(require_keys$4());
	var _slice = _interopRequireDefault(require_slice());
	var _map = _interopRequireDefault(require_map$3());
	var _filter = _interopRequireDefault(require_filter());
	var _keys2 = _interopRequireDefault(require_keys());
	var _concat = _interopRequireDefault(require_concat());
	var _includes = _interopRequireDefault(require_includes());
	var _sort = _interopRequireDefault(require_sort());
	var _splice = _interopRequireDefault(require_splice());
	var _promise = _interopRequireDefault(require_promise());
	var _find = _interopRequireDefault(require_find());
	var _isArray = _interopRequireDefault(require_is_array());
	var _entries = _interopRequireDefault(require_entries$4());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _encode = _interopRequireDefault(require_encode());
	var _promiseUtils = require_promiseUtils();
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _OfflineQuery = _interopRequireDefault(require_OfflineQuery());
	var _LocalDatastoreUtils = require_LocalDatastoreUtils();
	/** CountOptions - no json since count() returns a number, not objects */
	/**
	* Converts a string into a regex that matches it.
	* Surrounding with \Q .. \E does this, we just need to escape any \E's in
	* the text separately.
	*
	* @param s
	* @private
	* @returns {string}
	*/
	function quote(s) {
		return "\\Q" + s.replace("\\E", "\\E\\\\E\\Q") + "\\E";
	}
	/**
	* Extracts the class name from queries. If not all queries have the same
	* class name an error will be thrown.
	*
	* @param queries
	* @private
	* @returns {string}
	*/
	function _getClassNameFromQueries(queries) {
		let className = null;
		(0, _forEach.default)(queries).call(queries, (q) => {
			if (!className) className = q.className;
			if (className !== q.className) throw new Error("All queries must be for the same class.");
		});
		return className;
	}
	function handleSelectResult(data, select) {
		const serverDataMask = {};
		(0, _forEach.default)(select).call(select, (field) => {
			const hasSubObjectSelect = (0, _indexOf.default)(field).call(field, ".") !== -1;
			if (!hasSubObjectSelect && !Object.hasOwn(data, field)) data[field] = void 0;
			else if (hasSubObjectSelect) {
				const pathComponents = field.split(".");
				let obj = data;
				let serverMask = serverDataMask;
				(0, _forEach.default)(pathComponents).call(pathComponents, (component, index, arr) => {
					if (obj && !Object.hasOwn(obj, component)) obj[component] = void 0;
					if (obj && typeof obj === "object") obj = obj[component];
					if (index < arr.length - 1) {
						if (!serverMask[component]) serverMask[component] = {};
						serverMask = serverMask[component];
					}
				});
			}
		});
		if ((0, _keys.default)(serverDataMask).length > 0) copyMissingDataWithMask(_CoreManager.default.getObjectStateController().getServerData({
			id: data.objectId,
			className: data.className
		}), data, serverDataMask, false);
	}
	function copyMissingDataWithMask(src, dest, mask, copyThisLevel) {
		if (copyThisLevel) {
			for (const key in src) if (Object.hasOwn(src, key) && !Object.hasOwn(dest, key)) dest[key] = src[key];
		}
		for (const key in mask) if (dest[key] !== void 0 && dest[key] !== null && src !== void 0 && src !== null) copyMissingDataWithMask(src[key], dest[key], mask[key], true);
	}
	function handleOfflineSort(a, b, sorts) {
		let order = sorts[0];
		const isDescending = (0, _slice.default)(order).call(order, 0, 1) === "-";
		if (isDescending) order = order.substring(1);
		if (order === "_created_at") order = "createdAt";
		if (order === "_updated_at") order = "updatedAt";
		if (!/^[A-Za-z][0-9A-Za-z_]*$/.test(order) || order === "password") throw new _ParseError.default(_ParseError.default.INVALID_KEY_NAME, "Invalid Key: ".concat(order));
		const field1 = a.get(order);
		const field2 = b.get(order);
		if (field1 < field2) return isDescending ? 1 : -1;
		if (field1 > field2) return isDescending ? -1 : 1;
		if (sorts.length > 1) return handleOfflineSort(a, b, (0, _slice.default)(sorts).call(sorts, 1));
		return 0;
	}
	/**
	* Creates a new parse Parse.Query for the given Parse.Object subclass.
	*
	* <p>Parse.Query defines a query that is used to fetch Parse.Objects. The
	* most common use case is finding all objects that match a query through the
	* <code>find</code> method. for example, this sample code fetches all objects
	* of class <code>myclass</code>. it calls a different function depending on
	* whether the fetch succeeded or not.
	*
	* <pre>
	* var query = new Parse.Query(myclass);
	* query.find().then((results) => {
	*   // results is an array of parse.object.
	* }).catch((error) =>  {
	*  // error is an instance of parse.error.
	* });</pre></p>
	*
	* <p>a Parse.Query can also be used to retrieve a single object whose id is
	* known, through the get method. for example, this sample code fetches an
	* object of class <code>myclass</code> and id <code>myid</code>. it calls a
	* different function depending on whether the fetch succeeded or not.
	*
	* <pre>
	* var query = new Parse.Query(myclass);
	* query.get(myid).then((object) => {
	*     // object is an instance of parse.object.
	* }).catch((error) =>  {
	*  // error is an instance of parse.error.
	* });</pre></p>
	*
	* <p>a Parse.Query can also be used to count the number of objects that match
	* the query without retrieving all of those objects. for example, this
	* sample code counts the number of objects of the class <code>myclass</code>
	* <pre>
	* var query = new Parse.Query(myclass);
	* query.count().then((number) => {
	*     // there are number instances of myclass.
	* }).catch((error) => {
	*     // error is an instance of Parse.Error.
	* });</pre></p>
	*
	* @alias Parse.Query
	*/
	var ParseQuery = class ParseQuery {
		/**
		* @param {(string | Parse.Object)} objectClass An instance of a subclass of Parse.Object, or a Parse className string.
		*/
		constructor(objectClass) {
			/**
			* @property {string} className
			*/
			(0, _defineProperty2.default)(this, "className", void 0);
			(0, _defineProperty2.default)(this, "_where", void 0);
			(0, _defineProperty2.default)(this, "_watch", void 0);
			(0, _defineProperty2.default)(this, "_include", void 0);
			(0, _defineProperty2.default)(this, "_exclude", void 0);
			(0, _defineProperty2.default)(this, "_select", void 0);
			(0, _defineProperty2.default)(this, "_limit", void 0);
			(0, _defineProperty2.default)(this, "_skip", void 0);
			(0, _defineProperty2.default)(this, "_count", void 0);
			(0, _defineProperty2.default)(this, "_order", void 0);
			(0, _defineProperty2.default)(this, "_readPreference", void 0);
			(0, _defineProperty2.default)(this, "_includeReadPreference", void 0);
			(0, _defineProperty2.default)(this, "_subqueryReadPreference", void 0);
			(0, _defineProperty2.default)(this, "_queriesLocalDatastore", void 0);
			(0, _defineProperty2.default)(this, "_localDatastorePinName", void 0);
			(0, _defineProperty2.default)(this, "_extraOptions", void 0);
			(0, _defineProperty2.default)(this, "_hint", void 0);
			(0, _defineProperty2.default)(this, "_explain", void 0);
			(0, _defineProperty2.default)(this, "_xhrRequest", void 0);
			(0, _defineProperty2.default)(this, "_comment", void 0);
			if (typeof objectClass === "string") {
				if (objectClass === "User" && _CoreManager.default.get("PERFORM_USER_REWRITE")) this.className = "_User";
				else this.className = objectClass;
			} else if (objectClass instanceof _ParseObject.default) this.className = objectClass.className;
			else if (typeof objectClass === "function") {
				const objClass = objectClass;
				if (typeof objClass.className === "string") this.className = objClass.className;
				else {
					const obj = new objClass();
					this.className = obj.className;
				}
			} else throw new TypeError("A ParseQuery must be constructed with a ParseObject or class name.");
			this._where = {};
			this._watch = [];
			this._include = [];
			this._exclude = [];
			this._count = false;
			this._limit = -1;
			this._skip = 0;
			this._readPreference = null;
			this._includeReadPreference = null;
			this._subqueryReadPreference = null;
			this._queriesLocalDatastore = false;
			this._localDatastorePinName = null;
			this._extraOptions = {};
			this._xhrRequest = {
				task: null,
				onchange: () => {}
			};
			this._comment = null;
		}
		/**
		* Adds constraint that at least one of the passed in queries matches.
		*
		* @param {Array} queries
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		_orQuery(queries) {
			const queryJSON = (0, _map.default)(queries).call(queries, (q) => {
				return q.toJSON().where;
			});
			this._where.$or = queryJSON;
			return this;
		}
		/**
		* Adds constraint that all of the passed in queries match.
		*
		* @param {Array} queries
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		_andQuery(queries) {
			const queryJSON = (0, _map.default)(queries).call(queries, (q) => {
				return q.toJSON().where;
			});
			this._where.$and = queryJSON;
			return this;
		}
		/**
		* Adds constraint that none of the passed in queries match.
		*
		* @param {Array} queries
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		_norQuery(queries) {
			const queryJSON = (0, _map.default)(queries).call(queries, (q) => {
				return q.toJSON().where;
			});
			this._where.$nor = queryJSON;
			return this;
		}
		/**
		* Helper for condition queries
		*
		* @param key
		* @param condition
		* @param value
		* @returns {Parse.Query}
		*/
		_addCondition(key, condition, value) {
			if (!this._where[key] || typeof this._where[key] === "string") this._where[key] = {};
			this._where[key][condition] = (0, _encode.default)(value, false, true);
			return this;
		}
		/**
		* Converts string for regular expression at the beginning
		*
		* @param string
		* @returns {string}
		*/
		_regexStartWith(string) {
			return "^" + quote(string);
		}
		async _handleOfflineQuery(params) {
			var _context;
			_OfflineQuery.default.validateQuery(this);
			const objects = await _CoreManager.default.getLocalDatastore()._serializeObjectsFromPinName(this._localDatastorePinName);
			let results = (0, _filter.default)(_context = (0, _map.default)(objects).call(objects, (json, _index, arr) => {
				const object = _ParseObject.default.fromJSON(json, false);
				if (json._localId && !json.objectId) object._localId = json._localId;
				if (!_OfflineQuery.default.matchesQuery(this.className, object, arr, this)) return null;
				return object;
			})).call(_context, (object) => object !== null);
			if ((0, _keys2.default)(params)) {
				let keys = (0, _keys2.default)(params).split(",");
				keys = (0, _concat.default)(keys).call(keys, [
					"className",
					"objectId",
					"createdAt",
					"updatedAt",
					"ACL"
				]);
				results = (0, _map.default)(results).call(results, (object) => {
					var _context2;
					const json = object._toFullJSON();
					(0, _forEach.default)(_context2 = (0, _keys.default)(json)).call(_context2, (key) => {
						if (!(0, _includes.default)(keys).call(keys, key)) delete json[key];
					});
					return _ParseObject.default.fromJSON(json, false);
				});
			}
			if (params.order) {
				const sorts = params.order.split(",");
				(0, _sort.default)(results).call(results, (a, b) => {
					return handleOfflineSort(a, b, sorts);
				});
			}
			let count;
			if (params.count) count = results.length;
			if (params.skip) {
				if (params.skip >= results.length) results = [];
				else results = (0, _splice.default)(results).call(results, params.skip, results.length);
			}
			let limit = results.length;
			if (params.limit !== 0 && params.limit < results.length) limit = params.limit;
			results = (0, _splice.default)(results).call(results, 0, limit);
			if (typeof count === "number") return {
				results,
				count
			};
			return results;
		}
		/**
		* Returns a JSON representation of this query.
		*
		* @returns {object} The JSON representation of the query.
		*/
		toJSON() {
			const params = { where: this._where };
			if (this._watch.length) params.watch = this._watch.join(",");
			if (this._include.length) params.include = this._include.join(",");
			if (this._exclude.length) params.excludeKeys = this._exclude.join(",");
			if (this._select) params.keys = this._select.join(",");
			if (this._count) params.count = 1;
			if (this._limit >= 0) params.limit = this._limit;
			if (this._skip > 0) params.skip = this._skip;
			if (this._order) params.order = this._order.join(",");
			if (this._readPreference) params.readPreference = this._readPreference;
			if (this._includeReadPreference) params.includeReadPreference = this._includeReadPreference;
			if (this._subqueryReadPreference) params.subqueryReadPreference = this._subqueryReadPreference;
			if (this._hint) params.hint = this._hint;
			if (this._explain) params.explain = true;
			if (this._comment) params.comment = this._comment;
			for (const key in this._extraOptions) params[key] = this._extraOptions[key];
			return params;
		}
		/**
		* Return a query with conditions from json, can be useful to send query from server side to client
		* Not static, all query conditions was set before calling this method will be deleted.
		* For example on the server side we have
		* var query = new Parse.Query("className");
		* query.equalTo(key: value);
		* query.limit(100);
		* ... (others queries)
		* Create JSON representation of Query Object
		* var jsonFromServer = query.fromJSON();
		*
		* On client side getting query:
		* var query = new Parse.Query("className");
		* query.fromJSON(jsonFromServer);
		*
		* and continue to query...
		* query.skip(100).find().then(...);
		*
		* @param {QueryJSON} json from Parse.Query.toJSON() method
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withJSON(json) {
			if (json.where) this._where = json.where;
			if (json.watch) this._watch = json.watch.split(",");
			if (json.include) this._include = json.include.split(",");
			if ((0, _keys2.default)(json)) this._select = (0, _keys2.default)(json).split(",");
			if (json.excludeKeys) this._exclude = json.excludeKeys.split(",");
			if (json.count) this._count = json.count === 1;
			if (json.limit) this._limit = json.limit;
			if (json.skip) this._skip = json.skip;
			if (json.order) this._order = json.order.split(",");
			if (json.readPreference) this._readPreference = json.readPreference;
			if (json.includeReadPreference) this._includeReadPreference = json.includeReadPreference;
			if (json.subqueryReadPreference) this._subqueryReadPreference = json.subqueryReadPreference;
			if (json.hint) this._hint = json.hint;
			if (json.explain) this._explain = !!json.explain;
			if (json.comment) this._comment = json.comment;
			for (const key in json) if (Object.hasOwn(json, key)) {
				var _context3;
				if ((0, _indexOf.default)(_context3 = [
					"where",
					"include",
					"keys",
					"count",
					"limit",
					"skip",
					"order",
					"readPreference",
					"includeReadPreference",
					"subqueryReadPreference",
					"hint",
					"explain",
					"comment"
				]).call(_context3, key) === -1) this._extraOptions[key] = json[key];
			}
			return this;
		}
		/**
		* Static method to restore Parse.Query by json representation
		* Internally calling Parse.Query.withJSON
		*
		* @param {string} className
		* @param {QueryJSON} json from Parse.Query.toJSON() method
		* @returns {Parse.Query} new created query
		*/
		static fromJSON(className, json) {
			return new ParseQuery(className).withJSON(json);
		}
		/**
		* Constructs a Parse.Object whose id is already known by fetching data from
		* the server. Unlike the <code>first</code> method, it never returns undefined.
		*
		* @param {string} objectId The id of the object to be fetched.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		*   <li>json: Return raw json without converting to Parse.Object
		* </ul>
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		get(objectId, options) {
			this.equalTo("objectId", objectId);
			const firstOptions = _ParseObject.default._getRequestOptions(options);
			return this.first(firstOptions).then((response) => {
				if (response) return response;
				const errorObject = new _ParseError.default(_ParseError.default.OBJECT_NOT_FOUND, "Object not found.");
				return _promise.default.reject(errorObject);
			});
		}
		/**
		* Retrieves a list of ParseObjects that satisfy this query.
		*
		* @param {object} options Valid options
		* are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		*   <li>json: Return raw json without converting to Parse.Object
		* </ul>
		* @returns {Promise} A promise that is resolved with the results when
		* the query completes.
		*/
		find(options) {
			const findOptions = _ParseObject.default._getRequestOptions(options);
			this._setRequestTask(findOptions);
			const controller = _CoreManager.default.getQueryController();
			const select = this._select;
			if (this._queriesLocalDatastore) return this._handleOfflineQuery(this.toJSON());
			return (0, _find.default)(controller).call(controller, this.className, this.toJSON(), findOptions).then((response) => {
				var _response$results;
				if (this._explain) return response.results;
				const results = (_response$results = response.results) === null || _response$results === void 0 ? void 0 : (0, _map.default)(_response$results).call(_response$results, (data) => {
					const override = response.className || this.className;
					if (!data.className) data.className = override;
					if (select) handleSelectResult(data, select);
					if (findOptions.json) return data;
					else return _ParseObject.default.fromJSON(data, !select);
				});
				const count = response.count;
				if (typeof count === "number") return {
					results,
					count
				};
				else return results;
			});
		}
		/**
		* Retrieves a complete list of ParseObjects that satisfy this query.
		* Using `eachBatch` under the hood to fetch all the valid objects.
		*
		* @param {object} options Valid options are:<ul>
		*   <li>batchSize: How many objects to yield in each batch (default: 100)
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>json: Return raw JSON without converting to Parse.Object.
		* </ul>
		* @returns {Promise} A promise that is resolved with the results when
		* the query completes.
		*/
		async findAll(options) {
			let result = [];
			await this.eachBatch((objects) => {
				result = [...result, ...objects];
			}, options);
			return result;
		}
		/**
		* Counts the number of objects that match this query.
		*
		* @param {object} options
		* @param {boolean} [options.useMasterKey]
		* @param {string} [options.sessionToken]
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise} A promise that is resolved with the count when
		* the query completes.
		*/
		count(options) {
			options = options || {};
			const findOptions = _ParseObject.default._getRequestOptions(options);
			this._setRequestTask(findOptions);
			const controller = _CoreManager.default.getQueryController();
			const params = this.toJSON();
			params.limit = 0;
			params.count = 1;
			return (0, _find.default)(controller).call(controller, this.className, params, findOptions).then((result) => {
				return result.count;
			});
		}
		/**
		* Executes a distinct query and returns unique values
		*
		* @param {string} key A field to find distinct values
		* @returns {Promise} A promise that is resolved with the query completes.
		*/
		distinct(key) {
			const distinctOptions = { useMasterKey: true };
			this._setRequestTask(distinctOptions);
			const params = {
				distinct: key,
				where: this._where,
				hint: this._hint
			};
			return _CoreManager.default.getQueryController().aggregate(this.className, params, distinctOptions).then((results) => {
				return results.results;
			});
		}
		/**
		* Executes an aggregate query and returns aggregate results
		*
		* @param {(Array|object)} pipeline Array or Object of stages to process query
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*       be used for this request. Defaults to `true` when not provided, for
		*       backward compatibility.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code triggers.
		*   <li>rawValues: When `true`, disables schema-based value transformation
		*       in the pipeline. Pipeline values are interpreted using MongoDB
		*       Extended JSON (EJSON), so typed values such as `{ $date: '...' }`,
		*       `{ $oid: '...' }`, `{ $numberDecimal: '...' }`, etc. are converted
		*       to their corresponding BSON types by the server. Requires Parse
		*       Server 9.9.0+
		*   <li>rawFieldNames: When `true`, disables automatic field-name
		*       transformation (e.g. `createdAt` → `_created_at`) in the pipeline.
		*       Users write native MongoDB field names directly. Requires Parse
		*       Server 9.9.0+
		* </ul>
		* @returns {Promise} A promise that is resolved with the query completes.
		*/
		aggregate(pipeline, options) {
			if (!(0, _isArray.default)(pipeline) && typeof pipeline !== "object") throw new Error("Invalid pipeline must be Array or Object");
			if ((0, _keys.default)(this._where || {}).length) {
				if (!(0, _isArray.default)(pipeline)) pipeline = [pipeline];
				pipeline.unshift({ $match: this._where });
			}
			const params = {
				pipeline,
				hint: this._hint,
				explain: this._explain,
				readPreference: this._readPreference
			};
			if ((options === null || options === void 0 ? void 0 : options.rawValues) !== void 0) params.rawValues = options.rawValues;
			if ((options === null || options === void 0 ? void 0 : options.rawFieldNames) !== void 0) params.rawFieldNames = options.rawFieldNames;
			const aggregateOptions = _ParseObject.default._getRequestOptions(options);
			if (aggregateOptions.useMasterKey === void 0) aggregateOptions.useMasterKey = true;
			this._setRequestTask(aggregateOptions);
			return _CoreManager.default.getQueryController().aggregate(this.className, params, aggregateOptions).then((results) => {
				return results.results;
			});
		}
		/**
		* Retrieves at most one Parse.Object that satisfies this query.
		*
		* Returns the object if there is one, otherwise undefined.
		*
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		*   <li>json: Return raw json without converting to Parse.Object
		* </ul>
		* @returns {Promise} A promise that is resolved with the object when
		* the query completes.
		*/
		first() {
			let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			const findOptions = _ParseObject.default._getRequestOptions(options);
			this._setRequestTask(findOptions);
			const controller = _CoreManager.default.getQueryController();
			const params = this.toJSON();
			params.limit = 1;
			const select = this._select;
			if (this._queriesLocalDatastore) return this._handleOfflineQuery(params).then((objects) => {
				if (!objects[0]) return;
				return objects[0];
			});
			return (0, _find.default)(controller).call(controller, this.className, params, findOptions).then((response) => {
				const objects = response.results;
				if (!objects[0]) return;
				if (!objects[0].className) objects[0].className = this.className;
				if (select) handleSelectResult(objects[0], select);
				if (findOptions.json) return objects[0];
				else return _ParseObject.default.fromJSON(objects[0], !select);
			});
		}
		/**
		* Iterates over objects matching a query, calling a callback for each batch.
		* If the callback returns a promise, the iteration will not continue until
		* that promise has been fulfilled. If the callback returns a rejected
		* promise, then iteration will stop with that error. The items are processed
		* in an unspecified order. The query may not have any sort order, and may
		* not use limit or skip.
		*
		* @param {Function} callback Callback that will be called with each result
		*     of the query.
		* @param {object} options Valid options are:<ul>
		*   <li>batchSize: How many objects to yield in each batch (default: 100)
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeFind` trigger.
		* </ul>
		* @returns {Promise} A promise that will be fulfilled once the
		*     iteration has completed.
		*/
		eachBatch(callback, options) {
			options = options || {};
			if (this._order || this._skip || this._limit >= 0) return _promise.default.reject("Cannot iterate on a query with sort, skip, or limit.");
			const query = ParseQuery.fromJSON(this.className, this.toJSON());
			query.ascending("objectId");
			query._limit = options.batchSize || 100;
			const findOptions = _ParseObject.default._getRequestOptions(options);
			let finished = false;
			let previousResults = [];
			return (0, _promiseUtils.continueWhile)(() => {
				return !finished;
			}, async () => {
				const [results] = await _promise.default.all([(0, _find.default)(query).call(query, findOptions), _promise.default.resolve(previousResults.length > 0 && callback(previousResults))]);
				if (results.length >= query._limit) {
					if (findOptions.json) query.greaterThan("objectId", results[results.length - 1].objectId);
					else query.greaterThan("objectId", results[results.length - 1].id);
					previousResults = results;
				} else if (results.length > 0) {
					await _promise.default.resolve(callback(results));
					finished = true;
				} else finished = true;
			});
		}
		/**
		* Iterates over each result of a query, calling a callback for each one. If
		* the callback returns a promise, the iteration will not continue until
		* that promise has been fulfilled. If the callback returns a rejected
		* promise, then iteration will stop with that error. The items are
		* processed in an unspecified order. The query may not have any sort order,
		* and may not use limit or skip.
		*
		* @param {Function} callback Callback that will be called with each result
		*     of the query.
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		*   <li>json: Return raw json without converting to Parse.Object
		* </ul>
		* @returns {Promise} A promise that will be fulfilled once the
		*     iteration has completed.
		*/
		each(callback, options) {
			return this.eachBatch((results) => {
				let callbacksDone = _promise.default.resolve();
				(0, _forEach.default)(results).call(results, (result) => {
					callbacksDone = callbacksDone.then(() => {
						return callback(result);
					});
				});
				return callbacksDone;
			}, options);
		}
		/**
		* Adds a hint to force index selection. (https://docs.mongodb.com/manual/reference/operator/meta/hint/)
		*
		* @param {(string|object)} value String or Object of index that should be used when executing query
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		hint(value) {
			if (typeof value === "undefined") delete this._hint;
			this._hint = value;
			return this;
		}
		/**
		* Investigates the query execution plan. Useful for optimizing queries. (https://docs.mongodb.com/manual/reference/operator/meta/explain/)
		*
		* @param {boolean} explain Used to toggle the information on the query plan.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		explain() {
			let explain = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			if (typeof explain !== "boolean") throw new Error("You can only set explain to a boolean value");
			this._explain = explain;
			return this;
		}
		/**
		* Iterates over each result of a query, calling a callback for each one. If
		* the callback returns a promise, the iteration will not continue until
		* that promise has been fulfilled. If the callback returns a rejected
		* promise, then iteration will stop with that error. The items are
		* processed in an unspecified order. The query may not have any sort order,
		* and may not use limit or skip.
		*
		* @param {Function} callback Callback <ul>
		*   <li>currentObject: The current Parse.Object being processed in the array.</li>
		*   <li>index: The index of the current Parse.Object being processed in the array.</li>
		*   <li>query: The query map was called upon.</li>
		* </ul>
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise} A promise that will be fulfilled once the
		*     iteration has completed.
		*/
		async map(callback, options) {
			const array = [];
			let index = 0;
			await this.each((object) => {
				return _promise.default.resolve(callback(object, index, this)).then((result) => {
					array.push(result);
					index += 1;
				});
			}, options);
			return array;
		}
		/**
		* Iterates over each result of a query, calling a callback for each one. If
		* the callback returns a promise, the iteration will not continue until
		* that promise has been fulfilled. If the callback returns a rejected
		* promise, then iteration will stop with that error. The items are
		* processed in an unspecified order. The query may not have any sort order,
		* and may not use limit or skip.
		*
		* @param {Function} callback Callback <ul>
		*   <li>accumulator: The accumulator accumulates the callback's return values. It is the accumulated value previously returned in the last invocation of the callback.</li>
		*   <li>currentObject: The current Parse.Object being processed in the array.</li>
		*   <li>index: The index of the current Parse.Object being processed in the array.</li>
		* </ul>
		* @param {*} initialValue A value to use as the first argument to the first call of the callback. If no initialValue is supplied, the first object in the query will be used and skipped.
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise} A promise that will be fulfilled once the
		*     iteration has completed.
		*/
		async reduce(callback, initialValue, options) {
			let accumulator = initialValue;
			let index = 0;
			await this.each((object) => {
				if (index === 0 && initialValue === void 0) {
					accumulator = object;
					index += 1;
					return;
				}
				return _promise.default.resolve(callback(accumulator, object, index)).then((result) => {
					accumulator = result;
					index += 1;
				});
			}, options);
			if (index === 0 && initialValue === void 0) throw new TypeError("Reducing empty query result set with no initial value");
			return accumulator;
		}
		/**
		* Iterates over each result of a query, calling a callback for each one. If
		* the callback returns a promise, the iteration will not continue until
		* that promise has been fulfilled. If the callback returns a rejected
		* promise, then iteration will stop with that error. The items are
		* processed in an unspecified order. The query may not have any sort order,
		* and may not use limit or skip.
		*
		* @param {Function} callback Callback <ul>
		*   <li>currentObject: The current Parse.Object being processed in the array.</li>
		*   <li>index: The index of the current Parse.Object being processed in the array.</li>
		*   <li>query: The query filter was called upon.</li>
		* </ul>
		* @param {object} options Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>sessionToken: A valid session token, used for making a request on
		*       behalf of a specific user.
		* </ul>
		* @returns {Promise} A promise that will be fulfilled once the
		*     iteration has completed.
		*/
		async filter(callback, options) {
			const array = [];
			let index = 0;
			await this.each((object) => {
				return _promise.default.resolve(callback(object, index, this)).then((flag) => {
					if (flag) array.push(object);
					index += 1;
				});
			}, options);
			return array;
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be equal to the provided value.
		*
		* @param {string} key The key to check.
		* @param value The value that the Parse.Object must contain.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		equalTo(key, value) {
			if (key && typeof key === "object") {
				var _context4;
				(0, _forEach.default)(_context4 = (0, _entries.default)(key)).call(_context4, (_ref) => {
					let [k, val] = _ref;
					return this.equalTo(k, val);
				});
				return this;
			}
			if (typeof value === "undefined") return this.doesNotExist(key);
			this._where[key] = (0, _encode.default)(value, false, true);
			return this;
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be not equal to the provided value.
		*
		* @param {string} key The key to check.
		* @param value The value that must not be equalled.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		notEqualTo(key, value) {
			if (key && typeof key === "object") {
				var _context5;
				(0, _forEach.default)(_context5 = (0, _entries.default)(key)).call(_context5, (_ref2) => {
					let [k, val] = _ref2;
					return this.notEqualTo(k, val);
				});
				return this;
			}
			return this._addCondition(key, "$ne", value);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be less than the provided value.
		*
		* @param {string} key The key to check.
		* @param value The value that provides an upper bound.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		lessThan(key, value) {
			return this._addCondition(key, "$lt", value);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be greater than the provided value.
		*
		* @param {string} key The key to check.
		* @param value The value that provides an lower bound.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		greaterThan(key, value) {
			return this._addCondition(key, "$gt", value);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be less than or equal to the provided value.
		*
		* @param {string} key The key to check.
		* @param value The value that provides an upper bound.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		lessThanOrEqualTo(key, value) {
			return this._addCondition(key, "$lte", value);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be greater than or equal to the provided value.
		*
		* @param {string} key The key to check.
		* @param {*} value The value that provides an lower bound.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		greaterThanOrEqualTo(key, value) {
			return this._addCondition(key, "$gte", value);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be contained in the provided list of values.
		*
		* @param {string} key The key to check.
		* @param {Array<*>} values The values that will match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		containedIn(key, values) {
			return this._addCondition(key, "$in", values);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* not be contained in the provided list of values.
		*
		* @param {string} key The key to check.
		* @param {Array<*>} values The values that will not match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		notContainedIn(key, values) {
			return this._addCondition(key, "$nin", values);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* be contained by the provided list of values. Get objects where all array elements match.
		*
		* @param {string} key The key to check.
		* @param {Array} values The values that will match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		containedBy(key, values) {
			return this._addCondition(key, "$containedBy", values);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* contain each one of the provided list of values.
		*
		* @param {string} key The key to check.  This key's value must be an array.
		* @param {Array} values The values that will match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		containsAll(key, values) {
			return this._addCondition(key, "$all", values);
		}
		/**
		* Adds a constraint to the query that requires a particular key's value to
		* contain each one of the provided list of values starting with given strings.
		*
		* @param {string} key The key to check.  This key's value must be an array.
		* @param {Array<string>} values The string values that will match as starting string.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		containsAllStartingWith(key, values) {
			if (!(0, _isArray.default)(values)) values = [values];
			const regexObject = (0, _map.default)(values).call(values, (value) => {
				return { $regex: this._regexStartWith(value) };
			});
			return this.containsAll(key, regexObject);
		}
		/**
		* Adds a constraint for finding objects that contain the given key.
		*
		* @param {string} key The key that should exist.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		exists(key) {
			return this._addCondition(key, "$exists", true);
		}
		/**
		* Adds a constraint for finding objects that do not contain a given key.
		*
		* @param {string} key The key that should not exist
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		doesNotExist(key) {
			return this._addCondition(key, "$exists", false);
		}
		/**
		* Adds a regular expression constraint for finding string values that match
		* the provided regular expression.
		* This may be slow for large datasets.
		*
		* @param {string} key The key that the string to match is stored in.
		* @param {RegExp | string} regex The regular expression pattern to match.
		* @param {string} modifiers The regular expression mode.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		matches(key, regex, modifiers) {
			this._addCondition(key, "$regex", regex);
			if (!modifiers) modifiers = "";
			if (typeof regex !== "string") {
				if (regex.ignoreCase) modifiers += "i";
				if (regex.multiline) modifiers += "m";
			}
			if (modifiers.length) this._addCondition(key, "$options", modifiers);
			return this;
		}
		/**
		* Adds a constraint that requires that a key's value matches a Parse.Query
		* constraint.
		*
		* @param {string} key The key that the contains the object to match the
		*                     query.
		* @param {Parse.Query} query The query that should match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		matchesQuery(key, query) {
			const queryJSON = query.toJSON();
			queryJSON.className = query.className;
			return this._addCondition(key, "$inQuery", queryJSON);
		}
		/**
		* Adds a constraint that requires that a key's value not matches a
		* Parse.Query constraint.
		*
		* @param {string} key The key that the contains the object to match the
		*                     query.
		* @param {Parse.Query} query The query that should not match.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		doesNotMatchQuery(key, query) {
			const queryJSON = query.toJSON();
			queryJSON.className = query.className;
			return this._addCondition(key, "$notInQuery", queryJSON);
		}
		/**
		* Adds a constraint that requires that a key's value matches a value in
		* an object returned by a different Parse.Query.
		*
		* @param {string} key The key that contains the value that is being
		*                     matched.
		* @param {string} queryKey The key in the objects returned by the query to
		*                          match against.
		* @param {Parse.Query} query The query to run.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		matchesKeyInQuery(key, queryKey, query) {
			const queryJSON = query.toJSON();
			queryJSON.className = query.className;
			return this._addCondition(key, "$select", {
				key: queryKey,
				query: queryJSON
			});
		}
		/**
		* Adds a constraint that requires that a key's value not match a value in
		* an object returned by a different Parse.Query.
		*
		* @param {string} key The key that contains the value that is being
		*                     excluded.
		* @param {string} queryKey The key in the objects returned by the query to
		*                          match against.
		* @param {Parse.Query} query The query to run.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		doesNotMatchKeyInQuery(key, queryKey, query) {
			const queryJSON = query.toJSON();
			queryJSON.className = query.className;
			return this._addCondition(key, "$dontSelect", {
				key: queryKey,
				query: queryJSON
			});
		}
		/**
		* Adds a constraint for finding string values that contain a provided
		* string.  This may be slow for large datasets.
		*
		* @param {string} key The key that the string to match is stored in.
		* @param {string} substring The substring that the value must contain.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		contains(key, substring) {
			if (typeof substring !== "string") throw new Error("The value being searched for must be a string.");
			return this._addCondition(key, "$regex", quote(substring));
		}
		/**
		* Adds a constraint for finding string values that contain a provided
		* string. This may be slow for large datasets. Requires Parse-Server > 2.5.0
		*
		* In order to sort you must use select and ascending ($score is required)
		*  <pre>
		*   query.fullText('field', 'term');
		*   query.ascending('$score');
		*   query.select('$score');
		*  </pre>
		*
		* To retrieve the weight / rank
		*  <pre>
		*   object->get('score');
		*  </pre>
		*
		* You can define optionals by providing an object as a third parameter
		*  <pre>
		*   query.fullText('field', 'term', { language: 'es', diacriticSensitive: true });
		*  </pre>
		*
		* @param {string} key The key that the string to match is stored in.
		* @param {string} value The string to search
		* @param {object} options (Optional)
		* @param {string} options.language The language that determines the list of stop words for the search and the rules for the stemmer and tokenizer.
		* @param {boolean} options.caseSensitive A boolean flag to enable or disable case sensitive search.
		* @param {boolean} options.diacriticSensitive A boolean flag to enable or disable diacritic sensitive search.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		fullText(key, value, options) {
			options = options || {};
			if (!key) throw new Error("A key is required.");
			if (!value) throw new Error("A search term is required");
			if (typeof value !== "string") throw new Error("The value being searched for must be a string.");
			const fullOptions = {};
			fullOptions.$term = value;
			for (const option in options) switch (option) {
				case "language":
					fullOptions.$language = options[option];
					break;
				case "caseSensitive":
					fullOptions.$caseSensitive = options[option];
					break;
				case "diacriticSensitive":
					fullOptions.$diacriticSensitive = options[option];
					break;
				default: throw new Error("Unknown option: ".concat(option));
			}
			return this._addCondition(key, "$text", { $search: fullOptions });
		}
		/**
		* Method to sort the full text search by text score
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		sortByTextScore() {
			this.ascending("$score");
			this.select(["$score"]);
			return this;
		}
		/**
		* Adds a constraint for finding string values that start with a provided
		* string.  This query will use the backend index, so it will be fast even
		* for large datasets.
		*
		* @param {string} key The key that the string to match is stored in.
		* @param {string} prefix The substring that the value must start with.
		* @param {string} modifiers The regular expression mode.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		startsWith(key, prefix, modifiers) {
			if (typeof prefix !== "string") throw new Error("The value being searched for must be a string.");
			return this.matches(key, this._regexStartWith(prefix), modifiers);
		}
		/**
		* Adds a constraint for finding string values that end with a provided
		* string.  This will be slow for large datasets.
		*
		* @param {string} key The key that the string to match is stored in.
		* @param {string} suffix The substring that the value must end with.
		* @param {string} modifiers The regular expression mode.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		endsWith(key, suffix, modifiers) {
			if (typeof suffix !== "string") throw new Error("The value being searched for must be a string.");
			return this.matches(key, quote(suffix) + "$", modifiers);
		}
		/**
		* Adds a proximity based constraint for finding objects with key point
		* values near the point given.
		*
		* @param {string} key The key that the Parse.GeoPoint is stored in.
		* @param {Parse.GeoPoint} point The reference Parse.GeoPoint that is used.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		near(key, point) {
			if (!(point instanceof _ParseGeoPoint.default)) point = new _ParseGeoPoint.default(point);
			return this._addCondition(key, "$nearSphere", point);
		}
		/**
		* Adds a proximity based constraint for finding objects with key point
		* values near the point given and within the maximum distance given.
		*
		* @param {string} key The key that the Parse.GeoPoint is stored in.
		* @param {Parse.GeoPoint} point The reference Parse.GeoPoint that is used.
		* @param {number} maxDistance Maximum distance (in radians) of results to return.
		* @param {boolean} sorted A Bool value that is true if results should be
		* sorted by distance ascending, false is no sorting is required,
		* defaults to true.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withinRadians(key, point, maxDistance, sorted) {
			if (sorted || sorted === void 0) {
				this.near(key, point);
				return this._addCondition(key, "$maxDistance", maxDistance);
			} else return this._addCondition(key, "$geoWithin", { $centerSphere: [[point.longitude, point.latitude], maxDistance] });
		}
		/**
		* Adds a proximity based constraint for finding objects with key point
		* values near the point given and within the maximum distance given.
		* Radius of earth used is 3958.8 miles.
		*
		* @param {string} key The key that the Parse.GeoPoint is stored in.
		* @param {Parse.GeoPoint} point The reference Parse.GeoPoint that is used.
		* @param {number} maxDistance Maximum distance (in miles) of results to return.
		* @param {boolean} sorted A Bool value that is true if results should be
		* sorted by distance ascending, false is no sorting is required,
		* defaults to true.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withinMiles(key, point, maxDistance, sorted) {
			return this.withinRadians(key, point, maxDistance / 3958.8, sorted);
		}
		/**
		* Adds a proximity based constraint for finding objects with key point
		* values near the point given and within the maximum distance given.
		* Radius of earth used is 6371.0 kilometers.
		*
		* @param {string} key The key that the Parse.GeoPoint is stored in.
		* @param {Parse.GeoPoint} point The reference Parse.GeoPoint that is used.
		* @param {number} maxDistance Maximum distance (in kilometers) of results to return.
		* @param {boolean} sorted A Bool value that is true if results should be
		* sorted by distance ascending, false is no sorting is required,
		* defaults to true.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withinKilometers(key, point, maxDistance, sorted) {
			return this.withinRadians(key, point, maxDistance / 6371, sorted);
		}
		/**
		* Adds a constraint to the query that requires a particular key's
		* coordinates be contained within a given rectangular geographic bounding
		* box.
		*
		* @param {string} key The key to be constrained.
		* @param {Parse.GeoPoint} southwest
		*     The lower-left inclusive corner of the box.
		* @param {Parse.GeoPoint} northeast
		*     The upper-right inclusive corner of the box.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withinGeoBox(key, southwest, northeast) {
			if (!(southwest instanceof _ParseGeoPoint.default)) southwest = new _ParseGeoPoint.default(southwest);
			if (!(northeast instanceof _ParseGeoPoint.default)) northeast = new _ParseGeoPoint.default(northeast);
			this._addCondition(key, "$within", { $box: [southwest, northeast] });
			return this;
		}
		/**
		* Adds a constraint to the query that requires a particular key's
		* coordinates be contained within and on the bounds of a given polygon.
		* Supports closed and open (last point is connected to first) paths
		*
		* Polygon must have at least 3 points
		*
		* @param {string} key The key to be constrained.
		* @param {Array} points Array of Coordinates / GeoPoints
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withinPolygon(key, points) {
			return this._addCondition(key, "$geoWithin", { $polygon: points });
		}
		/**
		* Add a constraint to the query that requires a particular key's
		* coordinates that contains a ParseGeoPoint
		*
		* @param {string} key The key to be constrained.
		* @param {Parse.GeoPoint} point
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		polygonContains(key, point) {
			return this._addCondition(key, "$geoIntersects", { $point: point });
		}
		/**
		* Sorts the results in ascending order by the given key.
		*
		* @param {(string|string[])} keys The key to order by, which is a
		* string of comma separated values, or an Array of keys, or multiple keys.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		ascending() {
			this._order = [];
			for (var _len = arguments.length, keys = new Array(_len), _key = 0; _key < _len; _key++) keys[_key] = arguments[_key];
			return this.addAscending.apply(this, keys);
		}
		/**
		* Sorts the results in ascending order by the given key,
		* but can also add secondary sort descriptors without overwriting _order.
		*
		* @param {(string|string[])} keys The key to order by, which is a
		* string of comma separated values, or an Array of keys, or multiple keys.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		addAscending() {
			if (!this._order) this._order = [];
			for (var _len2 = arguments.length, keys = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) keys[_key2] = arguments[_key2];
			(0, _forEach.default)(keys).call(keys, (key) => {
				var _context6;
				if ((0, _isArray.default)(key)) key = key.join();
				this._order = (0, _concat.default)(_context6 = this._order).call(_context6, key.replace(/\s/g, "").split(","));
			});
			return this;
		}
		/**
		* Sorts the results in descending order by the given key.
		*
		* @param {(string|string[])} keys The key to order by, which is a
		* string of comma separated values, or an Array of keys, or multiple keys.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		descending() {
			this._order = [];
			for (var _len3 = arguments.length, keys = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) keys[_key3] = arguments[_key3];
			return this.addDescending.apply(this, keys);
		}
		/**
		* Sorts the results in descending order by the given key,
		* but can also add secondary sort descriptors without overwriting _order.
		*
		* @param {(string|string[])} keys The key to order by, which is a
		* string of comma separated values, or an Array of keys, or multiple keys.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		addDescending() {
			if (!this._order) this._order = [];
			for (var _len4 = arguments.length, keys = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) keys[_key4] = arguments[_key4];
			(0, _forEach.default)(keys).call(keys, (key) => {
				var _context7, _context8;
				if ((0, _isArray.default)(key)) key = key.join();
				this._order = (0, _concat.default)(_context7 = this._order).call(_context7, (0, _map.default)(_context8 = key.replace(/\s/g, "").split(",")).call(_context8, (k) => {
					return "-" + k;
				}));
			});
			return this;
		}
		/**
		* Sets the number of results to skip before returning any results.
		* This is useful for pagination.
		* Default is to skip zero results.
		*
		* @param {number} n the number of results to skip.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		skip(n) {
			if (typeof n !== "number" || n < 0) throw new Error("You can only skip by a positive number");
			this._skip = n;
			return this;
		}
		/**
		* Sets the limit of the number of results to return. The default limit is 100.
		*
		* @param {number} n the number of results to limit to.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		limit(n) {
			if (typeof n !== "number") throw new Error("You can only set the limit to a numeric value");
			this._limit = n;
			return this;
		}
		/**
		* Sets the flag to include with response the total number of objects satisfying this query,
		* despite limits/skip. Might be useful for pagination.
		* Note that result of this query will be wrapped as an object with
		* `results`: holding {ParseObject} array and `count`: integer holding total number
		*
		* @param {boolean} includeCount false - disable, true - enable.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		withCount() {
			let includeCount = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
			if (typeof includeCount !== "boolean") throw new Error("You can only set withCount to a boolean value");
			this._count = includeCount;
			return this;
		}
		/**
		* Includes nested Parse.Objects for the provided key.  You can use dot
		* notation to specify which fields in the included object are also fetched.
		*
		* You can include all nested Parse.Objects by passing in '*'.
		* Requires Parse Server 3.0.0+
		* <pre>query.include('*');</pre>
		*
		* @param {...string|Array<string>} keys The name(s) of the key(s) to include.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		include() {
			for (var _len5 = arguments.length, keys = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) keys[_key5] = arguments[_key5];
			(0, _forEach.default)(keys).call(keys, (key) => {
				if ((0, _isArray.default)(key)) {
					var _context9;
					this._include = (0, _concat.default)(_context9 = this._include).call(_context9, key);
				} else this._include.push(key);
			});
			return this;
		}
		/**
		* Includes all nested Parse.Objects one level deep.
		*
		* Requires Parse Server 3.0.0+
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		includeAll() {
			return this.include("*");
		}
		/**
		* Restricts the fields of the returned Parse.Objects to include only the
		* provided keys.  If this is called multiple times, then all of the keys
		* specified in each of the calls will be included.
		*
		* When selecting `authData` on `Parse.User`, only auth data of currently
		* configured auth providers is returned. Auth data of providers that are no
		* longer configured is not included. To return all auth data regardless of
		* the provider configuration, do not select `authData`.
		*
		* @param {...string|Array<string>} keys The name(s) of the key(s) to include.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		select() {
			if (!this._select) this._select = [];
			for (var _len6 = arguments.length, keys = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) keys[_key6] = arguments[_key6];
			(0, _forEach.default)(keys).call(keys, (key) => {
				if ((0, _isArray.default)(key)) {
					var _context0;
					this._select = (0, _concat.default)(_context0 = this._select).call(_context0, key);
				} else this._select.push(key);
			});
			return this;
		}
		/**
		* Restricts the fields of the returned Parse.Objects to all keys except the
		* provided keys. Exclude takes precedence over select and include.
		*
		* Requires Parse Server 3.6.0+
		*
		* @param {...string|Array<string>} keys The name(s) of the key(s) to exclude.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		exclude() {
			for (var _len7 = arguments.length, keys = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) keys[_key7] = arguments[_key7];
			(0, _forEach.default)(keys).call(keys, (key) => {
				if ((0, _isArray.default)(key)) {
					var _context1;
					this._exclude = (0, _concat.default)(_context1 = this._exclude).call(_context1, key);
				} else this._exclude.push(key);
			});
			return this;
		}
		/**
		* Restricts live query to trigger only for watched fields.
		*
		* Requires Parse Server 6.0.0+
		*
		* @param {...string|Array<string>} keys The name(s) of the key(s) to watch.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		watch() {
			for (var _len8 = arguments.length, keys = new Array(_len8), _key8 = 0; _key8 < _len8; _key8++) keys[_key8] = arguments[_key8];
			(0, _forEach.default)(keys).call(keys, (key) => {
				if ((0, _isArray.default)(key)) {
					var _context10;
					this._watch = (0, _concat.default)(_context10 = this._watch).call(_context10, key);
				} else this._watch.push(key);
			});
			return this;
		}
		/**
		* Changes the read preference that the backend will use when performing the query to the database.
		*
		* @param {string} readPreference The read preference for the main query.
		* @param {string} includeReadPreference The read preference for the queries to include pointers.
		* @param {string} subqueryReadPreference The read preference for the sub queries.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		readPreference(readPreference, includeReadPreference, subqueryReadPreference) {
			this._readPreference = readPreference;
			this._includeReadPreference = includeReadPreference || null;
			this._subqueryReadPreference = subqueryReadPreference || null;
			return this;
		}
		/**
		* Subscribe this query to get liveQuery updates
		*
		* @param {string} sessionToken (optional) Defaults to the currentUser
		* @returns {Promise<LiveQuerySubscription>} Returns the liveQuerySubscription, it's an event emitter
		* which can be used to get liveQuery updates.
		*/
		async subscribe(sessionToken) {
			const currentUser = await _CoreManager.default.getUserController().currentUserAsync();
			if (!sessionToken) sessionToken = currentUser ? currentUser.getSessionToken() || void 0 : void 0;
			const liveQueryClient = await _CoreManager.default.getLiveQueryController().getDefaultLiveQueryClient();
			if (liveQueryClient.shouldOpen()) liveQueryClient.open();
			const subscription = liveQueryClient.subscribe(this, sessionToken);
			return subscription.subscribePromise.then(() => {
				return subscription;
			});
		}
		/**
		* Constructs a Parse.Query that is the OR of the passed in queries.  For
		* example:
		* <pre>var compoundQuery = Parse.Query.or(query1, query2, query3);</pre>
		*
		* will create a compoundQuery that is an or of the query1, query2, and
		* query3.
		*
		* @param {...Parse.Query} queries The list of queries to OR.
		* @static
		* @returns {Parse.Query} The query that is the OR of the passed in queries.
		*/
		static or() {
			for (var _len9 = arguments.length, queries = new Array(_len9), _key9 = 0; _key9 < _len9; _key9++) queries[_key9] = arguments[_key9];
			const className = _getClassNameFromQueries(queries);
			const query = new ParseQuery(className);
			query._orQuery(queries);
			return query;
		}
		/**
		* Constructs a Parse.Query that is the AND of the passed in queries.  For
		* example:
		* <pre>var compoundQuery = Parse.Query.and(query1, query2, query3);</pre>
		*
		* will create a compoundQuery that is an and of the query1, query2, and
		* query3.
		*
		* @param {...Parse.Query} queries The list of queries to AND.
		* @static
		* @returns {Parse.Query} The query that is the AND of the passed in queries.
		*/
		static and() {
			for (var _len0 = arguments.length, queries = new Array(_len0), _key0 = 0; _key0 < _len0; _key0++) queries[_key0] = arguments[_key0];
			const className = _getClassNameFromQueries(queries);
			const query = new ParseQuery(className);
			query._andQuery(queries);
			return query;
		}
		/**
		* Constructs a Parse.Query that is the NOR of the passed in queries.  For
		* example:
		* <pre>const compoundQuery = Parse.Query.nor(query1, query2, query3);</pre>
		*
		* will create a compoundQuery that is a nor of the query1, query2, and
		* query3.
		*
		* @param {...Parse.Query} queries The list of queries to NOR.
		* @static
		* @returns {Parse.Query} The query that is the NOR of the passed in queries.
		*/
		static nor() {
			for (var _len1 = arguments.length, queries = new Array(_len1), _key1 = 0; _key1 < _len1; _key1++) queries[_key1] = arguments[_key1];
			const className = _getClassNameFromQueries(queries);
			const query = new ParseQuery(className);
			query._norQuery(queries);
			return query;
		}
		/**
		* Change the source of this query to the server.
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		fromNetwork() {
			this._queriesLocalDatastore = false;
			this._localDatastorePinName = null;
			return this;
		}
		/**
		* Changes the source of this query to all pinned objects.
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		fromLocalDatastore() {
			return this.fromPinWithName(null);
		}
		/**
		* Changes the source of this query to the default group of pinned objects.
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		fromPin() {
			return this.fromPinWithName(_LocalDatastoreUtils.DEFAULT_PIN);
		}
		/**
		* Changes the source of this query to a specific group of pinned objects.
		*
		* @param {string} name The name of query source.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		fromPinWithName(name) {
			if (_CoreManager.default.getLocalDatastore().checkIfEnabled()) {
				this._queriesLocalDatastore = true;
				this._localDatastorePinName = name;
			}
			return this;
		}
		/**
		* Cancels the current network request (if any is running).
		*
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		cancel() {
			if (this._xhrRequest.task && typeof this._xhrRequest.task.abort === "function") {
				this._xhrRequest.task._aborted = true;
				this._xhrRequest.task.abort();
				this._xhrRequest.task = null;
				this._xhrRequest.onchange = () => {};
				return this;
			}
			this._xhrRequest.onchange = () => this.cancel();
			return this;
		}
		_setRequestTask(options) {
			options.requestTask = (task) => {
				this._xhrRequest.task = task;
				this._xhrRequest.onchange();
			};
		}
		/**
		* Sets a comment to the query so that the query
		* can be identified when using a the profiler for MongoDB.
		*
		* @param {string} value a comment can make your profile data easier to interpret and trace.
		* @returns {Parse.Query} Returns the query, so you can chain this call.
		*/
		comment(value) {
			if (value == null) {
				delete this._comment;
				return this;
			}
			if (typeof value !== "string") throw new Error("The value of a comment to be sent with this query must be a string.");
			this._comment = value;
			return this;
		}
	};
	var DefaultController = {
		find(className, params, options) {
			return _CoreManager.default.getRESTController().request("GET", "classes/" + className, params, options);
		},
		aggregate(className, params, options) {
			return _CoreManager.default.getRESTController().request("GET", "aggregate/" + className, params, options);
		}
	};
	_CoreManager.default.setParseQuery(ParseQuery);
	_CoreManager.default.setQueryController(DefaultController);
	exports.default = ParseQuery;
}));
//#endregion
//#region node_modules/parse/lib/browser/Storage.js
var require_Storage = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	exports.default = {
		async() {
			return !!_CoreManager.default.getStorageController().async;
		},
		getItem(path) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) throw new Error("Synchronous storage is not supported by the current storage controller");
			return controller.getItem(path);
		},
		getItemAsync(path) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) return controller.getItemAsync(path);
			return _promise.default.resolve(controller.getItem(path));
		},
		setItem(path, value) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) throw new Error("Synchronous storage is not supported by the current storage controller");
			return controller.setItem(path, value);
		},
		setItemAsync(path, value) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) return controller.setItemAsync(path, value);
			return _promise.default.resolve(controller.setItem(path, value));
		},
		removeItem(path) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) throw new Error("Synchronous storage is not supported by the current storage controller");
			return controller.removeItem(path);
		},
		removeItemAsync(path) {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) return controller.removeItemAsync(path);
			return _promise.default.resolve(controller.removeItem(path));
		},
		getAllKeys() {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) throw new Error("Synchronous storage is not supported by the current storage controller");
			return controller.getAllKeys();
		},
		getAllKeysAsync() {
			const controller = _CoreManager.default.getStorageController();
			if (controller.async === 1) return controller.getAllKeysAsync();
			return _promise.default.resolve(controller.getAllKeys());
		},
		generatePath(path) {
			if (!_CoreManager.default.get("APPLICATION_ID")) throw new Error("You need to call Parse.initialize before using Parse.");
			if (typeof path !== "string") throw new Error("Tried to get a Storage path that was not a String.");
			if (path[0] === "/") path = path.substr(1);
			return "Parse/" + _CoreManager.default.get("APPLICATION_ID") + "/" + path;
		},
		_clear() {
			const controller = _CoreManager.default.getStorageController();
			if (Object.hasOwn(controller, "clear")) controller.clear();
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/EventuallyQueue.js
var require_EventuallyQueue = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var _splice = _interopRequireDefault(require_splice());
	var _findIndex = _interopRequireDefault(require_find_index());
	var _setInterval2 = _interopRequireDefault(require_set_interval());
	var _find = _interopRequireDefault(require_find());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _ParseQuery = _interopRequireDefault(require_ParseQuery());
	var _Storage = _interopRequireDefault(require_Storage());
	var QUEUE_KEY = "Parse/Eventually/Queue";
	var queueCache = [];
	var dirtyCache = true;
	var polling = void 0;
	/**
	* Provides utility functions to queue objects that will be
	* saved to the server at a later date.
	*
	* @class Parse.EventuallyQueue
	* @static
	*/
	var EventuallyQueue = {
		/**
		* Add object to queue with save operation.
		*
		* @function save
		* @name Parse.EventuallyQueue.save
		* @param {ParseObject} object Parse.Object to be saved eventually
		* @param {object} [serverOptions] See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Object.html#save Parse.Object.save} options.
		* @returns {Promise} A promise that is fulfilled if object is added to queue.
		* @static
		* @see Parse.Object#saveEventually
		*/
		save(object, serverOptions) {
			return this.enqueue("save", object, serverOptions);
		},
		/**
		* Add object to queue with save operation.
		*
		* @function destroy
		* @name Parse.EventuallyQueue.destroy
		* @param {ParseObject} object Parse.Object to be destroyed eventually
		* @param {object} [serverOptions] See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Object.html#destroy Parse.Object.destroy} options
		* @returns {Promise} A promise that is fulfilled if object is added to queue.
		* @static
		* @see Parse.Object#destroyEventually
		*/
		destroy(object, serverOptions) {
			return this.enqueue("destroy", object, serverOptions);
		},
		/**
		* Generate unique identifier to avoid duplicates and maintain previous state.
		*
		* @param {string} action save / destroy
		* @param {object} object Parse.Object to be queued
		* @returns {string}
		* @static
		* @ignore
		*/
		generateQueueId(action, object) {
			object._getId();
			const { className, id, _localId } = object;
			return [
				action,
				className,
				id,
				object.get("hash") || _localId
			].join("_");
		},
		/**
		* Build queue object and add to queue.
		*
		* @param {string} action save / destroy
		* @param {object} object Parse.Object to be queued
		* @param {object} [serverOptions]
		* @returns {Promise} A promise that is fulfilled if object is added to queue.
		* @static
		* @ignore
		*/
		async enqueue(action, object, serverOptions) {
			const queueData = await this.getQueue();
			const queueId = this.generateQueueId(action, object);
			let index = this.queueItemExists(queueData, queueId);
			if (index > -1) {
				for (const prop in queueData[index].object) if (typeof object.get(prop) === "undefined") object.set(prop, queueData[index].object[prop]);
			} else index = queueData.length;
			queueData[index] = {
				queueId,
				action,
				object: object.toJSON(),
				serverOptions: serverOptions || {},
				id: object.id,
				className: object.className,
				hash: object.get("hash"),
				createdAt: /* @__PURE__ */ new Date()
			};
			return this.setQueue(queueData);
		},
		store(data) {
			return _Storage.default.setItemAsync(QUEUE_KEY, (0, _stringify.default)(data));
		},
		load() {
			return _Storage.default.getItemAsync(QUEUE_KEY);
		},
		/**
		* Sets the in-memory queue from local storage and returns.
		*
		* @function getQueue
		* @name Parse.EventuallyQueue.getQueue
		* @returns {Promise<Queue>}
		* @static
		*/
		async getQueue() {
			if (dirtyCache) {
				queueCache = JSON.parse(await this.load() || "[]");
				dirtyCache = false;
			}
			return queueCache;
		},
		/**
		* Saves the queue to local storage
		*
		* @param {Queue} queue Queue containing Parse.Object data.
		* @returns {Promise} A promise that is fulfilled when queue is stored.
		* @static
		* @ignore
		*/
		setQueue(queue) {
			queueCache = queue;
			return this.store(queueCache);
		},
		/**
		* Removes Parse.Object data from queue.
		*
		* @param {string} queueId Unique identifier for Parse.Object data.
		* @returns {Promise} A promise that is fulfilled when queue is stored.
		* @static
		* @ignore
		*/
		async remove(queueId) {
			const queueData = await this.getQueue();
			const index = this.queueItemExists(queueData, queueId);
			if (index > -1) {
				(0, _splice.default)(queueData).call(queueData, index, 1);
				await this.setQueue(queueData);
			}
		},
		/**
		* Removes all objects from queue.
		*
		* @function clear
		* @name Parse.EventuallyQueue.clear
		* @returns {Promise} A promise that is fulfilled when queue is cleared.
		* @static
		*/
		clear() {
			queueCache = [];
			return this.store([]);
		},
		/**
		* Return the index of a queueId in the queue. Returns -1 if not found.
		*
		* @param {Queue} queue Queue containing Parse.Object data.
		* @param {string} queueId Unique identifier for Parse.Object data.
		* @returns {number}
		* @static
		* @ignore
		*/
		queueItemExists(queue, queueId) {
			return (0, _findIndex.default)(queue).call(queue, (data) => data.queueId === queueId);
		},
		/**
		* Return the number of objects in the queue.
		*
		* @function length
		* @name Parse.EventuallyQueue.length
		* @returns {Promise<number>}
		* @static
		*/
		async length() {
			return (await this.getQueue()).length;
		},
		/**
		* Sends the queue to the server.
		*
		* @function sendQueue
		* @name Parse.EventuallyQueue.sendQueue
		* @returns {Promise<boolean>} Returns true if queue was sent successfully.
		* @static
		*/
		async sendQueue() {
			const queueData = [...await this.getQueue()];
			if (queueData.length === 0) return false;
			for (let i = 0; i < queueData.length; i += 1) {
				const queueObject = queueData[i];
				const { id, hash, className } = queueObject;
				const ObjectType = _ParseObject.default.extend(className);
				if (id) await this.process.byId(ObjectType, queueObject);
				else if (hash) await this.process.byHash(ObjectType, queueObject);
				else await this.process.create(ObjectType, queueObject);
			}
			return true;
		},
		/**
		* Build queue object and add to queue.
		*
		* @param {ParseObject} object Parse.Object to be processed
		* @param {QueueObject} queueObject Parse.Object data from the queue
		* @returns {Promise} A promise that is fulfilled when operation is performed.
		* @static
		* @ignore
		*/
		async sendQueueCallback(object, queueObject) {
			if (!object) return this.remove(queueObject.queueId);
			switch (queueObject.action) {
				case "save":
					if (typeof object.updatedAt !== "undefined" && object.updatedAt > new Date(queueObject.object.createdAt)) return this.remove(queueObject.queueId);
					try {
						await object.save(queueObject.object, queueObject.serverOptions);
						await this.remove(queueObject.queueId);
					} catch (e) {
						if (e.code !== _ParseError.default.CONNECTION_FAILED) await this.remove(queueObject.queueId);
					}
					break;
				case "destroy": try {
					await object.destroy(queueObject.serverOptions);
					await this.remove(queueObject.queueId);
				} catch (e) {
					if (e.code !== _ParseError.default.CONNECTION_FAILED) await this.remove(queueObject.queueId);
				}
			}
		},
		/**
		* Start polling server for network connection.
		* Will send queue if connection is established.
		*
		* @function poll
		* @name Parse.EventuallyQueue.poll
		* @param [ms] Milliseconds to ping the server. Default 2000ms
		* @static
		*/
		poll(ms) {
			if (polling) return;
			polling = (0, _setInterval2.default)(() => {
				_CoreManager.default.getRESTController().request("GET", "health").then((_ref) => {
					let { status } = _ref;
					if (status === "ok") {
						this.stopPoll();
						return this.sendQueue();
					}
				}).catch((e) => e);
			}, ms || 2e3);
		},
		/**
		* Turns off polling.
		*
		* @function stopPoll
		* @name Parse.EventuallyQueue.stopPoll
		* @static
		*/
		stopPoll() {
			clearInterval(polling);
			polling = void 0;
		},
		/**
		* Return true if pinging the server.
		*
		* @function isPolling
		* @name Parse.EventuallyQueue.isPolling
		* @returns {boolean}
		* @static
		*/
		isPolling() {
			return !!polling;
		},
		_setPolling(flag) {
			polling = flag;
		},
		process: {
			create(ObjectType, queueObject) {
				const object = new ObjectType();
				return EventuallyQueue.sendQueueCallback(object, queueObject);
			},
			async byId(ObjectType, queueObject) {
				const { sessionToken } = queueObject.serverOptions;
				const query = new _ParseQuery.default(ObjectType);
				query.equalTo("objectId", queueObject.id);
				const results = await (0, _find.default)(query).call(query, { sessionToken });
				return EventuallyQueue.sendQueueCallback(results[0], queueObject);
			},
			async byHash(ObjectType, queueObject) {
				const { sessionToken } = queueObject.serverOptions;
				const query = new _ParseQuery.default(ObjectType);
				query.equalTo("hash", queueObject.hash);
				const results = await (0, _find.default)(query).call(query, { sessionToken });
				if (results.length > 0) return EventuallyQueue.sendQueueCallback(results[0], queueObject);
				return EventuallyQueue.process.create(ObjectType, queueObject);
			}
		}
	};
	exports.default = EventuallyQueue;
}));
//#endregion
//#region node_modules/idb-keyval/dist/index.js
var dist_exports = /* @__PURE__ */ __exportAll({
	clear: () => clear,
	createStore: () => createStore,
	del: () => del,
	delMany: () => delMany,
	entries: () => entries,
	get: () => get,
	getMany: () => getMany,
	keys: () => keys,
	promisifyRequest: () => promisifyRequest,
	set: () => set,
	setMany: () => setMany,
	update: () => update,
	values: () => values
});
function promisifyRequest(request) {
	return new Promise((resolve, reject) => {
		request.oncomplete = request.onsuccess = () => resolve(request.result);
		request.onabort = request.onerror = () => reject(request.error);
	});
}
function createStore(dbName, storeName) {
	let dbp;
	const getDB = () => {
		if (dbp) return dbp;
		const request = indexedDB.open(dbName);
		request.onupgradeneeded = () => request.result.createObjectStore(storeName);
		dbp = promisifyRequest(request);
		dbp.then((db) => {
			db.onclose = () => dbp = void 0;
		}, () => {});
		return dbp;
	};
	return (txMode, callback) => getDB().then((db) => callback(db.transaction(storeName, txMode).objectStore(storeName)));
}
function defaultGetStore() {
	if (!defaultGetStoreFunc) defaultGetStoreFunc = createStore("keyval-store", "keyval");
	return defaultGetStoreFunc;
}
/**
* Get a value by its key.
*
* @param key
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function get(key, customStore = defaultGetStore()) {
	return customStore("readonly", (store) => promisifyRequest(store.get(key)));
}
/**
* Set a value with a key.
*
* @param key
* @param value
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function set(key, value, customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => {
		store.put(value, key);
		return promisifyRequest(store.transaction);
	});
}
/**
* Set multiple values at once. This is faster than calling set() multiple times.
* It's also atomic – if one of the pairs can't be added, none will be added.
*
* @param entries Array of entries, where each entry is an array of `[key, value]`.
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function setMany(entries, customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => {
		entries.forEach((entry) => store.put(entry[1], entry[0]));
		return promisifyRequest(store.transaction);
	});
}
/**
* Get multiple values by their keys
*
* @param keys
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function getMany(keys, customStore = defaultGetStore()) {
	return customStore("readonly", (store) => Promise.all(keys.map((key) => promisifyRequest(store.get(key)))));
}
/**
* Update a value. This lets you see the old value and update it as an atomic operation.
*
* @param key
* @param updater A callback that takes the old value and returns a new value.
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function update(key, updater, customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => new Promise((resolve, reject) => {
		store.get(key).onsuccess = function() {
			try {
				store.put(updater(this.result), key);
				resolve(promisifyRequest(store.transaction));
			} catch (err) {
				reject(err);
			}
		};
	}));
}
/**
* Delete a particular key from the store.
*
* @param key
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function del(key, customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => {
		store.delete(key);
		return promisifyRequest(store.transaction);
	});
}
/**
* Delete multiple keys at once.
*
* @param keys List of keys to delete.
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function delMany(keys, customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => {
		keys.forEach((key) => store.delete(key));
		return promisifyRequest(store.transaction);
	});
}
/**
* Clear all values in the store.
*
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function clear(customStore = defaultGetStore()) {
	return customStore("readwrite", (store) => {
		store.clear();
		return promisifyRequest(store.transaction);
	});
}
function eachCursor(store, callback) {
	store.openCursor().onsuccess = function() {
		if (!this.result) return;
		callback(this.result);
		this.result.continue();
	};
	return promisifyRequest(store.transaction);
}
/**
* Get all keys in the store.
*
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function keys(customStore = defaultGetStore()) {
	return customStore("readonly", (store) => {
		if (store.getAllKeys) return promisifyRequest(store.getAllKeys());
		const items = [];
		return eachCursor(store, (cursor) => items.push(cursor.key)).then(() => items);
	});
}
/**
* Get all values in the store.
*
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function values(customStore = defaultGetStore()) {
	return customStore("readonly", (store) => {
		if (store.getAll) return promisifyRequest(store.getAll());
		const items = [];
		return eachCursor(store, (cursor) => items.push(cursor.value)).then(() => items);
	});
}
/**
* Get all entries in the store. Each entry is an array of `[key, value]`.
*
* @param customStore Method to get a custom store. Use with caution (see the docs).
*/
function entries(customStore = defaultGetStore()) {
	return customStore("readonly", (store) => {
		if (store.getAll && store.getAllKeys) return Promise.all([promisifyRequest(store.getAllKeys()), promisifyRequest(store.getAll())]).then(([keys, values]) => keys.map((key, i) => [key, values[i]]));
		const items = [];
		return customStore("readonly", (store) => eachCursor(store, (cursor) => items.push([cursor.key, cursor.value])).then(() => items));
	});
}
var defaultGetStoreFunc;
var init_dist = __esmMin((() => {}));
//#endregion
//#region node_modules/parse/lib/browser/IndexedDBStorageController.js
var require_IndexedDBStorageController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _keysInstanceProperty = require_keys();
	require_define_property$3()(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _idbKeyval = (init_dist(), __toCommonJS(dist_exports));
	var IndexedDBStorageController;
	if (typeof window !== "undefined" && window.indexedDB) try {
		const ParseStore = (0, _idbKeyval.createStore)("parseDB", "parseStore");
		IndexedDBStorageController = {
			async: 1,
			getItemAsync(path) {
				return (0, _idbKeyval.get)(path, ParseStore);
			},
			setItemAsync(path, value) {
				return (0, _idbKeyval.set)(path, value, ParseStore);
			},
			removeItemAsync(path) {
				return (0, _idbKeyval.del)(path, ParseStore);
			},
			getAllKeysAsync() {
				return _keysInstanceProperty(_idbKeyval)(ParseStore);
			},
			clear() {
				return (0, _idbKeyval.clear)(ParseStore);
			}
		};
	} catch (_) {
		IndexedDBStorageController = void 0;
	}
	else IndexedDBStorageController = void 0;
	exports.default = IndexedDBStorageController;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseInstallation.js
var require_ParseInstallation = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _entries = _interopRequireDefault(require_entries$4());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var DEVICE_TYPES = {
		IOS: "ios",
		MACOS: "macos",
		TVOS: "tvos",
		FCM: "fcm",
		ANDROID: "android",
		WEB: "web"
	};
	/**
	* Parse.Installation is a local representation of installation data that can be saved and retrieved from the Parse cloud.
	* This class is a subclass of a Parse.Object, and retains the same functionality of a Parse.Object, but also extends it with installation-specific features.
	*
	* <p>A valid Parse.Installation can only be instantiated via <code>Parse.Installation.currentInstallation()</code>
	*
	* Parse.Installation objects which have a valid <code>deviceToken</code> and are saved to the Parse cloud can be used to target push notifications.
	* </p>
	*
	* @alias Parse.Installation
	*/
	var ParseInstallation = class extends _ParseObject.default {
		/**
		* @param {object} attributes The initial set of data to store in the object.
		*/
		constructor(attributes) {
			super("_Installation");
			if (attributes && typeof attributes === "object") try {
				this.set(attributes || {});
			} catch (_) {
				throw new Error("Can't create an invalid Installation");
			}
		}
		/**
		* A unique identifier for this installation’s client application. In iOS, this is the Bundle Identifier.
		*
		* @property {string} appIdentifier
		* @static
		* @returns {string}
		*/
		get appIdentifier() {
			return this.get("appIdentifier");
		}
		/**
		* The version string of the client application to which this installation belongs.
		*
		* @property {string} appVersion
		* @static
		* @returns {string}
		*/
		get appVersion() {
			return this.get("appVersion");
		}
		/**
		* The display name of the client application to which this installation belongs.
		*
		* @property {string} appName
		* @static
		* @returns {string}
		*/
		get appName() {
			return this.get("appName");
		}
		/**
		* The current value of the icon badge for iOS apps.
		* Changes to this value on the server will be used
		* for future badge-increment push notifications.
		*
		* @property {number} badge
		* @static
		* @returns {number}
		*/
		get badge() {
			return this.get("badge");
		}
		/**
		* An array of the channels to which a device is currently subscribed.
		*
		* @property {string[]} channels
		* @static
		* @returns {string[]}
		*/
		get channels() {
			return this.get("channels");
		}
		/**
		* Token used to deliver push notifications to the device.
		*
		* @property {string} deviceToken
		* @static
		* @returns {string}
		*/
		get deviceToken() {
			return this.get("deviceToken");
		}
		/**
		* The type of device, “ios”, “android”, “web”, etc.
		*
		* @property {string} deviceType
		* @static
		* @returns {string}
		*/
		get deviceType() {
			return this.get("deviceType");
		}
		/**
		* Gets the GCM sender identifier for this installation
		*
		* @property {string} GCMSenderId
		* @static
		* @returns {string}
		*/
		get GCMSenderId() {
			return this.get("GCMSenderId");
		}
		/**
		* Universally Unique Identifier (UUID) for the device used by Parse. It must be unique across all of an app’s installations.
		*
		* @property {string} installationId
		* @static
		* @returns {string}
		*/
		get installationId() {
			return this.get("installationId");
		}
		/**
		* Gets the local identifier for this installation
		*
		* @property {string} localeIdentifier
		* @static
		* @returns {string}
		*/
		get localeIdentifier() {
			return this.get("localeIdentifier");
		}
		/**
		* Gets the parse server version for this installation
		*
		* @property {string} parseVersion
		* @static
		* @returns {string}
		*/
		get parseVersion() {
			return this.get("parseVersion");
		}
		/**
		* This field is reserved for directing Parse to the push delivery network to be used.
		*
		* @property {string} pushType
		* @static
		* @returns {string}
		*/
		get pushType() {
			return this.get("pushType");
		}
		/**
		* Gets the time zone for this installation
		*
		* @property {string} timeZone
		* @static
		* @returns {string}
		*/
		get timeZone() {
			return this.get("timeZone");
		}
		/**
		* Returns the device types for used for Push Notifications.
		*
		* <pre>
		* Parse.Installation.DEVICE_TYPES.IOS
		* Parse.Installation.DEVICE_TYPES.MACOS
		* Parse.Installation.DEVICE_TYPES.TVOS
		* Parse.Installation.DEVICE_TYPES.FCM
		* Parse.Installation.DEVICE_TYPES.ANDROID
		* Parse.Installation.DEVICE_TYPES.WEB
		* </pre
		*
		* @property {object} DEVICE_TYPES
		* @static
		* @returns {object}
		*/
		static get DEVICE_TYPES() {
			return DEVICE_TYPES;
		}
		/**
		* Wrap the default fetch behavior with functionality to update local storage.
		* If the installation is deleted on the server, retry the fetch as a save operation.
		*
		* @param {...any} args
		* @returns {Promise}
		*/
		async fetch() {
			for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
			try {
				await super.fetch.apply(this, args);
			} catch (e) {
				if (e.code !== _ParseError.default.OBJECT_NOT_FOUND) throw e;
				delete this.id;
				this._getId();
				this._markAllFieldsDirty();
				await super.save.apply(this, args);
			}
			await _CoreManager.default.getInstallationController().updateInstallationOnDisk(this);
			return this;
		}
		/**
		* Wrap the default save behavior with functionality to update the local storage.
		* If the installation is deleted on the server, retry saving a new installation.
		*
		* @param {...any} args
		* @returns {Promise}
		*/
		async save() {
			for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) args[_key2] = arguments[_key2];
			try {
				await super.save.apply(this, args);
			} catch (e) {
				if (e.code !== _ParseError.default.OBJECT_NOT_FOUND) throw e;
				delete this.id;
				this._getId();
				this._markAllFieldsDirty();
				await super.save.apply(this, args);
			}
			await _CoreManager.default.getInstallationController().updateInstallationOnDisk(this);
			return this;
		}
		_markAllFieldsDirty() {
			for (const [key, value] of (0, _entries.default)(this.attributes)) this.set(key, value);
		}
		/**
		* Get the current Parse.Installation from disk. If doesn't exists, create an new installation.
		*
		* <pre>
		* const installation = await Parse.Installation.currentInstallation();
		* installation.set('deviceToken', '123');
		* await installation.save();
		* </pre>
		*
		* @returns {Promise} A promise that resolves to the local installation object.
		*/
		static currentInstallation() {
			return _CoreManager.default.getInstallationController().currentInstallation();
		}
	};
	_ParseObject.default.registerSubclass("_Installation", ParseInstallation);
	exports.default = ParseInstallation;
}));
//#endregion
//#region node_modules/parse/lib/browser/InstallationController.js
var require_InstallationController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _Storage = _interopRequireDefault(require_Storage());
	var _ParseInstallation = _interopRequireDefault(require_ParseInstallation());
	var _uuid = _interopRequireDefault(require_uuid());
	var CURRENT_INSTALLATION_KEY = "currentInstallation";
	var CURRENT_INSTALLATION_ID_KEY = "currentInstallationId";
	var iidCache = null;
	var currentInstallationCache = null;
	var currentInstallationCacheMatchesDisk = false;
	exports.default = {
		async updateInstallationOnDisk(installation) {
			const path = _Storage.default.generatePath(CURRENT_INSTALLATION_KEY);
			await _Storage.default.setItemAsync(path, (0, _stringify.default)(installation.toJSON()));
			this._setCurrentInstallationCache(installation);
		},
		async currentInstallationId() {
			if (typeof iidCache === "string") return iidCache;
			const path = _Storage.default.generatePath(CURRENT_INSTALLATION_ID_KEY);
			let iid = await _Storage.default.getItemAsync(path);
			if (!iid) {
				iid = (0, _uuid.default)();
				return _Storage.default.setItemAsync(path, iid).then(() => {
					iidCache = iid;
					return iid;
				});
			}
			iidCache = iid;
			return iid;
		},
		async currentInstallation() {
			if (currentInstallationCache) return currentInstallationCache;
			if (currentInstallationCacheMatchesDisk) return null;
			const path = _Storage.default.generatePath(CURRENT_INSTALLATION_KEY);
			let installationData = await _Storage.default.getItemAsync(path);
			currentInstallationCacheMatchesDisk = true;
			if (installationData) {
				installationData = JSON.parse(installationData);
				installationData.className = "_Installation";
				const current = _ParseInstallation.default.fromJSON(installationData);
				currentInstallationCache = current;
				return current;
			}
			const installationId = await this.currentInstallationId();
			const installation = new _ParseInstallation.default();
			installation.set("deviceType", _ParseInstallation.default.DEVICE_TYPES.WEB);
			installation.set("installationId", installationId);
			installation.set("parseVersion", _CoreManager.default.get("VERSION"));
			currentInstallationCache = installation;
			await _Storage.default.setItemAsync(path, (0, _stringify.default)(installation.toJSON()));
			return installation;
		},
		_clearCache() {
			iidCache = null;
			currentInstallationCache = null;
			currentInstallationCacheMatchesDisk = false;
		},
		_setInstallationIdCache(iid) {
			iidCache = iid;
		},
		_setCurrentInstallationCache(installation) {
			let matchesDisk = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
			currentInstallationCache = installation;
			currentInstallationCacheMatchesDisk = matchesDisk;
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.string.ends-with.js
var require_es_string_ends_with = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this_clause();
	var getOwnPropertyDescriptor = require_object_get_own_property_descriptor().f;
	var toLength = require_to_length();
	var toString = require_to_string();
	var notARegExp = require_not_a_regexp();
	var requireObjectCoercible = require_require_object_coercible();
	var correctIsRegExpLogic = require_correct_is_regexp_logic();
	var IS_PURE = require_is_pure();
	var slice = uncurryThis("".slice);
	var min = Math.min;
	var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic("endsWith");
	$({
		target: "String",
		proto: true,
		forced: !(!IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function() {
			var descriptor = getOwnPropertyDescriptor(String.prototype, "endsWith");
			return descriptor && !descriptor.writable;
		}()) && !CORRECT_IS_REGEXP_LOGIC
	}, { endsWith: function endsWith(searchString) {
		var that = toString(requireObjectCoercible(this));
		notARegExp(searchString);
		var search = toString(searchString);
		var endPosition = arguments.length > 1 ? arguments[1] : void 0;
		var len = that.length;
		var end = endPosition === void 0 ? len : min(toLength(endPosition), len);
		return slice(that, end - search.length, end) === search;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/string/virtual/ends-with.js
var require_ends_with$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_string_ends_with();
	module.exports = require_get_built_in_prototype_method()("String", "endsWith");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/ends-with.js
var require_ends_with$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_ends_with$3();
	var StringPrototype = String.prototype;
	module.exports = function(it) {
		var own = it.endsWith;
		return typeof it == "string" || it === StringPrototype || isPrototypeOf(StringPrototype, it) && own === StringPrototype.endsWith ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/ends-with.js
var require_ends_with$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_ends_with$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/ends-with.js
var require_ends_with = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_ends_with$1();
}));
//#endregion
//#region node_modules/core-js-pure/stable/set-timeout.js
var require_set_timeout$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_web_timers();
	module.exports = require_path().setTimeout;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/set-timeout.js
var require_set_timeout = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_set_timeout$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-reduce.js
var require_array_reduce = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aCallable = require_a_callable();
	var toObject = require_to_object();
	var IndexedObject = require_indexed_object();
	var lengthOfArrayLike = require_length_of_array_like();
	var $TypeError = TypeError;
	var REDUCE_EMPTY = "Reduce of empty array with no initial value";
	var createMethod = function(IS_RIGHT) {
		return function(that, callbackfn, argumentsLength, memo) {
			var O = toObject(that);
			var self = IndexedObject(O);
			var length = lengthOfArrayLike(O);
			aCallable(callbackfn);
			if (length === 0 && argumentsLength < 2) throw new $TypeError(REDUCE_EMPTY);
			var index = IS_RIGHT ? length - 1 : 0;
			var i = IS_RIGHT ? -1 : 1;
			if (argumentsLength < 2) while (true) {
				if (index in self) {
					memo = self[index];
					index += i;
					break;
				}
				index += i;
				if (IS_RIGHT ? index < 0 : length <= index) throw new $TypeError(REDUCE_EMPTY);
			}
			for (; IS_RIGHT ? index >= 0 : length > index; index += i) if (index in self) memo = callbackfn(memo, self[index], index, O);
			return memo;
		};
	};
	module.exports = {
		left: createMethod(false),
		right: createMethod(true)
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.reduce.js
var require_es_array_reduce = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $reduce = require_array_reduce().left;
	var arrayMethodIsStrict = require_array_method_is_strict();
	var CHROME_VERSION = require_environment_v8_version();
	$({
		target: "Array",
		proto: true,
		forced: !require_environment_is_node() && CHROME_VERSION > 79 && CHROME_VERSION < 83 || !arrayMethodIsStrict("reduce")
	}, { reduce: function reduce(callbackfn) {
		var length = arguments.length;
		return $reduce(this, callbackfn, length, length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/reduce.js
var require_reduce$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_reduce();
	module.exports = require_get_built_in_prototype_method()("Array", "reduce");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/reduce.js
var require_reduce$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_reduce$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.reduce;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.reduce ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/reduce.js
var require_reduce$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_reduce$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/reduce.js
var require_reduce = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_reduce$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/Xhr.weapp.js
var require_Xhr_weapp = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.polyfillFetch = polyfillFetch;
	var _reduce = _interopRequireDefault(require_reduce());
	var _keys = _interopRequireDefault(require_keys$4());
	var _promise = _interopRequireDefault(require_promise());
	/* istanbul ignore file */
	function parseResponse(res) {
		var _context;
		let headers = res.header || {};
		headers = (0, _reduce.default)(_context = (0, _keys.default)(headers)).call(_context, (map, key) => {
			map[key.toLowerCase()] = headers[key];
			return map;
		}, {});
		return {
			status: res.statusCode,
			json: () => {
				if (typeof res.data === "object") return _promise.default.resolve(res.data);
				let json = {};
				try {
					json = JSON.parse(res.data);
				} catch (err) {
					console.error(err);
				}
				return _promise.default.resolve(json);
			},
			headers: {
				keys: () => (0, _keys.default)(headers),
				get: (n) => headers[n.toLowerCase()],
				has: (n) => n.toLowerCase() in headers,
				entries: () => {
					const all = [];
					for (const key in headers) if (headers[key]) all.push([key, headers[key]]);
					return all;
				}
			}
		};
	}
	function polyfillFetch() {
		const typedGlobal = global;
		if (typeof typedGlobal.fetch !== "function") typedGlobal.fetch = (url, options) => {
			const dataType = url.match(/\.(txt|json|html|txt|csv)/) ? "text" : "arraybuffer";
			return new _promise.default((resolve, reject) => {
				wx.request({
					url,
					method: options.method || "GET",
					data: options.body,
					header: options.headers,
					dataType,
					responseType: dataType,
					success: (response) => resolve(parseResponse(response)),
					fail: (error) => reject(error)
				});
			});
		};
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/RESTController.js
var require_RESTController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty2 = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _endsWith = _interopRequireDefault(require_ends_with());
	var _slice = _interopRequireDefault(require_slice());
	var _startsWith = _interopRequireDefault(require_starts_with());
	var _promise = _interopRequireDefault(require_promise());
	var _stringify = _interopRequireDefault(require_stringify());
	var _includes = _interopRequireDefault(require_includes());
	var _forEach = _interopRequireDefault(require_for_each());
	var _setTimeout2 = _interopRequireDefault(require_set_timeout());
	var _uuid = _interopRequireDefault(require_uuid());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _promiseUtils = require_promiseUtils();
	require_Xhr_weapp();
	function ownKeys(e, r) {
		var t = _Object$keys(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context4, _context5;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty2(_context4 = ownKeys(Object(t), !0)).call(_context4, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty2(_context5 = ownKeys(Object(t))).call(_context5, function(r) {
				_Object$defineProperty(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	/** Base options for all Parse requests that go through _getRequestOptions */
	var useXDomainRequest = false;
	if (typeof XDomainRequest !== "undefined" && !("withCredentials" in new XMLHttpRequest())) useXDomainRequest = true;
	function getPath(base, pathname) {
		if ((0, _endsWith.default)(base).call(base, "/")) base = (0, _slice.default)(base).call(base, 0, -1);
		if (!(0, _startsWith.default)(pathname).call(pathname, "/")) pathname = "/" + pathname;
		return base + pathname;
	}
	function ajaxIE9(method, url, data, _headers, options) {
		return new _promise.default((resolve, reject) => {
			const xdr = new XDomainRequest();
			xdr.onload = function() {
				let response;
				try {
					response = JSON.parse(xdr.responseText);
				} catch (e) {
					reject(e);
				}
				if (response) resolve({ response });
			};
			xdr.onerror = xdr.ontimeout = function() {
				reject({ responseText: (0, _stringify.default)({
					code: _ParseError.default.X_DOMAIN_REQUEST,
					error: "IE's XDomainRequest does not supply error info."
				}) });
			};
			xdr.onprogress = function() {
				if (options && typeof options.progress === "function") options.progress(xdr.responseText);
			};
			xdr.open(method, url);
			xdr.send(data);
			if (options && typeof options.requestTask === "function") options.requestTask(xdr);
		});
	}
	var RESTController = {
		async ajax(method, url, data, headers, options) {
			var _context;
			if (useXDomainRequest) return ajaxIE9(method, url, data, headers, options);
			if (typeof fetch !== "function") throw new Error("Cannot make a request: Fetch API not found.");
			const promise = (0, _promiseUtils.resolvingPromise)();
			const isIdempotent = _CoreManager.default.get("IDEMPOTENCY") && (0, _includes.default)(_context = ["POST", "PUT"]).call(_context, method);
			const requestId = isIdempotent ? (0, _uuid.default)() : "";
			let attempts = 0;
			const dispatch = async function() {
				const controller = new AbortController();
				const { signal } = controller;
				headers = headers || {};
				if (typeof headers["Content-Type"] !== "string") headers["Content-Type"] = "text/plain";
				if (_CoreManager.default.get("IS_NODE")) headers["User-Agent"] = "Parse/" + _CoreManager.default.get("VERSION") + " (NodeJS " + process.versions.node + ")";
				if (isIdempotent) headers["X-Parse-Request-Id"] = requestId;
				const customHeaders = _CoreManager.default.get("REQUEST_HEADERS");
				for (const key in customHeaders) headers[key] = customHeaders[key];
				if (options && typeof options.requestTask === "function") options.requestTask(controller);
				try {
					var _context3;
					const fetchOptions = {
						method,
						headers,
						signal,
						redirect: "manual"
					};
					if (data) {
						fetchOptions.body = data;
						if (typeof ReadableStream !== "undefined" && data instanceof ReadableStream) fetchOptions.duplex = "half";
					}
					const response = await fetch(url, fetchOptions);
					const { status } = response;
					if (status >= 200 && status < 300) {
						var _context2;
						let result;
						const responseHeaders = {};
						const availableHeaders = response.headers.get("access-control-expose-headers") || "";
						(0, _forEach.default)(_context2 = availableHeaders.split(", ")).call(_context2, (header) => {
							if (header && response.headers.has(header)) responseHeaders[header] = response.headers.get(header);
						});
						if (options && typeof options.progress === "function" && response.body) {
							const reader = response.body.getReader();
							const length = +response.headers.get("Content-Length") || 0;
							if (length === 0) {
								options.progress(null, null, null);
								result = await response.json();
							} else {
								let recieved = 0;
								const chunks = [];
								while (true) {
									const { done, value } = await reader.read();
									if (done) break;
									chunks.push(value);
									recieved += (value === null || value === void 0 ? void 0 : value.length) || 0;
									options.progress(recieved / length, recieved, length);
								}
								const body = new Uint8Array(recieved);
								let offset = 0;
								for (const chunk of chunks) {
									body.set(chunk, offset);
									offset += chunk.length;
								}
								const jsonString = new TextDecoder().decode(body);
								result = JSON.parse(jsonString);
							}
						} else result = await response.json();
						promise.resolve({
							status,
							response: result,
							headers: responseHeaders
						});
					} else if (status >= 400 && status < 500) {
						const error = await response.json();
						promise.reject(error);
					} else if ((0, _includes.default)(_context3 = [
						301,
						302,
						303,
						307,
						308
					]).call(_context3, status)) {
						const location = response.headers.get("location");
						promise.resolve({
							status,
							location,
							method: status === 303 ? "GET" : method,
							dropBody: status === 303
						});
					} else if (status >= 500 || status === 0) {
						if (!(typeof ReadableStream !== "undefined" && data instanceof ReadableStream) && ++attempts < _CoreManager.default.get("REQUEST_ATTEMPT_LIMIT")) {
							const delay = Math.round(Math.random() * 125 * Math.pow(2, attempts));
							(0, _setTimeout2.default)(dispatch, delay);
						} else if (status === 0) promise.reject("Unable to connect to the Parse API");
						else {
							const error = await response.json();
							promise.reject(error);
						}
					} else promise.reject(response);
				} catch (error) {
					var _error$cause;
					if (error.name === "AbortError") promise.resolve({
						response: { results: [] },
						status: 0
					});
					else if (((_error$cause = error.cause) === null || _error$cause === void 0 ? void 0 : _error$cause.code) === "ECONNREFUSED") promise.reject("Unable to connect to the Parse API");
					else promise.reject(error);
				}
			};
			dispatch();
			return promise;
		},
		request(method, path, data, options) {
			options = options || {};
			const url = getPath(_CoreManager.default.get("SERVER_URL"), path);
			const payload = {};
			if (data && typeof data === "object") for (const k in data) payload[k] = data[k];
			const context = options.context;
			if (context !== void 0) payload._context = context;
			if (method !== "POST") {
				payload._method = method;
				method = "POST";
			}
			payload._ApplicationId = _CoreManager.default.get("APPLICATION_ID");
			const jsKey = _CoreManager.default.get("JAVASCRIPT_KEY");
			if (jsKey) payload._JavaScriptKey = jsKey;
			payload._ClientVersion = _CoreManager.default.get("VERSION");
			let useMasterKey = options.useMasterKey;
			if (typeof useMasterKey === "undefined") useMasterKey = _CoreManager.default.get("USE_MASTER_KEY");
			if (useMasterKey) {
				if (_CoreManager.default.get("MASTER_KEY")) {
					delete payload._JavaScriptKey;
					payload._MasterKey = _CoreManager.default.get("MASTER_KEY");
				} else throw new Error("Cannot use the Master Key, it has not been provided.");
			}
			if (options.useMaintenanceKey) payload._MaintenanceKey = _CoreManager.default.get("MAINTENANCE_KEY");
			if (_CoreManager.default.get("FORCE_REVOCABLE_SESSION")) payload._RevocableSession = "1";
			const installationId = options.installationId;
			let installationIdPromise;
			if (installationId && typeof installationId === "string") installationIdPromise = _promise.default.resolve(installationId);
			else installationIdPromise = _CoreManager.default.getInstallationController().currentInstallationId();
			return installationIdPromise.then((iid) => {
				payload._InstallationId = iid;
				const userController = _CoreManager.default.getUserController();
				if (options && typeof options.sessionToken === "string") return _promise.default.resolve(options.sessionToken);
				else if (userController) return userController.currentUserAsync().then((user) => {
					if (user) return _promise.default.resolve(user.getSessionToken());
					return _promise.default.resolve(null);
				});
				return _promise.default.resolve(null);
			}).then((token) => {
				if (token) payload._SessionToken = token;
				const payloadString = (0, _stringify.default)(payload);
				return RESTController.ajax(method, url, payloadString, {}, options).then(async (result) => {
					if (result.location) {
						let newURL = getPath(result.location, path);
						let newMethod = result.method;
						let newBody = result.dropBody ? void 0 : payloadString;
						for (let i = 0; i < 5; i += 1) {
							const r = await RESTController.ajax(newMethod, newURL, newBody, {}, options);
							if (!r.location) {
								result = r;
								break;
							}
							newURL = getPath(r.location, path);
							newMethod = r.method;
							newBody = r.dropBody ? void 0 : payloadString;
						}
					}
					const { response, status, headers } = result;
					if (options.returnStatus) return _objectSpread(_objectSpread({}, response), {}, {
						_status: status,
						_headers: headers
					});
					else return response;
				});
			}).catch(RESTController.handleError);
		},
		handleError(errorJSON) {
			let error;
			if (errorJSON.code || errorJSON.error || errorJSON.message) error = new _ParseError.default(errorJSON.code, errorJSON.error || errorJSON.message);
			else error = new _ParseError.default(_ParseError.default.CONNECTION_FAILED, "XMLHttpRequest failed: " + (0, _stringify.default)(errorJSON));
			return _promise.default.reject(error);
		}
	};
	exports.default = RESTController;
}));
//#endregion
//#region node_modules/parse/lib/browser/Analytics.js
var require_Analytics = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.track = track;
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	/**
	* Parse.Analytics provides an interface to Parse's logging and analytics
	* backend.
	*
	* @class Parse.Analytics
	* @static
	* @hideconstructor
	*/
	/**
	* Tracks the occurrence of a custom event with additional dimensions.
	* Parse will store a data point at the time of invocation with the given
	* event name.
	*
	* Dimensions will allow segmentation of the occurrences of this custom
	* event. Keys and values should be {@code String}s, and will throw
	* otherwise.
	*
	* To track a user signup along with additional metadata, consider the
	* following:
	* <pre>
	* var dimensions = {
	*  gender: 'm',
	*  source: 'web',
	*  dayType: 'weekend'
	* };
	* Parse.Analytics.track('signup', dimensions);
	* </pre>
	*
	* There is a default limit of 8 dimensions per event tracked.
	*
	* @function track
	* @name Parse.Analytics.track
	* @param {string} name The name of the custom event to report to Parse as
	* having happened.
	* @param {object} dimensions The dictionary of information by which to
	* segment this event.
	* @returns {Promise} A promise that is resolved when the round-trip
	* to the server completes.
	*/
	function track(name, dimensions) {
		name = name || "";
		name = name.replace(/^\s*/, "");
		name = name.replace(/\s*$/, "");
		if (name.length === 0) throw new TypeError("A name for the custom event must be provided");
		for (const key in dimensions) if (typeof key !== "string" || typeof dimensions[key] !== "string") throw new TypeError("track() dimensions expects keys and values of type \"string\".");
		return _CoreManager.default.getAnalyticsController().track(name, dimensions);
	}
	_CoreManager.default.setAnalyticsController({ track(name, dimensions) {
		return _CoreManager.default.getRESTController().request("POST", "events/" + name, { dimensions });
	} });
}));
//#endregion
//#region node_modules/parse/lib/browser/isRevocableSession.js
var require_isRevocableSession = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = isRevocableSession;
	var _indexOf = _interopRequireDefault(require_index_of());
	function isRevocableSession(token) {
		return (0, _indexOf.default)(token).call(token, "r:") > -1;
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseUser.js
var require_ParseUser = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty2 = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty2(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _promise = _interopRequireDefault(require_promise());
	var _defineProperty3 = _interopRequireDefault(require_define_property$3());
	var _stringify = _interopRequireDefault(require_stringify());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _isRevocableSession = _interopRequireDefault(require_isRevocableSession());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _Storage = _interopRequireDefault(require_Storage());
	function ownKeys(e, r) {
		var t = _Object$keys(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context, _context2;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty(_context = ownKeys(Object(t), !0)).call(_context, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty(_context2 = ownKeys(Object(t))).call(_context2, function(r) {
				_Object$defineProperty2(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var CURRENT_USER_KEY = "currentUser";
	var canUseCurrentUser = !_CoreManager.default.get("IS_NODE");
	var currentUserCacheMatchesDisk = false;
	var currentUserCache = null;
	var authProviders = {};
	/**
	* <p>A Parse.User object is a local representation of a user persisted to the
	* Parse cloud. This class is a subclass of a Parse.Object, and retains the
	* same functionality of a Parse.Object, but also extends it with various
	* user specific methods, like authentication, signing up, and validation of
	* uniqueness.</p>
	*
	* @alias Parse.User
	* @augments Parse.Object
	*/
	var ParseUser = class ParseUser extends _ParseObject.default {
		/**
		* @param {object} attributes The initial set of data to store in the user.
		*/
		constructor(attributes) {
			super("_User");
			if (attributes && typeof attributes === "object") try {
				this.set(attributes || {});
			} catch (_) {
				throw new Error("Can't create an invalid Parse User");
			}
		}
		/**
		* Request a revocable session token to replace the older style of token.
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		* </ul>
		* @returns {Promise} A promise that is resolved when the replacement
		*   token has been fetched.
		*/
		_upgradeToRevocableSession(options) {
			const upgradeOptions = _ParseObject.default._getRequestOptions(options);
			return _CoreManager.default.getUserController().upgradeToRevocableSession(this, upgradeOptions);
		}
		/**
		* Parse allows you to link your users with {@link https://docs.parseplatform.org/parse-server/guide/#oauth-and-3rd-party-authentication 3rd party authentication}, enabling
		* your users to sign up or log into your application using their existing identities.
		* Since 2.9.0
		*
		* @see {@link https://docs.parseplatform.org/js/guide/#linking-users Linking Users}
		* @param {string | AuthProvider} provider Name of auth provider or {@link https://parseplatform.org/Parse-SDK-JS/api/master/AuthProvider.html AuthProvider}
		* @param {object} options
		* @param {object} [options.authData] AuthData to link with
		* <ul>
		*   <li>If provider is string, options is {@link http://docs.parseplatform.org/parse-server/guide/#supported-3rd-party-authentications authData}
		*   <li>If provider is AuthProvider, options is saveOpts
		* </ul>
		* @param {object} saveOpts useMasterKey / sessionToken
		* @returns {Promise} A promise that is fulfilled with the user is linked
		*/
		linkWith(provider, options) {
			let saveOpts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			saveOpts.sessionToken = saveOpts.sessionToken || this.getSessionToken() || "";
			let authType;
			if (typeof provider === "string") {
				authType = provider;
				if (authProviders[provider]) provider = authProviders[provider];
				else {
					const authProvider = {
						restoreAuthentication() {
							return true;
						},
						getAuthType() {
							return authType;
						}
					};
					authProviders[authProvider.getAuthType()] = authProvider;
					provider = authProvider;
				}
			} else authType = provider.getAuthType();
			if (options && Object.hasOwn(options, "authData")) {
				const authData = this.get("authData") || {};
				if (typeof authData !== "object") throw new Error("Invalid type: authData field should be an object");
				authData[authType] = options.authData;
				const oldAnonymousData = authData.anonymous;
				this.stripAnonymity();
				return _CoreManager.default.getUserController().linkWith(this, authData, saveOpts).catch((e) => {
					delete authData[authType];
					this.restoreAnonimity(oldAnonymousData);
					throw e;
				});
			} else return new _promise.default((resolve, reject) => {
				provider.authenticate({
					success: (provider, result) => {
						const opts = {};
						opts.authData = result;
						this.linkWith(provider, opts, saveOpts).then(() => {
							resolve(this);
						}, (error) => {
							reject(error);
						});
					},
					error: (_provider, error) => {
						reject(error);
					}
				});
			});
		}
		/**
		* @param provider
		* @param options
		* @param {object} [options.authData]
		* @param saveOpts
		* @deprecated since 2.9.0 see {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.User.html#linkWith linkWith}
		* @returns {Promise}
		*/
		_linkWith(provider, options) {
			let saveOpts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			return this.linkWith(provider, options, saveOpts);
		}
		/**
		* Synchronizes auth data for a provider (e.g. puts the access token in the
		* right place to be used by the Facebook SDK).
		*
		* @param provider
		*/
		_synchronizeAuthData(provider) {
			if (!this.isCurrent() || !provider) return;
			let authType;
			if (typeof provider === "string") {
				authType = provider;
				provider = authProviders[authType];
			} else authType = provider.getAuthType();
			const authData = this.get("authData");
			if (!provider || !authData || typeof authData !== "object") return;
			if (!provider.restoreAuthentication(authData[authType])) this._unlinkFrom(provider);
		}
		/**
		* Synchronizes authData for all providers.
		*/
		_synchronizeAllAuthData() {
			const authData = this.get("authData");
			if (typeof authData !== "object") return;
			for (const key in authData) this._synchronizeAuthData(key);
		}
		/**
		* Removes null values from authData (which exist temporarily for unlinking)
		*/
		_cleanupAuthData() {
			if (!this.isCurrent()) return;
			const authData = this.get("authData");
			if (typeof authData !== "object") return;
			for (const key in authData) if (!authData[key]) delete authData[key];
		}
		/**
		* Unlinks a user from a service.
		*
		* @param {string | AuthProvider} provider Name of auth provider or {@link https://parseplatform.org/Parse-SDK-JS/api/master/AuthProvider.html AuthProvider}
		* @param {object} options MasterKey / SessionToken
		* @returns {Promise} A promise that is fulfilled when the unlinking
		*     finishes.
		*/
		_unlinkFrom(provider, options) {
			return this.linkWith(provider, { authData: null }, options).then(() => {
				this._synchronizeAuthData(provider);
				return _promise.default.resolve(this);
			});
		}
		/**
		* Checks whether a user is linked to a service.
		*
		* @param {object} provider service to link to
		* @returns {boolean} true if link was successful
		*/
		_isLinked(provider) {
			let authType;
			if (typeof provider === "string") authType = provider;
			else authType = provider.getAuthType();
			const authData = this.get("authData") || {};
			if (typeof authData !== "object") return false;
			return !!authData[authType];
		}
		/**
		* Deauthenticates all providers.
		*/
		_logOutWithAll() {
			const authData = this.get("authData");
			if (typeof authData !== "object") return;
			for (const key in authData) this._logOutWith(key);
		}
		/**
		* Deauthenticates a single provider (e.g. removing access tokens from the
		* Facebook SDK).
		*
		* @param {object} provider service to logout of
		*/
		_logOutWith(provider) {
			if (!this.isCurrent()) return;
			if (typeof provider === "string") provider = authProviders[provider];
			if (provider && provider.deauthenticate) provider.deauthenticate();
		}
		/**
		* Class instance method used to maintain specific keys when a fetch occurs.
		* Used to ensure that the session token is not lost.
		*
		* @returns {object} sessionToken
		*/
		_preserveFieldsOnFetch() {
			return { sessionToken: this.get("sessionToken") };
		}
		/**
		* Returns true if <code>current</code> would return this user.
		*
		* @returns {boolean} true if user is cached on disk
		*/
		isCurrent() {
			const current = ParseUser.current();
			return !!current && current.id === this.id;
		}
		/**
		* Returns true if <code>current</code> would return this user.
		*
		* @returns {Promise<boolean>} true if user is cached on disk
		*/
		async isCurrentAsync() {
			const current = await ParseUser.currentAsync();
			return !!current && current.id === this.id;
		}
		stripAnonymity() {
			const authData = this.get("authData");
			if (authData && typeof authData === "object" && Object.hasOwn(authData, "anonymous")) authData.anonymous = null;
		}
		restoreAnonimity(anonymousData) {
			if (anonymousData) {
				const authData = this.get("authData");
				authData.anonymous = anonymousData;
			}
		}
		/**
		* Returns get("username").
		*
		* @returns {string}
		*/
		getUsername() {
			const username = this.get("username");
			if (username == null || typeof username === "string") return username;
			return "";
		}
		/**
		* Calls set("username", username, options) and returns the result.
		*
		* @param {string} username
		*/
		setUsername(username) {
			this.stripAnonymity();
			this.set("username", username);
		}
		/**
		* Calls set("password", password, options) and returns the result.
		*
		* @param {string} password User's Password
		*/
		setPassword(password) {
			this.set("password", password);
		}
		/**
		* Returns get("email").
		*
		* @returns {string} User's Email
		*/
		getEmail() {
			const email = this.get("email");
			if (email == null || typeof email === "string") return email;
			return "";
		}
		/**
		* Calls set("email", email) and returns the result.
		*
		* @param {string} email
		* @returns {boolean}
		*/
		setEmail(email) {
			return this.set("email", email);
		}
		/**
		* Returns the session token for this user, if the user has been logged in,
		* or if it is the result of a query with the master key. Otherwise, returns
		* undefined.
		*
		* @returns {string} the session token, or undefined
		*/
		getSessionToken() {
			const token = this.get("sessionToken");
			if (token == null || typeof token === "string") return token;
			return "";
		}
		/**
		* Checks whether this user is the current user and has been authenticated.
		*
		* @returns {boolean} whether this user is the current user and is logged in.
		*/
		authenticated() {
			const current = ParseUser.current();
			return !!this.get("sessionToken") && !!current && current.id === this.id;
		}
		/**
		* Signs up a new user. You should call this instead of save for
		* new Parse.Users. This will create a new Parse.User on the server, and
		* also persist the session on disk so that you can access the user using
		* <code>current</code>.
		*
		* <p>A username and password must be set before calling signUp.</p>
		*
		* @param {object} attrs Extra fields to set on the new user, or null.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>installationId: the installationId which made the request
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeLogin` and `afterLogin` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled when the signup
		*     finishes.
		*/
		signUp(attrs, options) {
			const signupOptions = _ParseObject.default._getRequestOptions(options);
			return _CoreManager.default.getUserController().signUp(this, attrs, signupOptions);
		}
		/**
		* Logs in a Parse.User. On success, this saves the session to disk,
		* so you can retrieve the currently logged in user using
		* <code>current</code>.
		*
		* <p>A username and password must be set before calling logIn.</p>
		*
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		*   <li>usePost: Use POST method to make the request (default: true)
		*   <li>installationId: the installationId which made the request
		*   <li>context: A dictionary that is accessible in Cloud Code `beforeLogin` and `afterLogin` triggers.
		* </ul>
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login is complete.
		*/
		logIn() {
			let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			const loginOptions = _ParseObject.default._getRequestOptions(options);
			if (!Object.hasOwn(loginOptions, "usePost")) loginOptions.usePost = true;
			return _CoreManager.default.getUserController().logIn(this, loginOptions);
		}
		/**
		* Wrap the default save behavior with functionality to save to local
		* storage if this is current user.
		*
		* @param {...any} args
		* @returns {Promise}
		*/
		async save() {
			for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
			await super.save.apply(this, args);
			if (await this.isCurrentAsync()) return _CoreManager.default.getUserController().updateUserOnDisk(this);
			return this;
		}
		/**
		* Wrap the default destroy behavior with functionality that logs out
		* the current user when it is destroyed
		*
		* @param {...any} args
		* @returns {Parse.User}
		*/
		async destroy() {
			for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) args[_key2] = arguments[_key2];
			await super.destroy.apply(this, args);
			if (await this.isCurrentAsync()) return _CoreManager.default.getUserController().removeUserFromDisk();
			return this;
		}
		/**
		* Wrap the default fetch behavior with functionality to save to local
		* storage if this is current user.
		*
		* @param {...any} args
		* @returns {Parse.User}
		*/
		async fetch() {
			for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) args[_key3] = arguments[_key3];
			await super.fetch.apply(this, args);
			if (await this.isCurrentAsync()) return _CoreManager.default.getUserController().updateUserOnDisk(this);
			return this;
		}
		/**
		* Wrap the default fetchWithInclude behavior with functionality to save to local
		* storage if this is current user.
		*
		* @param {...any} args
		* @returns {Parse.User}
		*/
		async fetchWithInclude() {
			for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) args[_key4] = arguments[_key4];
			await super.fetchWithInclude.apply(this, args);
			if (await this.isCurrentAsync()) return _CoreManager.default.getUserController().updateUserOnDisk(this);
			return this;
		}
		/**
		* Verify whether a given password is the password of the current user.
		*
		* @param {string} password The password to be verified.
		* @param {object} options The options.
		* @param {boolean} [options.ignoreEmailVerification] Set to `true` to bypass email verification and verify
		* the password regardless of whether the email has been verified. This requires the master key.
		* @returns {Promise} A promise that is fulfilled with a user when the password is correct.
		*/
		verifyPassword(password, options) {
			const username = this.getUsername() || "";
			return ParseUser.verifyPassword(username, password, options);
		}
		static readOnlyAttributes() {
			return ["sessionToken"];
		}
		/**
		* Adds functionality to the existing Parse.User class.
		*
		* @param {object} protoProps A set of properties to add to the prototype
		* @param {object} classProps A set of static properties to add to the class
		* @static
		* @returns {Parse.User} The newly extended Parse.User class
		*/
		static extend(protoProps, classProps) {
			if (protoProps) {
				for (const prop in protoProps) if (prop !== "className") (0, _defineProperty3.default)(ParseUser.prototype, prop, {
					value: protoProps[prop],
					enumerable: false,
					writable: true,
					configurable: true
				});
			}
			if (classProps) {
				for (const prop in classProps) if (prop !== "className") (0, _defineProperty3.default)(ParseUser, prop, {
					value: classProps[prop],
					enumerable: false,
					writable: true,
					configurable: true
				});
			}
			return ParseUser;
		}
		/**
		* Retrieves the currently logged in ParseUser with a valid session,
		* either from memory or localStorage, if necessary.
		*
		* @static
		* @returns {Parse.User} The currently logged in Parse.User.
		*/
		static current() {
			if (!canUseCurrentUser) return null;
			return _CoreManager.default.getUserController().currentUser();
		}
		/**
		* Retrieves the currently logged in ParseUser from asynchronous Storage.
		*
		* @static
		* @returns {Promise} A Promise that is resolved with the currently
		*   logged in Parse User
		*/
		static currentAsync() {
			if (!canUseCurrentUser) return _promise.default.resolve(null);
			return _CoreManager.default.getUserController().currentUserAsync();
		}
		/**
		* Signs up a new user with a username (or email) and password.
		* This will create a new Parse.User on the server, and also persist the
		* session in localStorage so that you can access the user using
		* {@link #current}.
		*
		* @param {string} username The username (or email) to sign up with.
		* @param {string} password The password to sign up with.
		* @param {object} attrs Extra fields to set on the new user.
		* @param {object} options
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the signup completes.
		*/
		static signUp(username, password, attrs, options) {
			attrs = attrs || {};
			attrs.username = username;
			attrs.password = password;
			return new this(attrs).signUp({}, options);
		}
		/**
		* Logs in a user with a username (or email) and password. On success, this
		* saves the session to disk, so you can retrieve the currently logged in
		* user using <code>current</code>.
		*
		* @param {string} username The username (or email) to log in with.
		* @param {string} password The password to log in with.
		* @param {object} options
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login completes.
		*/
		static logIn(username, password, options) {
			if (typeof username !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Username must be a string."));
			else if (typeof password !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Password must be a string."));
			const user = new this();
			user._finishFetch({
				username,
				password
			});
			return user.logIn(options);
		}
		/**
		* Logs in a user with a username (or email) and password, and authData. On success, this
		* saves the session to disk, so you can retrieve the currently logged in
		* user using <code>current</code>.
		*
		* @param {string} username The username (or email) to log in with.
		* @param {string} password The password to log in with.
		* @param {object} authData The authData to log in with.
		* @param {object} options
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login completes.
		*/
		static logInWithAdditionalAuth(username, password, authData, options) {
			if (typeof username !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Username must be a string."));
			if (typeof password !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Password must be a string."));
			if (Object.prototype.toString.call(authData) !== "[object Object]") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Auth must be an object."));
			const user = new this();
			user._finishFetch({
				username,
				password,
				authData
			});
			return user.logIn(options);
		}
		/**
		* Logs in a user with an objectId. On success, this saves the session
		* to disk, so you can retrieve the currently logged in user using
		* <code>current</code>.
		*
		* @param {string} userId The objectId for the user.
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login completes.
		*/
		static loginAs(userId) {
			if (!userId) throw new _ParseError.default(_ParseError.default.USERNAME_MISSING, "Cannot log in as user with an empty user id");
			const controller = _CoreManager.default.getUserController();
			const user = new this();
			return controller.loginAs(user, userId);
		}
		/**
		* Logs in a user with a session token. On success, this saves the session
		* to disk, so you can retrieve the currently logged in user using
		* <code>current</code>.
		*
		* @param {string} sessionToken The sessionToken to log in with.
		* @param {object} options
		* @param {boolean} [options.useMasterKey]
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login completes.
		*/
		static become(sessionToken, options) {
			if (!canUseCurrentUser) throw new Error("It is not memory-safe to become a user in a server environment");
			const becomeOptions = _ParseObject.default._getRequestOptions(options);
			becomeOptions.sessionToken = sessionToken;
			const controller = _CoreManager.default.getUserController();
			const user = new this();
			return controller.become(user, becomeOptions);
		}
		/**
		* Retrieves a user with a session token.
		*
		* @param {string} sessionToken The sessionToken to get user with.
		* @param {object} options
		* @param {boolean} [options.useMasterKey]
		* @static
		* @returns {Promise} A promise that is fulfilled with the user is fetched.
		*/
		static me(sessionToken, options) {
			const controller = _CoreManager.default.getUserController();
			const meOptions = _ParseObject.default._getRequestOptions(options);
			meOptions.sessionToken = sessionToken;
			const user = new this();
			return controller.me(user, meOptions);
		}
		/**
		* Logs in a user with a session token. On success, this saves the session
		* to disk, so you can retrieve the currently logged in user using
		* <code>current</code>. If there is no session token the user will not logged in.
		*
		* @param {object} userJSON The JSON map of the User's data
		* @static
		* @returns {Promise} A promise that is fulfilled with the user when
		*     the login completes.
		*/
		static hydrate(userJSON) {
			const controller = _CoreManager.default.getUserController();
			const user = new this();
			return controller.hydrate(user, userJSON);
		}
		/**
		* Static version of {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.User.html#linkWith linkWith}
		*
		* @param provider
		* @param options
		* @param {object} [options.authData]
		* @param saveOpts
		* @static
		* @returns {Promise}
		*/
		static logInWith(provider, options, saveOpts) {
			return new this().linkWith(provider, options, saveOpts);
		}
		/**
		* Logs out the currently logged in user session. This will remove the
		* session from disk, log out of linked services, and future calls to
		* <code>current</code> will return <code>null</code>.
		*
		* @param {object} options
		* @static
		* @returns {Promise} A promise that is resolved when the session is
		*   destroyed on the server.
		*/
		static logOut(options) {
			return _CoreManager.default.getUserController().logOut(options);
		}
		/**
		* Requests a password reset email to be sent to the specified email address
		* associated with the user account. This email allows the user to securely
		* reset their password on the Parse site.
		*
		* @param {string} email The email address associated with the user that
		*     forgot their password.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		* </ul>
		* @static
		* @returns {Promise}
		*/
		static requestPasswordReset(email, options) {
			const requestOptions = _ParseObject.default._getRequestOptions(options);
			return _CoreManager.default.getUserController().requestPasswordReset(email, requestOptions);
		}
		/**
		* Request an email verification.
		*
		* @param {string} email The email address associated with the user that
		*     needs to verify their email.
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		* </ul>
		* @static
		* @returns {Promise}
		*/
		static requestEmailVerification(email, options) {
			const requestOptions = _ParseObject.default._getRequestOptions(options);
			return _CoreManager.default.getUserController().requestEmailVerification(email, requestOptions);
		}
		/**
		* Verify whether a given password is the password of the current user.
		* @static
		*
		* @param {string} username  The username of the user whose password should be verified.
		* @param {string} password The password to be verified.
		* @param {object} options The options.
		* @param {boolean} [options.ignoreEmailVerification] Set to `true` to bypass email verification and verify
		* the password regardless of whether the email has been verified. This requires the master key.
		* @returns {Promise} A promise that is fulfilled with a user when the password is correct.
		*/
		static verifyPassword(username, password, options) {
			if (typeof username !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Username must be a string."));
			if (typeof password !== "string") return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Password must be a string."));
			return _CoreManager.default.getUserController().verifyPassword(username, password, options || {});
		}
		/**
		* Allow someone to define a custom User class without className
		* being rewritten to _User. The default behavior is to rewrite
		* User to _User for legacy reasons. This allows developers to
		* override that behavior.
		*
		* @param {boolean} isAllowed Whether or not to allow custom User class
		* @static
		*/
		static allowCustomUserClass(isAllowed) {
			_CoreManager.default.set("PERFORM_USER_REWRITE", !isAllowed);
		}
		/**
		* Allows a legacy application to start using revocable sessions. If the
		* current session token is not revocable, a request will be made for a new,
		* revocable session.
		* It is not necessary to call this method from cloud code unless you are
		* handling user signup or login from the server side. In a cloud code call,
		* this function will not attempt to upgrade the current token.
		*
		* @param {object} options
		* @static
		* @returns {Promise} A promise that is resolved when the process has
		*   completed. If a replacement session token is requested, the promise
		*   will be resolved after a new token has been fetched.
		*/
		static enableRevocableSession(options) {
			options = options || {};
			_CoreManager.default.set("FORCE_REVOCABLE_SESSION", true);
			if (canUseCurrentUser) {
				const current = ParseUser.current();
				if (current) return current._upgradeToRevocableSession(options);
			}
			return _promise.default.resolve();
		}
		/**
		* Enables the use of become or the current user in a server
		* environment. These features are disabled by default, since they depend on
		* global objects that are not memory-safe for most servers.
		*
		* @static
		*/
		static enableUnsafeCurrentUser() {
			canUseCurrentUser = true;
		}
		/**
		* Disables the use of become or the current user in any environment.
		* These features are disabled on servers by default, since they depend on
		* global objects that are not memory-safe for most servers.
		*
		* @static
		*/
		static disableUnsafeCurrentUser() {
			canUseCurrentUser = false;
		}
		/**
		* When registering users with {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.User.html#linkWith linkWith} a basic auth provider
		* is automatically created for you.
		*
		* For advanced authentication, you can register an Auth provider to
		* implement custom authentication, deauthentication.
		*
		* @param provider
		* @see {@link https://parseplatform.org/Parse-SDK-JS/api/master/AuthProvider.html AuthProvider}
		* @see {@link https://docs.parseplatform.org/js/guide/#custom-authentication-module Custom Authentication Module}
		* @static
		*/
		static _registerAuthenticationProvider(provider) {
			authProviders[provider.getAuthType()] = provider;
			ParseUser.currentAsync().then((current) => {
				if (current) current._synchronizeAuthData(provider.getAuthType());
			});
		}
		/**
		* @param provider
		* @param options
		* @param {object} [options.authData]
		* @param saveOpts
		* @deprecated since 2.9.0 see {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.User.html#logInWith logInWith}
		* @static
		* @returns {Promise}
		*/
		static _logInWith(provider, options, saveOpts) {
			return new this().linkWith(provider, options, saveOpts);
		}
		static _clearCache() {
			currentUserCache = null;
			currentUserCacheMatchesDisk = false;
		}
		static _setCurrentUserCache(user) {
			currentUserCache = user;
		}
	};
	_ParseObject.default.registerSubclass("_User", ParseUser);
	var DefaultController = {
		updateUserOnDisk(user) {
			const path = _Storage.default.generatePath(CURRENT_USER_KEY);
			const json = user.toJSON();
			delete json.password;
			json.className = "_User";
			let userData = (0, _stringify.default)(json);
			if (_CoreManager.default.get("ENCRYPTED_USER")) userData = _CoreManager.default.getCryptoController().encrypt(json, _CoreManager.default.get("ENCRYPTED_KEY"));
			return _Storage.default.setItemAsync(path, userData).then(() => {
				return user;
			});
		},
		removeUserFromDisk() {
			const path = _Storage.default.generatePath(CURRENT_USER_KEY);
			currentUserCacheMatchesDisk = true;
			currentUserCache = null;
			return _Storage.default.removeItemAsync(path);
		},
		setCurrentUser(user) {
			currentUserCache = user;
			user._cleanupAuthData();
			user._synchronizeAllAuthData();
			return DefaultController.updateUserOnDisk(user);
		},
		currentUser() {
			if (currentUserCache) return currentUserCache;
			if (currentUserCacheMatchesDisk) return null;
			if (_Storage.default.async()) throw new Error("Cannot call currentUser() when using a platform with an async storage system. Call currentUserAsync() instead.");
			const path = _Storage.default.generatePath(CURRENT_USER_KEY);
			let userData = _Storage.default.getItem(path);
			currentUserCacheMatchesDisk = true;
			if (!userData) {
				currentUserCache = null;
				return null;
			}
			if (_CoreManager.default.get("ENCRYPTED_USER")) userData = _CoreManager.default.getCryptoController().decrypt(userData, _CoreManager.default.get("ENCRYPTED_KEY"));
			userData = JSON.parse(userData);
			if (!userData.className) userData.className = "_User";
			if (userData._id) {
				if (userData.objectId !== userData._id) userData.objectId = userData._id;
				delete userData._id;
			}
			if (userData._sessionToken) {
				userData.sessionToken = userData._sessionToken;
				delete userData._sessionToken;
			}
			const current = _ParseObject.default.fromJSON(userData);
			currentUserCache = current;
			current._synchronizeAllAuthData();
			return current;
		},
		currentUserAsync() {
			if (currentUserCache) return _promise.default.resolve(currentUserCache);
			if (currentUserCacheMatchesDisk) return _promise.default.resolve(null);
			const path = _Storage.default.generatePath(CURRENT_USER_KEY);
			return _Storage.default.getItemAsync(path).then((userData) => {
				currentUserCacheMatchesDisk = true;
				if (!userData) {
					currentUserCache = null;
					return _promise.default.resolve(null);
				}
				if (_CoreManager.default.get("ENCRYPTED_USER")) userData = _CoreManager.default.getCryptoController().decrypt(userData.toString(), _CoreManager.default.get("ENCRYPTED_KEY"));
				userData = JSON.parse(userData);
				if (!userData.className) userData.className = "_User";
				if (userData._id) {
					if (userData.objectId !== userData._id) userData.objectId = userData._id;
					delete userData._id;
				}
				if (userData._sessionToken) {
					userData.sessionToken = userData._sessionToken;
					delete userData._sessionToken;
				}
				const current = _ParseObject.default.fromJSON(userData);
				currentUserCache = current;
				current._synchronizeAllAuthData();
				return _promise.default.resolve(current);
			});
		},
		signUp(user, attrs, options) {
			const username = attrs && attrs.username || user.get("username");
			const password = attrs && attrs.password || user.get("password");
			if (!username || !username.length) return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Cannot sign up user with an empty username."));
			if (!password || !password.length) return _promise.default.reject(new _ParseError.default(_ParseError.default.OTHER_CAUSE, "Cannot sign up user with an empty password."));
			return user.save(attrs, options).then(() => {
				user._finishFetch({ password: void 0 });
				if (canUseCurrentUser) return DefaultController.setCurrentUser(user);
				return user;
			});
		},
		logIn(user, options) {
			const RESTController = _CoreManager.default.getRESTController();
			const stateController = _CoreManager.default.getObjectStateController();
			const auth = {
				username: user.get("username"),
				password: user.get("password"),
				authData: user.get("authData")
			};
			return RESTController.request(options.usePost ? "POST" : "GET", "login", auth, options).then((response) => {
				user._migrateId(response.objectId);
				user._setExisted(true);
				stateController.setPendingOp(user._getStateIdentifier(), "username", void 0);
				stateController.setPendingOp(user._getStateIdentifier(), "password", void 0);
				response.password = void 0;
				user._finishFetch(response);
				if (!canUseCurrentUser) return _promise.default.resolve(user);
				return DefaultController.setCurrentUser(user);
			});
		},
		loginAs(user, userId) {
			return _CoreManager.default.getRESTController().request("POST", "loginAs", { userId }, { useMasterKey: true }).then((response) => {
				user._finishFetch(response);
				user._setExisted(true);
				if (!canUseCurrentUser) return _promise.default.resolve(user);
				return DefaultController.setCurrentUser(user);
			});
		},
		become(user, options) {
			return _CoreManager.default.getRESTController().request("GET", "users/me", {}, options).then((response) => {
				user._finishFetch(response);
				user._setExisted(true);
				return DefaultController.setCurrentUser(user);
			});
		},
		hydrate(user, userJSON) {
			user._finishFetch(userJSON);
			user._setExisted(true);
			if (userJSON.sessionToken && canUseCurrentUser) return DefaultController.setCurrentUser(user);
			else return _promise.default.resolve(user);
		},
		me(user, options) {
			return _CoreManager.default.getRESTController().request("GET", "users/me", {}, options).then((response) => {
				user._finishFetch(response);
				user._setExisted(true);
				return user;
			});
		},
		logOut(options) {
			const RESTController = _CoreManager.default.getRESTController();
			if (options !== null && options !== void 0 && options.sessionToken) return RESTController.request("POST", "logout", {}, options);
			return DefaultController.currentUserAsync().then((currentUser) => {
				const path = _Storage.default.generatePath(CURRENT_USER_KEY);
				let promise = _Storage.default.removeItemAsync(path);
				if (currentUser !== null) {
					const currentSession = currentUser.getSessionToken();
					if (currentSession && (0, _isRevocableSession.default)(currentSession)) promise = promise.then(() => {
						return RESTController.request("POST", "logout", {}, { sessionToken: currentSession });
					});
					currentUser._logOutWithAll();
					currentUser._finishFetch({ sessionToken: void 0 });
				}
				currentUserCacheMatchesDisk = true;
				currentUserCache = null;
				return promise;
			});
		},
		requestPasswordReset(email, options) {
			return _CoreManager.default.getRESTController().request("POST", "requestPasswordReset", { email }, options);
		},
		async upgradeToRevocableSession(user, options) {
			const token = user.getSessionToken();
			if (!token) return _promise.default.reject(new _ParseError.default(_ParseError.default.SESSION_MISSING, "Cannot upgrade a user with no session token"));
			options.sessionToken = token;
			const result = await _CoreManager.default.getRESTController().request("POST", "upgradeToRevocableSession", {}, options);
			user._finishFetch({ sessionToken: (result === null || result === void 0 ? void 0 : result.sessionToken) || "" });
			if (await user.isCurrentAsync()) return DefaultController.setCurrentUser(user);
			return _promise.default.resolve(user);
		},
		linkWith(user, authData, options) {
			return user.save({ authData }, options).then(() => {
				if (canUseCurrentUser) return DefaultController.setCurrentUser(user);
				return user;
			});
		},
		verifyPassword(username, password, options) {
			const RESTController = _CoreManager.default.getRESTController();
			const data = _objectSpread({
				username,
				password
			}, options.ignoreEmailVerification !== void 0 && { ignoreEmailVerification: options.ignoreEmailVerification });
			return RESTController.request("GET", "verifyPassword", data, options);
		},
		requestEmailVerification(email, options) {
			return _CoreManager.default.getRESTController().request("POST", "verificationEmailRequest", { email }, options);
		}
	};
	_CoreManager.default.setParseUser(ParseUser);
	_CoreManager.default.setUserController(DefaultController);
	exports.default = ParseUser;
}));
//#endregion
//#region node_modules/parse/lib/browser/AnonymousUtils.js
var require_AnonymousUtils = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _ParseUser = _interopRequireDefault(require_ParseUser());
	var _uuid = _interopRequireDefault(require_uuid());
	var registered = false;
	exports.default = {
		/**
		* Gets whether the user has their account linked to anonymous user.
		*
		* @function isLinked
		* @name Parse.AnonymousUtils.isLinked
		* @param {Parse.User} user User to check for.
		*     The user must be logged in on this device.
		* @returns {boolean} <code>true</code> if the user has their account
		*     linked to an anonymous user.
		* @static
		*/
		isLinked(user) {
			const provider = this._getAuthProvider();
			return user._isLinked(provider.getAuthType());
		},
		/**
		* Logs in a user Anonymously.
		*
		* @function logIn
		* @name Parse.AnonymousUtils.logIn
		* @param {object} options MasterKey / SessionToken.
		* @returns {Promise} Logged in user
		* @static
		*/
		logIn(options) {
			const provider = this._getAuthProvider();
			return _ParseUser.default.logInWith(provider.getAuthType(), provider.getAuthData(), options);
		},
		/**
		* Links Anonymous User to an existing PFUser.
		*
		* @function link
		* @name Parse.AnonymousUtils.link
		* @param {Parse.User} user User to link. This must be the current user.
		* @param {object} options MasterKey / SessionToken.
		* @returns {Promise} Linked with User
		* @static
		*/
		link(user, options) {
			const provider = this._getAuthProvider();
			return user.linkWith(provider.getAuthType(), provider.getAuthData(), options);
		},
		/**
		* Returns true if Authentication Provider has been registered for use.
		*
		* @function isRegistered
		* @name Parse.AnonymousUtils.isRegistered
		* @returns {boolean}
		* @static
		*/
		isRegistered() {
			return registered;
		},
		_getAuthProvider() {
			const provider = {
				restoreAuthentication() {
					return true;
				},
				getAuthType() {
					return "anonymous";
				},
				getAuthData() {
					return { authData: { id: (0, _uuid.default)() } };
				}
			};
			if (!registered) {
				_ParseUser.default._registerAuthenticationProvider(provider);
				registered = true;
			}
			return provider;
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/Cloud.js
var require_Cloud = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.getJobStatus = getJobStatus;
	exports.getJobsData = getJobsData;
	exports.run = run;
	exports.startJob = startJob;
	var _keys = _interopRequireDefault(require_keys$4());
	var _promise = _interopRequireDefault(require_promise());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _decode = _interopRequireDefault(require_decode());
	var _encode = _interopRequireDefault(require_encode());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseQuery = _interopRequireDefault(require_ParseQuery());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	/**
	* Contains functions for calling and declaring
	* <a href="/docs/cloud_code_guide#functions">cloud functions</a>.
	* <p><strong><em>
	*   Some functions are only available from Cloud Code.
	* </em></strong></p>
	*
	* @class Parse.Cloud
	* @static
	* @hideconstructor
	*/
	/**
	* Makes a call to a cloud function.
	*
	* @function run
	* @name Parse.Cloud.run
	* @param {string} name The function name.
	* @param {object} data The parameters to send to the cloud function.
	* @param {object} options
	* Valid options are:<ul>
	*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
	*     be used for this request.
	*   <li>sessionToken: A valid session token, used for making a request on
	*        behalf of a specific user.
	*   <li>installationId: the installationId which made the request
	*   <li>context: A dictionary that is accessible in Cloud Code triggers.
	* </ul>
	* @returns {Promise} A promise that will be resolved with the result
	* of the function.
	*/
	function run(name, data, options) {
		if (typeof name !== "string" || name.length === 0) throw new TypeError("Cloud function name must be a string.");
		const requestOptions = _ParseObject.default._getRequestOptions(options);
		return _CoreManager.default.getCloudController().run(name, data, requestOptions);
	}
	/**
	* Gets data for the current set of cloud jobs.
	*
	* @function getJobsData
	* @name Parse.Cloud.getJobsData
	* @returns {Promise} A promise that will be resolved with the result
	* of the function.
	*/
	function getJobsData() {
		return _CoreManager.default.getCloudController().getJobsData({ useMasterKey: true });
	}
	/**
	* Starts a given cloud job, which will process asynchronously.
	*
	* @function startJob
	* @name Parse.Cloud.startJob
	* @param {string} name The function name.
	* @param {object} data The parameters to send to the cloud function.
	* @returns {Promise} A promise that will be resolved with the jobStatusId
	* of the job.
	*/
	function startJob(name, data) {
		if (typeof name !== "string" || name.length === 0) throw new TypeError("Cloud job name must be a string.");
		return _CoreManager.default.getCloudController().startJob(name, data, { useMasterKey: true });
	}
	/**
	* Gets job status by Id
	*
	* @function getJobStatus
	* @name Parse.Cloud.getJobStatus
	* @param {string} jobStatusId The Id of Job Status.
	* @returns {Parse.Object} Status of Job.
	*/
	function getJobStatus(jobStatusId) {
		return new _ParseQuery.default("_JobStatus").get(jobStatusId, { useMasterKey: true });
	}
	_CoreManager.default.setCloudController({
		run(name, data, options) {
			const RESTController = _CoreManager.default.getRESTController();
			const payload = (0, _encode.default)(data, true);
			return RESTController.request("POST", "functions/" + name, payload, options).then((res) => {
				if (typeof res === "object" && (0, _keys.default)(res).length > 0 && !Object.hasOwn(res, "result")) throw new _ParseError.default(_ParseError.default.INVALID_JSON, "The server returned an invalid response.");
				const decoded = (0, _decode.default)(res);
				if (decoded && Object.hasOwn(decoded, "result")) return _promise.default.resolve(decoded.result);
				return _promise.default.resolve(void 0);
			});
		},
		getJobsData(options) {
			return _CoreManager.default.getRESTController().request("GET", "cloud_code/jobs/data", null, options);
		},
		async startJob(name, data, options) {
			var _response$_headers;
			const RESTController = _CoreManager.default.getRESTController();
			const payload = (0, _encode.default)(data, true);
			options.returnStatus = true;
			return (_response$_headers = (await RESTController.request("POST", "jobs/" + name, payload, options))._headers) === null || _response$_headers === void 0 ? void 0 : _response$_headers["X-Parse-Job-Status-Id"];
		}
	});
}));
//#endregion
//#region node_modules/core-js-pure/internals/collection-strong.js
var require_collection_strong = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var create = require_object_create();
	var defineBuiltInAccessor = require_define_built_in_accessor();
	var defineBuiltIns = require_define_built_ins();
	var bind = require_function_bind_context();
	var anInstance = require_an_instance();
	var isNullOrUndefined = require_is_null_or_undefined();
	var iterate = require_iterate();
	var defineIterator = require_iterator_define();
	var createIterResultObject = require_create_iter_result_object();
	var setSpecies = require_set_species();
	var DESCRIPTORS = require_descriptors();
	var fastKey = require_internal_metadata().fastKey;
	var InternalStateModule = require_internal_state();
	var setInternalState = InternalStateModule.set;
	var internalStateGetterFor = InternalStateModule.getterFor;
	module.exports = {
		getConstructor: function(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
			var Constructor = wrapper(function(that, iterable) {
				anInstance(that, Prototype);
				setInternalState(that, {
					type: CONSTRUCTOR_NAME,
					index: create(null),
					first: null,
					last: null,
					size: 0
				});
				if (!DESCRIPTORS) that.size = 0;
				if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], {
					that,
					AS_ENTRIES: IS_MAP
				});
			});
			var Prototype = Constructor.prototype;
			var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);
			var define = function(that, key, value) {
				var state = getInternalState(that);
				var entry = getEntry(that, key);
				var previous, index;
				if (entry) entry.value = value;
				else {
					state.last = entry = {
						index: index = fastKey(key, true),
						key,
						value,
						previous: previous = state.last,
						next: null,
						removed: false
					};
					if (!state.first) state.first = entry;
					if (previous) previous.next = entry;
					if (DESCRIPTORS) state.size++;
					else that.size++;
					if (index !== "F") state.index[index] = entry;
				}
				return that;
			};
			var getEntry = function(that, key) {
				var state = getInternalState(that);
				var index = fastKey(key);
				var entry;
				if (index !== "F") return state.index[index];
				for (entry = state.first; entry; entry = entry.next) if (entry.key === key) return entry;
			};
			defineBuiltIns(Prototype, {
				clear: function clear() {
					var that = this;
					var state = getInternalState(that);
					var entry = state.first;
					while (entry) {
						entry.removed = true;
						if (entry.previous) entry.previous = entry.previous.next = null;
						entry = entry.next;
					}
					state.first = state.last = null;
					state.index = create(null);
					if (DESCRIPTORS) state.size = 0;
					else that.size = 0;
				},
				"delete": function(key) {
					var that = this;
					var state = getInternalState(that);
					var entry = getEntry(that, key);
					if (entry) {
						var next = entry.next;
						var prev = entry.previous;
						delete state.index[entry.index];
						entry.removed = true;
						if (prev) prev.next = next;
						if (next) next.previous = prev;
						if (state.first === entry) state.first = next;
						if (state.last === entry) state.last = prev;
						if (DESCRIPTORS) state.size--;
						else that.size--;
					}
					return !!entry;
				},
				forEach: function forEach(callbackfn) {
					var state = getInternalState(this);
					var boundFunction = bind(callbackfn, arguments.length > 1 ? arguments[1] : void 0);
					var entry;
					while (entry = entry ? entry.next : state.first) {
						boundFunction(entry.value, entry.key, this);
						while (entry && entry.removed) entry = entry.previous;
					}
				},
				has: function has(key) {
					return !!getEntry(this, key);
				}
			});
			defineBuiltIns(Prototype, IS_MAP ? {
				get: function get(key) {
					var entry = getEntry(this, key);
					return entry && entry.value;
				},
				set: function set(key, value) {
					return define(this, key === 0 ? 0 : key, value);
				}
			} : { add: function add(value) {
				return define(this, value = value === 0 ? 0 : value, value);
			} });
			if (DESCRIPTORS) defineBuiltInAccessor(Prototype, "size", {
				configurable: true,
				get: function() {
					return getInternalState(this).size;
				}
			});
			return Constructor;
		},
		setStrong: function(Constructor, CONSTRUCTOR_NAME, IS_MAP) {
			var ITERATOR_NAME = CONSTRUCTOR_NAME + " Iterator";
			var getInternalCollectionState = internalStateGetterFor(CONSTRUCTOR_NAME);
			var getInternalIteratorState = internalStateGetterFor(ITERATOR_NAME);
			defineIterator(Constructor, CONSTRUCTOR_NAME, function(iterated, kind) {
				setInternalState(this, {
					type: ITERATOR_NAME,
					target: iterated,
					state: getInternalCollectionState(iterated),
					kind,
					last: null
				});
			}, function() {
				var state = getInternalIteratorState(this);
				var kind = state.kind;
				var entry = state.last;
				while (entry && entry.removed) entry = entry.previous;
				if (!state.target || !(state.last = entry = entry ? entry.next : state.state.first)) {
					state.target = null;
					return createIterResultObject(void 0, true);
				}
				if (kind === "keys") return createIterResultObject(entry.key, false);
				if (kind === "values") return createIterResultObject(entry.value, false);
				return createIterResultObject([entry.key, entry.value], false);
			}, IS_MAP ? "entries" : "values", !IS_MAP, true);
			setSpecies(CONSTRUCTOR_NAME);
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.map.constructor.js
var require_es_map_constructor = /* @__PURE__ */ __commonJSMin((() => {
	require_collection()("Map", function(init) {
		return function Map() {
			return init(this, arguments.length ? arguments[0] : void 0);
		};
	}, require_collection_strong());
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.map.js
var require_es_map = /* @__PURE__ */ __commonJSMin((() => {
	require_es_map_constructor();
}));
//#endregion
//#region node_modules/core-js-pure/internals/map-helpers.js
var require_map_helpers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var caller = require_caller();
	var Map = getBuiltIn("Map");
	module.exports = {
		Map,
		set: caller("set", 2),
		get: caller("get", 1),
		has: caller("has", 1),
		remove: caller("delete", 1),
		proto: Map.prototype
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.map.group-by.js
var require_es_map_group_by = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var uncurryThis = require_function_uncurry_this();
	var aCallable = require_a_callable();
	var requireObjectCoercible = require_require_object_coercible();
	var iterate = require_iterate();
	var doesNotExceedSafeInteger = require_does_not_exceed_safe_integer();
	var MapHelpers = require_map_helpers();
	var IS_PURE = require_is_pure();
	var fails = require_fails();
	var Map = MapHelpers.Map;
	var has = MapHelpers.has;
	var get = MapHelpers.get;
	var set = MapHelpers.set;
	var push = uncurryThis([].push);
	var DOES_NOT_WORK_WITH_PRIMITIVES = IS_PURE || fails(function() {
		return Map.groupBy("ab", function(it) {
			return it;
		}).get("a").length !== 1;
	});
	$({
		target: "Map",
		stat: true,
		forced: IS_PURE || DOES_NOT_WORK_WITH_PRIMITIVES
	}, { groupBy: function groupBy(items, callbackfn) {
		requireObjectCoercible(items);
		aCallable(callbackfn);
		var map = new Map();
		var k = 0;
		iterate(items, function(value) {
			doesNotExceedSafeInteger(k);
			var key = callbackfn(value, k++);
			if (!has(map, key)) set(map, key, [value]);
			else push(get(map, key), value);
		});
		return map;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.map.get-or-insert.js
var require_es_map_get_or_insert = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var MapHelpers = require_map_helpers();
	var IS_PURE = require_is_pure();
	var get = MapHelpers.get;
	var has = MapHelpers.has;
	var set = MapHelpers.set;
	$({
		target: "Map",
		proto: true,
		real: true,
		forced: IS_PURE
	}, { getOrInsert: function getOrInsert(key, value) {
		if (has(this, key)) return get(this, key);
		set(this, key, value);
		return value;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.map.get-or-insert-computed.js
var require_es_map_get_or_insert_computed = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var aCallable = require_a_callable();
	var MapHelpers = require_map_helpers();
	var IS_PURE = require_is_pure();
	var get = MapHelpers.get;
	var has = MapHelpers.has;
	var set = MapHelpers.set;
	$({
		target: "Map",
		proto: true,
		real: true,
		forced: IS_PURE
	}, { getOrInsertComputed: function getOrInsertComputed(key, callbackfn) {
		var hasKey = has(this, key);
		aCallable(callbackfn);
		if (hasKey) return get(this, key);
		if (key === 0 && 1 / key === -Infinity) key = 0;
		var value = callbackfn(key);
		set(this, key, value);
		return value;
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/map/index.js
var require_map$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_map();
	require_es_map_group_by();
	require_es_map_get_or_insert();
	require_es_map_get_or_insert_computed();
	require_es_object_to_string();
	require_es_string_iterator();
	module.exports = require_path().Map;
}));
//#endregion
//#region node_modules/core-js-pure/stable/map/index.js
var require_map$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_map$2();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/map.js
var require_map = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_map$1();
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/entries.js
var require_entries$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	module.exports = require_get_built_in_prototype_method()("Array", "entries");
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/virtual/entries.js
var require_entries$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_entries$3();
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/entries.js
var require_entries$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_web_dom_collections_iterator();
	var classof = require_classof();
	var hasOwn = require_has_own_property();
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_entries$2();
	var ArrayPrototype = Array.prototype;
	var DOMIterables = {
		DOMTokenList: true,
		NodeList: true
	};
	module.exports = function(it) {
		var own = it.entries;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.entries || hasOwn(DOMIterables, classof(it)) ? method : own;
	};
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/entries.js
var require_entries = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_entries$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/object-assign.js
var require_object_assign = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var DESCRIPTORS = require_descriptors();
	var uncurryThis = require_function_uncurry_this();
	var call = require_function_call();
	var fails = require_fails();
	var objectKeys = require_object_keys();
	var getOwnPropertySymbolsModule = require_object_get_own_property_symbols();
	var propertyIsEnumerableModule = require_object_property_is_enumerable();
	var toObject = require_to_object();
	var IndexedObject = require_indexed_object();
	var $assign = Object.assign;
	var defineProperty = Object.defineProperty;
	var concat = uncurryThis([].concat);
	module.exports = !$assign || fails(function() {
		if (DESCRIPTORS && $assign({ b: 1 }, $assign(defineProperty({}, "a", {
			enumerable: true,
			get: function() {
				defineProperty(this, "b", {
					value: 3,
					enumerable: false
				});
			}
		}), { b: 2 })).b !== 1) return true;
		var A = {};
		var B = {};
		var symbol = Symbol("assign detection");
		var alphabet = "abcdefghijklmnopqrst";
		A[symbol] = 7;
		alphabet.split("").forEach(function(chr) {
			B[chr] = chr;
		});
		return $assign({}, A)[symbol] !== 7 || objectKeys($assign({}, B)).join("") !== alphabet;
	}) ? function assign(target, source) {
		var T = toObject(target);
		var argumentsLength = arguments.length;
		var index = 1;
		var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
		var propertyIsEnumerable = propertyIsEnumerableModule.f;
		while (argumentsLength > index) {
			var S = IndexedObject(arguments[index++]);
			var keys = getOwnPropertySymbols ? concat(objectKeys(S), getOwnPropertySymbols(S)) : objectKeys(S);
			var length = keys.length;
			var j = 0;
			var key;
			while (length > j) {
				key = keys[j++];
				if (!DESCRIPTORS || call(propertyIsEnumerable, S, key)) T[key] = S[key];
			}
		}
		return T;
	} : $assign;
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.object.assign.js
var require_es_object_assign = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var assign = require_object_assign();
	$({
		target: "Object",
		stat: true,
		arity: 2,
		forced: Object.assign !== assign
	}, { assign });
}));
//#endregion
//#region node_modules/core-js-pure/es/object/assign.js
var require_assign$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_object_assign();
	module.exports = require_path().Object.assign;
}));
//#endregion
//#region node_modules/core-js-pure/stable/object/assign.js
var require_assign$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_assign$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/object/assign.js
var require_assign = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_assign$1();
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.every.js
var require_es_array_every = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var $every = require_array_iteration().every;
	$({
		target: "Array",
		proto: true,
		forced: !require_array_method_is_strict()("every")
	}, { every: function every(callbackfn) {
		return $every(this, callbackfn, arguments.length > 1 ? arguments[1] : void 0);
	} });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/every.js
var require_every$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_every();
	module.exports = require_get_built_in_prototype_method()("Array", "every");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/every.js
var require_every$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_every$3();
	var ArrayPrototype = Array.prototype;
	module.exports = function(it) {
		var own = it.every;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.every ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/every.js
var require_every$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_every$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/every.js
var require_every = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_every$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseRole.js
var require_ParseRole = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseACL = _interopRequireDefault(require_ParseACL());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	/**
	* Represents a Role on the Parse server. Roles represent groupings of
	* Users for the purposes of granting permissions (e.g. specifying an ACL
	* for an Object). Roles are specified by their sets of child users and
	* child roles, all of which are granted any permissions that the parent
	* role has.
	*
	* <p>Roles must have a name (which cannot be changed after creation of the
	* role), and must specify an ACL.</p>
	*
	* @alias Parse.Role
	* @augments Parse.Object
	*/
	var ParseRole = class extends _ParseObject.default {
		/**
		* @param {string} name The name of the Role to create.
		* @param {Parse.ACL} acl The ACL for this role. Roles must have an ACL.
		* A Parse.Role is a local representation of a role persisted to the Parse
		* cloud.
		*/
		constructor(name, acl) {
			super("_Role");
			if (typeof name === "string" && acl instanceof _ParseACL.default) {
				this.setName(name);
				this.setACL(acl);
			}
		}
		/**
		* Gets the name of the role.  You can alternatively call role.get("name")
		*
		* @returns {string} the name of the role.
		*/
		getName() {
			const name = this.get("name");
			if (name == null || typeof name === "string") return name;
			return "";
		}
		/**
		* Sets the name for a role. This value must be set before the role has
		* been saved to the server, and cannot be set once the role has been
		* saved.
		*
		* <p>
		*   A role's name can only contain alphanumeric characters, _, -, and
		*   spaces.
		* </p>
		*
		* <p>This is equivalent to calling role.set("name", name)</p>
		*
		* @param {string} name The name of the role.
		* @param {object} options Standard options object with success and error
		*     callbacks.
		* @returns {Parse.Object} Returns the object, so you can chain this call.
		*/
		setName(name, options) {
			this._validateName(name);
			return this.set("name", name, options);
		}
		/**
		* Gets the Parse.Relation for the Parse.Users that are direct
		* children of this role. These users are granted any privileges that this
		* role has been granted (e.g. read or write access through ACLs). You can
		* add or remove users from the role through this relation.
		*
		* <p>This is equivalent to calling role.relation("users")</p>
		*
		* @returns {Parse.Relation} the relation for the users belonging to this
		*     role.
		*/
		getUsers() {
			return this.relation("users");
		}
		/**
		* Gets the Parse.Relation for the Parse.Roles that are direct
		* children of this role. These roles' users are granted any privileges that
		* this role has been granted (e.g. read or write access through ACLs). You
		* can add or remove child roles from this role through this relation.
		*
		* <p>This is equivalent to calling role.relation("roles")</p>
		*
		* @returns {Parse.Relation} the relation for the roles belonging to this
		*     role.
		*/
		getRoles() {
			return this.relation("roles");
		}
		_validateName(newName) {
			if (typeof newName !== "string") throw new _ParseError.default(_ParseError.default.OTHER_CAUSE, "A role's name must be a String.");
			if (!/^[0-9a-zA-Z\-_ ]+$/.test(newName)) throw new _ParseError.default(_ParseError.default.OTHER_CAUSE, "A role's name can be only contain alphanumeric characters, _, -, and spaces.");
		}
		validate(attrs, options) {
			const isInvalid = super.validate(attrs, options);
			if (isInvalid) return isInvalid;
			if ("name" in attrs && attrs.name !== this.getName()) {
				const newName = attrs.name;
				if (this.id && this.id !== attrs.objectId) return new _ParseError.default(_ParseError.default.OTHER_CAUSE, "A role's name can only be set before it has been saved.");
				try {
					this._validateName(newName);
				} catch (e) {
					return e;
				}
			}
			return false;
		}
	};
	_CoreManager.default.setParseRole(ParseRole);
	_ParseObject.default.registerSubclass("_Role", ParseRole);
	exports.default = ParseRole;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseCLP.js
var require_ParseCLP = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$keys2 = require_keys$4();
	var _Object$getOwnPropertySymbols = require_get_own_property_symbols();
	var _filterInstanceProperty = require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	var _forEachInstanceProperty = require_for_each();
	var _Object$getOwnPropertyDescriptors = require_get_own_property_descriptors();
	var _Object$defineProperties = require_define_properties();
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _map = _interopRequireDefault(require_map());
	var _entries = _interopRequireDefault(require_entries());
	var _assign = _interopRequireDefault(require_assign());
	var _slice = _interopRequireDefault(require_slice());
	var _includes = _interopRequireDefault(require_includes());
	var _every = _interopRequireDefault(require_every());
	var _keys = _interopRequireDefault(require_keys$4());
	var _isArray = _interopRequireDefault(require_is_array());
	var _ParseRole = _interopRequireDefault(require_ParseRole());
	var _ParseUser = _interopRequireDefault(require_ParseUser());
	function ownKeys(e, r) {
		var t = _Object$keys2(e);
		if (_Object$getOwnPropertySymbols) {
			var o = _Object$getOwnPropertySymbols(e);
			r && (o = _filterInstanceProperty(o).call(o, function(r) {
				return _Object$getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var _context2, _context3;
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? _forEachInstanceProperty(_context2 = ownKeys(Object(t), !0)).call(_context2, function(r) {
				(0, _defineProperty2.default)(e, r, t[r]);
			}) : _Object$getOwnPropertyDescriptors ? _Object$defineProperties(e, _Object$getOwnPropertyDescriptors(t)) : _forEachInstanceProperty(_context3 = ownKeys(Object(t))).call(_context3, function(r) {
				_Object$defineProperty(e, r, _Object$getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var PUBLIC_KEY = "*";
	var VALID_PERMISSIONS = new _map.default();
	VALID_PERMISSIONS.set("get", {});
	VALID_PERMISSIONS.set("find", {});
	VALID_PERMISSIONS.set("count", {});
	VALID_PERMISSIONS.set("create", {});
	VALID_PERMISSIONS.set("update", {});
	VALID_PERMISSIONS.set("delete", {});
	VALID_PERMISSIONS.set("addField", {});
	var VALID_PERMISSIONS_EXTENDED = new _map.default();
	VALID_PERMISSIONS_EXTENDED.set("protectedFields", {});
	exports.default = class ParseCLP {
		/**
		* @param {(Parse.User | Parse.Role | object)} userId The user to initialize the CLP for
		*/
		constructor(userId) {
			(0, _defineProperty2.default)(this, "permissionsMap", void 0);
			this.permissionsMap = {};
			for (const [operation, group] of (0, _entries.default)(VALID_PERMISSIONS).call(VALID_PERMISSIONS)) {
				this.permissionsMap[operation] = (0, _assign.default)({}, group);
				const action = operation.charAt(0).toUpperCase() + (0, _slice.default)(operation).call(operation, 1);
				this["get".concat(action, "RequiresAuthentication")] = function() {
					return this._getAccess(operation, "requiresAuthentication");
				};
				this["set".concat(action, "RequiresAuthentication")] = function(allowed) {
					this._setAccess(operation, "requiresAuthentication", allowed);
				};
				this["get".concat(action, "PointerFields")] = function() {
					return this._getAccess(operation, "pointerFields", false);
				};
				this["set".concat(action, "PointerFields")] = function(pointerFields) {
					this._setArrayAccess(operation, "pointerFields", pointerFields);
				};
				this["get".concat(action, "Access")] = function(entity) {
					return this._getAccess(operation, entity);
				};
				this["set".concat(action, "Access")] = function(entity, allowed) {
					this._setAccess(operation, entity, allowed);
				};
				this["getPublic".concat(action, "Access")] = function() {
					return this["get".concat(action, "Access")](PUBLIC_KEY);
				};
				this["setPublic".concat(action, "Access")] = function(allowed) {
					this["set".concat(action, "Access")](PUBLIC_KEY, allowed);
				};
				this["getRole".concat(action, "Access")] = function(role) {
					return this["get".concat(action, "Access")](this._getRoleName(role));
				};
				this["setRole".concat(action, "Access")] = function(role, allowed) {
					this["set".concat(action, "Access")](this._getRoleName(role), allowed);
				};
			}
			for (const [operation, group] of (0, _entries.default)(VALID_PERMISSIONS_EXTENDED).call(VALID_PERMISSIONS_EXTENDED)) this.permissionsMap[operation] = (0, _assign.default)({}, group);
			if (userId && typeof userId === "object") {
				if (userId instanceof _ParseUser.default) {
					this.setReadAccess(userId, true);
					this.setWriteAccess(userId, true);
				} else if (userId instanceof _ParseRole.default) {
					this.setRoleReadAccess(userId, true);
					this.setRoleWriteAccess(userId, true);
				} else for (const permission in userId) {
					var _context;
					const users = userId[permission];
					const isValidPermission = !!VALID_PERMISSIONS.get(permission);
					const isValidPermissionExtended = !!VALID_PERMISSIONS_EXTENDED.get(permission);
					const isValidGroupPermission = (0, _includes.default)(_context = ["readUserFields", "writeUserFields"]).call(_context, permission);
					if (typeof permission !== "string" || !(isValidPermission || isValidPermissionExtended || isValidGroupPermission)) throw new TypeError("Tried to create an CLP with an invalid permission type.");
					if (isValidGroupPermission) {
						if ((0, _every.default)(users).call(users, (pointer) => typeof pointer === "string")) {
							this.permissionsMap[permission] = users;
							continue;
						} else throw new TypeError("Tried to create an CLP with an invalid permission value.");
					}
					for (const user in users) {
						const allowed = users[user];
						if (typeof allowed !== "boolean" && !isValidPermissionExtended && user !== "pointerFields") throw new TypeError("Tried to create an CLP with an invalid permission value.");
						this.permissionsMap[permission][user] = allowed;
					}
				}
			} else if (typeof userId === "function") throw new TypeError("ParseCLP constructed with a function. Did you forget ()?");
		}
		/**
		* Returns a JSON-encoded version of the CLP.
		*
		* @returns {object}
		*/
		toJSON() {
			return _objectSpread({}, this.permissionsMap);
		}
		/**
		* Returns whether this CLP is equal to another object
		*
		* @param other The other object to compare to
		* @returns {boolean}
		*/
		equals(other) {
			if (!(other instanceof ParseCLP)) return false;
			const permissions = (0, _keys.default)(this.permissionsMap);
			const otherPermissions = (0, _keys.default)(other.permissionsMap);
			if (permissions.length !== otherPermissions.length) return false;
			for (const permission in this.permissionsMap) {
				if (!other.permissionsMap[permission]) return false;
				const users = (0, _keys.default)(this.permissionsMap[permission]);
				const otherUsers = (0, _keys.default)(other.permissionsMap[permission]);
				if (users.length !== otherUsers.length) return false;
				for (const user in this.permissionsMap[permission]) {
					if (!other.permissionsMap[permission][user]) return false;
					if (this.permissionsMap[permission][user] !== other.permissionsMap[permission][user]) return false;
				}
			}
			return true;
		}
		_getRoleName(role) {
			let name = role;
			if (role instanceof _ParseRole.default) name = role.getName();
			if (typeof name !== "string") throw new TypeError("role must be a Parse.Role or a String");
			return "role:".concat(name);
		}
		_parseEntity(entity) {
			let userId = entity;
			if (userId instanceof _ParseUser.default) {
				userId = userId.id;
				if (!userId) throw new Error("Cannot get access for a Parse.User without an id.");
			} else if (userId instanceof _ParseRole.default) userId = this._getRoleName(userId);
			if (typeof userId !== "string") throw new TypeError("userId must be a string.");
			return userId;
		}
		_setAccess(permission, userId, allowed) {
			userId = this._parseEntity(userId);
			if (typeof allowed !== "boolean") throw new TypeError("allowed must be either true or false.");
			if (!this.permissionsMap[permission][userId]) {
				if (!allowed) return;
				else this.permissionsMap[permission][userId] = {};
			}
			if (allowed) this.permissionsMap[permission][userId] = true;
			else delete this.permissionsMap[permission][userId];
		}
		_getAccess(permission, userId) {
			let returnBoolean = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
			userId = this._parseEntity(userId);
			const permissions = this.permissionsMap[permission][userId];
			if (returnBoolean) {
				if (!permissions) return false;
				return !!this.permissionsMap[permission][userId];
			}
			return permissions;
		}
		_setArrayAccess(permission, userId, fields) {
			userId = this._parseEntity(userId);
			if (!this.permissionsMap[permission][userId]) this.permissionsMap[permission][userId] = [];
			if (!fields || (0, _isArray.default)(fields) && fields.length === 0) delete this.permissionsMap[permission][userId];
			else if ((0, _isArray.default)(fields) && (0, _every.default)(fields).call(fields, (field) => typeof field === "string")) this.permissionsMap[permission][userId] = fields;
			else throw new TypeError("fields must be an array of strings or undefined.");
		}
		_setGroupPointerPermission(operation, pointerFields) {
			if (!this.permissionsMap[operation]) this.permissionsMap[operation] = [];
			if (!pointerFields || (0, _isArray.default)(pointerFields) && pointerFields.length === 0) delete this.permissionsMap[operation];
			else if ((0, _isArray.default)(pointerFields) && (0, _every.default)(pointerFields).call(pointerFields, (field) => typeof field === "string")) this.permissionsMap[operation] = pointerFields;
			else throw new TypeError("".concat(operation, ".pointerFields must be an array of strings or undefined."));
		}
		_getGroupPointerPermissions(operation) {
			return this.permissionsMap[operation] || [];
		}
		/**
		* Sets user pointer fields to allow permission for get/count/find operations.
		*
		* @param {string[]} pointerFields User pointer fields
		*/
		setReadUserFields(pointerFields) {
			this._setGroupPointerPermission("readUserFields", pointerFields);
		}
		/**
		* @returns {string[]} User pointer fields
		*/
		getReadUserFields() {
			return this._getGroupPointerPermissions("readUserFields") || [];
		}
		/**
		* Sets user pointer fields to allow permission for create/delete/update/addField operations
		*
		* @param {string[]} pointerFields User pointer fields
		*/
		setWriteUserFields(pointerFields) {
			this._setGroupPointerPermission("writeUserFields", pointerFields);
		}
		/**
		* @returns {string[]} User pointer fields
		*/
		getWriteUserFields() {
			return this._getGroupPointerPermissions("writeUserFields") || [];
		}
		/**
		* Sets whether the given user is allowed to retrieve fields from this class.
		*
		* @param userId An instance of Parse.User or its objectId.
		* @param {string[]} fields fields to be protected
		*/
		setProtectedFields(userId, fields) {
			this._setArrayAccess("protectedFields", userId, fields);
		}
		/**
		* Returns array of fields are accessable to this user.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role.
		* @returns {string[]}
		*/
		getProtectedFields(userId) {
			return this._getAccess("protectedFields", userId, false);
		}
		/**
		* Sets whether the given user is allowed to read from this class.
		*
		* @param userId An instance of Parse.User or its objectId.
		* @param {boolean} allowed whether that user should have read access.
		*/
		setReadAccess(userId, allowed) {
			this._setAccess("find", userId, allowed);
			this._setAccess("get", userId, allowed);
			this._setAccess("count", userId, allowed);
		}
		/**
		* Get whether the given user id is *explicitly* allowed to read from this class.
		* Even if this returns false, the user may still be able to access it if
		* getPublicReadAccess returns true or a role that the user belongs to has
		* write access.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role.
		* @returns {boolean}
		*/
		getReadAccess(userId) {
			return this._getAccess("find", userId) && this._getAccess("get", userId) && this._getAccess("count", userId);
		}
		/**
		* Sets whether the given user id is allowed to write to this class.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role..
		* @param {boolean} allowed Whether that user should have write access.
		*/
		setWriteAccess(userId, allowed) {
			this._setAccess("create", userId, allowed);
			this._setAccess("update", userId, allowed);
			this._setAccess("delete", userId, allowed);
			this._setAccess("addField", userId, allowed);
		}
		/**
		* Gets whether the given user id is *explicitly* allowed to write to this class.
		* Even if this returns false, the user may still be able to write it if
		* getPublicWriteAccess returns true or a role that the user belongs to has
		* write access.
		*
		* @param userId An instance of Parse.User or its objectId, or a Parse.Role.
		* @returns {boolean}
		*/
		getWriteAccess(userId) {
			return this._getAccess("create", userId) && this._getAccess("update", userId) && this._getAccess("delete", userId) && this._getAccess("addField", userId);
		}
		/**
		* Sets whether the public is allowed to read from this class.
		*
		* @param {boolean} allowed
		*/
		setPublicReadAccess(allowed) {
			this.setReadAccess(PUBLIC_KEY, allowed);
		}
		/**
		* Gets whether the public is allowed to read from this class.
		*
		* @returns {boolean}
		*/
		getPublicReadAccess() {
			return this.getReadAccess(PUBLIC_KEY);
		}
		/**
		* Sets whether the public is allowed to write to this class.
		*
		* @param {boolean} allowed
		*/
		setPublicWriteAccess(allowed) {
			this.setWriteAccess(PUBLIC_KEY, allowed);
		}
		/**
		* Gets whether the public is allowed to write to this class.
		*
		* @returns {boolean}
		*/
		getPublicWriteAccess() {
			return this.getWriteAccess(PUBLIC_KEY);
		}
		/**
		* Sets whether the public is allowed to protect fields in this class.
		*
		* @param {string[]} fields
		*/
		setPublicProtectedFields(fields) {
			this.setProtectedFields(PUBLIC_KEY, fields);
		}
		/**
		* Gets whether the public is allowed to read fields from this class.
		*
		* @returns {string[]}
		*/
		getPublicProtectedFields() {
			return this.getProtectedFields(PUBLIC_KEY);
		}
		/**
		* Gets whether users belonging to the given role are allowed
		* to read from this class. Even if this returns false, the role may
		* still be able to write it if a parent role has read access.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @returns {boolean} true if the role has read access. false otherwise.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		getRoleReadAccess(role) {
			return this.getReadAccess(this._getRoleName(role));
		}
		/**
		* Gets whether users belonging to the given role are allowed
		* to write to this user. Even if this returns false, the role may
		* still be able to write it if a parent role has write access.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @returns {boolean} true if the role has write access. false otherwise.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		getRoleWriteAccess(role) {
			return this.getWriteAccess(this._getRoleName(role));
		}
		/**
		* Sets whether users belonging to the given role are allowed
		* to read from this class.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @param {boolean} allowed Whether the given role can read this object.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		setRoleReadAccess(role, allowed) {
			this.setReadAccess(this._getRoleName(role), allowed);
		}
		/**
		* Sets whether users belonging to the given role are allowed
		* to write to this class.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @param {boolean} allowed Whether the given role can write this object.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		setRoleWriteAccess(role, allowed) {
			this.setWriteAccess(this._getRoleName(role), allowed);
		}
		/**
		* Gets whether users belonging to the given role are allowed
		* to count to this user. Even if this returns false, the role may
		* still be able to count it if a parent role has count access.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @returns {string[]}
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		getRoleProtectedFields(role) {
			return this.getProtectedFields(this._getRoleName(role));
		}
		/**
		* Sets whether users belonging to the given role are allowed
		* to set access field in this class.
		*
		* @param role The name of the role, or a Parse.Role object.
		* @param {string[]} fields Fields to be protected by Role.
		* @throws {TypeError} If role is neither a Parse.Role nor a String.
		*/
		setRoleProtectedFields(role, fields) {
			this.setProtectedFields(this._getRoleName(role), fields);
		}
	};
}));
//#endregion
//#region node_modules/events/events.js
var require_events = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var R = typeof Reflect === "object" ? Reflect : null;
	var ReflectApply = R && typeof R.apply === "function" ? R.apply : function ReflectApply(target, receiver, args) {
		return Function.prototype.apply.call(target, receiver, args);
	};
	var ReflectOwnKeys;
	if (R && typeof R.ownKeys === "function") ReflectOwnKeys = R.ownKeys;
	else if (Object.getOwnPropertySymbols) ReflectOwnKeys = function ReflectOwnKeys(target) {
		return Object.getOwnPropertyNames(target).concat(Object.getOwnPropertySymbols(target));
	};
	else ReflectOwnKeys = function ReflectOwnKeys(target) {
		return Object.getOwnPropertyNames(target);
	};
	function ProcessEmitWarning(warning) {
		if (console && console.warn) console.warn(warning);
	}
	var NumberIsNaN = Number.isNaN || function NumberIsNaN(value) {
		return value !== value;
	};
	function EventEmitter() {
		EventEmitter.init.call(this);
	}
	module.exports = EventEmitter;
	module.exports.once = once;
	EventEmitter.EventEmitter = EventEmitter;
	EventEmitter.prototype._events = void 0;
	EventEmitter.prototype._eventsCount = 0;
	EventEmitter.prototype._maxListeners = void 0;
	var defaultMaxListeners = 10;
	function checkListener(listener) {
		if (typeof listener !== "function") throw new TypeError("The \"listener\" argument must be of type Function. Received type " + typeof listener);
	}
	Object.defineProperty(EventEmitter, "defaultMaxListeners", {
		enumerable: true,
		get: function() {
			return defaultMaxListeners;
		},
		set: function(arg) {
			if (typeof arg !== "number" || arg < 0 || NumberIsNaN(arg)) throw new RangeError("The value of \"defaultMaxListeners\" is out of range. It must be a non-negative number. Received " + arg + ".");
			defaultMaxListeners = arg;
		}
	});
	EventEmitter.init = function() {
		if (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) {
			this._events = Object.create(null);
			this._eventsCount = 0;
		}
		this._maxListeners = this._maxListeners || void 0;
	};
	EventEmitter.prototype.setMaxListeners = function setMaxListeners(n) {
		if (typeof n !== "number" || n < 0 || NumberIsNaN(n)) throw new RangeError("The value of \"n\" is out of range. It must be a non-negative number. Received " + n + ".");
		this._maxListeners = n;
		return this;
	};
	function _getMaxListeners(that) {
		if (that._maxListeners === void 0) return EventEmitter.defaultMaxListeners;
		return that._maxListeners;
	}
	EventEmitter.prototype.getMaxListeners = function getMaxListeners() {
		return _getMaxListeners(this);
	};
	EventEmitter.prototype.emit = function emit(type) {
		var args = [];
		for (var i = 1; i < arguments.length; i++) args.push(arguments[i]);
		var doError = type === "error";
		var events = this._events;
		if (events !== void 0) doError = doError && events.error === void 0;
		else if (!doError) return false;
		if (doError) {
			var er;
			if (args.length > 0) er = args[0];
			if (er instanceof Error) throw er;
			var err = /* @__PURE__ */ new Error("Unhandled error." + (er ? " (" + er.message + ")" : ""));
			err.context = er;
			throw err;
		}
		var handler = events[type];
		if (handler === void 0) return false;
		if (typeof handler === "function") ReflectApply(handler, this, args);
		else {
			var len = handler.length;
			var listeners = arrayClone(handler, len);
			for (var i = 0; i < len; ++i) ReflectApply(listeners[i], this, args);
		}
		return true;
	};
	function _addListener(target, type, listener, prepend) {
		var m;
		var events;
		var existing;
		checkListener(listener);
		events = target._events;
		if (events === void 0) {
			events = target._events = Object.create(null);
			target._eventsCount = 0;
		} else {
			if (events.newListener !== void 0) {
				target.emit("newListener", type, listener.listener ? listener.listener : listener);
				events = target._events;
			}
			existing = events[type];
		}
		if (existing === void 0) {
			existing = events[type] = listener;
			++target._eventsCount;
		} else {
			if (typeof existing === "function") existing = events[type] = prepend ? [listener, existing] : [existing, listener];
			else if (prepend) existing.unshift(listener);
			else existing.push(listener);
			m = _getMaxListeners(target);
			if (m > 0 && existing.length > m && !existing.warned) {
				existing.warned = true;
				var w = /* @__PURE__ */ new Error("Possible EventEmitter memory leak detected. " + existing.length + " " + String(type) + " listeners added. Use emitter.setMaxListeners() to increase limit");
				w.name = "MaxListenersExceededWarning";
				w.emitter = target;
				w.type = type;
				w.count = existing.length;
				ProcessEmitWarning(w);
			}
		}
		return target;
	}
	EventEmitter.prototype.addListener = function addListener(type, listener) {
		return _addListener(this, type, listener, false);
	};
	EventEmitter.prototype.on = EventEmitter.prototype.addListener;
	EventEmitter.prototype.prependListener = function prependListener(type, listener) {
		return _addListener(this, type, listener, true);
	};
	function onceWrapper() {
		if (!this.fired) {
			this.target.removeListener(this.type, this.wrapFn);
			this.fired = true;
			if (arguments.length === 0) return this.listener.call(this.target);
			return this.listener.apply(this.target, arguments);
		}
	}
	function _onceWrap(target, type, listener) {
		var state = {
			fired: false,
			wrapFn: void 0,
			target,
			type,
			listener
		};
		var wrapped = onceWrapper.bind(state);
		wrapped.listener = listener;
		state.wrapFn = wrapped;
		return wrapped;
	}
	EventEmitter.prototype.once = function once(type, listener) {
		checkListener(listener);
		this.on(type, _onceWrap(this, type, listener));
		return this;
	};
	EventEmitter.prototype.prependOnceListener = function prependOnceListener(type, listener) {
		checkListener(listener);
		this.prependListener(type, _onceWrap(this, type, listener));
		return this;
	};
	EventEmitter.prototype.removeListener = function removeListener(type, listener) {
		var list, events, position, i, originalListener;
		checkListener(listener);
		events = this._events;
		if (events === void 0) return this;
		list = events[type];
		if (list === void 0) return this;
		if (list === listener || list.listener === listener) {
			if (--this._eventsCount === 0) this._events = Object.create(null);
			else {
				delete events[type];
				if (events.removeListener) this.emit("removeListener", type, list.listener || listener);
			}
		} else if (typeof list !== "function") {
			position = -1;
			for (i = list.length - 1; i >= 0; i--) if (list[i] === listener || list[i].listener === listener) {
				originalListener = list[i].listener;
				position = i;
				break;
			}
			if (position < 0) return this;
			if (position === 0) list.shift();
			else spliceOne(list, position);
			if (list.length === 1) events[type] = list[0];
			if (events.removeListener !== void 0) this.emit("removeListener", type, originalListener || listener);
		}
		return this;
	};
	EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
	EventEmitter.prototype.removeAllListeners = function removeAllListeners(type) {
		var listeners, events = this._events, i;
		if (events === void 0) return this;
		if (events.removeListener === void 0) {
			if (arguments.length === 0) {
				this._events = Object.create(null);
				this._eventsCount = 0;
			} else if (events[type] !== void 0) {
				if (--this._eventsCount === 0) this._events = Object.create(null);
				else delete events[type];
			}
			return this;
		}
		if (arguments.length === 0) {
			var keys = Object.keys(events);
			var key;
			for (i = 0; i < keys.length; ++i) {
				key = keys[i];
				if (key === "removeListener") continue;
				this.removeAllListeners(key);
			}
			this.removeAllListeners("removeListener");
			this._events = Object.create(null);
			this._eventsCount = 0;
			return this;
		}
		listeners = events[type];
		if (typeof listeners === "function") this.removeListener(type, listeners);
		else if (listeners !== void 0) for (i = listeners.length - 1; i >= 0; i--) this.removeListener(type, listeners[i]);
		return this;
	};
	function _listeners(target, type, unwrap) {
		var events = target._events;
		if (events === void 0) return [];
		var evlistener = events[type];
		if (evlistener === void 0) return [];
		if (typeof evlistener === "function") return unwrap ? [evlistener.listener || evlistener] : [evlistener];
		return unwrap ? unwrapListeners(evlistener) : arrayClone(evlistener, evlistener.length);
	}
	EventEmitter.prototype.listeners = function listeners(type) {
		return _listeners(this, type, true);
	};
	EventEmitter.prototype.rawListeners = function rawListeners(type) {
		return _listeners(this, type, false);
	};
	EventEmitter.listenerCount = function(emitter, type) {
		if (typeof emitter.listenerCount === "function") return emitter.listenerCount(type);
		else return listenerCount.call(emitter, type);
	};
	EventEmitter.prototype.listenerCount = listenerCount;
	function listenerCount(type) {
		var events = this._events;
		if (events !== void 0) {
			var evlistener = events[type];
			if (typeof evlistener === "function") return 1;
			else if (evlistener !== void 0) return evlistener.length;
		}
		return 0;
	}
	EventEmitter.prototype.eventNames = function eventNames() {
		return this._eventsCount > 0 ? ReflectOwnKeys(this._events) : [];
	};
	function arrayClone(arr, n) {
		var copy = new Array(n);
		for (var i = 0; i < n; ++i) copy[i] = arr[i];
		return copy;
	}
	function spliceOne(list, index) {
		for (; index + 1 < list.length; index++) list[index] = list[index + 1];
		list.pop();
	}
	function unwrapListeners(arr) {
		var ret = new Array(arr.length);
		for (var i = 0; i < ret.length; ++i) ret[i] = arr[i].listener || arr[i];
		return ret;
	}
	function once(emitter, name) {
		return new Promise(function(resolve, reject) {
			function errorListener(err) {
				emitter.removeListener(name, resolver);
				reject(err);
			}
			function resolver() {
				if (typeof emitter.removeListener === "function") emitter.removeListener("error", errorListener);
				resolve([].slice.call(arguments));
			}
			eventTargetAgnosticAddListener(emitter, name, resolver, { once: true });
			if (name !== "error") addErrorHandlerIfEventEmitter(emitter, errorListener, { once: true });
		});
	}
	function addErrorHandlerIfEventEmitter(emitter, handler, flags) {
		if (typeof emitter.on === "function") eventTargetAgnosticAddListener(emitter, "error", handler, flags);
	}
	function eventTargetAgnosticAddListener(emitter, name, listener, flags) {
		if (typeof emitter.on === "function") {
			if (flags.once) emitter.once(name, listener);
			else emitter.on(name, listener);
		} else if (typeof emitter.addEventListener === "function") emitter.addEventListener(name, function wrapListener(arg) {
			if (flags.once) emitter.removeEventListener(name, wrapListener);
			listener(arg);
		});
		else throw new TypeError("The \"emitter\" argument must be of type EventEmitter. Received type " + typeof emitter);
	}
}));
//#endregion
//#region node_modules/parse/lib/browser/EventEmitter.js
var require_EventEmitter = /* @__PURE__ */ __commonJSMin(((exports) => {
	require_define_property$3()(exports, "__esModule", { value: true });
	exports.default = void 0;
	/**
	* This is a simple wrapper to unify EventEmitter implementations across platforms.
	*/
	var EventEmitter;
	try {
		EventEmitter = require_events().EventEmitter;
	} catch (_) {}
	exports.default = EventEmitter;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseConfig.js
var require_ParseConfig = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _stringify = _interopRequireDefault(require_stringify());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _decode = _interopRequireDefault(require_decode());
	var _encode = _interopRequireDefault(require_encode());
	var _escape = _interopRequireDefault(require_escape());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _Storage = _interopRequireDefault(require_Storage());
	/**
	* Parse.Config is a local representation of configuration data that
	* can be set from the Parse dashboard.
	*
	* @alias Parse.Config
	*/
	var ParseConfig = class {
		constructor() {
			(0, _defineProperty2.default)(this, "attributes", void 0);
			(0, _defineProperty2.default)(this, "_escapedAttributes", void 0);
			this.attributes = {};
			this._escapedAttributes = {};
		}
		/**
		* Gets the value of an attribute.
		*
		* @param {string} attr The name of an attribute.
		* @returns {*}
		*/
		get(attr) {
			return this.attributes[attr];
		}
		/**
		* Gets the HTML-escaped value of an attribute.
		*
		* @param {string} attr The name of an attribute.
		* @returns {string}
		*/
		escape(attr) {
			const html = this._escapedAttributes[attr];
			if (html) return html;
			const val = this.attributes[attr];
			let escaped = "";
			if (val != null) escaped = (0, _escape.default)(val.toString());
			this._escapedAttributes[attr] = escaped;
			return escaped;
		}
		/**
		* Retrieves the most recently-fetched configuration object, either from
		* memory or from local storage if necessary.
		*
		* @static
		* @returns {Parse.Config} The most recently-fetched Parse.Config if it
		*     exists, else an empty Parse.Config.
		*/
		static current() {
			return _CoreManager.default.getConfigController().current();
		}
		/**
		* Gets a new configuration object from the server.
		*
		* @static
		* @param {object} options
		* Valid options are:<ul>
		*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
		*     be used for this request.
		* </ul>
		* @returns {Promise} A promise that is resolved with a newly-created
		*     configuration object when the get completes.
		*/
		static get(options) {
			return _CoreManager.default.getConfigController().get(options);
		}
		/**
		* Save value keys to the server.
		*
		* @static
		* @param {object} attrs The config parameters and values.
		* @param {object} masterKeyOnlyFlags The flags that define whether config parameters listed
		* in `attrs` should be retrievable only by using the master key.
		* For example: `param1: true` makes `param1` only retrievable by using the master key.
		* If a parameter is not provided or set to `false`, it can be retrieved without
		* using the master key.
		* @returns {Promise} A promise that is resolved with a newly-created
		*     configuration object or with the current with the update.
		*/
		static save(attrs, masterKeyOnlyFlags) {
			const controller = _CoreManager.default.getConfigController();
			return controller.save(attrs, masterKeyOnlyFlags).then(() => {
				return controller.get({ useMasterKey: true });
			}, (error) => {
				return _promise.default.reject(error);
			});
		}
		/**
		* Used for testing
		*
		* @private
		*/
		static _clearCache() {
			currentConfig = null;
		}
	};
	var currentConfig = null;
	var CURRENT_CONFIG_KEY = "currentConfig";
	function decodePayload(data) {
		try {
			const json = JSON.parse(data);
			if (json && typeof json === "object") return (0, _decode.default)(json);
		} catch (_) {
			return null;
		}
	}
	_CoreManager.default.setConfigController({
		current() {
			if (currentConfig) return currentConfig;
			const config = new ParseConfig();
			const storagePath = _Storage.default.generatePath(CURRENT_CONFIG_KEY);
			if (!_Storage.default.async()) {
				const configData = _Storage.default.getItem(storagePath);
				if (configData) {
					const attributes = decodePayload(configData);
					if (attributes) {
						config.attributes = attributes;
						currentConfig = config;
					}
				}
				return config;
			}
			return _Storage.default.getItemAsync(storagePath).then((configData) => {
				if (configData) {
					const attributes = decodePayload(configData);
					if (attributes) {
						config.attributes = attributes;
						currentConfig = config;
					}
				}
				return config;
			});
		},
		get(options) {
			return _CoreManager.default.getRESTController().request("GET", "config", {}, options).then((response) => {
				if (!response || !response.params) {
					const error = new _ParseError.default(_ParseError.default.INVALID_JSON, "Config JSON response invalid.");
					return _promise.default.reject(error);
				}
				const config = new ParseConfig();
				config.attributes = {};
				for (const attr in response.params) config.attributes[attr] = (0, _decode.default)(response.params[attr]);
				currentConfig = config;
				return _Storage.default.setItemAsync(_Storage.default.generatePath(CURRENT_CONFIG_KEY), (0, _stringify.default)(response.params)).then(() => {
					return config;
				});
			});
		},
		save(attrs, masterKeyOnlyFlags) {
			const RESTController = _CoreManager.default.getRESTController();
			const encodedAttrs = {};
			for (const key in attrs) encodedAttrs[key] = (0, _encode.default)(attrs[key]);
			return RESTController.request("PUT", "config", {
				params: encodedAttrs,
				masterKeyOnly: masterKeyOnlyFlags
			}, { useMasterKey: true }).then((response) => {
				if (response && response.result) return _promise.default.resolve();
				else {
					const error = new _ParseError.default(_ParseError.default.INTERNAL_SERVER_ERROR, "Error occured updating Config.");
					return _promise.default.reject(error);
				}
			});
		}
	});
	exports.default = ParseConfig;
}));
//#endregion
//#region node_modules/parse/lib/browser/FacebookUtils.js
var require_FacebookUtils = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _ParseUser = _interopRequireDefault(require_ParseUser());
	var initialized = false;
	var requestedPermissions;
	var initOptions;
	var provider = {
		authenticate(options) {
			if (typeof FB === "undefined") options.error(this, "Facebook SDK not found.");
			FB.login((response) => {
				if (response.authResponse) {
					if (options.success) options.success(this, {
						id: response.authResponse.userID,
						access_token: response.authResponse.accessToken,
						expiration_date: new Date(response.authResponse.expiresIn * 1e3 + (/* @__PURE__ */ new Date()).getTime()).toJSON()
					});
				} else if (options.error) options.error(this, response);
			}, { scope: requestedPermissions });
		},
		restoreAuthentication(authData) {
			if (authData) {
				const newOptions = {};
				if (initOptions) for (const key in initOptions) newOptions[key] = initOptions[key];
				newOptions.status = false;
				const existingResponse = FB.getAuthResponse();
				if (existingResponse && existingResponse.userID !== authData.id) FB.logout();
				FB.init(newOptions);
			}
			return true;
		},
		getAuthType() {
			return "facebook";
		},
		deauthenticate() {
			this.restoreAuthentication(null);
		}
	};
	exports.default = {
		/**
		* Initializes Parse Facebook integration.  Call this function after you
		* have loaded the Facebook Javascript SDK with the same parameters
		* as you would pass to<code>
		* <a href=
		* "https://developers.facebook.com/docs/reference/javascript/FB.init/">
		* FB.init()</a></code>.  Parse.FacebookUtils will invoke FB.init() for you
		* with these arguments.
		*
		* @function init
		* @name Parse.FacebookUtils.init
		* @param {object} options Facebook options argument as described here:
		*   <a href=
		*   "https://developers.facebook.com/docs/reference/javascript/FB.init/">
		*   FB.init()</a>. The status flag will be coerced to 'false' because it
		*   interferes with Parse Facebook integration. Call FB.getLoginStatus()
		*   explicitly if this behavior is required by your application.
		*/
		init(options) {
			if (typeof FB === "undefined") throw new Error("The Facebook JavaScript SDK must be loaded before calling init.");
			initOptions = {};
			if (options) for (const key in options) initOptions[key] = options[key];
			if (initOptions.status && typeof console !== "undefined") (console.warn || console.log || function() {}).call(console, "The \"status\" flag passed into FB.init, when set to true, can interfere with Parse Facebook integration, so it has been suppressed. Please call FB.getLoginStatus() explicitly if you require this behavior.");
			initOptions.status = false;
			FB.init(initOptions);
			_ParseUser.default._registerAuthenticationProvider(provider);
			initialized = true;
		},
		/**
		* Gets whether the user has their account linked to Facebook.
		*
		* @function isLinked
		* @name Parse.FacebookUtils.isLinked
		* @param {Parse.User} user User to check for a facebook link.
		*     The user must be logged in on this device.
		* @returns {boolean} <code>true</code> if the user has their account
		*     linked to Facebook.
		*/
		isLinked(user) {
			return user._isLinked("facebook");
		},
		/**
		* Logs in a user using Facebook. This method delegates to the Facebook
		* SDK to authenticate the user, and then automatically logs in (or
		* creates, in the case where it is a new user) a Parse.User.
		*
		* Standard API:
		*
		* <code>logIn(permission: string, authData: Object);</code>
		*
		* Advanced API: Used for handling your own oAuth tokens
		* {@link https://docs.parseplatform.org/rest/guide/#linking-users}
		*
		* <code>logIn(authData: Object, options?: Object);</code>
		*
		* @function logIn
		* @name Parse.FacebookUtils.logIn
		* @param {(string | object)} permissions The permissions required for Facebook
		*    log in.  This is a comma-separated string of permissions.
		*    Alternatively, supply a Facebook authData object as described in our
		*    REST API docs if you want to handle getting facebook auth tokens
		*    yourself.
		* @param {object} options MasterKey / SessionToken. Alternatively can be used for authData if permissions is a string
		* @returns {Promise}
		*/
		logIn(permissions, options) {
			if (!permissions || typeof permissions === "string") {
				if (!initialized) throw new Error("You must initialize FacebookUtils before calling logIn.");
				requestedPermissions = permissions;
				return _ParseUser.default.logInWith("facebook", options);
			}
			return _ParseUser.default.logInWith("facebook", { authData: permissions }, options);
		},
		/**
		* Links Facebook to an existing PFUser. This method delegates to the
		* Facebook SDK to authenticate the user, and then automatically links
		* the account to the Parse.User.
		*
		* Standard API:
		*
		* <code>link(user: Parse.User, permission: string, authData?: Object);</code>
		*
		* Advanced API: Used for handling your own oAuth tokens
		* {@link https://docs.parseplatform.org/rest/guide/#linking-users}
		*
		* <code>link(user: Parse.User, authData: Object, options?: FullOptions);</code>
		*
		* @function link
		* @name Parse.FacebookUtils.link
		* @param {Parse.User} user User to link to Facebook. This must be the
		*     current user.
		* @param {(string | object)} permissions The permissions required for Facebook
		*    log in.  This is a comma-separated string of permissions.
		*    Alternatively, supply a Facebook authData object as described in our
		*    REST API docs if you want to handle getting facebook auth tokens
		*    yourself.
		* @param {object} options MasterKey / SessionToken. Alternatively can be used for authData if permissions is a string
		* @returns {Promise}
		*/
		link(user, permissions, options) {
			if (!permissions || typeof permissions === "string") {
				if (!initialized) throw new Error("You must initialize FacebookUtils before calling link.");
				requestedPermissions = permissions;
				return user.linkWith("facebook", options);
			}
			return user.linkWith("facebook", { authData: permissions }, options);
		},
		/**
		* Unlinks the Parse.User from a Facebook account.
		*
		* @function unlink
		* @name Parse.FacebookUtils.unlink
		* @param {Parse.User} user User to unlink from Facebook. This must be the
		*     current user.
		* @param {object} options Standard options object with success and error
		*    callbacks.
		* @returns {Promise}
		*/
		unlink: function(user, options) {
			if (!initialized) throw new Error("You must initialize FacebookUtils before calling unlink.");
			return user._unlinkFrom("facebook", options);
		},
		_getAuthProvider() {
			return provider;
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseHooks.js
var require_ParseHooks = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.create = create;
	exports.createFunction = createFunction;
	exports.createTrigger = createTrigger;
	exports.getFunction = getFunction;
	exports.getFunctions = getFunctions;
	exports.getTrigger = getTrigger;
	exports.getTriggers = getTriggers;
	exports.remove = remove;
	exports.removeFunction = removeFunction;
	exports.removeTrigger = removeTrigger;
	exports.update = update;
	exports.updateFunction = updateFunction;
	exports.updateTrigger = updateTrigger;
	var _promise = _interopRequireDefault(require_promise());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _decode = _interopRequireDefault(require_decode());
	var _ParseError = _interopRequireDefault(require_ParseError());
	function getFunctions() {
		return _CoreManager.default.getHooksController().get("functions");
	}
	function getTriggers() {
		return _CoreManager.default.getHooksController().get("triggers");
	}
	function getFunction(name) {
		return _CoreManager.default.getHooksController().get("functions", name);
	}
	function getTrigger(className, triggerName) {
		return _CoreManager.default.getHooksController().get("triggers", className, triggerName);
	}
	function createFunction(functionName, url) {
		return create({
			functionName,
			url
		});
	}
	function createTrigger(className, triggerName, url) {
		return create({
			className,
			triggerName,
			url
		});
	}
	function create(hook) {
		return _CoreManager.default.getHooksController().create(hook);
	}
	function updateFunction(functionName, url) {
		return update({
			functionName,
			url
		});
	}
	function updateTrigger(className, triggerName, url) {
		return update({
			className,
			triggerName,
			url
		});
	}
	function update(hook) {
		return _CoreManager.default.getHooksController().update(hook);
	}
	function removeFunction(functionName) {
		return remove({ functionName });
	}
	function removeTrigger(className, triggerName) {
		return remove({
			className,
			triggerName
		});
	}
	function remove(hook) {
		return _CoreManager.default.getHooksController().remove(hook);
	}
	_CoreManager.default.setHooksController({
		get(type, functionName, triggerName) {
			let url = "hooks/" + type;
			if (functionName) {
				url += "/" + functionName;
				if (triggerName) url += "/" + triggerName;
			}
			return this.sendRequest("GET", url);
		},
		create(hook) {
			let url;
			if (hook.functionName && hook.url) url = "hooks/functions";
			else if (hook.className && hook.triggerName && hook.url) url = "hooks/triggers";
			else return _promise.default.reject({
				error: "invalid hook declaration",
				code: 143
			});
			return this.sendRequest("POST", url, hook);
		},
		remove(hook) {
			let url;
			if (hook.functionName) {
				url = "hooks/functions/" + hook.functionName;
				delete hook.functionName;
			} else if (hook.className && hook.triggerName) {
				url = "hooks/triggers/" + hook.className + "/" + hook.triggerName;
				delete hook.className;
				delete hook.triggerName;
			} else return _promise.default.reject({
				error: "invalid hook declaration",
				code: 143
			});
			return this.sendRequest("PUT", url, { __op: "Delete" });
		},
		update(hook) {
			let url;
			if (hook.functionName && hook.url) {
				url = "hooks/functions/" + hook.functionName;
				delete hook.functionName;
			} else if (hook.className && hook.triggerName && hook.url) {
				url = "hooks/triggers/" + hook.className + "/" + hook.triggerName;
				delete hook.className;
				delete hook.triggerName;
			} else return _promise.default.reject({
				error: "invalid hook declaration",
				code: 143
			});
			return this.sendRequest("PUT", url, hook);
		},
		sendRequest(method, url, body) {
			return _CoreManager.default.getRESTController().request(method, url, body, { useMasterKey: true }).then((res) => {
				const decoded = (0, _decode.default)(res);
				if (decoded) return _promise.default.resolve(decoded);
				return _promise.default.reject(new _ParseError.default(_ParseError.default.INVALID_JSON, "The server returned an invalid response."));
			});
		}
	});
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.constructor.js
var require_es_set_constructor = /* @__PURE__ */ __commonJSMin((() => {
	require_collection()("Set", function(init) {
		return function Set() {
			return init(this, arguments.length ? arguments[0] : void 0);
		};
	}, require_collection_strong());
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.js
var require_es_set = /* @__PURE__ */ __commonJSMin((() => {
	require_es_set_constructor();
}));
//#endregion
//#region node_modules/core-js-pure/internals/a-set.js
var require_a_set = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var tryToString = require_try_to_string();
	var $TypeError = TypeError;
	module.exports = function(it) {
		if (typeof it == "object" && "size" in it && "has" in it && "add" in it && "delete" in it && "keys" in it) return it;
		throw new $TypeError(tryToString(it) + " is not a set");
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-helpers.js
var require_set_helpers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getBuiltIn = require_get_built_in();
	var caller = require_caller();
	var Set = getBuiltIn("Set");
	var SetPrototype = Set.prototype;
	module.exports = {
		Set,
		add: caller("add", 1),
		has: caller("has", 1),
		remove: caller("delete", 1),
		proto: SetPrototype
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/iterate-simple.js
var require_iterate_simple = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var call = require_function_call();
	module.exports = function(record, fn, ITERATOR_INSTEAD_OF_RECORD) {
		var iterator = ITERATOR_INSTEAD_OF_RECORD ? record : record.iterator;
		var next = record.next;
		var step, result;
		while (!(step = call(next, iterator)).done) {
			result = fn(step.value);
			if (result !== void 0) return result;
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-iterate.js
var require_set_iterate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var iterateSimple = require_iterate_simple();
	module.exports = function(set, fn, interruptible) {
		return interruptible ? iterateSimple(set.keys(), fn, true) : set.forEach(fn);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-clone.js
var require_set_clone = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SetHelpers = require_set_helpers();
	var iterate = require_set_iterate();
	var Set = SetHelpers.Set;
	var add = SetHelpers.add;
	module.exports = function(set) {
		var result = new Set();
		iterate(set, function(it) {
			add(result, it);
		});
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-size.js
var require_set_size = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(set) {
		return set.size;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-iterator-direct.js
var require_get_iterator_direct = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(obj) {
		return {
			iterator: obj,
			next: obj.next,
			done: false
		};
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/get-set-record.js
var require_get_set_record = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aCallable = require_a_callable();
	var anObject = require_an_object();
	var call = require_function_call();
	var toIntegerOrInfinity = require_to_integer_or_infinity();
	var getIteratorDirect = require_get_iterator_direct();
	var INVALID_SIZE = "Invalid size";
	var $RangeError = RangeError;
	var $TypeError = TypeError;
	var max = Math.max;
	var SetRecord = function(set, intSize) {
		this.set = set;
		this.size = max(intSize, 0);
		this.has = aCallable(set.has);
		this.keys = aCallable(set.keys);
	};
	SetRecord.prototype = {
		getIterator: function() {
			return getIteratorDirect(anObject(call(this.keys, this.set)));
		},
		includes: function(it) {
			return call(this.has, this.set, it);
		}
	};
	module.exports = function(obj) {
		anObject(obj);
		var numSize = +obj.size;
		if (numSize !== numSize) throw new $TypeError(INVALID_SIZE);
		var intSize = toIntegerOrInfinity(numSize);
		if (intSize < 0) throw new $RangeError(INVALID_SIZE);
		return new SetRecord(obj, intSize);
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-difference.js
var require_set_difference = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var SetHelpers = require_set_helpers();
	var clone = require_set_clone();
	var size = require_set_size();
	var getSetRecord = require_get_set_record();
	var iterateSet = require_set_iterate();
	var iterateSimple = require_iterate_simple();
	var has = SetHelpers.has;
	var remove = SetHelpers.remove;
	module.exports = function difference(other) {
		var O = aSet(this);
		var otherRec = getSetRecord(other);
		var result = clone(O);
		if (size(result) <= otherRec.size) iterateSet(result, function(e) {
			if (otherRec.includes(e)) remove(result, e);
		});
		else iterateSimple(otherRec.getIterator(), function(e) {
			if (has(result, e)) remove(result, e);
		});
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-method-accept-set-like.js
var require_set_method_accept_set_like = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function() {
		return false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.difference.v2.js
var require_es_set_difference_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var difference = require_set_difference();
	var fails = require_fails();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("difference", function(result) {
			return result.size === 0;
		}) || fails(function() {
			var setLike = {
				size: 1,
				has: function() {
					return true;
				},
				keys: function() {
					var index = 0;
					return { next: function() {
						var done = index++ > 1;
						if (baseSet.has(1)) baseSet.clear();
						return {
							done,
							value: 2
						};
					} };
				}
			};
			var baseSet = /* @__PURE__ */ new Set([
				1,
				2,
				3,
				4
			]);
			return baseSet.difference(setLike).size !== 3;
		})
	}, { difference });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-intersection.js
var require_set_intersection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var SetHelpers = require_set_helpers();
	var size = require_set_size();
	var getSetRecord = require_get_set_record();
	var iterateSet = require_set_iterate();
	var iterateSimple = require_iterate_simple();
	var Set = SetHelpers.Set;
	var add = SetHelpers.add;
	var has = SetHelpers.has;
	module.exports = function intersection(other) {
		var O = aSet(this);
		var otherRec = getSetRecord(other);
		var result = new Set();
		if (size(O) > otherRec.size) iterateSimple(otherRec.getIterator(), function(e) {
			if (has(O, e)) add(result, e);
		});
		else iterateSet(O, function(e) {
			if (otherRec.includes(e)) add(result, e);
		});
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.intersection.v2.js
var require_es_set_intersection_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var fails = require_fails();
	var intersection = require_set_intersection();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("intersection", function(result) {
			return result.size === 2 && result.has(1) && result.has(2);
		}) || fails(function() {
			return String(Array.from((/* @__PURE__ */ new Set([
				1,
				2,
				3
			])).intersection(/* @__PURE__ */ new Set([3, 2])))) !== "3,2";
		})
	}, { intersection });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-is-disjoint-from.js
var require_set_is_disjoint_from = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var has = require_set_helpers().has;
	var size = require_set_size();
	var getSetRecord = require_get_set_record();
	var iterateSet = require_set_iterate();
	var iterateSimple = require_iterate_simple();
	var iteratorClose = require_iterator_close();
	module.exports = function isDisjointFrom(other) {
		var O = aSet(this);
		var otherRec = getSetRecord(other);
		if (size(O) <= otherRec.size) return iterateSet(O, function(e) {
			if (otherRec.includes(e)) return false;
		}, true) !== false;
		var iterator = otherRec.getIterator();
		return iterateSimple(iterator, function(e) {
			if (has(O, e)) return iteratorClose(iterator.iterator, "normal", false);
		}) !== false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.is-disjoint-from.v2.js
var require_es_set_is_disjoint_from_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var isDisjointFrom = require_set_is_disjoint_from();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("isDisjointFrom", function(result) {
			return !result;
		})
	}, { isDisjointFrom });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-is-subset-of.js
var require_set_is_subset_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var size = require_set_size();
	var iterate = require_set_iterate();
	var getSetRecord = require_get_set_record();
	module.exports = function isSubsetOf(other) {
		var O = aSet(this);
		var otherRec = getSetRecord(other);
		if (size(O) > otherRec.size) return false;
		return iterate(O, function(e) {
			if (!otherRec.includes(e)) return false;
		}, true) !== false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.is-subset-of.v2.js
var require_es_set_is_subset_of_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var isSubsetOf = require_set_is_subset_of();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("isSubsetOf", function(result) {
			return result;
		})
	}, { isSubsetOf });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-is-superset-of.js
var require_set_is_superset_of = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var has = require_set_helpers().has;
	var size = require_set_size();
	var getSetRecord = require_get_set_record();
	var iterateSimple = require_iterate_simple();
	var iteratorClose = require_iterator_close();
	module.exports = function isSupersetOf(other) {
		var O = aSet(this);
		var otherRec = getSetRecord(other);
		if (size(O) < otherRec.size) return false;
		var iterator = otherRec.getIterator();
		return iterateSimple(iterator, function(e) {
			if (!has(O, e)) return iteratorClose(iterator.iterator, "normal", false);
		}) !== false;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.is-superset-of.v2.js
var require_es_set_is_superset_of_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var isSupersetOf = require_set_is_superset_of();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("isSupersetOf", function(result) {
			return !result;
		})
	}, { isSupersetOf });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-symmetric-difference.js
var require_set_symmetric_difference = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var SetHelpers = require_set_helpers();
	var clone = require_set_clone();
	var getSetRecord = require_get_set_record();
	var iterateSimple = require_iterate_simple();
	var add = SetHelpers.add;
	var has = SetHelpers.has;
	var remove = SetHelpers.remove;
	module.exports = function symmetricDifference(other) {
		var O = aSet(this);
		var keysIter = getSetRecord(other).getIterator();
		var result = clone(O);
		iterateSimple(keysIter, function(e) {
			if (has(O, e)) remove(result, e);
			else add(result, e);
		});
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-method-get-keys-before-cloning-detection.js
var require_set_method_get_keys_before_cloning_detection = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(METHOD_NAME) {
		try {
			var baseSet = /* @__PURE__ */ new Set();
			var result = baseSet[METHOD_NAME]({
				size: 0,
				has: function() {
					return true;
				},
				keys: function() {
					return Object.defineProperty({}, "next", { get: function() {
						baseSet.clear();
						baseSet.add(4);
						return function() {
							return { done: true };
						};
					} });
				}
			});
			return result.size === 1 && result.values().next().value === 4;
		} catch (error) {
			return false;
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.symmetric-difference.v2.js
var require_es_set_symmetric_difference_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var symmetricDifference = require_set_symmetric_difference();
	var setMethodGetKeysBeforeCloning = require_set_method_get_keys_before_cloning_detection();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("symmetricDifference") || !setMethodGetKeysBeforeCloning("symmetricDifference")
	}, { symmetricDifference });
}));
//#endregion
//#region node_modules/core-js-pure/internals/set-union.js
var require_set_union = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var aSet = require_a_set();
	var add = require_set_helpers().add;
	var clone = require_set_clone();
	var getSetRecord = require_get_set_record();
	var iterateSimple = require_iterate_simple();
	module.exports = function union(other) {
		var O = aSet(this);
		var keysIter = getSetRecord(other).getIterator();
		var result = clone(O);
		iterateSimple(keysIter, function(it) {
			add(result, it);
		});
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.set.union.v2.js
var require_es_set_union_v2 = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var union = require_set_union();
	var setMethodGetKeysBeforeCloning = require_set_method_get_keys_before_cloning_detection();
	$({
		target: "Set",
		proto: true,
		real: true,
		forced: !require_set_method_accept_set_like()("union") || !setMethodGetKeysBeforeCloning("union")
	}, { union });
}));
//#endregion
//#region node_modules/core-js-pure/es/set/index.js
var require_set$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	require_es_set();
	require_es_set_difference_v2();
	require_es_set_intersection_v2();
	require_es_set_is_disjoint_from_v2();
	require_es_set_is_subset_of_v2();
	require_es_set_is_superset_of_v2();
	require_es_set_symmetric_difference_v2();
	require_es_set_union_v2();
	require_es_string_iterator();
	module.exports = require_path().Set;
}));
//#endregion
//#region node_modules/core-js-pure/stable/set/index.js
var require_set$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parent = require_set$2();
	require_web_dom_collections_iterator();
	module.exports = parent;
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/set.js
var require_set = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_set$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/call-with-safe-iteration-closing.js
var require_call_with_safe_iteration_closing = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var anObject = require_an_object();
	var iteratorClose = require_iterator_close();
	module.exports = function(iterator, fn, value, ENTRIES) {
		try {
			return ENTRIES ? fn(anObject(value)[0], value[1]) : fn(value);
		} catch (error) {
			iteratorClose(iterator, "throw", error);
		}
	};
}));
//#endregion
//#region node_modules/core-js-pure/internals/array-from.js
var require_array_from = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var bind = require_function_bind_context();
	var call = require_function_call();
	var toObject = require_to_object();
	var callWithSafeIterationClosing = require_call_with_safe_iteration_closing();
	var isArrayIteratorMethod = require_is_array_iterator_method();
	var isConstructor = require_is_constructor();
	var lengthOfArrayLike = require_length_of_array_like();
	var createProperty = require_create_property();
	var setArrayLength = require_array_set_length();
	var getIterator = require_get_iterator_internal();
	var getIteratorMethod = require_get_iterator_method_internal();
	var iteratorClose = require_iterator_close();
	var doesNotExceedSafeInteger = require_does_not_exceed_safe_integer();
	var $Array = Array;
	module.exports = function from(arrayLike) {
		var IS_CONSTRUCTOR = isConstructor(this);
		var argumentsLength = arguments.length;
		var mapfn = argumentsLength > 1 ? arguments[1] : void 0;
		var mapping = mapfn !== void 0;
		if (mapping) mapfn = bind(mapfn, argumentsLength > 2 ? arguments[2] : void 0);
		var O = toObject(arrayLike);
		var iteratorMethod = getIteratorMethod(O);
		var index = 0;
		var length, result, step, iterator, next, value;
		if (iteratorMethod && !(this === $Array && isArrayIteratorMethod(iteratorMethod))) {
			result = IS_CONSTRUCTOR ? new this() : [];
			iterator = getIterator(O, iteratorMethod);
			next = iterator.next;
			for (; !(step = call(next, iterator)).done; index++) {
				try {
					doesNotExceedSafeInteger(index);
				} catch (error) {
					iteratorClose(iterator, "throw", error);
				}
				value = mapping ? callWithSafeIterationClosing(iterator, mapfn, [step.value, index], true) : step.value;
				try {
					createProperty(result, index, value);
				} catch (error) {
					iteratorClose(iterator, "throw", error);
				}
			}
		} else {
			length = lengthOfArrayLike(O);
			result = IS_CONSTRUCTOR ? new this(length) : $Array(length);
			for (; length > index; index++) {
				value = mapping ? mapfn(O[index], index) : O[index];
				createProperty(result, index, value);
			}
		}
		setArrayLength(result, index);
		return result;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.array.from.js
var require_es_array_from = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var from = require_array_from();
	$({
		target: "Array",
		stat: true,
		forced: !require_check_correctness_of_iteration()(function(iterable) {
			Array.from(iterable);
		})
	}, { from });
}));
//#endregion
//#region node_modules/core-js-pure/es/array/from.js
var require_from$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_string_iterator();
	require_es_array_from();
	module.exports = require_path().Array.from;
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/from.js
var require_from$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_from$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/array/from.js
var require_from = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_from$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/StorageController.react-native.js
var require_StorageController_react_native = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	exports.default = {
		async: 1,
		getItemAsync(path) {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().getItem(path, (err, value) => {
					if (err) reject(err);
					else resolve(value || null);
				});
			});
		},
		setItemAsync(path, value) {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().setItem(path, value, (err) => {
					if (err) reject(err);
					else resolve();
				});
			});
		},
		removeItemAsync(path) {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().removeItem(path, (err) => {
					if (err) reject(err);
					else resolve();
				});
			});
		},
		getAllKeysAsync() {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().getAllKeys((err, keys) => {
					if (err) reject(err);
					else resolve(keys || []);
				});
			});
		},
		multiGet(keys) {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().multiGet(keys, (err, result) => {
					if (err) reject(err);
					else resolve(result || null);
				});
			});
		},
		multiRemove(keys) {
			return new _promise.default((resolve, reject) => {
				_CoreManager.default.getAsyncStorage().multiRemove(keys, (err) => {
					if (err) reject(err);
					else resolve(keys);
				});
			});
		},
		clear() {
			return _CoreManager.default.getAsyncStorage().clear();
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/LocalDatastoreController.react-native.js
var require_LocalDatastoreController_react_native = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var _forEach = _interopRequireDefault(require_for_each());
	var _map = _interopRequireDefault(require_map$3());
	var _LocalDatastoreUtils = require_LocalDatastoreUtils();
	var _StorageController = _interopRequireDefault(require_StorageController_react_native());
	exports.default = {
		async fromPinWithName(name) {
			const values = await _StorageController.default.getItemAsync(name);
			if (!values) return [];
			return JSON.parse(values);
		},
		async pinWithName(name, value) {
			try {
				const values = (0, _stringify.default)(value);
				await _StorageController.default.setItemAsync(name, values);
			} catch (e) {
				console.error(e.message);
			}
		},
		unPinWithName(name) {
			return _StorageController.default.removeItemAsync(name);
		},
		async getAllContents() {
			const keys = await _StorageController.default.getAllKeysAsync();
			const batch = [];
			for (let i = 0; i < keys.length; i += 1) {
				const key = keys[i];
				if ((0, _LocalDatastoreUtils.isLocalDatastoreKey)(key)) batch.push(key);
			}
			const LDS = {};
			let results = [];
			try {
				results = await _StorageController.default.multiGet(batch);
			} catch (error) {
				console.error("Error getAllContents: ", error);
				return {};
			}
			(0, _forEach.default)(results).call(results, (pair) => {
				const [key, value] = pair;
				try {
					LDS[key] = JSON.parse(value);
				} catch (_) {
					LDS[key] = null;
				}
			});
			return LDS;
		},
		async getRawStorage() {
			var _context;
			const keys = await _StorageController.default.getAllKeysAsync();
			const storage = {};
			const results = await _StorageController.default.multiGet(keys);
			(0, _map.default)(_context = results).call(_context, (pair) => {
				const [key, value] = pair;
				storage[key] = value;
			});
			return storage;
		},
		async clear() {
			const keys = await _StorageController.default.getAllKeysAsync();
			const batch = [];
			for (let i = 0; i < keys.length; i += 1) {
				const key = keys[i];
				if ((0, _LocalDatastoreUtils.isLocalDatastoreKey)(key)) batch.push(key);
			}
			await _StorageController.default.multiRemove(batch).catch((error) => console.error("Error clearing local datastore: ", error));
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/LocalDatastoreController.default.js
var require_LocalDatastoreController_default = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var _reduce = _interopRequireDefault(require_reduce());
	var _promise = _interopRequireDefault(require_promise());
	var _map = _interopRequireDefault(require_map$3());
	var _LocalDatastoreUtils = require_LocalDatastoreUtils();
	var _Storage = _interopRequireDefault(require_Storage());
	exports.default = {
		async fromPinWithName(name) {
			const values = await _Storage.default.getItemAsync(name);
			if (!values) return [];
			return JSON.parse(values);
		},
		pinWithName(name, value) {
			const values = (0, _stringify.default)(value);
			return _Storage.default.setItemAsync(name, values);
		},
		unPinWithName(name) {
			return _Storage.default.removeItemAsync(name);
		},
		async getAllContents() {
			const keys = await _Storage.default.getAllKeysAsync();
			return (0, _reduce.default)(keys).call(keys, async (previousPromise, key) => {
				const LDS = await previousPromise;
				if ((0, _LocalDatastoreUtils.isLocalDatastoreKey)(key)) {
					const value = await _Storage.default.getItemAsync(key);
					try {
						LDS[key] = JSON.parse(value);
					} catch (error) {
						console.error("Error getAllContents: ", error);
					}
				}
				return LDS;
			}, _promise.default.resolve({}));
		},
		async getRawStorage() {
			const keys = await _Storage.default.getAllKeysAsync();
			return (0, _reduce.default)(keys).call(keys, async (previousPromise, key) => {
				const LDS = await previousPromise;
				LDS[key] = await _Storage.default.getItemAsync(key);
				return LDS;
			}, _promise.default.resolve({}));
		},
		async clear() {
			const keys = await _Storage.default.getAllKeysAsync();
			const toRemove = [];
			for (const key of keys) if ((0, _LocalDatastoreUtils.isLocalDatastoreKey)(key)) toRemove.push(key);
			const promises = (0, _map.default)(toRemove).call(toRemove, this.unPinWithName);
			return _promise.default.all(promises);
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/LocalDatastoreController.js
var require_LocalDatastoreController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	_interopRequireDefault(require_LocalDatastoreController_react_native());
	exports.default = _interopRequireDefault(require_LocalDatastoreController_default()).default;
}));
//#endregion
//#region node_modules/parse/lib/browser/LocalDatastore.js
var require_LocalDatastore = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _set = _interopRequireDefault(require_set());
	var _keys = _interopRequireDefault(require_keys$4());
	var _filter = _interopRequireDefault(require_filter());
	var _includes = _interopRequireDefault(require_includes());
	var _startsWith = _interopRequireDefault(require_starts_with());
	var _isArray = _interopRequireDefault(require_is_array());
	var _map = _interopRequireDefault(require_map$3());
	var _concat = _interopRequireDefault(require_concat());
	var _from = _interopRequireDefault(require_from());
	var _find = _interopRequireDefault(require_find());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _LocalDatastoreController = _interopRequireDefault(require_LocalDatastoreController());
	var _ParseQuery = _interopRequireDefault(require_ParseQuery());
	var _LocalDatastoreUtils = require_LocalDatastoreUtils();
	/**
	* Provides a local datastore which can be used to store and retrieve <code>Parse.Object</code>. <br />
	* To enable this functionality, call <code>Parse.enableLocalDatastore()</code>.
	*
	* Pin object to add to local datastore
	*
	* <pre>await object.pin();</pre>
	* <pre>await object.pinWithName('pinName');</pre>
	*
	* Query pinned objects
	*
	* <pre>query.fromLocalDatastore();</pre>
	* <pre>query.fromPin();</pre>
	* <pre>query.fromPinWithName();</pre>
	*
	* <pre>const localObjects = await query.find();</pre>
	*
	* @class Parse.LocalDatastore
	* @static
	*/
	var LocalDatastore = {
		isEnabled: false,
		isSyncing: false,
		fromPinWithName(name) {
			return _CoreManager.default.getLocalDatastoreController().fromPinWithName(name);
		},
		async pinWithName(name, value) {
			return _CoreManager.default.getLocalDatastoreController().pinWithName(name, value);
		},
		async unPinWithName(name) {
			return _CoreManager.default.getLocalDatastoreController().unPinWithName(name);
		},
		_getAllContents() {
			return _CoreManager.default.getLocalDatastoreController().getAllContents();
		},
		async _getRawStorage() {
			return _CoreManager.default.getLocalDatastoreController().getRawStorage();
		},
		async _clear() {
			return _CoreManager.default.getLocalDatastoreController().clear();
		},
		async _handlePinAllWithName(name, objects) {
			const pinName = this.getPinName(name);
			const toPinPromises = [];
			const objectKeys = [];
			for (const parent of objects) {
				const children = this._getChildren(parent);
				const parentKey = this.getKeyForObject(parent);
				const json = parent._toFullJSON(void 0, true);
				if (parent._localId) json._localId = parent._localId;
				children[parentKey] = json;
				for (const objectKey in children) {
					objectKeys.push(objectKey);
					toPinPromises.push(this.pinWithName(objectKey, [children[objectKey]]));
				}
			}
			const fromPinPromise = this.fromPinWithName(pinName);
			const [pinned] = await _promise.default.all([fromPinPromise, toPinPromises]);
			const toPin = [...new _set.default([...pinned || [], ...objectKeys])];
			return this.pinWithName(pinName, toPin);
		},
		async _handleUnPinAllWithName(name, objects) {
			const localDatastore = await this._getAllContents();
			const pinName = this.getPinName(name);
			const promises = [];
			let objectKeys = [];
			for (const parent of objects) {
				const children = this._getChildren(parent);
				const parentKey = this.getKeyForObject(parent);
				objectKeys.push(parentKey, ...(0, _keys.default)(children));
			}
			objectKeys = [...new _set.default(objectKeys)];
			let pinned = localDatastore[pinName] || [];
			pinned = (0, _filter.default)(pinned).call(pinned, (item) => !(0, _includes.default)(objectKeys).call(objectKeys, item));
			if (pinned.length == 0) {
				promises.push(this.unPinWithName(pinName));
				delete localDatastore[pinName];
			} else {
				promises.push(this.pinWithName(pinName, pinned));
				localDatastore[pinName] = pinned;
			}
			for (const objectKey of objectKeys) {
				let hasReference = false;
				for (const key in localDatastore) if (key === _LocalDatastoreUtils.DEFAULT_PIN || (0, _startsWith.default)(key).call(key, _LocalDatastoreUtils.PIN_PREFIX)) {
					const pinnedObjects = localDatastore[key] || [];
					if ((0, _includes.default)(pinnedObjects).call(pinnedObjects, objectKey)) {
						hasReference = true;
						break;
					}
				}
				if (!hasReference) promises.push(this.unPinWithName(objectKey));
			}
			return _promise.default.all(promises);
		},
		_getChildren(object) {
			const encountered = {};
			const json = object._toFullJSON(void 0, true);
			for (const key in json) if (json[key] && json[key].__type && json[key].__type === "Object") this._traverse(json[key], encountered);
			return encountered;
		},
		_traverse(object, encountered) {
			if (!object.objectId) return;
			else {
				const objectKey = this.getKeyForObject(object);
				if (encountered[objectKey]) return;
				encountered[objectKey] = object;
			}
			for (const key in object) {
				let json = object[key];
				if (!object[key]) json = object;
				if (json.__type && json.__type === "Object") this._traverse(json, encountered);
			}
		},
		async _serializeObjectsFromPinName(name) {
			var _context;
			const localDatastore = await this._getAllContents();
			const allObjects = [];
			for (const key in localDatastore) if ((0, _startsWith.default)(key).call(key, _LocalDatastoreUtils.OBJECT_PREFIX)) allObjects.push(localDatastore[key][0]);
			if (!name) return allObjects;
			const pinned = localDatastore[this.getPinName(name)];
			if (!(0, _isArray.default)(pinned)) return [];
			const promises = (0, _map.default)(pinned).call(pinned, (objectKey) => this.fromPinWithName(objectKey));
			let objects = await _promise.default.all(promises);
			objects = (0, _concat.default)(_context = []).call(_context, ...objects);
			return (0, _filter.default)(objects).call(objects, (object) => object != null);
		},
		async _serializeObject(objectKey, localDatastore) {
			let LDS = localDatastore;
			if (!LDS) LDS = await this._getAllContents();
			if (!LDS[objectKey] || LDS[objectKey].length === 0) return null;
			const root = LDS[objectKey][0];
			const queue = [];
			const meta = {};
			let uniqueId = 0;
			meta[uniqueId] = root;
			queue.push(uniqueId);
			while (queue.length !== 0) {
				const subTreeRoot = meta[queue.shift()];
				for (const field in subTreeRoot) {
					const value = subTreeRoot[field];
					if (value.__type && value.__type === "Object") {
						const key = this.getKeyForObject(value);
						if (LDS[key] && LDS[key].length > 0) {
							const pointer = LDS[key][0];
							uniqueId++;
							meta[uniqueId] = pointer;
							subTreeRoot[field] = pointer;
							queue.push(uniqueId);
						}
					}
				}
			}
			return root;
		},
		async _updateObjectIfPinned(object) {
			if (!this.isEnabled) return;
			const objectKey = this.getKeyForObject(object);
			const pinned = await this.fromPinWithName(objectKey);
			if (!pinned || pinned.length === 0) return;
			return this.pinWithName(objectKey, [object._toFullJSON()]);
		},
		async _destroyObjectIfPinned(object) {
			if (!this.isEnabled) return;
			const localDatastore = await this._getAllContents();
			const objectKey = this.getKeyForObject(object);
			if (!localDatastore[objectKey]) return;
			const promises = [this.unPinWithName(objectKey)];
			delete localDatastore[objectKey];
			for (const key in localDatastore) if (key === _LocalDatastoreUtils.DEFAULT_PIN || (0, _startsWith.default)(key).call(key, _LocalDatastoreUtils.PIN_PREFIX)) {
				let pinned = localDatastore[key] || [];
				if ((0, _includes.default)(pinned).call(pinned, objectKey)) {
					pinned = (0, _filter.default)(pinned).call(pinned, (item) => item !== objectKey);
					if (pinned.length == 0) {
						promises.push(this.unPinWithName(key));
						delete localDatastore[key];
					} else {
						promises.push(this.pinWithName(key, pinned));
						localDatastore[key] = pinned;
					}
				}
			}
			return _promise.default.all(promises);
		},
		async _updateLocalIdForObject(localId, object) {
			var _context2, _context3;
			if (!this.isEnabled) return;
			const localKey = (0, _concat.default)(_context2 = (0, _concat.default)(_context3 = "".concat(_LocalDatastoreUtils.OBJECT_PREFIX)).call(_context3, object.className, "_")).call(_context2, localId);
			const objectKey = this.getKeyForObject(object);
			const unsaved = await this.fromPinWithName(localKey);
			if (!unsaved || unsaved.length === 0) return;
			const promises = [this.unPinWithName(localKey), this.pinWithName(objectKey, unsaved)];
			const localDatastore = await this._getAllContents();
			for (const key in localDatastore) if (key === _LocalDatastoreUtils.DEFAULT_PIN || (0, _startsWith.default)(key).call(key, _LocalDatastoreUtils.PIN_PREFIX)) {
				let pinned = localDatastore[key] || [];
				if ((0, _includes.default)(pinned).call(pinned, localKey)) {
					pinned = (0, _filter.default)(pinned).call(pinned, (item) => item !== localKey);
					pinned.push(objectKey);
					promises.push(this.pinWithName(key, pinned));
					localDatastore[key] = pinned;
				}
			}
			return _promise.default.all(promises);
		},
		/**
		* Updates Local Datastore from Server
		*
		* <pre>
		* await Parse.LocalDatastore.updateFromServer();
		* </pre>
		*
		* @function updateFromServer
		* @name Parse.LocalDatastore.updateFromServer
		* @static
		*/
		async updateFromServer() {
			var _context4;
			if (!this.checkIfEnabled() || this.isSyncing) return;
			const localDatastore = await this._getAllContents();
			const keys = [];
			for (const key in localDatastore) if ((0, _startsWith.default)(key).call(key, _LocalDatastoreUtils.OBJECT_PREFIX)) keys.push(key);
			if (keys.length === 0) return;
			this.isSyncing = true;
			const pointersHash = {};
			for (const key of keys) {
				let [, , className, objectId] = key.split("_");
				if (key.split("_").length === 5 && key.split("_")[3] === "User") {
					className = "_User";
					objectId = key.split("_")[4];
				}
				if ((0, _startsWith.default)(objectId).call(objectId, "local")) continue;
				if (!(className in pointersHash)) pointersHash[className] = new _set.default();
				pointersHash[className].add(objectId);
			}
			const queryPromises = (0, _map.default)(_context4 = (0, _keys.default)(pointersHash)).call(_context4, (className) => {
				const objectIds = (0, _from.default)(pointersHash[className]);
				const query = new _ParseQuery.default(className);
				query.limit(objectIds.length);
				if (objectIds.length === 1) query.equalTo("objectId", objectIds[0]);
				else query.containedIn("objectId", objectIds);
				return (0, _find.default)(query).call(query);
			});
			try {
				const responses = await _promise.default.all(queryPromises);
				const objects = (0, _concat.default)([]).apply([], responses);
				const pinPromises = (0, _map.default)(objects).call(objects, (object) => {
					const objectKey = this.getKeyForObject(object);
					return this.pinWithName(objectKey, object._toFullJSON());
				});
				await _promise.default.all(pinPromises);
				this.isSyncing = false;
			} catch (error) {
				console.error("Error syncing LocalDatastore: ", error);
				this.isSyncing = false;
			}
		},
		getKeyForObject(object) {
			var _context5, _context6;
			const objectId = object.objectId || object._getId();
			return (0, _concat.default)(_context5 = (0, _concat.default)(_context6 = "".concat(_LocalDatastoreUtils.OBJECT_PREFIX)).call(_context6, object.className, "_")).call(_context5, objectId);
		},
		getPinName(pinName) {
			if (!pinName || pinName === _LocalDatastoreUtils.DEFAULT_PIN) return _LocalDatastoreUtils.DEFAULT_PIN;
			return _LocalDatastoreUtils.PIN_PREFIX + pinName;
		},
		checkIfEnabled() {
			if (!this.isEnabled) console.error("Parse.enableLocalDatastore() must be called first");
			return this.isEnabled;
		}
	};
	exports.default = LocalDatastore;
	_CoreManager.default.setLocalDatastoreController(_LocalDatastoreController.default);
	_CoreManager.default.setLocalDatastore(LocalDatastore);
}));
//#endregion
//#region node_modules/parse/lib/browser/Push.js
var require_Push = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.getPushStatus = getPushStatus;
	exports.send = send;
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseQuery = _interopRequireDefault(require_ParseQuery());
	/** Options for Push.send operation */
	/**
	* Contains functions to deal with Push in Parse.
	*
	* @class Parse.Push
	* @static
	* @hideconstructor
	*/
	/**
	* Sends a push notification.
	* **Available in Cloud Code only.**
	*
	* See {@link https://docs.parseplatform.org/js/guide/#push-notifications Push Notification Guide}
	*
	* @function send
	* @name Parse.Push.send
	* @param {object} data -  The data of the push notification. Valid fields
	* are:
	*   <ol>
	*     <li>channels - An Array of channels to push to.</li>
	*     <li>push_time - A Date object for when to send the push.</li>
	*     <li>expiration_time -  A Date object for when to expire
	*         the push.</li>
	*     <li>expiration_interval - The seconds from now to expire the push.</li>
	*     <li>where - A Parse.Query over Parse.Installation that is used to match
	*         a set of installations to push to.</li>
	*     <li>data - The data to send as part of the push.</li>
	*   <ol>
	* @param {object} options Valid options
	* are:<ul>
	*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
	*     be used for this request.
	* </ul>
	* @returns {Promise} A promise that is fulfilled when the push request
	*     completes and returns `pushStatusId`.
	*/
	function send(data) {
		let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		if (data.where && data.where instanceof _ParseQuery.default) data.where = data.where.toJSON().where;
		if (data.push_time && typeof data.push_time === "object") data.push_time = data.push_time.toJSON();
		if (data.expiration_time && typeof data.expiration_time === "object") data.expiration_time = data.expiration_time.toJSON();
		if (data.expiration_time && data.expiration_interval) throw new Error("expiration_time and expiration_interval cannot both be set.");
		const pushOptions = { useMasterKey: true };
		if (Object.hasOwn(options, "useMasterKey")) pushOptions.useMasterKey = options.useMasterKey;
		return _CoreManager.default.getPushController().send(data, pushOptions);
	}
	/**
	* Gets push status by Id
	*
	* @function getPushStatus
	* @name Parse.Push.getPushStatus
	* @param {string} pushStatusId The Id of Push Status.
	* @param {object} options Valid options
	* are:<ul>
	*   <li>useMasterKey: In Cloud Code and Node only, causes the Master Key to
	*     be used for this request.
	* </ul>
	* @returns {Parse.Object} Status of Push.
	*/
	function getPushStatus(pushStatusId) {
		let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
		const pushOptions = { useMasterKey: true };
		if (Object.hasOwn(options, "useMasterKey")) pushOptions.useMasterKey = options.useMasterKey;
		return new _ParseQuery.default("_PushStatus").get(pushStatusId, pushOptions);
	}
	_CoreManager.default.setPushController({ async send(data, options) {
		var _response$_headers;
		options.returnStatus = true;
		return (_response$_headers = (await _CoreManager.default.getRESTController().request("POST", "push", data, options))._headers) === null || _response$_headers === void 0 ? void 0 : _response$_headers["X-Parse-Push-Status-Id"];
	} });
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseSchema.js
var require_ParseSchema = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _indexOf = _interopRequireDefault(require_index_of());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _ParseCLP = _interopRequireDefault(require_ParseCLP());
	var FIELD_TYPES = [
		"String",
		"Number",
		"Boolean",
		"Bytes",
		"Date",
		"File",
		"GeoPoint",
		"Polygon",
		"Array",
		"Object",
		"Pointer",
		"Relation"
	];
	/**
	* A Parse.Schema object is for handling schema data from Parse.
	* <p>All the schemas methods require MasterKey.
	*
	* When adding fields, you may set required and default values. (Requires Parse Server 3.7.0+)
	*
	* <pre>
	* const options = { required: true, defaultValue: 'hello world' };
	* const schema = new Parse.Schema('MyClass');
	* schema.addString('field', options);
	* schema.addIndex('index_name', { 'field': 1 });
	* schema.save();
	* </pre>
	* </p>
	*
	* @alias Parse.Schema
	*/
	var ParseSchema = class {
		/**
		* @param {string} className Parse Class string.
		*/
		constructor(className) {
			(0, _defineProperty2.default)(this, "className", void 0);
			(0, _defineProperty2.default)(this, "_fields", void 0);
			(0, _defineProperty2.default)(this, "_indexes", void 0);
			(0, _defineProperty2.default)(this, "_clp", void 0);
			if (typeof className === "string") {
				if (className === "User" && _CoreManager.default.get("PERFORM_USER_REWRITE")) this.className = "_User";
				else this.className = className;
			}
			this._fields = {};
			this._indexes = {};
		}
		/**
		* Static method to get all schemas
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		static all() {
			return _CoreManager.default.getSchemaController().get("").then((response) => {
				if (response.results.length === 0) throw new Error("Schema not found.");
				return response.results;
			});
		}
		/**
		* Get the Schema from Parse
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		get() {
			this.assertClassName();
			return _CoreManager.default.getSchemaController().get(this.className).then((response) => {
				if (!response) throw new Error("Schema not found.");
				return response;
			});
		}
		/**
		* Create a new Schema on Parse
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		save() {
			this.assertClassName();
			const controller = _CoreManager.default.getSchemaController();
			const params = {
				className: this.className,
				fields: this._fields,
				indexes: this._indexes,
				classLevelPermissions: this._clp
			};
			return controller.create(this.className, params);
		}
		/**
		* Update a Schema on Parse
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		update() {
			this.assertClassName();
			const controller = _CoreManager.default.getSchemaController();
			const params = {
				className: this.className,
				fields: this._fields,
				indexes: this._indexes,
				classLevelPermissions: this._clp
			};
			this._fields = {};
			this._indexes = {};
			return controller.update(this.className, params);
		}
		/**
		* Removing a Schema from Parse
		* Can only be used on Schema without objects
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		delete() {
			this.assertClassName();
			return _CoreManager.default.getSchemaController().delete(this.className);
		}
		/**
		* Removes all objects from a Schema (class) in Parse.
		* EXERCISE CAUTION, running this will delete all objects for this schema and cannot be reversed
		*
		* @returns {Promise} A promise that is resolved with the result when
		* the query completes.
		*/
		purge() {
			this.assertClassName();
			return _CoreManager.default.getSchemaController().purge(this.className);
		}
		/**
		* Assert if ClassName has been filled
		*
		* @private
		*/
		assertClassName() {
			if (!this.className) throw new Error("You must set a Class Name before making any request.");
		}
		/**
		* Sets Class Level Permissions when creating / updating a Schema.
		* EXERCISE CAUTION, running this may override CLP for this schema and cannot be reversed
		*
		* @param {object | Parse.CLP} clp Class Level Permissions
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		setCLP(clp) {
			if (clp instanceof _ParseCLP.default) this._clp = clp.toJSON();
			else this._clp = clp;
			return this;
		}
		/**
		* Adding a Field to Create / Update a Schema
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {string} type Can be a (String|Number|Boolean|Date|Parse.File|Parse.GeoPoint|Array|Object|Pointer|Parse.Relation)
		* @param {object} options
		* Valid options are:<ul>
		*   <li>required: If field is not set, save operation fails (Requires Parse Server 3.7.0+)
		*   <li>defaultValue: If field is not set, a default value is selected (Requires Parse Server 3.7.0+)
		*   <li>targetClass: Required if type is Pointer or Parse.Relation
		* </ul>
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addField(name, type, options) {
			type = type || "String";
			options = options || {};
			if (!name) throw new Error("field name may not be null.");
			if ((0, _indexOf.default)(FIELD_TYPES).call(FIELD_TYPES, type) === -1) throw new Error("".concat(type, " is not a valid type."));
			if (type === "Pointer") return this.addPointer(name, options.targetClass, options);
			if (type === "Relation") return this.addRelation(name, options.targetClass);
			const fieldOptions = { type };
			if (typeof options.required === "boolean") fieldOptions.required = options.required;
			if (options.defaultValue !== void 0) fieldOptions.defaultValue = options.defaultValue;
			if (type === "Date") {
				if (options && options.defaultValue) fieldOptions.defaultValue = {
					__type: "Date",
					iso: new Date(options.defaultValue)
				};
			}
			if (type === "Bytes") {
				if (options && options.defaultValue) fieldOptions.defaultValue = {
					__type: "Bytes",
					base64: options.defaultValue
				};
			}
			this._fields[name] = fieldOptions;
			return this;
		}
		/**
		* Adding an Index to Create / Update a Schema
		*
		* @param {string} name Name of the index
		* @param {object} index { field: value }
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*
		* <pre>
		* schema.addIndex('index_name', { 'field': 1 });
		* </pre>
		*/
		addIndex(name, index) {
			if (!name) throw new Error("index name may not be null.");
			if (!index) throw new Error("index may not be null.");
			this._indexes[name] = index;
			return this;
		}
		/**
		* Adding String Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addString(name, options) {
			return this.addField(name, "String", options);
		}
		/**
		* Adding Number Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addNumber(name, options) {
			return this.addField(name, "Number", options);
		}
		/**
		* Adding Boolean Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addBoolean(name, options) {
			return this.addField(name, "Boolean", options);
		}
		/**
		* Adding Bytes Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addBytes(name, options) {
			return this.addField(name, "Bytes", options);
		}
		/**
		* Adding Date Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addDate(name, options) {
			return this.addField(name, "Date", options);
		}
		/**
		* Adding File Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addFile(name, options) {
			return this.addField(name, "File", options);
		}
		/**
		* Adding GeoPoint Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addGeoPoint(name, options) {
			return this.addField(name, "GeoPoint", options);
		}
		/**
		* Adding Polygon Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addPolygon(name, options) {
			return this.addField(name, "Polygon", options);
		}
		/**
		* Adding Array Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addArray(name, options) {
			return this.addField(name, "Array", options);
		}
		/**
		* Adding Object Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addObject(name, options) {
			return this.addField(name, "Object", options);
		}
		/**
		* Adding Pointer Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {string} targetClass Name of the target Pointer Class
		* @param {object} options See {@link https://parseplatform.org/Parse-SDK-JS/api/master/Parse.Schema.html#addField addField}
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addPointer(name, targetClass, options) {
			if (!name) throw new Error("field name may not be null.");
			if (!targetClass) throw new Error("You need to set the targetClass of the Pointer.");
			const fieldOptions = {
				type: "Pointer",
				targetClass
			};
			if (typeof (options === null || options === void 0 ? void 0 : options.required) === "boolean") fieldOptions.required = options.required;
			if ((options === null || options === void 0 ? void 0 : options.defaultValue) !== void 0) {
				fieldOptions.defaultValue = options.defaultValue;
				if (options.defaultValue instanceof _ParseObject.default) fieldOptions.defaultValue = options.defaultValue.toPointer();
			}
			this._fields[name] = fieldOptions;
			return this;
		}
		/**
		* Adding Relation Field
		*
		* @param {string} name Name of the field that will be created on Parse
		* @param {string} targetClass Name of the target Pointer Class
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		addRelation(name, targetClass) {
			if (!name) throw new Error("field name may not be null.");
			if (!targetClass) throw new Error("You need to set the targetClass of the Relation.");
			this._fields[name] = {
				type: "Relation",
				targetClass
			};
			return this;
		}
		/**
		* Deleting a Field to Update on a Schema
		*
		* @param {string} name Name of the field
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		deleteField(name) {
			this._fields[name] = { __op: "Delete" };
			return this;
		}
		/**
		* Deleting an Index to Update on a Schema
		*
		* @param {string} name Name of the field
		* @returns {Parse.Schema} Returns the schema, so you can chain this call.
		*/
		deleteIndex(name) {
			this._indexes[name] = { __op: "Delete" };
			return this;
		}
	};
	_CoreManager.default.setSchemaController({
		send(className, method) {
			let params = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
			return _CoreManager.default.getRESTController().request(method, "schemas/".concat(className), params, { useMasterKey: true });
		},
		get(className) {
			return this.send(className, "GET");
		},
		create(className, params) {
			return this.send(className, "POST", params);
		},
		update(className, params) {
			return this.send(className, "PUT", params);
		},
		delete(className) {
			return this.send(className, "DELETE");
		},
		purge(className) {
			return _CoreManager.default.getRESTController().request("DELETE", "purge/".concat(className), {}, { useMasterKey: true });
		}
	});
	exports.default = ParseSchema;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseSession.js
var require_ParseSession = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _isRevocableSession = _interopRequireDefault(require_isRevocableSession());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _ParseUser = _interopRequireDefault(require_ParseUser());
	/**
	* <p>A Parse.Session object is a local representation of a revocable session.
	* This class is a subclass of a Parse.Object, and retains the same
	* functionality of a Parse.Object.</p>
	*
	* @alias Parse.Session
	* @augments Parse.Object
	*/
	var ParseSession = class extends _ParseObject.default {
		/**
		* @param {object} attributes The initial set of data to store in the user.
		*/
		constructor(attributes) {
			super("_Session");
			if (attributes && typeof attributes === "object") try {
				this.set(attributes || {});
			} catch (_) {
				throw new Error("Can't create an invalid Session");
			}
		}
		/**
		* Returns the session token string.
		*
		* @returns {string}
		*/
		getSessionToken() {
			const token = this.get("sessionToken");
			if (typeof token === "string") return token;
			return "";
		}
		static readOnlyAttributes() {
			return [
				"createdWith",
				"expiresAt",
				"installationId",
				"restricted",
				"sessionToken",
				"user"
			];
		}
		/**
		* Retrieves the Session object for the currently logged in session.
		*
		* @param {object} options useMasterKey
		* @static
		* @returns {Promise} A promise that is resolved with the Parse.Session
		* object after it has been fetched. If there is no current user, the
		* promise will be rejected.
		*/
		static current(options) {
			const controller = _CoreManager.default.getSessionController();
			const sessionOptions = _ParseObject.default._getRequestOptions(options);
			return _ParseUser.default.currentAsync().then((user) => {
				if (!user) return _promise.default.reject("There is no current user.");
				sessionOptions.sessionToken = user.getSessionToken();
				return controller.getSession(sessionOptions);
			});
		}
		/**
		* Determines whether the current session token is revocable.
		* This method is useful for migrating Express.js or Node.js web apps to
		* use revocable sessions. If you are migrating an app that uses the Parse
		* SDK in the browser only, please use Parse.User.enableRevocableSession()
		* instead, so that sessions can be automatically upgraded.
		*
		* @static
		* @returns {boolean}
		*/
		static isCurrentSessionRevocable() {
			const currentUser = _ParseUser.default.current();
			if (currentUser) return (0, _isRevocableSession.default)(currentUser.getSessionToken() || "");
			return false;
		}
	};
	_ParseObject.default.registerSubclass("_Session", ParseSession);
	_CoreManager.default.setSessionController({ getSession(options) {
		const RESTController = _CoreManager.default.getRESTController();
		const session = new ParseSession();
		return RESTController.request("GET", "sessions/me", {}, options).then((sessionData) => {
			session._finishFetch(sessionData);
			session._setExisted(true);
			return session;
		});
	} });
	exports.default = ParseSession;
}));
//#endregion
//#region node_modules/core-js-pure/es/array/virtual/values.js
var require_values$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_array_iterator();
	require_es_object_to_string();
	module.exports = require_get_built_in_prototype_method()("Array", "values");
}));
//#endregion
//#region node_modules/core-js-pure/stable/array/virtual/values.js
var require_values$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_values$3();
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/values.js
var require_values$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_web_dom_collections_iterator();
	var classof = require_classof();
	var hasOwn = require_has_own_property();
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_values$2();
	var ArrayPrototype = Array.prototype;
	var DOMIterables = {
		DOMTokenList: true,
		NodeList: true
	};
	module.exports = function(it) {
		var own = it.values;
		return it === ArrayPrototype || isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.values || hasOwn(DOMIterables, classof(it)) ? method : own;
	};
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/values.js
var require_values = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_values$1();
}));
//#endregion
//#region node_modules/core-js-pure/internals/function-bind.js
var require_function_bind = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var uncurryThis = require_function_uncurry_this();
	var aCallable = require_a_callable();
	var isObject = require_is_object();
	var hasOwn = require_has_own_property();
	var arraySlice = require_array_slice();
	var NATIVE_BIND = require_function_bind_native();
	var $Function = Function;
	var concat = uncurryThis([].concat);
	var join = uncurryThis([].join);
	var factories = {};
	var construct = function(C, argsLength, args) {
		if (!hasOwn(factories, argsLength)) {
			var list = [];
			var i = 0;
			for (; i < argsLength; i++) list[i] = "a[" + i + "]";
			factories[argsLength] = $Function("C,a", "return new C(" + join(list, ",") + ")");
		}
		return factories[argsLength](C, args);
	};
	module.exports = NATIVE_BIND ? $Function.bind : function bind(that) {
		var F = aCallable(this);
		var Prototype = F.prototype;
		var partArgs = arraySlice(arguments, 1);
		var boundFunction = function bound() {
			var args = concat(partArgs, arraySlice(arguments));
			return this instanceof boundFunction ? construct(F, args.length, args) : F.apply(that, args);
		};
		if (isObject(Prototype)) boundFunction.prototype = Prototype;
		return boundFunction;
	};
}));
//#endregion
//#region node_modules/core-js-pure/modules/es.function.bind.js
var require_es_function_bind = /* @__PURE__ */ __commonJSMin((() => {
	var $ = require_export();
	var bind = require_function_bind();
	$({
		target: "Function",
		proto: true,
		forced: Function.bind !== bind
	}, { bind });
}));
//#endregion
//#region node_modules/core-js-pure/es/function/virtual/bind.js
var require_bind$3 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	require_es_function_bind();
	module.exports = require_get_built_in_prototype_method()("Function", "bind");
}));
//#endregion
//#region node_modules/core-js-pure/es/instance/bind.js
var require_bind$2 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isPrototypeOf = require_object_is_prototype_of();
	var method = require_bind$3();
	var FunctionPrototype = Function.prototype;
	module.exports = function(it) {
		var own = it.bind;
		return it === FunctionPrototype || isPrototypeOf(FunctionPrototype, it) && own === FunctionPrototype.bind ? method : own;
	};
}));
//#endregion
//#region node_modules/core-js-pure/stable/instance/bind.js
var require_bind$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_bind$2();
}));
//#endregion
//#region node_modules/@babel/runtime-corejs3/core-js-stable/instance/bind.js
var require_bind = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_bind$1();
}));
//#endregion
//#region node_modules/parse/lib/browser/LiveQuerySubscription.js
var require_LiveQuerySubscription = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _stringify = _interopRequireDefault(require_stringify());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _promiseUtils = require_promiseUtils();
	/**
	* Creates a new LiveQuery Subscription.
	* <a href="https://nodejs.org/api/events.html#events_class_eventemitter">cloud functions</a>.
	*
	* <p>Response Object - Contains data from the client that made the request
	* <ul>
	* <li>clientId</li>
	* <li>installationId - requires Parse Server 4.0.0+</li>
	* </ul>
	* </p>
	*
	* <p>Open Event - When you call query.subscribe(), we send a subscribe request to
	* the LiveQuery server, when we get the confirmation from the LiveQuery server,
	* this event will be emitted. When the client loses WebSocket connection to the
	* LiveQuery server, we will try to auto reconnect the LiveQuery server. If we
	* reconnect the LiveQuery server and successfully resubscribe the ParseQuery,
	* you'll also get this event.
	*
	* <pre>
	* subscription.on('open', (response) => {
	*
	* });</pre></p>
	*
	* <p>Create Event - When a new ParseObject is created and it fulfills the ParseQuery you subscribe,
	* you'll get this event. The object is the ParseObject which is created.
	*
	* <pre>
	* subscription.on('create', (object, response) => {
	*
	* });</pre></p>
	*
	* <p>Update Event - When an existing ParseObject (original) which fulfills the ParseQuery you subscribe
	* is updated (The ParseObject fulfills the ParseQuery before and after changes),
	* you'll get this event. The object is the ParseObject which is updated.
	* Its content is the latest value of the ParseObject.
	*
	* Parse-Server 3.1.3+ Required for original object parameter
	*
	* <pre>
	* subscription.on('update', (object, original, response) => {
	*
	* });</pre></p>
	*
	* <p>Enter Event - When an existing ParseObject's (original) old value doesn't fulfill the ParseQuery
	* but its new value fulfills the ParseQuery, you'll get this event. The object is the
	* ParseObject which enters the ParseQuery. Its content is the latest value of the ParseObject.
	*
	* Parse-Server 3.1.3+ Required for original object parameter
	*
	* <pre>
	* subscription.on('enter', (object, original, response) => {
	*
	* });</pre></p>
	*
	*
	* <p>Update Event - When an existing ParseObject's old value fulfills the ParseQuery but its new value
	* doesn't fulfill the ParseQuery, you'll get this event. The object is the ParseObject
	* which leaves the ParseQuery. Its content is the latest value of the ParseObject.
	*
	* <pre>
	* subscription.on('leave', (object, response) => {
	*
	* });</pre></p>
	*
	*
	* <p>Delete Event - When an existing ParseObject which fulfills the ParseQuery is deleted, you'll
	* get this event. The object is the ParseObject which is deleted.
	*
	* <pre>
	* subscription.on('delete', (object, response) => {
	*
	* });</pre></p>
	*
	*
	* <p>Close Event - When the client loses the WebSocket connection to the LiveQuery
	* server and we stop receiving events, you'll get this event.
	*
	* <pre>
	* subscription.on('close', () => {
	*
	* });</pre></p>
	*/
	var LiveQuerySubscription = class {
		constructor(id, query, sessionToken, client) {
			var _this = this;
			(0, _defineProperty2.default)(this, "id", void 0);
			(0, _defineProperty2.default)(this, "query", void 0);
			(0, _defineProperty2.default)(this, "sessionToken", void 0);
			(0, _defineProperty2.default)(this, "subscribePromise", void 0);
			(0, _defineProperty2.default)(this, "unsubscribePromise", void 0);
			(0, _defineProperty2.default)(this, "subscribed", void 0);
			(0, _defineProperty2.default)(this, "client", void 0);
			(0, _defineProperty2.default)(this, "emitter", void 0);
			(0, _defineProperty2.default)(this, "on", void 0);
			(0, _defineProperty2.default)(this, "emit", void 0);
			this.id = id;
			this.query = query;
			this.sessionToken = sessionToken;
			this.client = client;
			this.subscribePromise = (0, _promiseUtils.resolvingPromise)();
			this.unsubscribePromise = (0, _promiseUtils.resolvingPromise)();
			this.subscribed = false;
			const Emitter = _CoreManager.default.getEventEmitter();
			this.emitter = new Emitter();
			this.on = (eventName, listener) => this.emitter.on(eventName, listener);
			this.emit = function(eventName) {
				for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
				return _this.emitter.emit(eventName, ...args);
			};
			this.on("error", () => {});
		}
		/**
		* Close the subscription
		*
		* @returns {Promise}
		*/
		unsubscribe() {
			return _CoreManager.default.getLiveQueryController().getDefaultLiveQueryClient().then((liveQueryClient) => {
				this.emit("close");
				return liveQueryClient.unsubscribe(this);
			});
		}
		/**
		* Execute a query on this subscription.
		* The results will be delivered via the 'result' event.
		*/
		find() {
			if (this.client) this.client.connectPromise.then(() => {
				this.client.socket.send((0, _stringify.default)({
					op: "query",
					requestId: this.id
				}));
			});
		}
	};
	exports.default = LiveQuerySubscription;
}));
//#endregion
//#region node_modules/parse/lib/browser/LiveQueryClient.js
var require_LiveQueryClient = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _indexOf = _interopRequireDefault(require_index_of());
	var _map = _interopRequireDefault(require_map());
	var _keys = _interopRequireDefault(require_keys());
	var _stringify = _interopRequireDefault(require_stringify());
	var _forEach = _interopRequireDefault(require_for_each());
	var _values = _interopRequireDefault(require_values());
	var _setTimeout2 = _interopRequireDefault(require_set_timeout());
	var _map2 = _interopRequireDefault(require_map$3());
	var _bind = _interopRequireDefault(require_bind());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var _LiveQuerySubscription = _interopRequireDefault(require_LiveQuerySubscription());
	var _promiseUtils = require_promiseUtils();
	var _ParseError = _interopRequireDefault(require_ParseError());
	var CLIENT_STATE = {
		INITIALIZED: "initialized",
		CONNECTING: "connecting",
		CONNECTED: "connected",
		CLOSED: "closed",
		RECONNECTING: "reconnecting",
		DISCONNECTED: "disconnected"
	};
	var OP_TYPES = {
		CONNECT: "connect",
		SUBSCRIBE: "subscribe",
		UNSUBSCRIBE: "unsubscribe",
		ERROR: "error",
		QUERY: "query"
	};
	var OP_EVENTS = {
		CONNECTED: "connected",
		SUBSCRIBED: "subscribed",
		UNSUBSCRIBED: "unsubscribed",
		ERROR: "error",
		CREATE: "create",
		UPDATE: "update",
		ENTER: "enter",
		LEAVE: "leave",
		DELETE: "delete",
		RESULT: "result"
	};
	var CLIENT_EMMITER_TYPES = {
		CLOSE: "close",
		ERROR: "error",
		OPEN: "open"
	};
	var SUBSCRIPTION_EMMITER_TYPES = {
		OPEN: "open",
		CLOSE: "close",
		ERROR: "error",
		CREATE: "create",
		UPDATE: "update",
		ENTER: "enter",
		LEAVE: "leave",
		DELETE: "delete",
		RESULT: "result"
	};
	var generateInterval = (k) => {
		return Math.random() * Math.min(30, Math.pow(2, k) - 1) * 1e3;
	};
	/**
	* Creates a new LiveQueryClient.
	* <a href="https://nodejs.org/api/events.html#events_class_eventemitter">cloud functions</a>.
	*
	* A wrapper of a standard WebSocket client. We add several useful methods to
	* help you connect/disconnect to LiveQueryServer, subscribe/unsubscribe a ParseQuery easily.
	*
	* javascriptKey and masterKey are used for verifying the LiveQueryClient when it tries
	* to connect to the LiveQuery server
	*
	* We expose three events to help you monitor the status of the LiveQueryClient.
	*
	* <pre>
	* const LiveQueryClient = Parse.LiveQueryClient;
	* const client = new LiveQueryClient({
	*   applicationId: '',
	*   serverURL: '',
	*   javascriptKey: '',
	*   masterKey: ''
	*  });
	* </pre>
	*
	* Open - When we establish the WebSocket connection to the LiveQuery server, you'll get this event.
	* <pre>
	* client.on('open', () => {
	*
	* });</pre>
	*
	* Close - When we lose the WebSocket connection to the LiveQuery server, you'll get this event.
	* <pre>
	* client.on('close', () => {
	*
	* });</pre>
	*
	* Error - When some network error or LiveQuery server error happens, you'll get this event.
	* <pre>
	* client.on('error', (error) => {
	*
	* });</pre>
	*
	* @alias Parse.LiveQueryClient
	*/
	var LiveQueryClient = class {
		/**
		* Creates a new LiveQueryClient instance.
		*
		* @param options - Configuration options for the LiveQuery client
		* @param options.applicationId - The applicationId of your Parse app
		* @param options.serverURL - The URL of your LiveQuery server (must start with 'ws' or 'wss')
		* @param options.javascriptKey - (Optional) The JavaScript key for your Parse app
		* @param options.masterKey - (Optional) Your Parse Master Key (Node.js only!)
		* @param options.sessionToken - (Optional) Session token for authenticated requests
		* @param options.installationId - (Optional) Installation ID for the client
		*/
		constructor(_ref) {
			var _this = this;
			let { applicationId, serverURL, javascriptKey, masterKey, sessionToken, installationId } = _ref;
			(0, _defineProperty2.default)(this, "attempts", void 0);
			(0, _defineProperty2.default)(this, "id", void 0);
			(0, _defineProperty2.default)(this, "requestId", void 0);
			(0, _defineProperty2.default)(this, "applicationId", void 0);
			(0, _defineProperty2.default)(this, "serverURL", void 0);
			(0, _defineProperty2.default)(this, "javascriptKey", void 0);
			(0, _defineProperty2.default)(this, "masterKey", void 0);
			(0, _defineProperty2.default)(this, "sessionToken", void 0);
			(0, _defineProperty2.default)(this, "installationId", void 0);
			(0, _defineProperty2.default)(this, "additionalProperties", void 0);
			(0, _defineProperty2.default)(this, "connectPromise", void 0);
			(0, _defineProperty2.default)(this, "subscriptions", void 0);
			(0, _defineProperty2.default)(this, "socket", void 0);
			(0, _defineProperty2.default)(this, "state", void 0);
			(0, _defineProperty2.default)(this, "reconnectHandle", void 0);
			(0, _defineProperty2.default)(this, "emitter", void 0);
			(0, _defineProperty2.default)(this, "on", void 0);
			(0, _defineProperty2.default)(this, "emit", void 0);
			if (!serverURL || (0, _indexOf.default)(serverURL).call(serverURL, "ws") !== 0) throw new Error("You need to set a proper Parse LiveQuery server url before using LiveQueryClient");
			this.reconnectHandle = null;
			this.attempts = 1;
			this.id = 0;
			this.requestId = 1;
			this.serverURL = serverURL;
			this.applicationId = applicationId;
			this.javascriptKey = javascriptKey || void 0;
			this.masterKey = masterKey || void 0;
			this.sessionToken = sessionToken || void 0;
			this.installationId = installationId || void 0;
			this.additionalProperties = true;
			this.connectPromise = (0, _promiseUtils.resolvingPromise)();
			this.subscriptions = new _map.default();
			this.state = CLIENT_STATE.INITIALIZED;
			const EventEmitter = _CoreManager.default.getEventEmitter();
			this.emitter = new EventEmitter();
			this.on = (eventName, listener) => this.emitter.on(eventName, listener);
			this.emit = function(eventName) {
				for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
				return _this.emitter.emit(eventName, ...args);
			};
			this.on("error", () => {});
		}
		shouldOpen() {
			return this.state === CLIENT_STATE.INITIALIZED || this.state === CLIENT_STATE.DISCONNECTED;
		}
		/**
		* Subscribes to a ParseQuery
		*
		* If you provide the sessionToken, when the LiveQuery server gets ParseObject's
		* updates from parse server, it'll try to check whether the sessionToken fulfills
		* the ParseObject's ACL. The LiveQuery server will only send updates to clients whose
		* sessionToken is fit for the ParseObject's ACL. You can check the LiveQuery protocol
		* <a href="https://github.com/parse-community/parse-server/wiki/Parse-LiveQuery-Protocol-Specification">here</a> for more details. The subscription you get is the same subscription you get
		* from our Standard API.
		*
		* @param {ParseQuery} query - the ParseQuery you want to subscribe to
		* @param {string} sessionToken (optional)
		* @returns {LiveQuerySubscription | undefined}
		*/
		subscribe(query, sessionToken) {
			var _queryJSON$keys, _queryJSON$watch;
			if (!query) return;
			const className = query.className;
			const queryJSON = query.toJSON();
			const where = queryJSON.where;
			const keys = (_queryJSON$keys = (0, _keys.default)(queryJSON)) === null || _queryJSON$keys === void 0 ? void 0 : _queryJSON$keys.split(",");
			const watch = (_queryJSON$watch = queryJSON.watch) === null || _queryJSON$watch === void 0 ? void 0 : _queryJSON$watch.split(",");
			const subscribeRequest = {
				op: OP_TYPES.SUBSCRIBE,
				requestId: this.requestId,
				query: {
					className,
					where,
					keys,
					watch
				},
				sessionToken: void 0
			};
			if (sessionToken) subscribeRequest.sessionToken = sessionToken;
			const subscription = new _LiveQuerySubscription.default(this.requestId, query, sessionToken, this);
			this.subscriptions.set(this.requestId, subscription);
			this.requestId += 1;
			this.connectPromise.then(() => {
				this.socket.send((0, _stringify.default)(subscribeRequest));
			}).catch((error) => {
				subscription.subscribePromise.reject(error);
			});
			return subscription;
		}
		/**
		* After calling unsubscribe you'll stop receiving events from the subscription object.
		*
		* @param {object} subscription - subscription you would like to unsubscribe from.
		* @returns {Promise | undefined}
		*/
		async unsubscribe(subscription) {
			if (!subscription) return;
			const unsubscribeRequest = {
				op: OP_TYPES.UNSUBSCRIBE,
				requestId: subscription.id
			};
			return this.connectPromise.then(() => {
				return this.socket.send((0, _stringify.default)(unsubscribeRequest));
			}).then(() => {
				return subscription.unsubscribePromise;
			});
		}
		/**
		* After open is called, the LiveQueryClient will try to send a connect request
		* to the LiveQuery server.
		*
		*/
		open() {
			const WebSocketImplementation = _CoreManager.default.getWebSocketController();
			if (!WebSocketImplementation) {
				this.emit(CLIENT_EMMITER_TYPES.ERROR, "Can not find WebSocket implementation");
				return;
			}
			if (this.state !== CLIENT_STATE.RECONNECTING) this.state = CLIENT_STATE.CONNECTING;
			this.socket = new WebSocketImplementation(this.serverURL);
			this.socket.closingPromise = (0, _promiseUtils.resolvingPromise)();
			this.socket.onopen = () => {
				this._handleWebSocketOpen();
			};
			this.socket.onmessage = (event) => {
				this._handleWebSocketMessage(event);
			};
			this.socket.onclose = (event) => {
				var _this$socket$closingP;
				(_this$socket$closingP = this.socket.closingPromise) === null || _this$socket$closingP === void 0 || _this$socket$closingP.resolve(event);
				this._handleWebSocketClose();
			};
			this.socket.onerror = (error) => {
				this._handleWebSocketError(error);
			};
		}
		resubscribe() {
			var _context;
			(0, _forEach.default)(_context = this.subscriptions).call(_context, (subscription, requestId) => {
				var _queryJSON$keys2, _queryJSON$watch2;
				const query = subscription.query;
				const queryJSON = query.toJSON();
				const where = queryJSON.where;
				const keys = (_queryJSON$keys2 = (0, _keys.default)(queryJSON)) === null || _queryJSON$keys2 === void 0 ? void 0 : _queryJSON$keys2.split(",");
				const watch = (_queryJSON$watch2 = queryJSON.watch) === null || _queryJSON$watch2 === void 0 ? void 0 : _queryJSON$watch2.split(",");
				const className = query.className;
				const sessionToken = subscription.sessionToken;
				const subscribeRequest = {
					op: OP_TYPES.SUBSCRIBE,
					requestId,
					query: {
						className,
						where,
						keys,
						watch
					},
					sessionToken: void 0
				};
				if (sessionToken) subscribeRequest.sessionToken = sessionToken;
				this.connectPromise.then(() => {
					this.socket.send((0, _stringify.default)(subscribeRequest));
				});
			});
		}
		/**
		* This method will close the WebSocket connection to this LiveQueryClient,
		* cancel the auto reconnect and unsubscribe all subscriptions based on it.
		*
		* @returns {Promise | undefined} CloseEvent {@link https://developer.mozilla.org/en-US/docs/Web/API/WebSocket/close_event}
		*/
		async close() {
			var _this$socket, _this$socket2;
			if (this.state === CLIENT_STATE.INITIALIZED || this.state === CLIENT_STATE.DISCONNECTED) return;
			this.state = CLIENT_STATE.DISCONNECTED;
			(_this$socket = this.socket) === null || _this$socket === void 0 || _this$socket.close();
			for (const subscription of (0, _values.default)(_context2 = this.subscriptions).call(_context2)) {
				var _context2;
				subscription.subscribed = false;
				subscription.emit(SUBSCRIPTION_EMMITER_TYPES.CLOSE);
			}
			this._handleReset();
			this.emit(CLIENT_EMMITER_TYPES.CLOSE);
			return (_this$socket2 = this.socket) === null || _this$socket2 === void 0 ? void 0 : _this$socket2.closingPromise;
		}
		_handleReset() {
			this.attempts = 1;
			this.id = 0;
			this.requestId = 1;
			this.connectPromise = (0, _promiseUtils.resolvingPromise)();
			this.subscriptions = new _map.default();
		}
		_handleWebSocketOpen() {
			const connectRequest = {
				op: OP_TYPES.CONNECT,
				applicationId: this.applicationId,
				javascriptKey: this.javascriptKey,
				masterKey: this.masterKey,
				sessionToken: this.sessionToken,
				installationId: void 0
			};
			if (this.additionalProperties) connectRequest.installationId = this.installationId;
			this.socket.send((0, _stringify.default)(connectRequest));
		}
		_handleWebSocketMessage(event) {
			let data = event.data;
			if (typeof data === "string") data = JSON.parse(data);
			let subscription = null;
			if (data.requestId) subscription = this.subscriptions.get(data.requestId) || null;
			const response = {
				clientId: data.clientId,
				installationId: data.installationId
			};
			switch (data.op) {
				case OP_EVENTS.CONNECTED:
					if (this.state === CLIENT_STATE.RECONNECTING) this.resubscribe();
					this.emit(CLIENT_EMMITER_TYPES.OPEN);
					this.id = data.clientId;
					this.connectPromise.resolve();
					this.state = CLIENT_STATE.CONNECTED;
					break;
				case OP_EVENTS.SUBSCRIBED:
					if (subscription) {
						this.attempts = 1;
						subscription.subscribed = true;
						subscription.subscribePromise.resolve();
						(0, _setTimeout2.default)(() => subscription.emit(SUBSCRIPTION_EMMITER_TYPES.OPEN, response), 200);
					}
					break;
				case OP_EVENTS.ERROR: {
					const parseError = new _ParseError.default(data.code, data.error);
					if (!this.id) {
						this.connectPromise.reject(parseError);
						this.state = CLIENT_STATE.DISCONNECTED;
					}
					if (data.requestId) {
						if (subscription) {
							subscription.subscribePromise.reject(parseError);
							(0, _setTimeout2.default)(() => subscription.emit(SUBSCRIPTION_EMMITER_TYPES.ERROR, data.error), 200);
						}
					} else this.emit(CLIENT_EMMITER_TYPES.ERROR, data.error);
					if (data.error === "Additional properties not allowed") this.additionalProperties = false;
					if (data.reconnect) this._handleReconnect();
					break;
				}
				case OP_EVENTS.UNSUBSCRIBED:
					if (subscription) {
						this.subscriptions.delete(data.requestId);
						subscription.subscribed = false;
						subscription.unsubscribePromise.resolve();
					}
					break;
				case OP_EVENTS.RESULT:
					if (subscription) {
						var _context3;
						const objects = (0, _map2.default)(_context3 = data.results).call(_context3, (json) => {
							if (!json.className && subscription.query) json.className = subscription.query.className;
							return _ParseObject.default.fromJSON(json, false);
						});
						subscription.emit(SUBSCRIPTION_EMMITER_TYPES.RESULT, objects);
					}
					break;
				default: {
					if (!subscription) break;
					let override = false;
					if (data.original) {
						override = true;
						delete data.original.__type;
						for (const field in data.original) if (!(field in data.object)) data.object[field] = void 0;
						data.original = _ParseObject.default.fromJSON(data.original, false);
					}
					delete data.object.__type;
					const parseObject = _ParseObject.default.fromJSON(data.object, !(subscription.query && subscription.query._select) ? override : false);
					if (data.original) subscription.emit(data.op, parseObject, data.original, response);
					else subscription.emit(data.op, parseObject, response);
					const localDatastore = _CoreManager.default.getLocalDatastore();
					if (override && localDatastore.isEnabled) localDatastore._updateObjectIfPinned(parseObject).then(() => {});
				}
			}
		}
		_handleWebSocketClose() {
			if (this.state === CLIENT_STATE.DISCONNECTED) return;
			this.state = CLIENT_STATE.CLOSED;
			this.emit(CLIENT_EMMITER_TYPES.CLOSE);
			for (const subscription of (0, _values.default)(_context4 = this.subscriptions).call(_context4)) {
				var _context4;
				subscription.emit(SUBSCRIPTION_EMMITER_TYPES.CLOSE);
			}
			this._handleReconnect();
		}
		_handleWebSocketError(error) {
			this.emit(CLIENT_EMMITER_TYPES.ERROR, error);
			for (const subscription of (0, _values.default)(_context5 = this.subscriptions).call(_context5)) {
				var _context5;
				subscription.emit(SUBSCRIPTION_EMMITER_TYPES.ERROR, error);
			}
			this._handleReconnect();
		}
		_handleReconnect() {
			var _context6;
			if (this.state === CLIENT_STATE.DISCONNECTED) return;
			this.state = CLIENT_STATE.RECONNECTING;
			const time = generateInterval(this.attempts);
			if (this.reconnectHandle) clearTimeout(this.reconnectHandle);
			this.reconnectHandle = (0, _setTimeout2.default)((0, _bind.default)(_context6 = () => {
				this.attempts++;
				this.connectPromise = (0, _promiseUtils.resolvingPromise)();
				this.open();
			}).call(_context6, this), time);
		}
	};
	exports.default = LiveQueryClient;
}));
//#endregion
//#region node_modules/parse/lib/browser/ParseLiveQuery.js
var require_ParseLiveQuery = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _promise = _interopRequireDefault(require_promise());
	var _indexOf = _interopRequireDefault(require_index_of());
	var _defineProperty2 = _interopRequireDefault(require_defineProperty());
	var _LiveQueryClient = _interopRequireDefault(require_LiveQueryClient());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	function getLiveQueryClient() {
		return _CoreManager.default.getLiveQueryController().getDefaultLiveQueryClient();
	}
	/**
	* We expose three events to help you monitor the status of the WebSocket connection:
	*
	* <p>Open - When we establish the WebSocket connection to the LiveQuery server, you'll get this event.
	*
	* <pre>
	* Parse.LiveQuery.on('open', () => {
	*
	* });</pre></p>
	*
	* <p>Close - When we lose the WebSocket connection to the LiveQuery server, you'll get this event.
	*
	* <pre>
	* Parse.LiveQuery.on('close', () => {
	*
	* });</pre></p>
	*
	* <p>Error - When some network error or LiveQuery server error happens, you'll get this event.
	*
	* <pre>
	* Parse.LiveQuery.on('error', (error) => {
	*
	* });</pre></p>
	*
	* @class Parse.LiveQuery
	* @static
	*/
	var LiveQuery = class {
		constructor() {
			var _this = this;
			(0, _defineProperty2.default)(this, "emitter", void 0);
			(0, _defineProperty2.default)(this, "on", void 0);
			(0, _defineProperty2.default)(this, "emit", void 0);
			const Emitter = _CoreManager.default.getEventEmitter();
			this.emitter = new Emitter();
			this.on = (eventName, listener) => this.emitter.on(eventName, listener);
			this.emit = function(eventName) {
				for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) args[_key - 1] = arguments[_key];
				return _this.emitter.emit(eventName, ...args);
			};
			this.on("error", () => {});
		}
		/**
		* After open is called, the LiveQuery will try to send a connect request
		* to the LiveQuery server.
		*/
		async open() {
			(await getLiveQueryClient()).open();
		}
		/**
		* When you're done using LiveQuery, you can call Parse.LiveQuery.close().
		* This function will close the WebSocket connection to the LiveQuery server,
		* cancel the auto reconnect, and unsubscribe all subscriptions based on it.
		* If you call query.subscribe() after this, we'll create a new WebSocket
		* connection to the LiveQuery server.
		*/
		async close() {
			(await getLiveQueryClient()).close();
		}
	};
	exports.default = LiveQuery;
	var defaultLiveQueryClient;
	_CoreManager.default.setLiveQueryController({
		setDefaultLiveQueryClient(liveQueryClient) {
			defaultLiveQueryClient = liveQueryClient;
		},
		async getDefaultLiveQueryClient() {
			if (defaultLiveQueryClient) return defaultLiveQueryClient;
			const [currentUser, installationId] = await _promise.default.all([_CoreManager.default.getUserController().currentUserAsync(), _CoreManager.default.getInstallationController().currentInstallationId()]);
			const sessionToken = currentUser ? currentUser.getSessionToken() : void 0;
			let liveQueryServerURL = _CoreManager.default.get("LIVEQUERY_SERVER_URL");
			if (liveQueryServerURL && (0, _indexOf.default)(liveQueryServerURL).call(liveQueryServerURL, "ws") !== 0) throw new Error("You need to set a proper Parse LiveQuery server url before using LiveQueryClient");
			if (!liveQueryServerURL) {
				const serverURL = _CoreManager.default.get("SERVER_URL");
				liveQueryServerURL = ((0, _indexOf.default)(serverURL).call(serverURL, "https") === 0 ? "wss://" : "ws://") + serverURL.replace(/^https?:\/\//, "");
				_CoreManager.default.set("LIVEQUERY_SERVER_URL", liveQueryServerURL);
			}
			const applicationId = _CoreManager.default.get("APPLICATION_ID");
			const javascriptKey = _CoreManager.default.get("JAVASCRIPT_KEY");
			const masterKey = _CoreManager.default.get("MASTER_KEY");
			defaultLiveQueryClient = new _LiveQueryClient.default({
				applicationId,
				serverURL: liveQueryServerURL,
				javascriptKey,
				masterKey,
				sessionToken,
				installationId
			});
			const LiveQuery = _CoreManager.default.getLiveQuery();
			defaultLiveQueryClient.on("error", (error) => {
				LiveQuery.emit("error", error);
			});
			defaultLiveQueryClient.on("open", () => {
				LiveQuery.emit("open");
			});
			defaultLiveQueryClient.on("close", () => {
				LiveQuery.emit("close");
			});
			return defaultLiveQueryClient;
		},
		_clearCachedDefaultClient() {
			defaultLiveQueryClient = null;
		}
	});
}));
//#endregion
//#region node_modules/parse/lib/browser/StorageController.browser.js
var require_StorageController_browser = /* @__PURE__ */ __commonJSMin(((exports) => {
	require_define_property$3()(exports, "__esModule", { value: true });
	exports.default = void 0;
	exports.default = {
		async: 0,
		getItem(path) {
			return localStorage.getItem(path);
		},
		setItem(path, value) {
			try {
				localStorage.setItem(path, value);
			} catch (e) {
				console.log(e.message);
			}
		},
		removeItem(path) {
			localStorage.removeItem(path);
		},
		getAllKeys() {
			const keys = [];
			for (let i = 0; i < localStorage.length; i += 1) keys.push(localStorage.key(i));
			return keys;
		},
		clear() {
			localStorage.clear();
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/StorageController.weapp.js
var require_StorageController_weapp = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _keys = _interopRequireDefault(require_keys());
	exports.default = {
		async: 0,
		getItem(path) {
			return wx.getStorageSync(path);
		},
		setItem(path, value) {
			try {
				wx.setStorageSync(path, value);
			} catch (_) {}
		},
		removeItem(path) {
			wx.removeStorageSync(path);
		},
		getAllKeys() {
			const res = wx.getStorageInfoSync();
			return (0, _keys.default)(res);
		},
		clear() {
			wx.clearStorageSync();
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/StorageController.default.js
var require_StorageController_default = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	var _keys = _interopRequireDefault(require_keys$4());
	var memMap = {};
	exports.default = {
		async: 0,
		getItem(path) {
			if (Object.hasOwn(memMap, path)) return memMap[path];
			return null;
		},
		setItem(path, value) {
			memMap[path] = String(value);
		},
		removeItem(path) {
			delete memMap[path];
		},
		getAllKeys() {
			return (0, _keys.default)(memMap);
		},
		clear() {
			for (const key in memMap) if (Object.hasOwn(memMap, key)) delete memMap[key];
		}
	};
}));
//#endregion
//#region node_modules/parse/lib/browser/StorageController.js
var require_StorageController = /* @__PURE__ */ __commonJSMin(((exports) => {
	var _Object$defineProperty = require_define_property$3();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	_interopRequireDefault(require_StorageController_react_native());
	var _StorageController2 = _interopRequireDefault(require_StorageController_browser());
	_interopRequireDefault(require_StorageController_weapp());
	var StorageController = _interopRequireDefault(require_StorageController_default()).default;
	StorageController = _StorageController2.default;
	exports.default = StorageController;
}));
//#endregion
//#region node_modules/parse/lib/browser/WebSocketController.js
var require_WebSocketController = /* @__PURE__ */ __commonJSMin(((exports) => {
	require_define_property$3()(exports, "__esModule", { value: true });
	exports.default = void 0;
	var WebSocketController;
	try {
		WebSocketController = typeof WebSocket === "function" || typeof WebSocket === "object" ? WebSocket : null;
	} catch (_) {}
	exports.default = WebSocketController;
}));
//#endregion
//#region node_modules/parse/lib/browser/Parse.js
var require_Parse = /* @__PURE__ */ __commonJSMin(((exports) => {
	require_keys$4();
	require_get_own_property_symbols();
	require_filter();
	var _Object$getOwnPropertyDescriptor = require_get_own_property_descriptor();
	require_for_each();
	require_get_own_property_descriptors();
	require_define_properties();
	var _Object$defineProperty = require_define_property$3();
	var _WeakMap = require_weak_map();
	var _interopRequireDefault = require_interopRequireDefault();
	_Object$defineProperty(exports, "__esModule", { value: true });
	exports.default = void 0;
	_interopRequireDefault(require_defineProperty());
	var _promise = _interopRequireDefault(require_promise());
	var _decode2 = _interopRequireDefault(require_decode());
	var _encode2 = _interopRequireDefault(require_encode());
	var _CryptoController = _interopRequireDefault(require_CryptoController());
	var _EventuallyQueue = _interopRequireDefault(require_EventuallyQueue());
	var _IndexedDBStorageController = _interopRequireDefault(require_IndexedDBStorageController());
	var _InstallationController = _interopRequireDefault(require_InstallationController());
	var ParseOp = _interopRequireWildcard(require_ParseOp());
	var _RESTController = _interopRequireDefault(require_RESTController());
	var _ParseACL = _interopRequireDefault(require_ParseACL());
	var Analytics = _interopRequireWildcard(require_Analytics());
	var _AnonymousUtils = _interopRequireDefault(require_AnonymousUtils());
	var Cloud = _interopRequireWildcard(require_Cloud());
	var _ParseCLP = _interopRequireDefault(require_ParseCLP());
	var _CoreManager = _interopRequireDefault(require_CoreManager());
	var _EventEmitter = _interopRequireDefault(require_EventEmitter());
	var _ParseConfig = _interopRequireDefault(require_ParseConfig());
	var _ParseError = _interopRequireDefault(require_ParseError());
	var _FacebookUtils = _interopRequireDefault(require_FacebookUtils());
	var _ParseFile = _interopRequireDefault(require_ParseFile());
	_interopRequireWildcard(require_ParseHooks());
	var _ParseGeoPoint = _interopRequireDefault(require_ParseGeoPoint());
	var _ParsePolygon = _interopRequireDefault(require_ParsePolygon());
	var _ParseInstallation = _interopRequireDefault(require_ParseInstallation());
	var _LocalDatastore = _interopRequireDefault(require_LocalDatastore());
	var _ParseObject = _interopRequireDefault(require_ParseObject());
	var Push = _interopRequireWildcard(require_Push());
	var _ParseQuery = _interopRequireDefault(require_ParseQuery());
	var _ParseRelation = _interopRequireDefault(require_ParseRelation());
	var _ParseRole = _interopRequireDefault(require_ParseRole());
	var _ParseSchema = _interopRequireDefault(require_ParseSchema());
	var _ParseSession = _interopRequireDefault(require_ParseSession());
	var _Storage = _interopRequireDefault(require_Storage());
	var _ParseUser = _interopRequireDefault(require_ParseUser());
	var _ParseLiveQuery = _interopRequireDefault(require_ParseLiveQuery());
	var _LiveQueryClient = _interopRequireDefault(require_LiveQueryClient());
	var _LocalDatastoreController = _interopRequireDefault(require_LocalDatastoreController());
	var _StorageController = _interopRequireDefault(require_StorageController());
	var _WebSocketController = _interopRequireDefault(require_WebSocketController());
	function _interopRequireWildcard(e, t) {
		if ("function" == typeof _WeakMap) var r = new _WeakMap(), n = new _WeakMap();
		return (_interopRequireWildcard = function(e, t) {
			if (!t && e && e.__esModule) return e;
			var o, i, f = {
				__proto__: null,
				default: e
			};
			if (null === e || "object" != typeof e && "function" != typeof e) return f;
			if (o = t ? n : r) {
				if (o.has(e)) return o.get(e);
				o.set(e, f);
			}
			for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = _Object$defineProperty) && _Object$getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
			return f;
		})(e, t);
	}
	/**
	* The interface for the Parse SDK.
	* This interface can be augmented in build-specific type definitions (e.g., node.d.ts)
	* to provide environment-specific type signatures.
	*/
	var Parse = {
		ACL: _ParseACL.default,
		Analytics,
		AnonymousUtils: _AnonymousUtils.default,
		Cloud,
		CLP: _ParseCLP.default,
		CoreManager: _CoreManager.default,
		Config: _ParseConfig.default,
		Error: _ParseError.default,
		FacebookUtils: _FacebookUtils.default,
		File: _ParseFile.default,
		GeoPoint: _ParseGeoPoint.default,
		Polygon: _ParsePolygon.default,
		Installation: _ParseInstallation.default,
		LocalDatastore: _LocalDatastore.default,
		Object: _ParseObject.default,
		Op: {
			Set: ParseOp.SetOp,
			Unset: ParseOp.UnsetOp,
			Increment: ParseOp.IncrementOp,
			Add: ParseOp.AddOp,
			Remove: ParseOp.RemoveOp,
			AddUnique: ParseOp.AddUniqueOp,
			Relation: ParseOp.RelationOp
		},
		Push,
		Query: _ParseQuery.default,
		Relation: _ParseRelation.default,
		Role: _ParseRole.default,
		Schema: _ParseSchema.default,
		Session: _ParseSession.default,
		Storage: _Storage.default,
		User: _ParseUser.default,
		LiveQueryClient: _LiveQueryClient.default,
		IndexedDB: void 0,
		Hooks: void 0,
		Parse: void 0,
		set EventuallyQueue(queue) {
			_CoreManager.default.setEventuallyQueue(queue);
		},
		get EventuallyQueue() {
			return _CoreManager.default.getEventuallyQueue();
		},
		initialize(applicationId, javaScriptKey) {
			if (_CoreManager.default.get("IS_NODE") && !process.env.SERVER_RENDERING) console.log("It looks like you're using the browser version of the SDK in a node.js environment. You should require('parse/node') instead.");
			Parse._initialize(applicationId, javaScriptKey);
		},
		_initialize(applicationId, javaScriptKey, masterKey, maintenanceKey) {
			_CoreManager.default.set("APPLICATION_ID", applicationId);
			_CoreManager.default.set("JAVASCRIPT_KEY", javaScriptKey);
			_CoreManager.default.set("MAINTENANCE_KEY", maintenanceKey);
			_CoreManager.default.set("MASTER_KEY", masterKey);
			_CoreManager.default.set("USE_MASTER_KEY", false);
			_CoreManager.default.setIfNeeded("EventEmitter", _EventEmitter.default);
			_CoreManager.default.setIfNeeded("LiveQuery", new _ParseLiveQuery.default());
			_CoreManager.default.setIfNeeded("CryptoController", _CryptoController.default);
			_CoreManager.default.setIfNeeded("EventuallyQueue", _EventuallyQueue.default);
			_CoreManager.default.setIfNeeded("InstallationController", _InstallationController.default);
			_CoreManager.default.setIfNeeded("LocalDatastoreController", _LocalDatastoreController.default);
			_CoreManager.default.setIfNeeded("StorageController", _StorageController.default);
			_CoreManager.default.setIfNeeded("WebSocketController", _WebSocketController.default);
			Parse.IndexedDB = _CoreManager.default.setIfNeeded("IndexedDBStorageController", _IndexedDBStorageController.default);
		},
		setAsyncStorage(storage) {
			_CoreManager.default.setAsyncStorage(storage);
		},
		setLocalDatastoreController(controller) {
			_CoreManager.default.setLocalDatastoreController(controller);
		},
		getServerHealth() {
			return _CoreManager.default.getRESTController().request("GET", "health");
		},
		set applicationId(value) {
			_CoreManager.default.set("APPLICATION_ID", value);
		},
		get applicationId() {
			return _CoreManager.default.get("APPLICATION_ID");
		},
		set javaScriptKey(value) {
			_CoreManager.default.set("JAVASCRIPT_KEY", value);
		},
		get javaScriptKey() {
			return _CoreManager.default.get("JAVASCRIPT_KEY");
		},
		set masterKey(value) {
			_CoreManager.default.set("MASTER_KEY", value);
		},
		get masterKey() {
			return _CoreManager.default.get("MASTER_KEY");
		},
		set maintenanceKey(value) {
			_CoreManager.default.set("MAINTENANCE_KEY", value);
		},
		get maintenanceKey() {
			return _CoreManager.default.get("MAINTENANCE_KEY");
		},
		set serverURL(value) {
			_CoreManager.default.set("SERVER_URL", value);
		},
		get serverURL() {
			return _CoreManager.default.get("SERVER_URL");
		},
		set LiveQuery(liveQuery) {
			_CoreManager.default.setLiveQuery(liveQuery);
		},
		get LiveQuery() {
			return _CoreManager.default.getLiveQuery();
		},
		set liveQueryServerURL(value) {
			_CoreManager.default.set("LIVEQUERY_SERVER_URL", value);
		},
		get liveQueryServerURL() {
			return _CoreManager.default.get("LIVEQUERY_SERVER_URL");
		},
		set encryptedUser(value) {
			_CoreManager.default.set("ENCRYPTED_USER", value);
		},
		get encryptedUser() {
			return _CoreManager.default.get("ENCRYPTED_USER");
		},
		set secret(value) {
			_CoreManager.default.set("ENCRYPTED_KEY", value);
		},
		get secret() {
			return _CoreManager.default.get("ENCRYPTED_KEY");
		},
		set idempotency(value) {
			_CoreManager.default.set("IDEMPOTENCY", value);
		},
		get idempotency() {
			return _CoreManager.default.get("IDEMPOTENCY");
		},
		set allowCustomObjectId(value) {
			_CoreManager.default.set("ALLOW_CUSTOM_OBJECT_ID", value);
		},
		get allowCustomObjectId() {
			return _CoreManager.default.get("ALLOW_CUSTOM_OBJECT_ID");
		},
		set nodeLogging(value) {
			_CoreManager.default.set("NODE_LOGGING", value);
		},
		get nodeLogging() {
			return _CoreManager.default.get("NODE_LOGGING");
		},
		_request() {
			for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
			return _CoreManager.default.getRESTController().request.apply(null, args);
		},
		_ajax() {
			for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) args[_key2] = arguments[_key2];
			return _CoreManager.default.getRESTController().ajax.apply(null, args);
		},
		_decode(_, value) {
			return (0, _decode2.default)(value);
		},
		_encode(value, _, disallowObjects) {
			return (0, _encode2.default)(value, disallowObjects);
		},
		_getInstallationId() {
			return _CoreManager.default.getInstallationController().currentInstallationId();
		},
		enableLocalDatastore(polling, ms) {
			if (!this.applicationId) {
				console.log("'enableLocalDatastore' must be called after 'initialize'");
				return;
			}
			if (!this.LocalDatastore.isEnabled) {
				this.LocalDatastore.isEnabled = true;
				if (polling || typeof polling === "undefined") _CoreManager.default.getEventuallyQueue().poll(ms || 2e3);
			}
		},
		isLocalDatastoreEnabled() {
			return this.LocalDatastore.isEnabled;
		},
		dumpLocalDatastore() {
			if (!this.LocalDatastore.isEnabled) {
				console.log("Parse.enableLocalDatastore() must be called first");
				return _promise.default.resolve({});
			} else return Parse.LocalDatastore._getAllContents();
		},
		enableEncryptedUser() {
			this.encryptedUser = true;
		},
		isEncryptedUserEnabled() {
			return this.encryptedUser;
		}
	};
	_CoreManager.default.setRESTController(_RESTController.default);
	globalThis.Parse = Parse;
	Parse.Parse = Parse;
	exports.default = Parse;
}));
//#endregion
//#region node_modules/parse/index.js
var require_parse = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_Parse().default;
}));
//#endregion
export default require_parse();
